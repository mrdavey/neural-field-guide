import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { generativeSeed, lines } from "../seed";

const manifest = plannedCourseManifests.generative;

export const generativeDiffusionSpecs = [
  defineResearchLesson(manifest, generativeSeed({
    id: "corruption-denoising",
    plain: "Diffusion training deliberately corrupts data with known noise, then learns information needed to reverse that corruption step by step.",
    precise: "A common forward process samples $x_t=\\sqrt{\\bar\\alpha_t}x_0+\\sqrt{1-\\bar\\alpha_t}\\epsilon$ with $\\epsilon\\sim N(0,I)$. The schedule $\\bar\\alpha_t$ controls retained signal and noise. Because $q(x_t\\mid x_0)$ is known, any timestep can be sampled directly for training. Generation begins from noise and applies learned reverse transitions; forward corruption itself is not learned.",
    analogy: "It is like adding measured layers of fog to photographs and training a restorer who knows the fog level.", analogyBreak: "denoising predicts a distributional reverse step, not the unique original behind a noisy observation.",
    ideas: ["Keep clean data, sampled noise, timestep, and corrupted input in the training record.", "Interpret schedules through signal-to-noise, not timestep number alone.", "Distinguish the fixed forward process from learned reverse dynamics."],
    worked: "For scalar x0=2, alpha_bar=.64, and epsilon=−1, xt=.8×2+.6×(−1)=1.0. The same x0 and timestep produce different xt under another epsilon.",
    misconception: "The model does not learn to add noise, and a noisy input does not contain a uniquely recoverable clean example at high corruption.",
    objectives: { primary: "Compute and trace a closed-form forward corruption sample", decision: "Choose and audit a noise schedule using signal-to-noise evidence" },
    vocabulary: [{ term: "Forward process", meaning: "The fixed corruption distribution that gradually transforms data toward a simple noise distribution." }, { term: "Noise schedule", meaning: "The sequence controlling how much signal is retained at each timestep." }, { term: "Signal-to-noise ratio", meaning: "A ratio comparing retained clean-signal variance with injected-noise variance." }],
    primaryCheck: { prompt: "x0=3, alpha_bar=.25, epsilon=2. Compute xt.", expected: "sqrt(.25)=.5 and sqrt(.75)≈.866, so xt=.5×3+.866×2≈3.232.", retry: "Compute the signal coefficient and noise coefficient separately before adding their contributions." },
    decision: { explanation: "A schedule is an allocation of learning difficulty and sampling states, not decoration.", mechanism: "Plot alpha_bar, log-SNR, coefficient spacing, and reconstruction/noise-prediction error by timestep; compare schedules under matched training and sampling budgets.", workedExample: "A schedule spends many steps at near-zero SNR where the tiny model learns little, while another spreads log-SNR more evenly and improves mid-timestep errors.", boundary: "A schedule that helps one resolution, objective, or sampler need not transfer unchanged to another.", check: { prompt: "Two schedules both end near pure noise but allocate intermediate SNR differently. Are they equivalent?", expected: "No. Training-example difficulty and reverse discretization differ across intermediate states even if endpoints match.", retry: "Compare alpha_bar or log-SNR at several interior timesteps rather than only t=0 and T." } },
    quiz: { question: "Which part of a standard DDPM is fixed rather than learned?", options: ["The forward noising distribution", "Every reverse mean", "The training data", "The generated sample"], answer: 0, explanation: "The designer specifies forward corruption; the neural model predicts information for the reverse process." },
    transfer: { prompt: "At alpha_bar≈0, what information dominates xt?", options: [{ text: "Sampled noise, with little direct clean signal retained", feedback: "Correct: the signal coefficient approaches zero." }, { text: "The exact clean example", feedback: "Clean signal is nearly erased at this endpoint." }, { text: "The model parameters", feedback: "xt is produced by the forward corruption equation, not reverse-model output." }], answer: 0, worked: "Inspect the two coefficients: sqrt(alpha_bar) vanishes while sqrt(1−alpha_bar) approaches one.", retry: "Evaluate both coefficients at the endpoint before reasoning from timestep names." },
    lab: { title: "Forward corruption microscope", question: "How do retained signal and injected noise change across a schedule?", change: "Select low, middle, or high corruption and inspect coefficients and SNR.", observe: "Signal coefficient falls while noise coefficient rises; their squared contributions sum to one.", explain: "Use coefficients or SNR rather than saying the sample merely looks noisier.", complete: "Trace all three timesteps and identify the evidence still present at each.", boundary: "Scalar coefficients illustrate the process; no image model or reverse denoiser runs here.", controlLabel: "alpha_bar", cases: [{ label: ".90", resultLabel: "signal / noise", resultValue: ".95 / .32", meter: 10, detail: "Most clean signal remains and SNR is 9." }, { label: ".50", resultLabel: "signal / noise", resultValue: ".71 / .71", meter: 50, detail: "Signal and noise variance contributions are equal." }, { label: ".01", resultLabel: "signal / noise", resultValue: ".10 / .99", meter: 99, detail: "Almost all variance is noise and direct clean evidence is weak." }] },
    motionConcept: "distribution",
    code: { title: "Sample forward corruption", setup: "The scalar helper uses an explicit epsilon so every contribution can be checked.", predict: "What xt follows x0=2, alpha_bar=.64, epsilon=−1?", code: lines("import math", "x0, alpha_bar, epsilon = 2.0, 0.64, -1.0", "signal = math.sqrt(alpha_bar)*x0", "noise = math.sqrt(1-alpha_bar)*epsilon", "xt = signal + noise", "print(signal, noise, xt)"), observe: "The contributions are 1.6 and −0.6, yielding xt=1.0.", tryIt: "Use 1,000 seeded epsilons and verify the empirical mean stays near sqrt(alpha_bar)×x0." },
    sources: ["ddpm", "score"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "score-matching",
    plain: "A score is the gradient of log density with respect to the data; it points toward locally higher-density regions without requiring the normalization constant.",
    precise: "The score is $s(x)=\\nabla_x\\log p(x)$. For $N(\\mu,\\sigma^2)$ it is $-(x-\\mu)/\\sigma^2$. Under additive corruption $x_t=x_0+\\sigma_t\\epsilon$ with $\\epsilon\\sim N(0,I)$, the conditional score is $\\nabla_{x_t}\\log q(x_t\\mid x_0)=-(x_t-x_0)/\\sigma_t^2=-\\epsilon/\\sigma_t$. Denoising score matching minimizes a declared weighting of $\\|s_\\theta(x_t,t)+\\epsilon/\\sigma_t\\|^2$ over data, noise, and timesteps; the optimum recovers the marginal noisy-data score under the objective assumptions. Weighting by a function such as $\\lambda(t)=\\sigma_t^2$ changes which noise levels dominate. Langevin or reverse-SDE/ODE samplers use the learned field, so field error accumulates along trajectories.",
    analogy: "The score is a compass that points uphill on a log-density landscape.", analogyBreak: "local directions may be inaccurate far from data, and following a compass requires a step rule and noise model.",
    ideas: ["Distinguish scalar log density from its vector gradient field.", "Check analytic scores on simple distributions before fitting a network.", "Evaluate direction, magnitude, and trajectory consequences across noise levels."],
    worked: "For standard normal p, score at x=2 is −2. For one denoising target, let $x_0=2$, $\\sigma_t=.5$, and $\\epsilon=-1$, so $x_t=1.5$. The conditional target is $-(1.5-2)/.25=+2$, equivalently $-\\epsilon/\\sigma_t$. If the network predicts 1.4, unweighted squared error is $(1.4-2)^2=.36$ and $\\sigma_t^2$ weighting contributes $.25\\times.36=.09$.",
    misconception: "A score is not a quality rating and does not directly give normalized density or a generated sample.",
    objectives: { primary: "Compute and interpret analytic score vectors", decision: "Audit score-field errors by noise level and sampling consequence" },
    vocabulary: [{ term: "Score", meaning: "The gradient of log probability density with respect to the modeled variable." }, { term: "Score matching", meaning: "An objective that fits a score field without directly fitting normalized density values." }, { term: "Reverse SDE", meaning: "A stochastic differential equation that uses the learned score to reverse a forward noise process while retaining random diffusion." }, { term: "Probability-flow ODE", meaning: "A deterministic ordinary differential equation with the same marginals as the associated score SDE under ideal conditions." }, { term: "Vector field", meaning: "A vector assigned to each point, here representing local log-density direction and slope." }],
    primaryCheck: { prompt: "For N(mu=1,sigma²=4), what is the score at x=5?", expected: "Score is −(5−1)/4=−1, pointing back toward the mean.", retry: "Subtract the mean, divide by variance, and apply the negative sign." },
    decision: { explanation: "Average score error can hide the noise levels and regions that dominate sampler failure.", mechanism: "Verify conditional DSM targets first, then slice error by noise level, density region, direction cosine, norm calibration, weighting convention, and rollout consequence; use analytic targets on toy distributions and changed starts outside typical data regions.", workedExample: "A field has low mean squared error near modes but points outward between them, causing reverse trajectories to diverge from bridge regions.", boundary: "Correct conditional targets or scores on a finite grid do not prove global integrability, normalized density, or high-dimensional sampling quality.", check: { prompt: "With $x_0=2$, $\\sigma=.5$, $\\epsilon=-1$, and predicted score 1.4, compute $x_t$, the DSM target, unweighted squared error, and $\\sigma^2$-weighted contribution.", expected: "$x_t=1.5$; target is $-(1.5-2)/.25=2$; squared error is $(1.4-2)^2=.36$; multiplying by .25 gives .09. Reversing the target sign would train movement away from the conditional clean point.", retry: "Form the corrupted point, divide its negative displacement by variance, then compare prediction and target before applying the declared weight." } },
    quiz: { question: "Why does the score not need the partition function?", options: ["Its x-gradient removes constants independent of x", "Scores are already samples", "The partition is always one", "Noise normalizes it"], answer: 0, explanation: "The derivative of log Z with respect to x is zero when Z is constant in x." },
    transfer: { prompt: "A scalar 'score' says sample quality is 8/10. Is that the diffusion score?", options: [{ text: "No; diffusion score is a gradient vector with respect to the modeled variable", feedback: "Correct: the words refer to different mathematical objects." }, { text: "Yes, after dividing by ten", feedback: "Rescaling a rating does not create a log-density gradient." }, { text: "Yes, if the sample is Gaussian", feedback: "A Gaussian score still depends on x and points in data space." }], answer: 0, worked: "Write the type signature $s:R^D→R^D$ and reserve scalar ratings for separate evaluators.", retry: "Ask what variable is differentiated and what shape the output must have." },
    lab: { title: "Analytic and denoising score probe", question: "How do a density score and a conditional denoising target determine arrow direction?", change: "Select an analytic-density point, a corrupted training row, or a reversed-target failure.", observe: "Each valid arrow is negative displacement divided by the relevant variance; the reversed target points away from its clean condition.", explain: "Name whether displacement is from the density mean or from the paired clean sample before dividing by variance.", complete: "Reproduce both valid scores and diagnose why the sign-flipped DSM target fails.", boundary: "These one-dimensional fields are deterministic analytic fixtures, not learned high-dimensional score measurements.", controlLabel: "Score case", cases: [{ label: "N(1,4), x=5", resultLabel: "marginal score", resultValue: "−1", meter: 70, detail: "Negative displacement divided by variance points left toward the Gaussian mean." }, { label: "x0=2, xt=1.5, σ=.5", resultLabel: "conditional DSM target", resultValue: "+2", meter: 90, detail: "The noisy point lies .5 below its clean condition, so the target points right." }, { label: "target sign reversed", resultLabel: "one-step consequence", resultValue: "AWAY FROM x0", meter: 100, detail: "Using −2 instead of +2 pushes this corrupted point farther below the paired clean value." }] },
    motionConcept: "coordinates",
    code: { title: "Compute density and DSM targets", setup: "Analytic scalar functions expose both negative-displacement rules.", predict: "What signs appear above a density mean and below a paired clean sample?", code: lines("def gaussian_score(x, mean=1.0, variance=4.0):", "    return -(x-mean)/variance", "def dsm_target(xt, x0, sigma):", "    return -(xt-x0)/(sigma**2)", "print(gaussian_score(5))", "print(dsm_target(1.5, 2.0, 0.5))", "assert dsm_target(1.5,2.0,.5) == 2.0"), observe: "The marginal score is −1 and the conditional DSM target is +2 because they use different reference points and variances.", tryIt: "Sample epsilon explicitly and verify the equivalent target identity $-\\epsilon/\\sigma$." },
    sources: ["score"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "ddpm-objective",
    plain: "The common DDPM objective trains a network to predict the exact noise used to corrupt a clean example at a sampled timestep.",
    precise: "Let $a_t=\\sqrt{\\bar\\alpha_t}$ and $s_t=\\sqrt{1-\\bar\\alpha_t}$. Sample clean $x_0$, timestep t, and $\\epsilon\\sim N(0,I)$; form $x_t=a_tx_0+s_t\\epsilon$. Epsilon prediction minimizes $\\|\\epsilon-\\epsilon_\\theta(x_t,t)\\|^2$; clean-sample prediction targets $x_0$; velocity prediction targets $v=a_t\\epsilon-s_tx_0$. These are invertible rotations when $a_t^2+s_t^2=1$: from epsilon, $\\hat x_0=(x_t-s_t\\hat\\epsilon)/a_t$; from x0, $\\hat\\epsilon=(x_t-a_t\\hat x_0)/s_t$; and from velocity, $\\hat x_0=a_tx_t-s_t\\hat v$ and $\\hat\\epsilon=s_tx_t+a_t\\hat v$. Timestep sampling, schedule, conditioning, normalization, weighting, and reduction are part of the target contract.",
    analogy: "The network receives a fogged image plus the fog setting and must identify the exact fog pattern that was added.", analogyBreak: "accurate average noise prediction does not uniquely reconstruct every clean sample or guarantee good iterative sampling.",
    ideas: ["Record x0, t, epsilon, xt, prediction, and reduction together.", "Pass timestep information explicitly so one network represents many noise levels.", "Test target parameterization and weighting with algebraic conversions."],
    worked: "Use $x_0=2$, $a_t=.8$, $s_t=.6$, and $\\epsilon=-1.2$. Then $x_t=.8(2)+.6(-1.2)=.88$ and $v=.8(-1.2)-.6(2)=-2.16$. All three true targets recover the same observable: epsilon gives $(.88-.6(-1.2))/.8=2$; the x0 target is 2 directly; velocity gives $.8(.88)-.6(-2.16)=2$. For imperfect predictions $\\hat\\epsilon=-.9$, $\\hat x_0=1.9$, and $\\hat v=-2.0$, the comparable reconstructed-x0 values are 1.775, 1.9, and 1.904—not their incomparable raw target MSEs.",
    misconception: "The network target is not always the clean image; calling every diffusion loss reconstruction loss hides the chosen parameterization.",
    objectives: { primary: "Construct one DDPM noise-prediction training example", decision: "Compare epsilon, x0, and velocity targets without mixing their metrics" },
    vocabulary: [{ term: "Noise prediction", meaning: "A parameterization where the network estimates epsilon used in forward corruption." }, { term: "Clean-sample prediction", meaning: "A parameterization where the network directly estimates $x_0$ from the noisy input and timestep." }, { term: "Timestep conditioning", meaning: "Providing the noise level to the shared denoising network." }, { term: "Velocity target", meaning: "The schedule-dependent rotation $v=a_t\\epsilon-s_tx_0$, convertible back to the same $x_0$ and epsilon observables." }],
    primaryCheck: { prompt: "True epsilon is −1.2 and predicted epsilon is −0.9. What scalar squared-error contribution results?", expected: "The signed residual is −1.2−(−0.9)=−0.3, and squaring that residual gives a nonnegative loss contribution of 0.09.", retry: "Subtract prediction from target, then square; sign disappears only after the difference is formed." },
    decision: { explanation: "Target parameterizations need conversion to a common observable before comparison.", mechanism: "Declare $a_t$ and $s_t$, convert each prediction to reconstructed $x_0$ or epsilon with the identities above, then compare that shared error under matched timesteps, weighting, precision, training budget, and sampler schedule.", workedExample: "At $x_t=.88$, $a_t=.8$, and $s_t=.6$, predictions $\\hat\\epsilon=-.9$, direct $\\hat x_0=1.9$, and $\\hat v=-2.0$ convert to reconstructed $x_0$ values 1.775, 1.9, and 1.904. Against true $x_0=2$, their squared common-observable errors are about .0506, .0100, and .0092. This comparison is meaningful at the declared timestep; raw epsilon-, x0-, and velocity-MSE values are not on one scale.", boundary: "A lower reconstructed error at one timestep need not produce better samples when training weights and reverse discretization differ.", check: { prompt: "With $x_t=.88$, $a_t=.8$, $s_t=.6$, true $x_0=2$, $\\hat\\epsilon=-.9$, direct $\\hat x_0=1.9$, and $\\hat v=-2.0$, convert all predictions to x0 and compare squared errors.", expected: "Epsilon gives $(.88-.6(-.9))/.8=1.775$, error squared about .0506. Direct x0 gives 1.9, error squared .0100. Velocity gives $.8(.88)-.6(-2.0)=1.904$, error squared about .0092. Only after this conversion may the three errors be compared at this timestep.", retry: "Write $a_t$ and $s_t$ first; apply the epsilon and velocity conversions separately, then subtract the same true x0 from all three results." } },
    quiz: { question: "What inputs define one epsilon-prediction training case?", options: ["x0, sampled t, sampled epsilon, derived xt", "Only a clean sample", "Only final noise", "A human quality rating"], answer: 0, explanation: "The known forward process constructs xt and supplies the exact sampled epsilon target at timestep t." },
    transfer: { prompt: "A denoiser receives xt but not t. What ambiguity must it resolve?", options: [{ text: "The same-looking input may require different denoising strength at different noise levels", feedback: "Correct: timestep conditions the target distribution." }, { text: "Which dataset license applies", feedback: "That is important but not the mathematical ambiguity here." }, { text: "Which seed was best", feedback: "Seed identity does not replace noise-level information." }], answer: 0, worked: "Provide t or a continuous noise embedding and test changed-noise cases with matched xt ranges.", retry: "Compare the desired reverse update at low and high corruption for the same coordinate value." },
    lab: { title: "Diffusion target ledger", question: "Which fields make one DDPM training target reproducible and interpretable?", change: "Inspect complete, missing-timestep, and mixed-target records.", observe: "The contract fails when noise level or parameterization is absent.", explain: "Identify the first target ambiguity introduced by the missing field.", complete: "Classify all records and specify the repair.", boundary: "The records are deterministic fixtures; no neural loss is measured.", controlLabel: "Training record", cases: [{ label: "complete", resultLabel: "contract", resultValue: "VALID", meter: 10, detail: "x0, t, epsilon, xt, target type, schedule, and reduction are recorded." }, { label: "no timestep", resultLabel: "contract", resultValue: "AMBIGUOUS", meter: 85, detail: "The model cannot distinguish denoising strength across noise levels." }, { label: "mixed target", resultLabel: "contract", resultValue: "INVALID", meter: 100, detail: "Labels switch between epsilon and x0 without a declared conversion or indicator." }] },
    motionConcept: "training",
    code: { title: "Convert epsilon, x0, and velocity predictions", setup: "The scalar row converts all parameterizations to the same reconstructed-x0 observable.", predict: "Which imperfect prediction has the smallest x0 error in this one row?", code: lines("x0, a, s, epsilon = 2.0, .8, .6, -1.2", "xt = a*x0 + s*epsilon", "v = a*epsilon - s*x0", "assert abs((a*xt-s*v)-x0) < 1e-12", "epsilon_hat, direct_x0_hat, v_hat = -.9, 1.9, -2.0", "x0_hats = {", " 'epsilon': (xt-s*epsilon_hat)/a,", " 'x0': direct_x0_hat,", " 'velocity': a*xt-s*v_hat,", "}", "errors = {k:(value-x0)**2 for k,value in x0_hats.items()}", "print(round(xt,3), round(v,3), x0_hats, errors)"), observe: "The true row has xt=.88 and v=−2.16. Converted x0 estimates are 1.775, 1.9, and 1.904, with velocity smallest only for this declared row.", tryIt: "Change the timestep coefficients while preserving $a^2+s^2=1$, regenerate equivalent targets, and compare again." },
    sources: ["ddpm"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "diffusion-samplers-schedules",
    plain: "A diffusion sampler discretizes the learned reverse path; fewer steps reduce compute but change error accumulation, stochasticity, and sometimes sample diversity.",
    precise: "DDPM-style ancestral steps sample reverse transitions with injected variance; DDIM-style paths can use a non-Markovian formulation and deterministic eta=0 trajectories. A sampler consumes a trained prediction field, timestep sequence, parameterization conversion, variance rule, clipping or thresholding, and random source. Quality-versus-function-evaluation curves are more informative than step count alone.",
    analogy: "A sampler chooses the route and stride length for descending from noise toward data using the learned direction field.", analogyBreak: "shortcuts can leave the learned field's accurate region and a deterministic route may reduce output variation for fixed starting noise.",
    ideas: ["Count network function evaluations and stochastic draws explicitly.", "Hold starting noises and model weights fixed when comparing trajectories.", "Inspect intermediate states and failure onset, not only final image grids."],
    worked: "Trace one scalar deterministic DDIM step. Let $\\bar\\alpha_t=.64$, $\\bar\\alpha_{prev}=.81$, $x_t=.88$, and predicted noise $\\hat\\epsilon=-.9$. First $\\hat{x}_0=(x_t-\\sqrt{1-.64}\\hat\\epsilon)/\\sqrt{.64}=(.88+.54)/.8=1.775$. With $\\eta=0$, $x_{prev}=\\sqrt{.81}\\hat{x}_0+\\sqrt{1-.81}\\hat\\epsilon=.9(1.775)+\\sqrt{.19}(-.9)\\approx1.205$. A stochastic setting adds a schedule-derived $\\sigma_tz$ term and reduces the coefficient on the predicted-noise direction accordingly.",
    misconception: "A sampler with fewer steps is not automatically faster on every device or equivalent in quality; kernel overhead, batching, and model calls matter.",
    objectives: { primary: "Trace a reverse sampling schedule and its model evaluations", decision: "Design a matched trajectory comparison across sampler choices" },
    vocabulary: [{ term: "Reverse sampler", meaning: "The numerical procedure that uses a trained denoiser or score to transform noise into data." }, { term: "DDIM", meaning: "A non-Markovian diffusion sampling formulation that can use deterministic trajectories." }, { term: "Eta ($\\eta$)", meaning: "A DDIM control where zero removes added stochastic noise and larger values restore stochasticity." }, { term: "Function evaluation", meaning: "One call to the learned prediction network during numerical sampling." }, { term: "Deterministic trajectory", meaning: "A path fully determined by starting noise and settings, with no additional random draws." }],
    primaryCheck: { prompt: "Using $\\bar\\alpha_t=.64$, $\\bar\\alpha_{prev}=.81$, $x_t=.88$, $\\hat\\epsilon=-.9$, and deterministic $\\eta=0$, compute $\\hat{x}_0$ and $x_{prev}$. How many evaluations does timestep list [999,749,499,249,0] use at one call each?", expected: "$\\hat{x}_0=(.88-.6(-.9))/.8=1.775$. Then $x_{prev}=.9(1.775)+\\sqrt{.19}(-.9)\\approx1.205$. The five listed timesteps require five model evaluations.", retry: "Recover the clean estimate first, substitute it into the deterministic reverse update, then count network calls rather than intervals." },
    decision: { explanation: "Sampler comparison is a quality-cost curve over matched starting conditions, not a cherry-picked grid.", mechanism: "Reuse model checkpoint and starting-noise seeds, sweep evaluation counts, log wall time and exact hardware separately, preserve intermediate trajectories, and score all declared cases with uncertainty.", workedExample: "A 20-step deterministic sampler preserves common modes but loses a rare thin mode; a 50-step sampler recovers it at higher cost, producing a decision curve rather than a winner.", boundary: "A sampler ranking is conditional on checkpoint, schedule, parameterization, hardware, and evaluation construct.", check: { prompt: "Sampler B uses half the steps but each step calls the network twice. Is it half the model compute?", expected: "No. Compare total network evaluations and measured runtime under the pinned implementation; B may use equal evaluations.", retry: "Expand each step into primitive model calls before calculating cost." } },
    quiz: { question: "What should remain fixed in a sampler trajectory comparison?", options: ["Model weights and starting-noise seeds", "Only final filenames", "The best sample per method", "Different data normalization"], answer: 0, explanation: "Matched weights and starts isolate sampler effects more clearly." },
    transfer: { prompt: "Two sampler grids use different random starts. What comparison weakness remains?", options: [{ text: "Starting-noise variation is confounded with sampler choice", feedback: "Correct: pair the starts to attribute trajectory differences." }, { text: "No weakness because both are noise", feedback: "Different starts can substantially change outputs." }, { text: "Only image resolution matters", feedback: "Resolution does not remove seed confounding." }], answer: 0, worked: "Pair each starting noise across samplers, retain all pairs, and compare metrics and failures at matched function-evaluation counts.", retry: "Treat starting noise as an experimental input that must be held or paired." },
    lab: { title: "Sampler path and cost audit", question: "How do timestep spacing, eta, and primitive calls change a reverse trajectory comparison?", change: "Inspect a deterministic fine path, a stochastic coarse path, and an unpaired best-sample report.", observe: "Eta changes the random term, spacing changes discretization, and nominal steps can hide actual calls.", explain: "Write the $\\hat{x}_0$ and $x_{prev}$ terms, pair starting noise, then reduce cost to network evaluations.", complete: "Classify all records and name the path or attribution change in each.", boundary: "The cards specify sampler arithmetic and experiments; no runtime or sample-quality number is measured here.", controlLabel: "Sampler record", cases: [{ label: "50 steps, η=0", resultLabel: "path contract", resultValue: "DETERMINISTIC · 50 CALLS", meter: 50, detail: "Fixed start, weights, and schedule reproduce the same no-added-noise trajectory." }, { label: "20 steps, η=1", resultLabel: "path contract", resultValue: "COARSE + STOCHASTIC · 20 CALLS", meter: 70, detail: "Larger timestep gaps and an added random term both change the path; pair starts and sampler RNGs." }, { label: "20 best samples", resultLabel: "comparison", resultValue: "BIASED", meter: 100, detail: "Unpaired starts and post hoc best-sample selection prevent attribution." }] },
    motionConcept: "pipeline",
    code: { title: "Trace a deterministic DDIM step", setup: "The complete scalar update recovers x0, advances to the previous noise level, and keeps cost explicit.", predict: "Is the previous state closer to the recovered clean value?", code: lines("import math", "alpha_t, alpha_prev = 0.64, 0.81", "xt, eps_hat = 0.88, -0.9", "x0_hat = (xt-math.sqrt(1-alpha_t)*eps_hat)/math.sqrt(alpha_t)", "x_prev = math.sqrt(alpha_prev)*x0_hat + math.sqrt(1-alpha_prev)*eps_hat", "timesteps = [999,749,499,249,0]", "print(round(x0_hat,3), round(x_prev,3), len(timesteps))", "assert round(x0_hat,3) == 1.775", "assert round(x_prev,3) == 1.205"), observe: "The step recovers $\\hat{x}_0=1.775$, advances to $x_{prev}=1.205$, and the schedule declares five evaluations.", tryIt: "Add a nonzero sigma×z term and the corresponding reduced direction coefficient; keep z paired across treatments." },
    sources: ["ddpm", "ddim"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "diffusion-model-capstone", duration: 40,
    plain: "This capstone builds a small diffusion model from clean data through forward corruption, noise prediction, reverse sampling, checkpointing, and matched sampler evaluation.",
    precise: "The implementation pins data normalization, train/validation split, beta or alpha-bar schedule, timestep sampling, epsilon target, architecture, optimizer, EMA if used, seeds, and sampler. Required tests cover forward coefficients, target reconstruction identities, one-batch overfit, finite gradients, checkpoint resume, fixed-noise trajectories, and a bounded optional GPU run with raw artifacts.",
    analogy: "It is an instrumented assembly line where clean examples become noisy training cases and seeded noise becomes audited samples.", analogyBreak: "course-scale samples establish mechanism and implementation, not broad image-model competitiveness.",
    ideas: ["Build schedule and algebra tests before training the denoiser.", "Use fixed-noise trajectory panels plus held-out losses and coverage evidence.", "Separate CPU smoke invariants from optional GPU observations and provenance."],
    worked: "A tiny 16×16 shape model first overfits 32 examples, then trains a pinned budget. DDPM-50 and DDIM-20 share one checkpoint and all 32 canonical starting-noise tensors; every paired trajectory and denoiser evaluation count is preserved.",
    misconception: "A visually appealing sample is not evidence that the forward process, target conversion, held-out behavior, or sampler comparison is correct.",
    objectives: { primary: "Build and verify a complete small diffusion training and sampling pipeline", decision: "Run a paired sampler or schedule intervention with optional GPU evidence" },
    vocabulary: [{ term: "Exponential moving average", meaning: "A smoothed copy of model parameters updated across training steps for evaluation." }, { term: "Fixed-noise panel", meaning: "A preserved set of starting-noise tensors reused to compare checkpoints or samplers." }, { term: "Smoke run", meaning: "A short execution that checks integration and invariants without claiming convergence or quality." }],
    primaryCheck: { prompt: "Which exact tests should pass before interpreting a generated image?", expected: "Schedule identities, forward-sample algebra, target conversion, tensor shapes, timestep and optional condition paths, finite loss and gradients, one-batch overfit, fixed-noise sampling, and checkpoint resume should pass.", retry: "Trace every deterministic path from x0, timestep, epsilon, and any declared condition to loss; then list every state required to resume sampling and training." },
    decision: { explanation: "The intervention must be paired, budgeted, and labeled by evidence source.", mechanism: "Reproduce baseline smoke/full protocol, branch one schedule or sampler change, reuse starting noises and seed set, match model-call or training budgets, preserve raw logs/trajectories, and apply predeclared invariants plus variable observations.", workedExample: "Compare 20- versus 50-evaluation samplers on the same 32 start tensors; report trajectory failures and quality/coverage metrics without inventing expected numeric bands before a reviewed run exists.", boundary: "An optional Colab run belongs to its exact notebook, hardware, package pins, data snapshot, seeds, and budget; the browser does not execute it.", check: { prompt: "A Colab smoke run makes recognizable shapes after 100 steps. What claim is supported?", expected: "The pinned training and sampling path integrates for that short run and produced those preserved observations; convergence or general sample quality is not established.", retry: "Tie the sentence to the exact run budget and separate invariant checks from qualitative output." } },
    quiz: { question: "What makes a sampler ablation paired?", options: ["Same checkpoint and starting-noise tensors for every treatment", "Different best seeds", "Different preprocessing", "Only matching filenames"], answer: 0, explanation: "Paired starts and weights reduce variation unrelated to sampler choice." },
    transfer: { prompt: "The GPU provider differs between control and treatment. How should timing be handled?", options: [{ text: "Do not attribute raw timing differences to treatment; rerun on matched hardware or report separately", feedback: "Correct: hardware is a direct timing confound." }, { text: "Average times and claim speedup", feedback: "Averaging does not remove systematic hardware differences." }, { text: "Ignore timing provenance", feedback: "Runtime evidence requires exact hardware and workload." }], answer: 0, worked: "Keep algorithmic model-call counts comparable now, then obtain paired wall-time measurements on one pinned hardware class if speed is a claim.", retry: "List every factor that determines runtime and hold them fixed before attributing a difference." },
    lab: { title: "Diffusion evidence boundary gate", question: "Which result belongs to a deterministic test, smoke observation, or comparative experiment?", change: "Select an evidence record and inspect its provenance and strongest claim.", observe: "Claim strength changes with execution source, budget, pairing, and repetition.", explain: "Name the evidence type before interpreting the value or image.", complete: "Classify all records and state one forbidden claim for each.", boundary: "The gate teaches provenance; it is not itself a diffusion run.", controlLabel: "Evidence record", cases: [{ label: "algebra fixture", resultLabel: "supports", resultValue: "CORRECTNESS", meter: 30, detail: "Closed-form schedule and target identities validate arithmetic only." }, { label: "100-step smoke", resultLabel: "supports", resultValue: "INTEGRATION", meter: 55, detail: "The pinned short path executes; convergence and broad quality remain unknown." }, { label: "paired seeds", resultLabel: "supports", resultValue: "BOUNDED EFFECT", meter: 90, detail: "Matched budgets and all seed rows can support the predeclared comparison within scope." }] },
    motionConcept: "pipeline",
    code: { title: "Specify the diffusion build loop", setup: "Pseudocode names every input, state, and artifact before an optional GPU implementation.", predict: "Which tensors must be paired across sampler treatments?", code: lines("x0 = next(train_loader)", "t = timestep_rng.randint(batch=x0.shape[0])", "epsilon = noise_rng.normal_like(x0)", "xt = schedule.corrupt(x0, t, epsilon)", "epsilon_hat = model(xt, t)", "loss = mean((epsilon_hat-epsilon)**2)", "optimizer_step(loss)", "save(checkpoint, optimizer, ema, rngs, config, data_cursor)", "evaluate(checkpoint, fixed_start_noises, sampler_configs)"), observe: "Training randomness and fixed evaluation starts have separate named streams and preserved state.", tryIt: "Add a sampler treatment while keeping checkpoint and fixed_start_noises identical.", mode: "pseudocode" },
    sources: ["ddpm", "ddim", "reproducibility", "pytorch"],
  })),
];
