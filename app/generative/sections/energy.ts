import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { generativeSeed, lines } from "../seed";

const manifest = plannedCourseManifests.generative;

export const generativeEnergySpecs = [
  defineResearchLesson(manifest, generativeSeed({
    id: "change-of-variables",
    plain: "Change of variables moves probability density through an invertible transformation while correcting for how the transformation stretches space.",
    precise: "If $x=f(z)$ is invertible and $z\\sim p_Z$, then $p_X(x)=p_Z(f^{-1}(x))|\\det J_{f^{-1}}(x)|$. Equivalently $\\log p_X(x)=\\log p_Z(z)-\\log|\\det J_f(z)|$. The determinant measures local volume expansion; omitting it produces a function that generally does not integrate to one.",
    analogy: "Ink density thins when a rubber sheet stretches and thickens when the sheet compresses.", analogyBreak: "high-dimensional invertibility and determinant computation constrain which neural transformations are practical.",
    ideas: ["Trace both the transformed point and the local volume correction.", "Use absolute determinant because orientation flips do not create negative density.", "Check invertibility and normalization independently of visual plausibility."],
    worked: "Let z be uniform on [0,1] and x=2z. The transformed support is [0,2], inverse is z=x/2, and inverse derivative is 1/2, so p_X(x)=1/2 on [0,2] and integrates to one.",
    misconception: "Applying an invertible neural network to samples does not define the correct density unless the Jacobian correction is included and tractable.",
    objectives: { primary: "Compute a one-dimensional density transformation with its Jacobian", decision: "Diagnose invertibility, support, and volume-correction failures" },
    vocabulary: [{ term: "Jacobian", meaning: "The matrix of local output derivatives with respect to input coordinates." }, { term: "Determinant", meaning: "A signed local volume-scaling factor for a square linear transformation." }, { term: "Change of variables", meaning: "The probability rule that combines inverse mapping with local volume correction." }],
    primaryCheck: { prompt: "z is uniform on [0,1] and x=4z. What density does x have on its support?", expected: "The support is [0,4] and density is 1/4 because inverse derivative dz/dx is 1/4; the integral remains one.", retry: "Find the new interval length, then choose a constant density whose area over that interval is one." },
    decision: { explanation: "A proposed flow layer must satisfy a computational contract, not merely preserve input/output shape.", mechanism: "Provide forward, inverse, log-absolute-determinant, domain/support, and round-trip tests; compare analytical log determinant with finite differences in a tiny case.", workedExample: "The map x=z³ is invertible on all real numbers but its derivative is zero at z=0, creating a singular density point that needs careful support handling.", boundary: "Exact density under one parameterization does not ensure useful samples or a well-conditioned optimization landscape.", check: { prompt: "A layer maps both z=1 and z=−1 to x=1. Can it be a global normalizing-flow transform?", expected: "No. It is not one-to-one, so a single inverse and standard change-of-variables density are unavailable globally.", retry: "Apply the definition of invertible: every output must have exactly one input in the declared domain." } },
    quiz: { question: "Why does density include a Jacobian determinant?", options: ["To correct for local volume expansion or compression", "To add randomness", "To choose a seed", "To remove the inverse"], answer: 0, explanation: "Probability mass is preserved while coordinate volume changes, so density must adjust inversely." },
    transfer: { prompt: "A transform doubles every coordinate but leaves density values unchanged. What fails?", options: [{ text: "Normalization, because expanded volume was not compensated", feedback: "Correct: density must fall by the volume expansion factor." }, { text: "Only the random seed", feedback: "The error is deterministic density bookkeeping." }, { text: "Nothing if samples look plausible", feedback: "Plausibility does not repair an unnormalized density." }], answer: 0, worked: "Compute the determinant, subtract its log from base log density, and verify the transformed density integrates to one.", retry: "Track a small unit volume through the transform and ask how its density must change to preserve mass." },
    lab: { title: "Volume-correction bench", question: "How should density change when a one-dimensional transform stretches or compresses its support?", change: "Choose a scale factor and inspect support length, determinant, and transformed density.", observe: "Density moves inversely to absolute scale so total area remains one.", explain: "Connect determinant to the local volume change before checking normalization.", complete: "Trace all scales and explain why a negative scale uses absolute determinant.", boundary: "This one-dimensional uniform fixture does not expose high-dimensional Jacobian cost.", controlLabel: "Transform x=az", cases: [{ label: "a=.5", resultLabel: "density", resultValue: "2.0", meter: 80, detail: "Support compresses to length .5, so density doubles to preserve unit mass." }, { label: "a=2", resultLabel: "density", resultValue: "0.5", meter: 50, detail: "Support stretches to length 2, so density halves." }, { label: "a=−2", resultLabel: "density", resultValue: "0.5", meter: 50, detail: "Orientation flips but absolute scale is 2; density remains nonnegative." }] },
    motionConcept: "coordinates",
    code: { title: "Transform a uniform density", setup: "The complete scalar helper returns inverse point and log-density correction.", predict: "What log correction follows scale 4?", code: lines("import math", "def scale_flow(z, scale):", "    x = scale*z", "    log_abs_det = math.log(abs(scale))", "    return x, -log_abs_det", "x, log_correction = scale_flow(0.3, 4.0)", "print(x, round(log_correction,3))"), observe: "x is 1.2 and log-density correction is −log 4≈−1.386.", tryIt: "Pass scale 0 and add a defensive assertion explaining the singularity." },
    sources: ["realNvp", "deepLearning"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "normalizing-flows",
    plain: "A normalizing flow composes invertible transformations so a simple base distribution becomes a flexible distribution with exact sampling and density evaluation.",
    precise: "For $x=f_K\\circ\\cdots\\circ f_1(z)$, log density accumulates $\\log p_X(x)=\\log p_Z(z)-\\sum_k\\log|\\det J_{f_k}|$. Coupling layers split coordinates: one subset stays fixed while parameterizing an invertible affine transform of the other, making inverse and triangular Jacobian cheap. Expressivity, conditioning, architecture constraints, and numerical stability trade off.",
    analogy: "A flow repeatedly folds and stretches a simple sheet while keeping a reversible map of every move.", analogyBreak: "requiring a reversible path can make some topology changes or discrete data interfaces awkward.",
    ideas: ["Implement forward, inverse, and log determinant as one inseparable layer contract.", "Compose log determinants in the same order as transformations.", "Test round-trip error and density against an analytically solvable case before training."],
    worked: "An affine coupling keeps x1=z1 and sets x2=z2 exp(s(z1))+t(z1). The inverse recovers z2=(x2−t(x1))exp(−s(x1)); log determinant is s(z1), avoiding a full matrix determinant.",
    misconception: "Exact likelihood does not mean the learned density prioritizes perceptually or semantically important structure.",
    objectives: { primary: "Implement and verify an invertible coupling layer", decision: "Evaluate flow expressivity, conditioning, and likelihood boundaries" },
    vocabulary: [{ term: "Normalizing flow", meaning: "A composition of tractable invertible transformations applied to a base distribution." }, { term: "Coupling layer", meaning: "An invertible transform where one variable subset parameterizes changes to another subset." }, { term: "Round-trip error", meaning: "The difference between an input and the result of forward followed by inverse transformation." }],
    primaryCheck: { prompt: "In affine coupling, x2=z2 exp(s)+t. Write the inverse for z2 and the scalar log determinant.", expected: "z2=(x2−t)exp(−s), and log absolute determinant is s for that transformed coordinate.", retry: "Undo translation first, then scaling; take log of the positive exp(s) scale." },
    decision: { explanation: "Flow correctness requires algebraic, expressivity, and numerical evidence before likelihood comparison.", mechanism: "Test forward/inverse round trips over edge cases, compare analytical Jacobian with finite differences, verify base-to-data and data-to-base log densities, then test whether the architecture can represent the target support while monitoring scale outputs and numerical conditioning.", workedExample: "A one-dimensional continuous bijection maps a connected interval to another connected interval, so it cannot exactly turn one connected base interval into two separated intervals without retaining positive-density bridge mass. Separately, s=80 may make exp(s) overflow even when the symbolic inverse is valid.", boundary: "Passing round-trip tests at float64 does not guarantee stable float16 training, topology-changing expressivity, or good held-out density estimation.", check: { prompt: "A 1D flow with a continuous bijection fits two separated target intervals but assigns unwanted mass along the bridge between them; all values remain finite. Is this primarily expressivity or numerical instability?", expected: "It is an expressivity and topology boundary: the continuous image of connected support stays connected, so exact disconnected support is unavailable. Numerical instability would instead appear as overflow, NaN, ill-conditioning, or large round-trip error.", retry: "First test finite values and round trips. If they pass, compare the connectedness of base support and target support before changing dtype or clamps." } },
    quiz: { question: "Why are coupling layers common in flows?", options: ["They make inverse and Jacobian determinant tractable", "They remove the base distribution", "They guarantee semantic latents", "They require no optimization"], answer: 0, explanation: "The split creates a triangular Jacobian and an explicit inverse." },
    transfer: { prompt: "A layer has exact forward output but no implemented inverse. What flow claim is supported?", options: [{ text: "A transform component, not yet a verified normalizing-flow layer", feedback: "Correct: inverse and log determinant are required interfaces." }, { text: "Exact density and sampling", feedback: "Both require the missing inverse contract." }, { text: "Guaranteed stable training", feedback: "Forward execution alone says nothing about stability." }], answer: 0, worked: "Implement inverse and log determinant, add round-trip/Jacobian tests, then call it a verified flow layer.", retry: "List all three functions required by change of variables and mark the missing ones." },
    lab: { title: "Coupling-layer verifier", question: "Which evidence reveals algebraic, density, or numerical failure in an affine coupling layer?", change: "Select a layer trace and inspect round trip, log determinant, and scale range.", observe: "Different failures can coexist even when output shape remains correct.", explain: "Identify the first contract field that breaks.", complete: "Classify each trace and choose its specific repair path.", boundary: "These are deterministic layer traces, not a trained flow benchmark.", controlLabel: "Layer trace", cases: [{ label: "healthy", resultLabel: "round-trip error", resultValue: "2e−12", meter: 5, detail: "Inverse, analytical log determinant, and bounded scales agree in float64." }, { label: "wrong logdet", resultLabel: "density check", resultValue: "FAIL", meter: 85, detail: "Samples round-trip, but a missing sum over transformed dimensions corrupts density." }, { label: "scale overflow", resultLabel: "finite check", resultValue: "NON-FINITE", meter: 100, detail: "In float32, exp(90) overflows to positive infinity; later arithmetic can then produce NaN. Either result fails the finite-value gate despite an algebraically valid inverse." }] },
    motionConcept: "pipeline",
    code: { title: "Implement scalar affine coupling", setup: "A scalar companion variable parameterizes scale and shift for a second coordinate.", predict: "Will the round trip recover z2 exactly up to floating-point error?", code: lines("import math", "z1, z2 = 0.4, -1.2", "s, t = 0.2*z1, 0.5*z1", "x1 = z1", "x2 = z2*math.exp(s)+t", "recovered = (x2-t)*math.exp(-s)", "print(round(x2,4), round(recovered,4), round(s,4))", "assert abs(recovered-z2) < 1e-12"), observe: "The inverse recovers −1.2 and log determinant is s=0.08.", tryIt: "Deliberately omit exp(−s) in the inverse and make the round-trip assertion catch it." },
    sources: ["realNvp"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "energy-based-models",
    plain: "An energy-based model assigns low energy to preferred configurations and high energy to unlikely ones, defining probability only after normalization.",
    precise: "An EBM defines $p_\\theta(x)=\\exp[-E_\\theta(x)]/Z_\\theta$ with partition function $Z_\\theta=\\int\\exp[-E_\\theta(x)]dx$. Relative energies compare candidates without Z, but likelihood gradients include a positive-data term and a negative-model expectation that requires sampling. Energy scale, sampling quality, and support determine whether training is meaningful.",
    analogy: "A landscape lets a marble settle in low valleys that represent preferred states.", analogyBreak: "deep or numerous valleys can trap a sampler, and valley depth alone does not reveal normalized probability mass.",
    ideas: ["Distinguish unnormalized energy differences from normalized probabilities.", "Keep positive data examples and negative model samples visible in the gradient ledger.", "Audit whether the sampler explores modes before trusting training loss."],
    worked: "For energies [0,1,2], unnormalized weights are [1,.368,.135], Z≈1.503, and probabilities ≈[.665,.245,.090]. Adding 10 to every energy changes Z but not normalized probabilities.",
    misconception: "A model that gives data lower energy than one negative batch is not necessarily normalized, well sampled, or robust to unseen low-energy regions.",
    objectives: { primary: "Convert a finite energy table into normalized probabilities", decision: "Diagnose the coupled energy-learning and negative-sampling problem" },
    vocabulary: [{ term: "Energy", meaning: "An unconstrained scalar score where lower values correspond to greater modeled preference." }, { term: "Partition function", meaning: "The integral or sum that normalizes exponentiated negative energy into probability." }, { term: "Positive phase", meaning: "The likelihood-gradient contribution that lowers energy on observed data." }, { term: "Negative phase", meaning: "The model-expectation contribution that raises energy on configurations sampled from the current model." }, { term: "Negative sample", meaning: "A model-generated configuration used to approximate the negative phase." }],
    primaryCheck: { prompt: "Energies are [0, ln 2]. What normalized probabilities result?", expected: "Weights are [1,1/2], sum 1.5, so probabilities are [2/3,1/3].", retry: "Exponentiate negative energies, sum the weights, then divide each by the sum." },
    decision: { explanation: "EBM training quality is inseparable from the sampler that supplies the negative phase.", mechanism: "Track data energies, negative energies, chain initialization, mixing, mode occupancy, autocorrelation, and held-out diagnostics; compare samplers under matched energy evaluations and transition budgets.", workedExample: "Loss improves because short chains remain near one easy mode; a long-chain occupancy check reveals a second data mode is never visited.", boundary: "Low energy on observed examples does not establish normalized likelihood or absence of adversarial low-energy pockets.", check: { prompt: "All negative chains initialize from data and move one tiny step. Why can training look healthy while generation fails?", expected: "Negatives remain near observed points and do not represent the model distribution, so the negative-phase gradient misses remote low-energy regions and modes.", retry: "Compare chain initialization and final occupancy with independent long-run chains from broader starts." } },
    quiz: { question: "What makes an energy model a probability model?", options: ["Exponentiating negative energy and dividing by the partition function", "Choosing the lowest energy once", "Adding a seed", "Removing negative samples"], answer: 0, explanation: "Normalization over the declared space converts relative energies into probabilities." },
    transfer: { prompt: "An EBM ranks two candidates correctly but Z is intractable. Which claim remains valid?", options: [{ text: "Their relative energy ranking under the model", feedback: "Correct: pairwise ranking cancels the shared partition function." }, { text: "Their exact normalized probabilities", feedback: "Those require Z or a valid estimator." }, { text: "The sampler covers all modes", feedback: "Ranking two points supplies no mixing evidence." }], answer: 0, worked: "Use relative energies for the bounded comparison and label normalized density or coverage as unevaluated.", retry: "Write the probability ratio; note which shared term cancels and which broader claims still need it." },
    lab: { title: "Energy-to-probability landscape", question: "How do energy gaps and hidden low-energy states change normalized mass?", change: "Choose an energy table and inspect weights, normalization, and risk.", observe: "Relative ranking may stay while probability mass changes when another state enters.", explain: "Normalize across every declared state rather than inspecting the minimum alone.", complete: "Trace all tables and explain the hidden-state failure.", boundary: "Finite tables make Z exact; real continuous EBMs may require approximate sampling.", controlLabel: "Energy table", cases: [{ label: "[0,1,2]", resultLabel: "top probability", resultValue: "0.665", meter: 67, detail: "Exponentiated weights normalize across all three states." }, { label: "[10,11,12]", resultLabel: "top probability", resultValue: "0.665", meter: 67, detail: "A shared energy offset cancels after normalization." }, { label: "add E=−4", resultLabel: "hidden-state mass", resultValue: "0.973", meter: 97, detail: "A previously untested low-energy state dominates the partition and exposes an energy pocket." }] },
    motionConcept: "optimization",
    code: { title: "Normalize finite energies", setup: "A stable softmax over negative energy computes the exact finite partition.", predict: "Does adding 10 to every energy change probabilities?", code: lines("import math", "def energy_probs(energies):", "    scores = [-e for e in energies]", "    m = max(scores)", "    weights = [math.exp(s-m) for s in scores]", "    return [w/sum(weights) for w in weights]", "a = energy_probs([0,1,2])", "b = energy_probs([10,11,12])", "print([round(x,3) for x in a])", "assert all(abs(x-y)<1e-12 for x,y in zip(a,b))"), observe: "Both tables yield approximately [.665,.245,.090].", tryIt: "Add a fourth energy −4 and inspect how global normalization changes every probability." },
    sources: ["ebm", "deepLearning"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "mcmc-langevin-sampling",
    plain: "MCMC builds a chain whose long-run state distribution approaches a target; Langevin sampling uses energy gradients plus noise to move toward low-energy regions while still exploring.",
    precise: "MCMC requires a transition kernel $P$ for which the target $\\pi$ is invariant: $\\pi P=\\pi$. Convergence from arbitrary starts additionally needs access between relevant states (irreducibility) and no forced cycle (aperiodicity), plus the regularity conditions of the state space. Unadjusted Langevin proposes $x'=x-\\frac{\\epsilon}{2}\\nabla_xE(x)+\\sqrt{\\epsilon}\\xi$; finite step size generally introduces stationary bias. MALA treats that Gaussian update as proposal $q(x'\\mid x)$ and accepts with $\\alpha=\\min(1,\\frac{\\pi(x')q(x\\mid x')}{\\pi(x)q(x'\\mid x)})$, preserving the target under the usual Metropolis-Hastings conditions. Burn-in, autocorrelation, effective sample size, dispersed chains, and mode occupancy still diagnose whether a finite run mixed.",
    analogy: "A noisy hiker tends downhill but receives random shoves that can escape shallow valleys.", analogyBreak: "a finite chain can remain trapped, and correlated states are not equivalent to independent samples.",
    ideas: ["Trace drift and noise as separate update contributions.", "Evaluate chain mixing and mode occupancy rather than plotting only the final state.", "Report step size, burn-in, thinning policy, starts, and transition budget."],
    worked: "For the two-state kernel $P=[[0.8,0.2],[0.4,0.6]]$, $\\pi=[2/3,1/3]$ satisfies $\\pi P=[2/3,1/3]$; positive cross-state and self-transition probabilities make this finite chain irreducible and aperiodic. For a standard-normal MALA step from $x=0$ with $\\epsilon=0.5$ and proposed $y=2$, the forward log-proposal term (up to a shared constant) is −4, the reverse is −2.25, and $\\log\\pi(y)-\\log\\pi(x)=-2$. Thus log acceptance ratio is −0.25, $\\alpha\\approx0.779$; draw $u=0.90$ rejects and the chain remains at 0.",
    misconception: "More chain steps do not guarantee independent or representative samples when modes are separated or the discretization is biased.",
    objectives: { primary: "Compute one Langevin drift-and-noise update", decision: "Diagnose step-size, autocorrelation, and mode-mixing failures" },
    vocabulary: [{ term: "Markov chain", meaning: "A sequence whose next-state distribution depends on the current state under a transition rule." }, { term: "Invariant distribution", meaning: "A distribution unchanged by one transition: $\\pi P=\\pi$." }, { term: "Irreducible", meaning: "Every target-relevant region can eventually be reached from every other one." }, { term: "Aperiodic", meaning: "The chain is not forced to return only on a fixed repeating cycle." }, { term: "Burn-in", meaning: "Initial chain states excluded because the chain may still reflect its initialization." }, { term: "Effective sample size", meaning: "An estimate of how many independent observations correlated chain states are worth." }],
    primaryCheck: { prompt: "For E=x²/2, x=2, epsilon=.1, and xi=0, compute one unadjusted Langevin step.", expected: "Gradient is 2, drift is −epsilon/2×2=−0.1, noise is zero, so next x is 1.9.", retry: "Compute gradient, multiply by negative half step size, then add the separate noise term." },
    decision: { explanation: "Sampler comparison must use matched target evaluations and transition budgets with diagnostics beyond visual traces.", mechanism: "First verify target invariance or the Metropolis correction. Then run multiple dispersed chains, log acceptance, energy and mode occupancy, estimate autocorrelation/effective sample size, inspect between-chain agreement, and repeat across step sizes under a fixed gradient-evaluation budget.", workedExample: "A large unadjusted step visits both modes but samples the wrong within-mode variance; a small step has correct local variance but never crosses modes. Metropolis adjustment removes the discretization bias in principle but may trade it for rejection and slow mixing.", boundary: "Invariance is not finite-run convergence, and standard diagnostics can miss shared trapping when every chain starts in the same region or the target has unseen modes.", check: { prompt: "For the standard-normal MALA case $x=0$, $\\epsilon=.5$, proposal $y=2$, forward/reverse log-proposal terms −4/−2.25, and draw $u=.90$, compute the decision.", expected: "The target log-density difference is −2, so log ratio is −2−2.25−(−4)=−.25; $\\alpha=e^{-.25}\\approx.779$. Since .90 exceeds .779, reject y and retain x=0.", retry: "Add target log-density change and reverse-minus-forward proposal terms, exponentiate after clipping at zero, then compare the uniform draw." } },
    quiz: { question: "Why add noise to Langevin drift?", options: ["To explore the target distribution rather than only descend to a mode", "To remove gradients", "To make every state independent", "To calculate Z exactly"], answer: 0, explanation: "Pure gradient descent seeks minima; stochastic noise is required for distributional exploration." },
    transfer: { prompt: "A chain's energy stabilizes but it never changes mode. What is supported?", options: [{ text: "Local settling, not global mixing", feedback: "Correct: stable energy can occur inside one trapped mode." }, { text: "Independent target samples", feedback: "Autocorrelation and missing mode transitions contradict that claim." }, { text: "Exact partition estimation", feedback: "Energy trace alone does not estimate Z." }], answer: 0, worked: "Add dispersed chains, mode labels, autocorrelation, and a matched transition budget before claiming representative sampling.", retry: "Distinguish movement within a valley from transitions between valleys." },
    lab: { title: "Langevin step-size clinic", question: "How does step size trade slow mixing against discretization instability?", change: "Select a step-size trace and inspect movement, acceptance proxy, and mode evidence.", observe: "Small steps crawl, moderate steps explore locally, and oversized steps become unstable.", explain: "Separate transition distance from correctness of the stationary distribution.", complete: "Diagnose all three traces and state which additional mixing evidence is required.", boundary: "The readouts are teaching fixtures, not measured MCMC diagnostics.", controlLabel: "Step size", cases: [{ label: "ε=.001", resultLabel: "diagnosis", resultValue: "CRAWL", meter: 15, detail: "Updates are stable but highly correlated under a fixed transition budget." }, { label: "ε=.05", resultLabel: "diagnosis", resultValue: "LOCAL MIX", meter: 65, detail: "Movement and stability balance locally; cross-mode transitions still require evidence." }, { label: "ε=1.0", resultLabel: "diagnosis", resultValue: "UNSTABLE", meter: 100, detail: "Discretization overshoots typical regions and biases or diverges." }] },
    motionConcept: "optimization",
    code: { title: "Run seeded Langevin steps", setup: "The target is a standard normal with energy x²/2 and a local seeded random source.", predict: "Should the average magnitude tend to shrink from x=4?", code: lines("import math, random", "rng, x, epsilon = random.Random(3), 4.0, 0.05", "trace = [x]", "for _ in range(20):", "    gradient = x", "    x = x - 0.5*epsilon*gradient + math.sqrt(epsilon)*rng.gauss(0,1)", "    trace.append(x)", "print(round(trace[0],2), round(trace[-1],2))"), observe: "The exact endpoint is seeded; drift tends toward zero while noise prevents deterministic descent.", tryIt: "Compare 20 independent chains with one 400-step chain and discuss independence versus burn-in." },
    sources: ["score", "ebm", "mala"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "flow-energy-capstone", duration: 36,
    plain: "This capstone builds a two-dimensional target distribution, an invertible flow, and an energy sampler, then compares exact-density and iterative-sampling trade-offs under matched evidence.",
    precise: "The study pins one 2D target, train/validation draws, parameter budget, optimization steps, and evaluation suite. The flow supplies exact log density, direct sampling, inverse, and log determinants. The EBM supplies energies and Langevin chains with fixed gradient-evaluation budgets. Evidence includes density contours, held-out NLL where valid, precision/coverage proxies, mode occupancy, round-trip error, autocorrelation, failures, and compute counts.",
    analogy: "It is a controlled race between a reversible map and a learned landscape, with every lap and failure recorded.", analogyBreak: "a 2D winner cannot establish which family is superior for images, discrete data, or larger budgets.",
    ideas: ["Match target data, parameters, optimizer steps, and evaluation cases before comparison.", "Use family-appropriate diagnostics without forcing a false single ranking.", "Report direct-sampling speed and chain quality with their exact compute units."],
    worked: "On an eight-mode ring, a coupling flow may cover modes with distorted bridges while a short-chain EBM fits local valleys but misses transitions. The correct report preserves both failure shapes and budget counts.",
    misconception: "Comparing flow NLL with EBM final energy or counting one chain step as one direct sample creates an invalid family ranking.",
    objectives: { primary: "Build verified 2D flow and energy-model pipelines", decision: "Run a matched-target comparison with family-specific and shared evidence" },
    vocabulary: [{ term: "Matched target", meaning: "An identical data-generating distribution and split used by every treatment." }, { term: "Gradient evaluation budget", meaning: "The counted number of target or model gradient calls used by an iterative sampler." }, { term: "Mode occupancy", meaning: "The fraction of samples assigned to each declared target region." }],
    primaryCheck: { prompt: "Why is flow sample count not directly comparable to Langevin transition count?", expected: "A flow draw is one direct base-to-data transform, while a Langevin transition is one correlated update inside a chain; report transformations, gradient calls, burn-in, and effective samples separately.", retry: "Write the primitive operation and statistical unit produced by each sampler before comparing cost." },
    decision: { explanation: "The study should answer a bounded mechanism question, not declare a universal winner.", mechanism: "Predeclare target, budgets, seeds, shared metrics, family-specific diagnostics, and a decision rule; preserve raw samples/chains and explain where metrics are non-comparable.", workedExample: "Ask whether increasing EBM transition budget closes mode occupancy under the same trained energy, while flow architecture and evaluation draws remain fixed.", boundary: "The result is evidence about the chosen 2D target, implementations, and budgets only; scaling and perceptual representation remain open.", check: { prompt: "The EBM has better mode occupancy after 100× more gradient calls. Can it be called more efficient?", expected: "Not from that result. Quality improved under a much larger sampling budget; efficiency requires matched cost or a quality-versus-cost curve.", retry: "Put quality and exact primitive-operation counts in the same table and compare at matched points." } },
    quiz: { question: "Which comparison is valid?", options: ["Mode coverage versus exact primitive-operation budget curves", "Flow NLL versus raw EBM energy", "Best flow seed versus worst EBM seed", "One flow draw versus one Langevin transition"], answer: 0, explanation: "Shared constructs and explicit cost curves avoid mismatched units and cherry-picking." },
    transfer: { prompt: "A report says the EBM wins because energy=−8 is lower than flow NLL=2. What is wrong?", options: [{ text: "Raw energy and normalized NLL are different quantities and scales", feedback: "Correct: the numbers cannot be ranked directly." }, { text: "Negative numbers are always better", feedback: "Sign does not create common measurement units." }, { text: "The flow needs more modes", feedback: "No mode evidence appears in the comparison." }], answer: 0, worked: "Use shared held-out or sample metrics where valid, preserve family-specific diagnostics separately, and never subtract raw energy from NLL.", retry: "Write the mathematical definition and units of each displayed scalar." },
    lab: { title: "Flow-versus-energy comparison auditor", question: "Which result table supports a bounded family comparison?", change: "Inspect raw-score, unmatched-budget, and matched-evidence tables.", observe: "Only one table aligns constructs and compute while preserving family-specific diagnostics.", explain: "Name the mismatch in every rejected table.", complete: "Choose the valid table and write its strongest defensible conclusion.", boundary: "No models run here; the cards audit study design.", controlLabel: "Study table", cases: [{ label: "raw scores", resultLabel: "verdict", resultValue: "INVALID", meter: 100, detail: "Energy and NLL use different normalization and cannot be ranked." }, { label: "unmatched cost", resultLabel: "verdict", resultValue: "CONFOUNDED", meter: 80, detail: "The EBM uses 100× more gradient evaluations for its displayed samples." }, { label: "matched curves", resultLabel: "verdict", resultValue: "DEFENSIBLE", meter: 20, detail: "Quality-versus-cost, all seeds, occupancy, round trip, and autocorrelation remain visible." }] },
    motionConcept: "evaluation",
    code: { title: "Count family-specific sample cost", setup: "A tiny ledger prevents direct samples and chain transitions from sharing a false unit.", predict: "Which method uses more gradient evaluations for 100 retained examples?", code: lines("runs = {", " 'flow': {'samples':100, 'transforms':100, 'gradient_evals':0},", " 'ebm': {'samples':100, 'burn_in':200, 'steps_between':20, 'gradient_evals':2200},", "}", "for name,row in runs.items(): print(name,row)", "assert runs['ebm']['gradient_evals'] > runs['flow']['gradient_evals']"), observe: "The ledger reports operations rather than pretending both sample counts imply equal compute.", tryIt: "Add effective sample size and plot quality per effective sample versus gradient evaluations." },
    sources: ["realNvp", "ebm", "score", "precisionRecall"],
  })),
];
