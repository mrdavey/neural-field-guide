import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { generativeSeed, lines } from "../seed";

const manifest = plannedCourseManifests.generative;

export const generativeLatentSpecs = [
  defineResearchLesson(manifest, generativeSeed({
    id: "autoregressive-generators",
    plain: "An autoregressive generator creates a structured example one ordered variable at a time, conditioning each choice on everything already generated.",
    precise: "For an ordering $x_1,\\ldots,x_D$, the chain rule gives $p_\\theta(x)=\\prod_{i=1}^D p_\\theta(x_i\\mid x_{<i})$. Training can score all positions in parallel under a causal mask, while exact ancestral sampling remains sequential because each sampled value becomes later context. The ordering, discretization, context interface, and stopping rule are part of the model contract.",
    analogy: "It resembles completing a mosaic while covering every tile you have not reached yet.", analogyBreak: "the chosen traversal can privilege some spatial dependencies and slow sampling even when training is parallel.",
    ideas: ["Declare the serialization before constructing causal inputs and targets.", "Use a causal mask so no prediction reads the variable it must predict or any later variable.", "Evaluate both log-likelihood and order-sensitive sampling cost or failure."],
    worked: "A 2×2 binary image serialized [top-left, top-right, bottom-left, bottom-right] contributes four factors. When predicting bottom-left, only the two top pixels may be visible; changing bottom-right must not change that earlier logit.",
    misconception: "Autoregressive does not mean text-only or recurrent-network-only; it describes a probability factorization that Transformers, convolutions, or tables can implement.",
    objectives: { primary: "Construct and test an autoregressive factorization for a tiny image", decision: "Diagnose ordering and causal-leakage trade-offs" },
    vocabulary: [{ term: "Serialization", meaning: "A deterministic ordering that maps a structured object into a prediction sequence." }, { term: "Causal mask", meaning: "A visibility rule that prevents a position from reading future target variables." }, { term: "Ancestral sampling", meaning: "Sampling each conditional factor in dependency order and feeding its value forward." }, { term: "Inductive bias", meaning: "A structural preference that makes some relationships easier for a model to learn than others." }, { term: "Dependency distance", meaning: "The number of sequence positions separating variables whose relationship the model must capture." }],
    primaryCheck: { prompt: "In a row-major 2×2 image, which positions may the bottom-left prediction condition on?", expected: "It may use top-left and top-right only; bottom-left is the current target and bottom-right is future context.", retry: "Number the cells 1–4 in row-major order and reveal only indices strictly less than 3." },
    decision: { explanation: "Ordering is an inductive bias and systems choice, not a harmless formatting detail.", mechanism: "Compare candidate orderings by dependency distance, causal correctness, likelihood under the same representation, sampling steps, and changed-layout failures while holding model/data budgets fixed.", workedExample: "In a 4×4 image, row-major places vertical neighbors four sequence positions apart; a locality-preserving curve can shorten many neighbor distances, but both still require 16 sequential ancestral draws and both must pass the causal-prefix test.", boundary: "A better tiny-image ordering does not imply the same ranking at another resolution, architecture, or data distribution.", check: { prompt: "Row-major has mean neighbor dependency distance 2.5; a space-filling order has 1.4. Both pass causal-prefix tests and both need 16 sample steps. Which ordering does this evidence favor, and what trade-offs remain?", expected: "It favors the space-filling order for local dependency distance only. Causal validity and sampling steps tie; likelihood, decoding complexity, positional representation, and changed-layout performance still require matched evidence.", retry: "Make one comparison row each for dependency distance, sample order or steps, causal validity, likelihood, and implementation complexity; do not collapse unmatched fields into one verdict." } },
    quiz: { question: "Why can autoregressive training be parallel while exact sampling is sequential?", options: ["Training already has all target prefixes under a mask", "Sampling has no parameters", "Softmax disables batches", "Images cannot be parallel"], answer: 0, explanation: "Teacher-forced training exposes every true prefix at once, but generation must first sample each unknown prefix value." },
    transfer: { prompt: "Two length-four teacher-forced examples share $x_1,x_2,x_3$ but differ at target $x_4$. With shifted inputs $[BOS,x_1,x_2,x_3]$, which training logits must match?", options: [{ text: "Logits for positions one through four", feedback: "Correct: both shifted input sequences are identical, so every logit must match; only the fourth target label differs." }, { text: "Only logits one through three", feedback: "Position four reads $x_1,x_2,x_3$, not target $x_4$, so it must also remain invariant." }, { text: "None, because all targets are visible", feedback: "Targets are supplied to the loss, not embedded at their own prediction positions." }], answer: 0, worked: "Construct both shifted inputs and assert they are identical. Run the model in evaluation mode and assert all four logits match; any mismatch means the future target entered through an illegal path.", retry: "Write input position 4 explicitly: it contains $x_3$, while $x_4$ appears only in the target array." },
    lab: { title: "Serialization and mask probe", question: "Which spatial neighbor becomes visible before the target under each serialization?", change: "Select row-major, column-major, or a deliberately leaked layout.", observe: "The prefix and leakage verdict change while the same four pixels remain.", explain: "Map physical cells to sequence indices before reasoning about available context.", complete: "Trace all three cases and identify the one invalid factorization implementation.", boundary: "This is a four-variable dependency fixture, not a trained image model.", controlLabel: "Serialization", cases: [{ label: "row-major", resultLabel: "target 3 sees", resultValue: "TOP ROW", meter: 60, detail: "Indices 1 and 2 are both top-row pixels before bottom-left at index 3." }, { label: "column-major", resultLabel: "target 3 sees", resultValue: "LEFT COLUMN", meter: 60, detail: "Indices 1 and 2 are left-column pixels before top-right at index 3." }, { label: "leaked", resultLabel: "causal check", resultValue: "FAIL", meter: 100, detail: "The current target is copied into its own input, allowing trivial training loss." }] },
    motionConcept: "training",
    code: { title: "Build and test causal image pairs", setup: "Two flattened images differ only in the final target; shifting must remove that difference from every model input.", predict: "Should any of the four teacher-forced logits change?", code: lines("a = [1,0,1,1]", "b = [1,0,1,0]  # only future target x4 changes", "bos = -1", "inputs_a, inputs_b = [bos]+a[:-1], [bos]+b[:-1]", "targets_a, targets_b = a, b", "assert inputs_a == inputs_b == [-1,1,0,1]", "assert targets_a[:3] == targets_b[:3] and targets_a[3] != targets_b[3]", "# In evaluation mode: assert_allclose(model(inputs_a), model(inputs_b))", "print(list(zip(inputs_a, targets_a)))"), observe: "All four shifted inputs are identical; the fourth logit may use x1–x3 but cannot read the differing x4 target.", tryIt: "Serialize column-major, rebuild the shift, and repeat the all-logit prefix-invariance assertion." },
    sources: ["deepLearning"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "latent-variable-models",
    plain: "A latent-variable model explains an observation using an unobserved variable that captures hidden causes or compact factors.",
    precise: "The joint model factors as $p_\\theta(x,z)=p(z)p_\\theta(x\\mid z)$ and the marginal data density is $p_\\theta(x)=\\int p(z)p_\\theta(x\\mid z)dz$. Generation samples $z$ from the prior then $x$ from the decoder. Inference reverses the question using posterior $p_\\theta(z\\mid x)$, which is often intractable and motivates an approximation.",
    analogy: "A latent is an unseen recipe choice that explains why the finished dish has its observed properties.", analogyBreak: "the model may invent entangled or non-identifiable causes that do not correspond to human concepts.",
    ideas: ["Separate the generative direction prior→latent→observation from posterior inference.", "Marginalize hidden alternatives rather than selecting one explanation prematurely.", "Intervene on latent values to test what the decoder actually uses."],
    worked: "A binary latent z chooses fair coin A or biased coin B with prior [0.5,0.5]. If heads likelihoods are [0.5,0.9], marginal P(heads)=0.7 and posterior P(B|heads)=0.45/0.7≈0.643.",
    misconception: "A low-dimensional latent is not automatically disentangled, causal, identifiable, or semantically named.",
    objectives: { primary: "Trace prior, likelihood, marginal, and posterior in a finite latent model", decision: "Test whether a learned latent is used and what claim its geometry supports" },
    vocabulary: [{ term: "Latent variable", meaning: "An unobserved modeled variable that helps generate or explain observations." }, { term: "Marginal likelihood", meaning: "Observation probability after summing or integrating over possible latent values." }, { term: "Posterior", meaning: "A distribution over latent values after conditioning on an observation." }],
    primaryCheck: { prompt: "With equal latent priors and heads likelihoods 0.5 and 0.9, compute P(B|heads).", expected: "Joint mass for B and heads is 0.45; total heads mass is 0.25+0.45=0.70, so posterior is 0.643.", retry: "Compute prior times likelihood for each latent, then normalize the two joint masses." },
    decision: { explanation: "Latent interpretation needs intervention and changed-case evidence, not a visually convenient projection.", mechanism: "Hold decoder context fixed, vary one latent coordinate or sample, measure output changes and collateral effects, repeat across inputs, and report non-identifiability alternatives.", workedExample: "If swapping latent z changes background and object together, the coordinate is used but not evidence for a pure object factor.", boundary: "Equivalent latent rotations can produce the same observation distribution, so a unique semantic coordinate may be unidentifiable.", check: { prompt: "Two models have identical observation likelihood but rotated latent coordinates. Which has the true semantic axes?", expected: "The likelihood evidence cannot choose; without additional inductive assumptions or interventions, both parameterizations may be observationally equivalent.", retry: "Ask which observable prediction differs under the claimed semantic interpretation; if none does, the claim is not identified." } },
    quiz: { question: "How does a latent generator produce x?", options: ["Sample z from the prior, then sample x conditioned on z", "Infer z only after x exists", "Normalize x with softmax only", "Choose the lowest-loss training row"], answer: 0, explanation: "Generation follows the joint model from prior latent to conditional observation." },
    transfer: { prompt: "Changing z never changes decoder output. What is the strongest diagnosis?", options: [{ text: "The decoder is ignoring z under the tested conditions", feedback: "Correct: the latent-use intervention found no effect in scope." }, { text: "z is perfectly disentangled", feedback: "No-output effect supplies no semantic decomposition evidence." }, { text: "The posterior is calibrated", feedback: "Calibration was not evaluated by this intervention or result." }], answer: 0, worked: "Record the zero-effect intervention, then inspect decoder paths, posterior variation, and objective weighting before naming collapse.", retry: "Hold all other inputs fixed and verify that materially different z values reach the decoder." },
    lab: { title: "Finite latent inference", question: "How does one observation reweight hidden explanations?", change: "Select an observation likelihood profile and inspect the normalized posterior.", observe: "Posterior mass moves according to prior×likelihood rather than likelihood alone.", explain: "Name the joint masses before the normalization denominator.", complete: "Trace all cases and explain why an uninformative observation leaves the prior unchanged.", boundary: "A two-latent Bayes table demonstrates inference arithmetic, not neural representation quality.", controlLabel: "Observation evidence", cases: [{ label: "favors A", resultLabel: "P(A|x)", resultValue: "0.80", meter: 80, detail: "Equal priors times likelihoods [0.8,0.2] normalize directly to [0.8,0.2]." }, { label: "uninformative", resultLabel: "posterior", resultValue: "[.5,.5]", meter: 50, detail: "Equal likelihoods supply no evidence, so the posterior remains the prior." }, { label: "favors B", resultLabel: "P(B|x)", resultValue: "0.90", meter: 90, detail: "Likelihood ratio 1:9 with equal priors moves 90% posterior mass to B." }] },
    motionConcept: "distribution",
    code: { title: "Infer a finite latent posterior", setup: "The complete table computes joint and normalized posterior masses.", predict: "Does heads favor latent B?", code: lines("prior = [0.5,0.5]", "likelihood = [0.5,0.9]", "joint = [p*l for p,l in zip(prior, likelihood)]", "posterior = [v/sum(joint) for v in joint]", "print([round(v,3) for v in posterior])"), observe: "The posterior is approximately [0.357,0.643], so heads favors B without making it certain.", tryIt: "Change prior to [0.9,0.1] and explain how prior evidence competes with likelihood." },
    sources: ["vae", "deepLearning"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "amortized-inference-elbo",
    plain: "Amortized inference trains one encoder to quickly approximate a posterior for many observations; the ELBO balances reconstruction with closeness to a prior.",
    precise: "With approximation $q_\\phi(z\\mid x)$, the evidence lower bound is $\\mathbb E_q[\\log p_\\theta(x\\mid z)]-D_{KL}(q_\\phi(z\\mid x)\\|p(z))\\le\\log p_\\theta(x)$. The gap equals $D_{KL}(q_\\phi(z\\mid x)\\|p_\\theta(z\\mid x))$. Reparameterization writes $z=\\mu_\\phi(x)+\\sigma_\\phi(x)\\odot\\epsilon$ with $\\epsilon\\sim N(0,I)$ so gradients reach encoder parameters through samples.",
    analogy: "The encoder is a clerk who learns to propose likely hidden explanations without solving a fresh search for every case.", analogyBreak: "fast proposals can be systematically biased, and a tight bound is not guaranteed by fluent reconstructions.",
    ideas: ["Keep reconstruction and KL terms separate in the training ledger.", "Use reparameterization to expose randomness while retaining a differentiable path.", "Interpret the ELBO as a bound with an approximation gap, not exact evidence."],
    worked: "If expected reconstruction log-likelihood is −80 nats and KL(q||p)=5 nats, ELBO is −85 nats and negative ELBO loss is 85. Raising KL weight to 2 would optimize 90, a different objective rather than a tighter estimate.",
    misconception: "The ELBO is not the exact log-likelihood unless the approximate posterior equals the true posterior; changing term weights changes the objective's probabilistic interpretation.",
    objectives: { primary: "Compute ELBO terms and trace reparameterized gradients", decision: "Diagnose inference-gap and objective-weighting claims" },
    vocabulary: [{ term: "Amortized inference", meaning: "Using a shared learned function to predict approximate posterior parameters for many observations." }, { term: "ELBO", meaning: "A computable lower bound on log evidence formed from expected reconstruction log-likelihood minus KL." }, { term: "Reparameterization", meaning: "Expressing a random sample as differentiable parameters applied to parameter-free noise." }, { term: "Inference gap", meaning: "The difference between log evidence and the ELBO, equal to the KL from approximate to true posterior." }, { term: "Importance sampling", meaning: "Estimating a target expectation with weighted samples drawn from a proposal distribution." }],
    primaryCheck: { prompt: "Let $z=\\mu+\\sigma\\epsilon$, decoder output $y=2z$, squared loss $L=y^2$, with $\\mu=1$, $\\sigma=0.5$, and fixed $\\epsilon=-2$. Compute z, L, and $\\partial L/\\partial\\mu$; state the gradient path.", expected: "$z=1+0.5(-2)=0$, so $y=0$, $L=0$, and $\\partial L/\\partial\\mu=2y\\times2\\times1=0$. The path is $L\\to y\\to z\\to\\mu$ while epsilon is parameter-free noise. With epsilon=0 instead, z=1 and the same path gives gradient 8.", retry: "Write the arrows $\\mu\\to z\\to y\\to L$, calculate each local derivative, then multiply them; epsilon is an input to z, not a parameter blocking the path." },
    decision: { explanation: "Term movement must be interpreted causally; one improving scalar can hide worse inference or latent use.", mechanism: "Log reconstruction, KL by dimension/example, posterior statistics, latent-use interventions, and an importance-sampling estimate when feasible; vary one weight under matched data and compute.", workedExample: "A beta increase lowers KL and worsens reconstruction; calling the new total lower is meaningless unless totals use the same beta and the desired trade-off is declared.", boundary: "A tighter bound at course scale does not imply better perceptual samples or identifiable latent factors.", check: { prompt: "Two runs use KL weights 1 and 4. Can their total training losses be ranked as ELBO values?", expected: "Only beta=1 preserves the standard ELBO form; the weighted objectives can be compared as declared training criteria, not directly as the same evidence bound.", retry: "Expand each scalar into reconstruction plus coefficient×KL and compare like terms before totals." } },
    quiz: { question: "What does reparameterization achieve?", options: ["It removes all randomness", "It separates the random draw from the adjustable transformation", "It makes the approximation equal the true posterior", "It normalizes every dataset"], answer: 1, explanation: "Draw parameter-free noise first, then transform it with the model's adjustable location and scale. That keeps a usable learning path while preserving randomness; the exact expression is optional technical depth." },
    transfer: { prompt: "A report calls beta=4 loss an exact negative log-likelihood. What correction is required?", options: [{ text: "Call it a weighted variational objective and state the changed probabilistic interpretation", feedback: "Correct: beta changes the standard ELBO balance." }, { text: "Multiply samples by four", feedback: "Sample scaling does not repair the evidence claim." }, { text: "Remove the reconstruction term", feedback: "That would define a different and incomplete objective." }], answer: 0, worked: "Report each term and beta explicitly; reserve exact likelihood claims for a valid estimator with its assumptions.", retry: "Compare the displayed formula term by term with the beta=1 ELBO identity." },
    lab: { title: "ELBO term ledger", question: "How does changing KL weight alter the optimized objective and the claim it supports?", change: "Select beta and inspect the reconstruction, KL, and total ledger.", observe: "The total changes even when the underlying term measurements stay fixed.", explain: "Distinguish reweighting an objective from observing a tighter evidence bound.", complete: "Explain all three beta cases and identify which is the standard ELBO.", boundary: "Fixed terms illustrate bookkeeping; no encoder or decoder is trained in the browser.", controlLabel: "KL weight β", cases: [{ label: "β=0", resultLabel: "loss", resultValue: "80", meter: 40, detail: "Only reconstruction is optimized; prior matching disappears from this objective." }, { label: "β=1", resultLabel: "negative ELBO", resultValue: "85", meter: 55, detail: "Reconstruction 80 plus KL 5 is the standard negative ELBO ledger." }, { label: "β=4", resultLabel: "weighted loss", resultValue: "100", meter: 90, detail: "The stronger KL coefficient defines a beta-VAE-style objective, not the same ELBO value." }] },
    motionConcept: "training",
    code: { title: "Trace ELBO and reparameterization", setup: "The fixture exposes deterministic epsilon so the sample and term ledger are inspectable.", predict: "What z results from mu=1, sigma=.5, epsilon=−2?", code: lines("mu, sigma, epsilon = 1.0, 0.5, -2.0", "z = mu + sigma*epsilon", "reconstruction_nll, kl = 80.0, 5.0", "negative_elbo = reconstruction_nll + kl", "print(z, negative_elbo)"), observe: "z is 0 and negative ELBO is 85 in the fixed ledger.", tryIt: "Sweep epsilon while leaving loss terms fixed, then explain why a real Monte Carlo estimator varies." },
    sources: ["vae"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "vae-posterior-collapse",
    plain: "Posterior collapse occurs when a VAE's approximate posterior stays near the prior and the decoder learns to ignore the latent variable.",
    precise: "A powerful decoder can model $p(x)$ without information from $z$, making $q_\\phi(z\\mid x)\\approx p(z)$ and KL near zero. Diagnose with per-example and per-dimension KL, active units, mutual-information proxies, posterior variation, and interventions on z. KL annealing, free bits, weaker decoders, changed conditioning, or objective redesign can alter the trade-off but require matched evaluation.",
    analogy: "The encoder keeps writing notes, but the decoder has learned to finish the task without reading them.", analogyBreak: "a small KL can also be appropriate for simple data; collapse is a mechanism diagnosis, not a threshold alone.",
    ideas: ["Treat low KL as a clue and test whether decoder output responds to z.", "Inspect distributions across examples and dimensions rather than one mean.", "Evaluate interventions under matched reconstruction and sampling contracts."],
    worked: "Run A has reconstruction NLL 40 and KL 0.01; swapping z across inputs changes no output. Run B has NLL 42 and KL 3; swaps change class while preserving style. Only the intervention shows how the latent is used.",
    misconception: "Maximizing KL or counting active units is not the goal; the latent must carry useful information without destroying the declared generative objective.",
    objectives: { primary: "Diagnose posterior collapse with term and intervention evidence", decision: "Choose and evaluate one collapse mitigation under matched conditions" },
    vocabulary: [{ term: "Posterior collapse", meaning: "A failure where the approximate posterior resembles the prior and the decoder makes little use of z." }, { term: "Active unit", meaning: "A latent dimension whose posterior statistics vary meaningfully across data under a declared threshold." }, { term: "Free bits", meaning: "An objective modification that does not penalize a small amount of KL capacity in the usual way." }],
    primaryCheck: { prompt: "KL is near zero and swapping z leaves reconstruction unchanged. What joint diagnosis is supported?", expected: "The posterior carries little input-specific information and the decoder ignores z under the tested intervention, supporting posterior collapse in scope.", retry: "Use both evidence channels: posterior-to-prior proximity and a causal latent-use intervention." },
    decision: { explanation: "A mitigation succeeds only if latent use improves without an unacceptable cost elsewhere.", mechanism: "Branch from one checkpoint, change only the mitigation, match steps/seeds, and compare reconstruction, KL distribution, active units, prior samples, latent swaps, and held-out likelihood estimates.", workedExample: "KL annealing increases active units but worsens prior samples; the report should preserve both effects instead of declaring collapse solved from KL alone.", boundary: "Course-scale VAE evidence cannot establish universal disentanglement or the best mitigation for larger decoders.", check: { prompt: "Free bits raises KL but prior samples worsen sharply. Is the mitigation an unqualified success?", expected: "No. It changed latent use but failed another required generative criterion; report the trade-off and revisit the decision rule.", retry: "Return to the predeclared metrics and require the mitigation to pass both latent-use and sample/reconstruction gates." } },
    quiz: { question: "Which is the strongest collapse diagnostic?", options: ["KL near zero plus decoder invariance to latent interventions", "One sharp reconstruction", "A large latent width", "A low learning rate"], answer: 0, explanation: "Term evidence and intervention together directly test posterior proximity and decoder use." },
    transfer: { prompt: "KL rises after annealing, but every latent swap still leaves output unchanged. What can you conclude?", options: [{ text: "The objective ledger changed, but useful decoder dependence on z is not demonstrated", feedback: "Correct: intervention evidence still fails." }, { text: "Collapse is solved because KL is nonzero", feedback: "Nonzero KL alone does not establish useful latent information." }, { text: "The model is disentangled", feedback: "No semantic factor test was performed." }], answer: 0, worked: "Inspect posterior variation and trace the decoder path; keep the mitigation claim narrow until z interventions have repeatable effects.", retry: "Ask which observable output changes when only z changes." },
    lab: { title: "Collapse diagnostic panel", question: "Which evidence combination distinguishes low KL from decoder-level collapse?", change: "Select a run card and compare KL with latent-swap sensitivity.", observe: "The diagnosis depends on both posterior statistics and output intervention.", explain: "Name what each channel measures before combining them.", complete: "Classify all three runs and avoid calling high KL automatically healthy.", boundary: "The cards are deterministic diagnostic fixtures, not trained VAE results.", controlLabel: "Run card", cases: [{ label: "KL .01 / no effect", resultLabel: "diagnosis", resultValue: "COLLAPSE", meter: 95, detail: "Posterior matches prior and decoder output is invariant to z swaps." }, { label: "KL 3 / effect", resultLabel: "diagnosis", resultValue: "LATENT USED", meter: 70, detail: "Posterior varies and decoder responds; usefulness still needs task and sample checks." }, { label: "KL 18 / poor recon", resultLabel: "diagnosis", resultValue: "OVER-COSTLY", meter: 85, detail: "High latent information does not compensate for failed reconstruction and prior-sample gates." }] },
    motionConcept: "evaluation",
    code: { title: "Compute active latent dimensions", setup: "Posterior means across four examples expose which coordinates vary.", predict: "Which coordinate crosses variance threshold .05?", code: lines("from statistics import pvariance", "means = [[0.0,0.1],[0.0,0.4],[0.0,-0.3],[0.0,0.2]]", "columns = list(zip(*means))", "variance = [pvariance(c) for c in columns]", "active = [i for i,v in enumerate(variance) if v > 0.05]", "print(variance, active)"), observe: "Only coordinate 1 is active under this declared threshold.", tryIt: "Raise the threshold and explain why active-unit counts require a reported convention." },
    sources: ["vae"],
  })),
  defineResearchLesson(manifest, generativeSeed({
    id: "latent-models-capstone", duration: 36,
    plain: "The latent-generator capstone builds a minimal VAE whose encoder, reparameterized sample, decoder, losses, tests, and collapse probes are all inspectable.",
    precise: "The system trains on a bounded dataset with explicit train/validation split, seeded loader, diagonal-Gaussian encoder, reparameterization, Bernoulli or Gaussian observation model, and prior sampling. Required checks cover shapes, finite losses, one-batch overfit, checkpoint resume, posterior variation, latent swaps, and matched beta or annealing ablation.",
    analogy: "It is a transparent laboratory model with every pipe labeled and sampled material preserved.", analogyBreak: "a small VAE validates implementation and mechanisms but does not reproduce high-resolution generative modeling.",
    ideas: ["Prove correctness on one batch before interpreting prior samples.", "Preserve reconstruction, KL, posterior statistics, and interventions separately.", "Run one matched collapse intervention and report negative or mixed results."],
    worked: "A 2D latent VAE over 8×8 binary shapes overfits 32 cases, resumes exactly in deterministic mode, then compares beta=1 with KL annealing across three seeds using reconstruction, KL, active units, and latent-swap effects.",
    misconception: "A smooth latent interpolation grid is not enough to establish likelihood, coverage, disentanglement, or resistance to posterior collapse.",
    objectives: { primary: "Build and verify an end-to-end minimal VAE from scratch", decision: "Conduct a matched posterior-collapse intervention with preserved evidence" },
    vocabulary: [{ term: "Observation model", meaning: "The decoder distribution that assigns likelihood to x given z." }, { term: "Prior sample", meaning: "A generated observation obtained from z drawn from the declared prior rather than the encoder." }, { term: "Collapse probe", meaning: "A measurement or intervention designed to test whether posterior information reaches the decoder." }],
    primaryCheck: { prompt: "Why should a VAE overfit one tiny batch before a longer run?", expected: "It cheaply verifies that data, encoder, sampling path, decoder, loss signs, gradients, and optimizer can jointly reduce the objective.", retry: "List each component between input and update that a failed micro-overfit would keep suspect." },
    decision: { explanation: "The original experiment begins after the baseline is reproduced and its correctness checks pass.", mechanism: "Branch the same initialization into baseline and one mitigation, hold data/steps/seeds/evaluation fixed, preserve raw per-seed terms and samples, and use a predeclared rule combining latent use with reconstruction or held-out evidence.", workedExample: "Annealing counts as promising only if latent-swap effects and active units improve without crossing a predeclared reconstruction regression limit on most seeds.", boundary: "The conclusion applies to the bounded architecture, dataset, seed set, and budget; it is not a universal VAE recipe.", check: { prompt: "The mitigation wins on active units but loses the reconstruction gate on two of three seeds. What conclusion follows?", expected: "It does not meet the predeclared success rule; report a mixed or negative result and preserve the failure rows.", retry: "Apply the decision rule to each seed before averaging and do not change the gate after seeing outputs." } },
    quiz: { question: "What must happen before interpreting a VAE ablation?", options: ["Baseline shape, overfit, loss, and resume checks pass", "A sample looks smooth", "The latent width is large", "The best seed is selected"], answer: 0, explanation: "Correctness and reproducibility gates prevent implementation failures from masquerading as research findings." },
    transfer: { prompt: "One mitigation sample grid looks better, but held-out terms and seed rows are missing. What is the evidence status?", options: [{ text: "A qualitative observation requiring the planned measurements before a comparative claim", feedback: "Correct: preserve the image but do not promote it into a result." }, { text: "A proven improvement", feedback: "No matched, repeated evaluation supports that claim." }, { text: "A failed experiment", feedback: "The evidence is incomplete rather than necessarily negative." }], answer: 0, worked: "Run the pinned seed set, preserve term ledgers and probes, then apply the predeclared multi-metric decision rule.", retry: "Separate an observed example from an estimate over cases and seeds." },
    lab: { title: "VAE release gate", question: "Which evidence package is sufficient to begin the capstone ablation?", change: "Inspect implementation, baseline, and comparison readiness cards.", observe: "The gate advances only when earlier correctness evidence is complete.", explain: "Connect each missing artifact to the confound it prevents.", complete: "Order all three gates and state why a pretty sample cannot skip them.", boundary: "This checklist does not execute training or certify a model's quality.", controlLabel: "Evidence package", cases: [{ label: "shape + overfit", resultLabel: "status", resultValue: "BASELINE DEBUG", meter: 35, detail: "Core paths work, but held-out and resume evidence are still needed." }, { label: "baseline complete", resultLabel: "status", resultValue: "READY TO BRANCH", meter: 70, detail: "Correctness, validation, seeds, checkpoint, and probes are pinned before treatment." }, { label: "matched ablation", resultLabel: "status", resultValue: "DECIDE", meter: 100, detail: "All seed rows and predeclared gates are available for a bounded conclusion." }] },
    motionConcept: "pipeline",
    code: { title: "Define the VAE step contract", setup: "Pseudocode keeps encoder, random source, decoder, and term ownership explicit.", predict: "Which values must enter the checkpoint for exact resume?", code: lines("mu, logvar = encoder(x)", "epsilon = rng.normal_like(mu)", "z = mu + exp(0.5*logvar)*epsilon", "reconstruction = decoder(z)", "recon_nll = observation_nll(x, reconstruction)", "kl = gaussian_kl(mu, logvar)", "loss = mean(recon_nll + beta*kl)", "loss.backward(); optimizer.step()", "save(model, optimizer, scheduler, rng, config, data_cursor)"), observe: "The step separates measured terms and saves every state that controls continuation.", tryIt: "Remove RNG state from the checkpoint and predict which exact-resume comparison fails.", mode: "pseudocode" },
    sources: ["vae", "reproducibility", "pytorch"],
  })),
];
