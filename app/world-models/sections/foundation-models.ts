import { defineWorldModelLesson } from "../helpers";
import { wmSources } from "../sources";

export const worldModelFoundationModelSpecs = [
  defineWorldModelLesson({
    id: "video-tokenization", track: "wm-foundation-models", title: "Video as Sequential Data and Spatiotemporal Tokens", number: 29,
    plain: "Video world models compress frames or space-time patches into tokens so they can predict long visual sequences with manageable compute.",
    precise: "A video tensor $x[B,T,H,W,C]$ can be encoded into continuous features or discrete tokens $u[B,T',N]$, where $T'$ reflects temporal stride and $N$ spatial tokens per step. A tokenizer trades reconstruction fidelity, sequence length, temporal resolution, and codebook capacity. Causal dynamics predict later tokens from earlier tokens plus actions or conditioning; token boundaries determine what motion and small objects remain observable.",
    analogy: "It is turning a film into a compact storyboard of coded tiles.", analogyBreak: "tiles can hide sub-frame events, 3D geometry, identity, or exact action timing even when decoded video looks plausible.",
    ideas: ["Trace frame rate, spatial resolution, patch/stride, codebook, and resulting token count.", "Align action timing with the video token interval it controls.", "Audit reconstruction, motion, identity, and small-object failures separately."],
    worked: "A 16-frame $64\\times64$ clip uses $8\\times8$ patches and temporal stride 2: $T'=8$, $N=64$, so 512 visual tokens represent the clip. Doubling spatial resolution at the same patch size quadruples $N$ and attention cost pressure.",
    misconception: "A video token is not an object, action, or physical state by definition; it is a learned compression unit.",
    quiz: { question: "At fixed patch size, doubling image height and width changes spatial token count by about what factor?", options: ["2×", "4×", "No change", "8×"], answer: 1, explanation: "Both spatial axes double, so the patch grid contains four times as many tokens." }, lab: "wm-video", prerequisites: ["dyna-tdmpc-case-study"], sources: [wmSources.genie, wmSources.jepa],
    objectives: ["Compute spatiotemporal token counts from video shape and stride", "Audit a tokenizer for motion and control information lost at its boundaries"],
    vocabulary: [{ term: "Spatiotemporal token", meaning: "A compact code representing a region across space, time, or both." }, { term: "Temporal stride", meaning: "How many source frames separate encoded time steps." }, { term: "Codebook", meaning: "A finite collection of learned discrete latent codes." }],
    stages: ["Patch video", "Encode tokens", "Align time"], checkpoints: ["Write source frames, resolution, patch, and stride.", "Calculate tokens per encoded step and total sequence.", "Map each action/sensor event to the represented interval."],
    primaryCheck: { prompt: "How many tokens represent 12 frames of $32\\times32$ video with $8\\times8$ patches and temporal stride 3?", expected: "$T'=12/3=4$ encoded steps and $N=(32/8)^2=16$ tokens per step, for 64 visual tokens total.", retry: "Calculate temporal steps and spatial patches independently, then multiply." },
    decision: { explanation: "Tokenizer quality depends on which downstream distinctions survive compression, not reconstruction alone.", mechanism: "Use controlled clips varying one factor—speed, direction, contact, small object, identity—then measure token changes, reconstruction, future prediction, and action consequence.", workedExample: "Stride 4 reconstructs backgrounds well but misses a one-frame button press that changes the door. Stride 2 costs twice the temporal tokens and preserves the event; control evidence selects stride 2.", boundary: "A finer tokenizer increases cost and still may not identify hidden 3D or causal variables.", check: { prompt: "Two tokenizers have equal MSE; one preserves collision timing and one does not. Which is suitable for control?", expected: "The collision-preserving tokenizer, because event timing changes action consequences. Equal average reconstruction does not override the control slice.", retry: "List the event whose timing changes the planned action and inspect whether its token sequence differs." } },
    transfer: { prompt: "Which metric should block deployment for a contact task?", options: [{ text: "Slightly worse background PSNR", feedback: "Background detail may be nuisance." }, { text: "Missed short contact events that reverse action outcomes", feedback: "Correct: it removes a causal control distinction." }, { text: "Larger codebook metadata", feedback: "Cost matters but does not by itself prove incorrect state." }], answer: 1, worked: "Gate contact/event recall, then compare cost and reconstruction among passing tokenizers.", retry: "Apply task-critical gates before cosmetic averages." },
    motion: { concept: "segmentation", headline: "Video tokenization divides both space and time into learned prediction units.", intro: "A clip separates into temporal slices and spatial tiles, then action markers reveal when stride erases or preserves a short event.", labels: ["FRAMES", "PATCH + STRIDE", "TOKENS", "ACTION ALIGN"], titles: ["Begin with exact frame rate and resolution.", "Choose spatial and temporal boundaries.", "Encode each region into compact codes.", "Attach controls to the interval they affect."] },
  }),
  defineWorldModelLesson({
    id: "autoregressive-diffusion-dynamics", track: "wm-foundation-models", title: "Autoregressive and Diffusion Video Dynamics", number: 30,
    plain: "Autoregressive models extend a world one token or block at a time; diffusion models iteratively denoise a future sample under context and controls.",
    precise: "Autoregressive dynamics factorize $p(u_{1:n})=\\prod_i p(u_i\\mid u_{<i},c)$, offering explicit likelihood-style training and sequential generation. Diffusion learns to reverse a noise process for future video or latent blocks, often generating many dimensions in parallel per denoising step. Both require temporal conditioning, action control, sampling policy, and long-horizon consistency tests; architecture alone does not decide physical fidelity.",
    analogy: "One method writes a storyboard tile by tile; the other reveals a noisy future through repeated refinements.", analogyBreak: "both may create plausible but inconsistent worlds, and sampling speed depends on implementation details beyond the family name.",
    ideas: ["Trace the factorization or noise/denoise process and its conditioning inputs.", "Count sequential token steps or denoising evaluations under matched output contracts.", "Compare controllability, diversity, consistency, and latency rather than one visual sample."],
    worked: "Generating 8 future frames with 64 tokens each requires 512 token decisions in a fully token-autoregressive scheme. A diffusion model may update an entire future block for 30 denoising steps. These are different compute units and cannot be compared by 'steps' alone.",
    misconception: "Diffusion is not automatically noncausal or uncontrollable, and autoregression is not automatically more consistent; conditioning and training determine the contract.",
    quiz: { question: "What must be matched before comparing AR token steps with diffusion steps?", options: ["Only method name", "Output size/quality, model evaluations, hardware, and conditioning", "Publication year", "Random color palette"], answer: 1, explanation: "A token decision and a denoising evaluation do different work." }, lab: "wm-video", prerequisites: ["video-tokenization"], sources: [wmSources.genie, wmSources.videoDiffusion, wmSources.genie3],
    objectives: ["Trace autoregressive and diffusion generation processes for the same future block", "Design a matched evaluation of speed, diversity, control, and long-horizon consistency"],
    vocabulary: [{ term: "Autoregressive", meaning: "Factorizing a joint sequence into ordered conditional predictions." }, { term: "Diffusion", meaning: "Learning an iterative reverse process that turns noise into a structured sample." }, { term: "Sampling schedule", meaning: "The number and arrangement of generation or denoising updates used at inference." }],
    stages: ["Condition", "Generate", "Measure horizon"], checkpoints: ["List context, action, text, and randomness inputs.", "Count actual model evaluations and sequential dependencies.", "Score identity, geometry, action response, diversity, and latency by horizon."],
    primaryCheck: { prompt: "For the same two-token future block $[u_1,u_2]$ under context $c$, trace both generation paths and state what is available at every update.", expected: "Autoregression first samples $u_1\\sim p(u_1\\mid c)$, then samples $u_2\\sim p(u_2\\mid u_1,c)$, so the second decision consumes the first generated token. Diffusion starts the entire two-token block at noise $x_K$, then repeatedly applies a context-conditioned denoiser $x_K\\rightarrow x_{K-1}\\rightarrow\\cdots\\rightarrow x_0$ until $x_0$ represents both tokens. Compare model evaluations/latency and output quality, not the raw counts of token decisions versus denoising steps.", retry: "Draw two lanes for the identical output block: reveal ordered tokens left-to-right in one, and revise the whole noisy block at each scheduled noise level in the other." },
    decision: { explanation: "A model family is chosen from measured workload trade-offs and control evidence.", mechanism: "Freeze prompts/starts/actions, output resolution/horizon, hardware and quality gates; sample multiple seeds; report p50/p95 latency, memory, control adherence, diversity, and consistency failures.", workedExample: "AR is slower but preserves avatar identity for 30 s; diffusion is faster but action lag breaks control after 8 s. A real-time controller chooses only a model that meets both latency and action gates.", boundary: "Results at one sampler schedule and checkpoint do not generalize to every model in that family.", check: { prompt: "Diffusion has lower latency but misses steering commands 12% of the time. The control gate is 1%. Decide.", expected: "Reject it for that controller despite latency; either repair control conditioning or narrow use to noninteractive generation.", retry: "Apply non-compensable control fidelity before ranking speed." } },
    transfer: { prompt: "Which conclusion is valid?", options: [{ text: "Diffusion is always faster", feedback: "Implementation, schedule, and output differ." }, { text: "At the tested schedule and hardware, this diffusion checkpoint met latency but failed action fidelity", feedback: "Correct: conditions and boundary are explicit." }, { text: "Autoregression guarantees physics", feedback: "Factorization does not guarantee correct dynamics." }], answer: 1, worked: "Report checkpoint, sampler, hardware, evaluation set, and failures with each comparison.", retry: "Replace family-wide claims with the exact tested system and protocol." },
    motion: { concept: "decoding", headline: "Two generators allocate sequential computation differently.", intro: "One branch extends ordered tokens; the other revises a noisy future block, then both meet at the same controllability and horizon tests.", labels: ["CONTEXT", "AR PATH", "DENOISE PATH", "VERIFY"], titles: ["Fix history, actions, conditioning, and seed policy.", "Predict ordered future tokens or blocks.", "Iteratively refine a noisy future sample.", "Compare real compute and decision-relevant failures."] },
  }),
  defineWorldModelLesson({
    id: "latent-actions-passive-video", track: "wm-foundation-models", title: "Latent Actions from Passive Video", number: 31,
    plain: "A latent-action model infers compact transition controls from video when real action labels are missing.",
    precise: "Given adjacent encoded observations, an inverse/latent-action model infers code $\\hat a_t$ explaining their change; a dynamics model predicts future tokens conditioned on that code. Discrete bottlenecks can encourage a reusable control alphabet. The codes need not align with named motor commands, may absorb camera motion or exogenous events, and require interventions or downstream imitation to establish controllability.",
    analogy: "It watches many silent demonstrations and invents labels such as 'move-like-this' for recurring transitions.", analogyBreak: "an invented label can mix agent choice with wind, camera cuts, or another actor's behavior.",
    ideas: ["Separate observed transition, inferred latent action, and forward dynamics roles.", "Test code consistency, diversity, and intervention effects while controlling camera/exogenous change.", "Do not translate latent codes into physical commands without an alignment procedure and evidence."],
    worked: "Clips show a character move left/right but no keys. A 4-code action model assigns codes 0/1 to direction and 2/3 to jump variants. If code 0 also appears on leftward camera pans with stationary characters, it has not isolated agent control.",
    misconception: "Unsupervised latent actions are not ground-truth actions and do not prove agency or causal control.",
    quiz: { question: "What can a latent action accidentally encode?", options: ["Camera or exogenous motion", "Only true motor command", "No temporal change", "A verified reward"], answer: 0, explanation: "Without labels/interventions, any recurring transition factor may enter the code." }, lab: "wm-video", prerequisites: ["autoregressive-diffusion-dynamics"], sources: [wmSources.genie, wmSources.vjepa2],
    objectives: ["Trace latent-action inference and action-conditioned future prediction", "Test whether a learned action code represents controllable change rather than correlated motion"],
    vocabulary: [{ term: "Latent action", meaning: "An inferred compact code describing transition variation when explicit actions are unavailable." }, { term: "Inverse model", meaning: "A model inferring an action-like variable from before/after states." }, { term: "Controllability", meaning: "Evidence that changing the action input predictably changes the future in a usable way." }],
    stages: ["Observe transition", "Infer code", "Intervene and predict"], checkpoints: ["Hold timestamps and camera processing explicit.", "Inspect code usage, entropy, and relation to visible change.", "Swap codes from the same start and verify distinct outcomes."],
    primaryCheck: { prompt: "Describe the training loop for frames $x_t,x_{t+1}$ without action labels.", expected: "Infer latent code from the transition, then train dynamics to predict $x_{t+1}$ or its tokens/features from $x_t$ plus that code, with bottleneck and anti-collapse controls.", retry: "Draw an inverse arrow from before/after frames to code and a forward arrow from before frame plus code to after frame." },
    decision: { explanation: "A code is action-like only when interventions reproduce stable, agent-relevant effects.", mechanism: "From matched starts, swap codes, measure outcome direction/contact, control for camera/exogenous motion, align a subset with real actions where available, and test imitation or planning.", workedExample: "Code 2 predicts jump from stationary starts across backgrounds and aligns with a labeled jump subset; code 3 merely predicts camera zoom. Only code 2 has evidence of agent control.", boundary: "Stable predictive effects in video still do not map directly to safe robot commands or forces.", check: { prompt: "A code predicts motion but swapping it changes only camera viewpoint. Is it a control action?", expected: "Not for the agent-control claim. It models a transition factor; label it camera/view dynamics unless further evidence links it to controllable behavior.", retry: "Name what physical or rendered variable changes under the code intervention." } },
    transfer: { prompt: "Which evidence most strengthens the action claim?", options: [{ text: "Each code has a different color", feedback: "Visual separation is not controllability." }, { text: "Code swaps from matched states cause reproducible agent-motion differences and align with labeled actions", feedback: "Correct: intervention and alignment support the claim." }, { text: "Reconstruction is sharp", feedback: "Sharpness can ignore action meaning." }], answer: 1, worked: "Combine latent intervention, nuisance controls, and a small labeled alignment set.", retry: "Ask what changes when only the code changes." },
    motion: { concept: "routing", headline: "Latent actions route the same visual state toward different learned futures.", intro: "Observed before/after pairs infer a discrete code; intervention then selects one of several transition paths while camera-motion shortcuts remain marked.", labels: ["BEFORE + AFTER", "INFER CODE", "CONDITION", "INTERVENE"], titles: ["A transition supplies evidence without labels.", "A bottleneck invents a compact change code.", "Dynamics consumes state and inferred code.", "Code swaps test stable controllable effects."] },
  }),
  defineWorldModelLesson({
    id: "jepa-vjepa", track: "wm-foundation-models", title: "JEPA and V-JEPA Predictive Representations", number: 32,
    plain: "JEPA-style models predict representations of missing or future regions instead of reconstructing every raw value.",
    precise: "A context encoder processes visible space-time regions; a predictor uses context plus position/mask information to predict target-encoder embeddings for hidden regions. Target parameters are stopped or slowly updated so the objective cannot move both sides toward a trivial match. V-JEPA reports video feature prediction without pretrained image encoders, text, negative examples, or pixel reconstruction in its setup; action-conditioned planning requires an additional dynamics/control interface.",
    analogy: "It predicts the summary another observer would write about a hidden video segment.", analogyBreak: "agreement between summaries may omit fine geometry, uncertainty, actions, or safety-critical events.",
    ideas: ["Trace context regions, target regions, encoders, predictor, and gradient boundaries.", "Explain the anti-collapse mechanism and test feature diversity/interventions.", "Separate representation benchmarks from action-conditioned world-model planning evidence."],
    worked: "Visible frames 1–4 encode context; target encoder represents masked frames 5–6; predictor estimates those target features from context and positions. No pixel decoder is required, but a constant target would be a trivial failure if not prevented.",
    misconception: "Strong frozen-feature recognition does not by itself demonstrate long-horizon simulation or intervention-aware control.",
    quiz: { question: "What does V-JEPA's predictor target?", options: ["Raw future pixels only", "Learned target-encoder features for hidden video regions", "Ground-truth robot torques", "A text-only reward"], answer: 1, explanation: "The objective predicts representations rather than reconstructing raw pixels." }, lab: "wm-video", prerequisites: ["latent-actions-passive-video"], sources: [wmSources.jepa, wmSources.vjepa2],
    objectives: ["Trace context and target branches through a JEPA-style feature-prediction objective", "Separate action-free representation evidence from action-conditioned planning evidence"],
    vocabulary: [{ term: "Context encoder", meaning: "The network representing visible input regions used for prediction." }, { term: "Target encoder", meaning: "The branch producing representations for hidden regions." }, { term: "Joint embedding prediction", meaning: "Predicting a target representation from a context representation rather than raw observations." }],
    stages: ["Mask regions", "Encode branches", "Predict target features"], checkpoints: ["Specify visible and hidden space-time blocks without leakage.", "Document parameter sharing/update and stop-gradient boundary.", "Measure feature diversity, target match, and downstream transfer."],
    primaryCheck: { prompt: "Which branch may receive prediction-loss gradients directly in a stopped-target JEPA setup?", expected: "The context encoder and predictor receive gradients; the target representation is stopped for that loss or updated through a separate slow/shared rule, preventing both sides from trivially chasing each other.", retry: "Draw the loss arrow and place a stop sign before the target branch." },
    decision: { explanation: "World-model claims should match the demonstrated interface: understand/represent, predict features, or plan with actions.", mechanism: "Create an evidence table for inputs, actions, predicted target, horizon, downstream tasks, interventions, and real execution. Assign only the strongest label supported by every required column.", workedExample: "V-JEPA frozen features support motion recognition; V-JEPA 2-AC adds robot trajectories and action-conditioned prediction for image-goal planning. The second claim uses additional data and post-training.", boundary: "Reported planning on particular robot tasks does not certify general robotics, physical causality, or safe deployment.", check: { prompt: "An action-free video encoder predicts future features well. Can it plan robot actions immediately?", expected: "Not from that evidence alone. It needs an action/control model, objectives, and planning evaluation connecting proposed actions to future consequences.", retry: "Locate the input slot for a candidate action and the evaluation showing its effect." } },
    transfer: { prompt: "Which statement is accurate?", options: [{ text: "Feature prediction and robot planning are identical objectives", feedback: "Planning needs action-conditioned consequences." }, { text: "Action-free pretraining can supply representations; control requires an added aligned action model and evidence", feedback: "Correct: the stages are separated." }, { text: "No pixel decoder means no prediction", feedback: "Prediction occurs in feature space." }], answer: 1, worked: "State pretraining evidence and post-training/control evidence in separate rows.", retry: "Name the target space and whether actions appear as inputs." },
    motion: { concept: "multimodal", headline: "JEPA predicts hidden future representations through a protected target branch.", intro: "Visible and masked video streams remain separate; a stop-gradient boundary makes the information and optimization contract inspectable.", labels: ["VISIBLE", "TARGET", "PREDICT", "TRANSFER"], titles: ["The context encoder sees selected space-time regions.", "A target encoder represents hidden regions.", "The predictor matches target features without raw reconstruction.", "Separate tests establish recognition or action-conditioned planning."] },
  }),
  defineWorldModelLesson({
    id: "genie-interactive-worlds", track: "wm-foundation-models", title: "Genie and Generative Interactive Environments", number: 33,
    plain: "Genie-style models generate visual environments that respond frame by frame to learned action controls inferred from video.",
    precise: "Genie combines a spatiotemporal tokenizer, autoregressive dynamics, and a latent-action model trained from unlabeled internet videos. It can condition on images or other prompts and generate action-controllable future frames. Genie 3's official report describes real-time interaction and longer consistency, but those statements remain release evidence at a named date/configuration. Evaluation must separate prompt fidelity, action response, visual consistency, diversity, latency, and agent-training utility.",
    analogy: "It is an improvising game engine learned from recorded play rather than hand-coded rules.", analogyBreak: "the generated world may forget objects, violate physics, or expose controls that do not map to real actions.",
    ideas: ["Trace prompt/start state, tokenizer, latent action, dynamics, and generated frame loop.", "Evaluate interactivity as action-to-outcome response, not passive video quality.", "Label official demonstrations, published evidence, and local simulations with their exact boundaries."],
    worked: "A starting image of a platform scene is compressed by the tokenizer to visual tokens $u_0$. Because training video lacked key labels, the latent-action model maps a demonstrated leftward transition to code $a^L=2$. Autoregressive dynamics consume $(u_0,a^L)$ to generate next tokens $u_1$, the decoder renders frame $x_1$ with the character shifted left, and $u_1$ becomes context for the next control. Repeating code 2 for five steps and contrasting code 3 from the same start tests continued left-versus-right response and identity persistence.",
    misconception: "A generated interactive environment is not automatically an accurate simulator for robotics or a source of unlimited trustworthy training data.",
    quiz: { question: "Which component lets Genie infer controls without ground-truth action labels?", options: ["Latent-action model", "Only a text tokenizer", "A reward oracle", "A real robot"], answer: 0, explanation: "The latent-action model learns compact transition controls from video." }, lab: "wm-video", prerequisites: ["jepa-vjepa"], sources: [wmSources.genie, wmSources.genie3],
    objectives: ["Trace Genie's tokenizer, latent actions, autoregressive dynamics, and interaction loop", "Design an evidence-separated evaluation of controllability and consistency"],
    vocabulary: [{ term: "Generative interactive environment", meaning: "A generated visual world whose future responds to a control interface." }, { term: "Prompt fidelity", meaning: "How well generated content matches the supplied text, image, or starting condition." }, { term: "Consistency", meaning: "Persistence of objects, geometry, identity, and rules across interaction time." }],
    stages: ["Initialize world", "Apply control", "Generate and audit"], checkpoints: ["Pin prompt/start, model report, seed policy, and resolution.", "Hold start fixed and contrast controls over repeated trials.", "Score action effect, latency, identity, geometry, and failure horizon separately."],
    primaryCheck: { prompt: "Trace one continued Genie-style interaction from an image prompt and an unlabeled-video control: name tokenizer output, latent-action code, autoregressive dynamics input/output, decoded frame, and the next loop input.", expected: "The prompt/start frame is tokenized to $u_0$; a transition-trained latent-action model supplies code $a_0$; autoregressive dynamics predict next visual tokens conditioned on prior tokens and $a_0$; the decoder produces frame $x_1$; then $u_1$ becomes persisted context, a new control $a_1$ is applied, and generation repeats. A matched alternate code from the same $u_0$ tests whether interaction responds rather than merely plays passive video.", retry: "Use six boxes: prompt/frame, tokenizer, latent action, autoregressive dynamics, next-frame decoder, continued context. Feed the generated state—not the original prompt—into the next turn." },
    decision: { explanation: "An interactive demo supports only claims directly observable under its documented interface and protocol.", mechanism: "Record source type—paper, official report, local access, or browser fixture—then preserve prompts/actions/video, sampling settings, failures, and metrics. Never convert a quoted release number into a local measurement.", workedExample: "Course text cites Genie 3's official 24 fps/720p statement as a dated report. A local CSS lab demonstrating action branches is labeled simulation and makes no throughput claim.", boundary: "Vendor-selected examples can reveal capabilities but cannot estimate failure frequency without a sampling protocol.", check: { prompt: "You watch three official clips with no raw access. What can you report?", expected: "Report the demonstrated examples and official stated conditions with attribution; do not estimate reliability, coverage, or local performance frequency.", retry: "Separate 'shown or reported' from 'measured by our protocol'." } },
    transfer: { prompt: "Which sentence is evidence-honest?", options: [{ text: "Our course measured Genie 3 at 24 fps", feedback: "The course did not run that measurement." }, { text: "Google DeepMind's 5 Aug 2025 report states 24 fps at 720p; this lesson does not reproduce it", feedback: "Correct: attribution and boundary are explicit." }, { text: "A browser animation proves foundation-model physics", feedback: "A teaching fixture cannot establish model behavior." }], answer: 1, worked: "Attach source, date, configuration, and reproduction status to changing release claims.", retry: "Ask who executed the system and where the number came from." },
    motion: { concept: "agent", headline: "An interactive generator closes a visual loop around each control input.", intro: "The state persists across generated frames while alternative latent actions branch the next visual outcome and expose consistency failures.", labels: ["START", "CONTROL", "NEXT FRAME", "CONTINUE"], titles: ["A prompt or image initializes a generated world.", "A learned action code conditions the transition.", "Dynamics produces the next visual state.", "The new frame becomes context for another controlled step."] },
  }),
  defineWorldModelLesson({
    id: "foundation-world-models-case-study", track: "wm-foundation-models", title: "Case Study — Comparing Foundation World Models", number: 34, duration: 40,
    plain: "This case study compares DreamerV3, MuZero, V-JEPA 2, and Genie by their interfaces, training evidence, planning use, and limits—not by one vague notion of world understanding.",
    precise: "DreamerV3 learns latent dynamics and behavior from environment interaction; MuZero learns task-relevant dynamics for tree search; V-JEPA 2 separates large-scale action-free video pretraining from action-conditioned robot post-training; Genie learns token dynamics and latent actions for interactive visual generation. Each uses different data, targets, action semantics, compute, and evaluation, so a useful taxonomy records what is predicted, how actions enter, how decisions are made, and what evidence was released.",
    analogy: "They are different maps: a driving simulator, a search notebook, a predictive visual atlas, and an improvising environment engine.", analogyBreak: "the map labels do not provide a common benchmark or justify a total ranking.",
    ideas: ["Build a contract table before comparing capabilities or scale.", "Keep published facts, official release claims, local fixtures, and course inference in separate evidence tiers.", "Choose a system family from the learner's task and falsifiable requirements."],
    worked: "Rows include inputs/actions, target space, planner/policy, training data, online compute, evaluation, artifacts, and limits. MuZero may score task outcomes without pixel prediction; Genie may render interactive pixels without task reward; neither omission alone makes one universally stronger.",
    misconception: "'Foundation world model' is not a standardized guarantee of physics, agency, causality, robotics transfer, or safety.",
    quiz: { question: "What is the first step in comparing these systems?", options: ["Rank demo aesthetics", "Normalize their input/output/action and evidence contracts", "Choose the largest parameter count", "Assume all use the same data"], answer: 1, explanation: "Different interfaces and evidence must be made comparable before claims." }, lab: "wm-evaluation", prerequisites: ["genie-interactive-worlds"], sources: [wmSources.dreamerV3, wmSources.muzero, wmSources.vjepa2, wmSources.genie],
    objectives: ["Compare DreamerV3, MuZero, V-JEPA 2, and Genie through an explicit contract table", "Make a task-specific selection without turning incomparable evidence into a universal ranking"],
    vocabulary: [{ term: "Foundation world model", meaning: "A broad label for large reusable predictive models; its exact contract must be specified." }, { term: "Evidence tier", meaning: "The provenance class of a claim, such as local fixture, published paper, or official report." }, { term: "Contract table", meaning: "A structured comparison of inputs, actions, targets, decisions, data, evaluation, and limits." }],
    stages: ["Normalize contracts", "Attach evidence", "Select by task"], checkpoints: ["Complete every interface cell or mark it unknown.", "Link each fact to a dated primary source and reproduction status.", "State gates, trade-offs, and a condition that reverses the choice."],
    primaryCheck: { prompt: "Why is 'best world model' not answerable from these four papers alone?", expected: "They optimize different targets, use different data/actions/planners/budgets, and report different evaluations. A best claim needs a shared task and matched protocol.", retry: "Put the four systems in rows and mark every nonshared column before looking at results." },
    decision: { explanation: "Selection begins from a required interface and failure costs, then uses only compatible evidence.", mechanism: "Specify task, observations/actions, horizon, latency, safety, data access, and evaluation; eliminate systems lacking the interface; compare remaining evidence with confounds and uncertainty attached.", workedExample: "For image-goal robot planning, action-conditioned V-JEPA 2 evidence is relevant; passive V-JEPA recognition and Genie visual interaction are informative but not direct matched robot-control evidence.", boundary: "Published success on selected tasks does not prove deployment readiness in the learner's robot, sensors, or environment.", check: { prompt: "Choose a starting family for low-latency visual robot control and name missing evidence.", expected: "Start from an action-conditioned latent control model such as the V-JEPA 2-AC or Dreamer/TD-MPC family, then require matched robot dynamics, latency, safety, and OOD evaluation before deployment.", retry: "Filter by action interface first, then by real-time and safety gates." } },
    transfer: { prompt: "Which comparison is defensible?", options: [{ text: "Genie beats MuZero because its video looks richer", feedback: "Their targets and evaluations differ." }, { text: "For task X, system Y has the closest demonstrated interface, while these deployment gaps remain", feedback: "Correct: selection and uncertainty are scoped." }, { text: "Parameter count decides world understanding", feedback: "Scale alone does not define interface or quality." }], answer: 1, worked: "Deliver the filled table, sources, missing cells, decision, and reversal conditions.", retry: "Replace aesthetic or scale language with required inputs, actions, outputs, and tests." },
    motion: { concept: "evaluation", headline: "Foundation world models become comparable only after their contracts are made explicit.", intro: "A sliced scorecard separates representation, simulation, planning, action interface, evidence source, and deployment gap.", labels: ["INTERFACE", "TARGET", "DECISION USE", "EVIDENCE"], titles: ["List observations, actions, and conditioning.", "Name pixels, features, rewards, values, or policies predicted.", "Trace search, actor, MPC, or interactive control.", "Attach protocol, source tier, and limitations."] },
  }),
] as const;
