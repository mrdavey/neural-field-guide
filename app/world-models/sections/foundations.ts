import { defineWorldModelLesson } from "../helpers";
import { wmSources } from "../sources";

export const worldModelFoundationSpecs = [
  defineWorldModelLesson({
    id: "world-models", track: "wm-foundations", title: "What Is a World Model?", number: 1, duration: 12,
    plain: "A world model is a learned predictor of how an environment may change, especially after an agent takes an action.",
    precise: "At time $t$, a world model receives a history or compact state $s_t$ and a proposed action $a_t$, then predicts a distribution over useful consequences such as the next state $s_{t+1}$, observation $o_{t+1}$, reward $r_t$, and continuation flag $c_t$. The model is useful only through a downstream purpose—prediction, planning, representation learning, or policy learning—and it need not reproduce every pixel to support that purpose.",
    analogy: "It is like a small flight simulator an agent can consult before moving the real controls.", analogyBreak: "a learned model can omit rare hazards or invent exploitable shortcuts, so its simulated future is evidence to test rather than reality.",
    ideas: ["Separate the real environment from the learned predictor and from the policy that chooses actions.", "Condition predictions on actions when the question is what the agent can change.", "Judge a world model by the downstream decision and failure boundary, not visual plausibility alone."],
    worked: "In a $2\\times2$ grid, state $s_t=(0,0)$ and action right predict $s_{t+1}=(0,1)$ with probability $0.9$ and a collision state with probability $0.1$. A planner can compare that distribution with the distribution for action down before executing either action in the real grid.",
    misconception: "A fluent video generator is not automatically a useful world model. Without a stable state/action interface and decision-relevant evaluation, attractive frames do not show that interventions are predicted correctly.",
    quiz: { question: "Which contract most clearly describes an action-conditioned world model?", options: ["Text prompt → attractive image", "Current state and action → distribution over next consequences", "Observation → class label only", "Reward → fixed action"], answer: 1, explanation: "The defining operational contract predicts how possible actions change future state, observation, reward, or continuation." }, lab: "wm-state",
    sources: [wmSources.worldModels, wmSources.mbrlSurvey],
    objectives: ["Trace the state-action-consequence contract of a world model", "Distinguish a decision-useful world model from an uncontrolled sequence generator"],
    vocabulary: [{ term: "Environment", meaning: "The process outside the agent that receives actions and produces observations and outcomes." }, { term: "World model", meaning: "A learned predictor of environment dynamics or other decision-relevant consequences." }, { term: "Policy", meaning: "A rule or model that chooses an action from the information available to the agent." }],
    stages: ["Name the interface", "Predict consequences", "Audit the use"], checkpoints: ["List the state or history, action, and every predicted output.", "Keep multiple possible futures when the transition is uncertain.", "Name the decision improved and one failure that would invalidate use."],
    primaryCheck: { prompt: "A robot sees a closed door and may push or wait. Name the inputs, predicted outputs, and the component that finally chooses.", expected: "The world model receives the current information state plus push or wait, predicts next observation/state and relevant reward or continuation distributions, and the planner or policy—not the world model alone—chooses the action.", retry: "Draw three boxes labeled environment, world model, and planner; place observations/actions on the arrows before answering." },
    decision: { explanation: "A model earns the name operationally when its predictions support a defined intervention or decision.", mechanism: "Hold the starting state fixed, vary the proposed action, compare predicted consequence distributions, and verify those predictions against real held-out transitions or closed-loop performance.", workedExample: "A video model predicts a door opening after either push or wait, while held-out transitions show it opens only after push. Its frames look plausible, but its action contrast is wrong, so it cannot support this door policy.", boundary: "Action-free predictive features may still be valuable representations; the narrower claim is that they do not yet establish controllable simulation.", check: { prompt: "Two models produce equally realistic driving video, but only one changes the future correctly when steering changes. Which is decision-useful and why?", expected: "The action-sensitive model is decision-useful because the counterfactual steering input changes its predicted trajectory in the verified direction. Realism alone cannot identify the effect of the action.", retry: "Compare the same initial frame under two actions and ask which model preserves the observed action-to-outcome difference." } },
    transfer: { prompt: "A weather video generator makes realistic storms but accepts no actions. What is the strongest defensible course label?", options: [{ text: "A verified control world model", feedback: "Control has not been tested because there is no action interface." }, { text: "A predictive sequence model that may supply representations, but not yet an action-conditioned controller model", feedback: "Correct: this preserves what was shown without inventing controllability." }, { text: "A policy because it generates future frames", feedback: "A policy chooses actions; generating observations is a different role." }], answer: 1, worked: "Label it by the demonstrated contract: predictive video generation. Add an action interface and counterfactual/closed-loop evaluation before calling it a control world model.", retry: "Return to the state → action → consequence diagram and identify the missing arrow." },
    motion: { concept: "pipeline", headline: "A world model sits between experience and a proposed future.", intro: "The trace separates observation, compact state, proposed action, and predicted consequence so prediction is not confused with choosing or executing.", labels: ["OBSERVE", "STATE", "ACTION", "CONSEQUENCE"], titles: ["The environment supplies incomplete evidence.", "History becomes a decision state.", "A candidate action conditions the forecast.", "Predicted consequences inform a separate chooser."] },
  }),
  defineWorldModelLesson({
    id: "dynamics-tensors", track: "wm-foundations", title: "Tensors and Shapes for Dynamics", number: 2,
    plain: "Shapes tell you which numbers represent batches, time steps, objects, features, actions, and predicted futures.",
    precise: "A trajectory batch may store observations $o[B,T,H,W,C]$, actions $a[B,T-1,A]$, latent states $z[B,T,D]$, and predicted next states $\\hat z[B,T-1,D]$. A transition $f(z_t,a_t)$ must align the batch and time pairs: action $a_t$ predicts the state after it, not the state before it. Matrix multiplications transform the final feature axis while masks identify valid time steps.",
    analogy: "Shapes are labels on every wire in a simulator schematic.", analogyBreak: "matching dimensions prove numerical compatibility, not that time or action alignment is semantically correct.",
    ideas: ["Name batch, time, spatial, action, and feature axes before computing.", "Align each action at time $t$ with the consequence at $t+1$.", "Use masks for padding and episode endings rather than training on invented transitions."],
    worked: "For $B=2$, $T=5$, latent width $D=8$, and action width $A=3$, $z[:,:-1]$ has shape $[2,4,8]$ and $a$ has $[2,4,3]$. Concatenating on the last axis gives $[2,4,11]$; a linear transition head $W[11,8]$ predicts four next latents $[2,4,8]$.",
    misconception: "A tensor with shape $[B,T,D]$ does not reveal whether index $t$ means before-action or after-action state. That convention must be documented and tested.",
    quiz: { question: "With latent states $z[B,T,D]$ and actions $a[B,T-1,A]$, which target naturally pairs with $a[:,t]$?", options: ["$z[:,t-1]$", "$z[:,t]$ only", "$z[:,t+1]$", "Every state"], answer: 2, explanation: "Action $a_t$ is conventionally the intervention between state $t$ and state $t+1$." }, lab: "wm-state", prerequisites: ["world-models"], sources: [wmSources.planet, wmSources.rssmCode],
    objectives: ["Trace trajectory tensor shapes through one latent transition", "Detect a time-alignment bug that valid tensor shapes cannot reveal"],
    vocabulary: [{ term: "Trajectory", meaning: "An ordered record of states or observations, actions, rewards, and episode boundaries." }, { term: "Latent width", meaning: "The number of learned features used to represent one compact state." }, { term: "Transition mask", meaning: "A Boolean marker saying whether a time pair is a real within-episode transition." }],
    stages: ["Label axes", "Pair time steps", "Project features"], checkpoints: ["Write a semantic name beside every dimension.", "Confirm action $t$ targets state $t+1$ and does not cross an episode end.", "Check the contracted dimension and surviving batch/time axes."],
    primaryCheck: { prompt: "Compute the concatenated input and predicted output shapes for $z[3,7,16]$ and $a[3,6,4]$.", expected: "Use $z[:,:-1]$ with shape $[3,6,16]$, concatenate actions to obtain $[3,6,20]$, and apply a $[20,16]$ projection to predict $[3,6,16]$ next states.", retry: "Write the transition pair $(z_t,a_t)\\rightarrow z_{t+1}$ first, then restore batch and feature axes." },
    decision: { explanation: "Semantic alignment must be tested independently of shape validity.", mechanism: "Create a tiny monotonic trajectory with known actions, build input-target pairs, decode their indices, and assert that no pair crosses padding or reset boundaries.", workedExample: "States [0,1,2,0,1] contain a reset before the second 0. The pair action-at-2 → next-state-0 must be masked as terminal, even though both sides fit the same scalar shape.", boundary: "A correct one-step alignment test does not establish that a long rollout remains accurate.", check: { prompt: "A batch concatenates two episodes and learns the final state of episode A → first state of episode B. Why can loss fall anyway, and what mask fixes it?", expected: "The shapes and repeated dataset ordering let the model memorize the artificial boundary transition. A continuation/terminal mask must exclude that pair from transition and reward losses.", retry: "Decode the last two time indices around every reset and mark whether the environment actually executed an action between them." } },
    transfer: { prompt: "Which batch is safe for transition loss?", options: [{ text: "Every adjacent pair in a padded array", feedback: "Padding and resets create false transitions." }, { text: "Only adjacent $(z_t,a_t,z_{t+1})$ triples whose continuation mask is true", feedback: "Correct: shape and episode semantics both agree." }, { text: "Only the first transition of each episode", feedback: "This is safe but discards almost all valid supervision." }], answer: 1, worked: "Build triples and multiply the per-transition loss by a valid-continuation mask before averaging over real transitions.", retry: "Find the action that physically connects each proposed state pair; if none exists, mask it." },
    motion: { concept: "coordinates", headline: "Dynamics tensors preserve time while transforming features.", intro: "The diagram keeps batch and transition axes visible while the last dimension expands with an action and contracts into a predicted state.", labels: ["STATE", "ACTION", "CONCAT", "NEXT STATE"], titles: ["Select the pre-action latent state.", "Align the action at the same transition index.", "Join only the feature axes.", "Project back to the latent-state width."] },
  }),
  defineWorldModelLesson({
    id: "stochastic-futures", track: "wm-foundations", title: "Probability and Stochastic Futures", number: 3,
    plain: "When the same situation can lead to different outcomes, a world model should predict a distribution rather than one averaged future.",
    precise: "A stochastic transition represents $p(s_{t+1}\\mid s_t,a_t)$ or $p(z_{t+1}\\mid z_t,a_t)$ with probabilities, densities, or samples. Expected values summarize a distribution but can describe an impossible midpoint. Aleatoric uncertainty represents outcome randomness under the data process; epistemic uncertainty represents limited knowledge and may shrink with better coverage.",
    analogy: "It is a weather forecast with several weighted paths instead of one painted sky.", analogyBreak: "model probabilities can be miscalibrated and do not reveal whether uncertainty comes from randomness or missing knowledge without additional evidence.",
    ideas: ["Preserve multiple plausible futures instead of averaging incompatible modes.", "Compute expectations only for quantities where an average supports the decision.", "Separate irreducible outcome variation from uncertainty caused by sparse experience."],
    worked: "A ball passes left with probability $0.5$ or right with probability $0.5$. Mean position is the obstacle center, a location the ball never occupies. A two-mode distribution preserves both collision-free paths; a squared-error point predictor invents the dangerous middle.",
    misconception: "Adding random noise does not guarantee calibrated stochastic prediction. The distribution must be scored against held-out outcomes and relevant slices.",
    quiz: { question: "Why can a mean next frame be misleading for a two-way future?", options: ["Means are always too large", "It can blend modes into an outcome that never occurs", "Probability cannot model images", "Only rewards may be stochastic"], answer: 1, explanation: "A point estimate can average mutually exclusive modes and produce a physically implausible midpoint." }, lab: "wm-uncertainty", prerequisites: ["dynamics-tensors"], sources: [wmSources.vae, wmSources.planet, wmSources.calibration],
    objectives: ["Represent multiple possible next states with a probability distribution", "Choose between expected-value and risk-sensitive decisions under uncertainty"],
    vocabulary: [{ term: "Stochastic", meaning: "Containing outcome variation that is represented with probabilities or samples." }, { term: "Aleatoric uncertainty", meaning: "Variation in outcomes that remains even with full knowledge of the data process." }, { term: "Epistemic uncertainty", meaning: "Uncertainty caused by limited data or model knowledge." }],
    stages: ["List outcomes", "Assign mass", "Score observations"], checkpoints: ["Keep mutually exclusive futures separate.", "Verify nonnegative probabilities sum to one.", "Check calibration and proper scoring on held-out cases."],
    primaryCheck: { prompt: "A door opens left or right with equal probability. Compare the distribution with a point prediction at the center.", expected: "The distribution assigns 0.5 to each valid side. The center point minimizes some average losses but is not a possible door state, so it is unsafe for collision planning.", retry: "List the outcomes observed in repeated identical starts before calculating their coordinate mean." },
    decision: { explanation: "A planner should use the part of the distribution aligned with the cost of failure.", mechanism: "For each action, combine predicted outcome probabilities with outcome costs; use expected cost for risk-neutral decisions or explicit tail/chance constraints when rare failure is unacceptable.", workedExample: "Action A yields cost 0 with probability 0.99 and cost 100 with probability 0.01, expected cost 1. Action B always costs 1.2. Expected cost chooses A, while a rule forbidding more than 0.5% catastrophic risk chooses B.", boundary: "A risk-sensitive rule cannot repair a model that assigns zero probability to an unseen hazard.", check: { prompt: "A warehouse policy has lower expected time but a 2% collision probability above a 0.5% limit. Which action rule applies?", expected: "Reject the policy under the declared chance constraint even if its expected time is lower; the non-compensable collision threshold controls the decision.", retry: "Write expected performance and the hard tail-risk gate on separate lines; apply the gate first." } },
    transfer: { prompt: "Which forecast best supports a forked-path planner?", options: [{ text: "One blurred path down the middle", feedback: "The blur averages modes and may be physically impossible." }, { text: "Two weighted path hypotheses plus calibration on held-out forks", feedback: "Correct: both futures and their evidence quality remain visible." }, { text: "One randomly selected path with no probability", feedback: "A sample hides how likely alternatives are." }], answer: 1, worked: "Represent the fork as a mixture, validate frequencies or proper scores, then compute the planning objective over the mixture.", retry: "Ask whether the representation preserves every observed outcome mode and its relative mass." },
    motion: { concept: "distribution", headline: "One state-action pair can branch into several legitimate futures.", intro: "Probability mass splits across outcome modes; the mean is shown separately so it cannot masquerade as a sampled world state.", labels: ["CONDITION", "BRANCH", "PROBABILITY", "DECISION"], titles: ["Hold the current state and action fixed.", "Keep distinct future modes separate.", "Assign and calibrate probability mass.", "Apply expected or risk-sensitive costs."] },
  }),
  defineWorldModelLesson({
    id: "learning-dynamics", track: "wm-foundations", title: "Neural Networks, Gradients and Dynamics Learning", number: 4,
    plain: "A dynamics network learns parameters that turn a current state and action into predictions whose errors are differentiable.",
    precise: "Let $\\hat z_{t+1}=f_\\theta(z_t,a_t)$ and loss $L(\\theta)=\\lVert\\hat z_{t+1}-z_{t+1}\\rVert^2$. A forward pass computes the prediction and loss; reverse-mode differentiation applies the chain rule to obtain $\\nabla_\\theta L$; an optimizer updates $\\theta$. Recurrent unrolling shares parameters across time, so gradients from several steps accumulate through the temporal computation graph.",
    analogy: "Learning adjusts the gears of a miniature simulator by tracing each prediction error back to the responsible gear.", analogyBreak: "a local gradient improves the chosen loss near current parameters; it does not guarantee physical truth, long-horizon stability, or a globally best model.",
    ideas: ["Separate forward prediction, backpropagated sensitivity, and optimizer update.", "Unrolling creates temporal credit paths through shared transition parameters.", "Verify gradients and one-step learning before interpreting long rollouts."],
    worked: "For scalar $\\hat z_{t+1}=wz_t+a_t$, take $z_t=2$, $a_t=1$, target 4, and $w=1$. Prediction is 3, loss $(3-4)^2=1$, and $dL/dw=2(3-4)z_t=-4$. With learning rate 0.1, $w$ becomes 1.4 and the new prediction 3.8 reduces loss to 0.04.",
    misconception: "A decreasing reconstruction or transition loss does not prove that imagined policies will work; the model can reduce average error while remaining wrong in decision-critical states.",
    quiz: { question: "In $\\hat z=wz+a$, which component decides how far $w$ moves after backprop computes $dL/dw$?", options: ["The tokenizer", "The optimizer and learning rate", "The action space", "The environment reset"], answer: 1, explanation: "Backprop computes sensitivity; the optimizer converts it into a parameter step." }, lab: "wm-state", prerequisites: ["stochastic-futures"], sources: [wmSources.worldModels, wmSources.dreamer, wmSources.pytorchAutograd],
    objectives: ["Trace one forward, backward, and update step for a transition model", "Diagnose why lower one-step loss can coexist with worse imagined control"],
    vocabulary: [{ term: "Dynamics function", meaning: "A parameterized transformation predicting how state changes under an action." }, { term: "Temporal unroll", meaning: "Repeated application of the same transition across several predicted time steps." }, { term: "Gradient", meaning: "A local sensitivity showing how a small parameter change would change the chosen loss." }],
    stages: ["Predict", "Differentiate", "Update and recheck"], checkpoints: ["Compute the output from named state, action, and parameters.", "Show each local derivative and temporal dependency.", "Run the forward pass again and compare the same loss."],
    primaryCheck: { prompt: "Recompute the scalar example with target 2 instead of 4; give gradient sign and update direction.", expected: "Prediction remains 3, so $dL/dw=2(3-2)2=4$. Gradient descent subtracts a positive amount, reducing $w$ from 1 toward 0.6 for learning rate 0.1.", retry: "Write prediction minus target first; its sign determines the direction before multiplying by the state." },
    decision: { explanation: "The training loss and the decision objective emphasize different parts of the state-action distribution.", mechanism: "Evaluate one-step loss, multi-step rollout error, and closed-loop task outcomes on held-out states, especially states visited by the learned planner rather than only the behavior policy.", workedExample: "Model A has mean one-step error 0.02 but underestimates collision motion in a rare corridor; its planner exploits the corridor and crashes. Model B has error 0.03 but preserves collision direction and succeeds closed-loop.", boundary: "One closed-loop task is still not evidence of broad physical accuracy; it establishes usefulness only for the tested policy and distribution.", check: { prompt: "Validation MSE improves while a planner begins choosing impossible shortcuts. Name the mismatch and the next two tests.", expected: "The model is accurate on average under validation data but exploitable under planner-induced states. Run multi-step/action-contrast tests and closed-loop rollouts on the shortcut states.", retry: "Separate the data distribution used for loss from the state distribution created by planning." } },
    transfer: { prompt: "Which evidence best supports deploying the updated transition model?", options: [{ text: "Training loss decreased", feedback: "Training loss alone can hide distribution shift and model exploitation." }, { text: "Gradient check, held-out one/multi-step tests, and closed-loop policy results all pass declared gates", feedback: "Correct: arithmetic, prediction, and decision evidence are separated." }, { text: "A sampled rollout looks smooth", feedback: "One smooth sample is neither calibrated nor decision evidence." }], answer: 1, worked: "Use a layered gate: gradient correctness, held-out prediction, rollout stability, then task/safety outcomes under the planner.", retry: "Ask what each measurement can falsify and add the missing layer." },
    motion: { concept: "training", headline: "A transition error becomes a parameter update through three separate mechanisms.", intro: "The loop shows prediction, loss, gradient, and update as different states so backprop is not confused with optimization or model validity.", labels: ["PREDICT", "LOSS", "GRADIENT", "UPDATE"], titles: ["Apply the current dynamics parameters.", "Compare with the observed next state.", "Trace sensitivity through the transition.", "Change parameters and rerun the same case."] },
  }),
  defineWorldModelLesson({
    id: "sequential-state", track: "wm-foundations", title: "State, Observation, Action and Trajectory", number: 5,
    plain: "A state summarizes what matters for predicting the future; an observation is only the evidence the agent receives about that state.",
    precise: "A controlled process evolves as $s_{t+1}\\sim p(s_{t+1}\\mid s_t,a_t)$ and emits $o_t\\sim p(o_t\\mid s_t)$. The Markov property says the chosen state makes earlier history unnecessary for the next-step distribution. In practice the true state may be hidden, so a learned recurrent state summarizes history $h_t=(o_{\\le t},a_{<t})$. A trajectory records ordered observations, actions, rewards, and termination.",
    analogy: "State is the complete game board; observation is the camera view through a narrow window.", analogyBreak: "learned state may omit an unobserved variable that later becomes decision-critical, so compactness is not proof of sufficiency.",
    ideas: ["Distinguish hidden environment state from the agent's observation and learned information state.", "Define the timing convention for observe, act, transition, reward, and termination.", "Test whether the proposed state predicts changed futures without needing omitted history."],
    worked: "A thermostat observes 20°C, but hidden state also includes heater momentum. Two homes with the same 20°C observation can warm or cool next depending on whether the heater was recently on. Adding recent action/history to the information state separates the futures.",
    misconception: "The latest frame is not automatically a Markov state. Velocity, occluded objects, delayed effects, and episode context may require history.",
    quiz: { question: "Two identical frames lead to different futures because an unseen ball moves in opposite directions. What is missing?", options: ["A larger reward", "State information such as velocity or history", "More action labels only", "A lower learning rate"], answer: 1, explanation: "The observation aliases distinct hidden states; history or another sensor is needed." }, lab: "wm-state", prerequisites: ["learning-dynamics"], sources: [wmSources.planet, wmSources.controlSuite],
    objectives: ["Trace the observation-state-action timing of one trajectory", "Test whether a proposed state is sufficient for predicting a changed future"],
    vocabulary: [{ term: "State", meaning: "Information sufficient for the modeled future transition under an action." }, { term: "Observation", meaning: "Sensor evidence emitted by the environment and available to the agent." }, { term: "Markov property", meaning: "Given the current state and action, earlier history adds no information about the next-state distribution." }],
    stages: ["Observe", "Update state", "Act and transition"], checkpoints: ["Name what the sensor reveals and hides.", "State exactly which history is summarized.", "Record action, next observation, reward, and termination in order."],
    primaryCheck: { prompt: "Write the event order for an agent that sees $o_t$, chooses $a_t$, and receives $o_{t+1},r_t,c_t$.", expected: "Observation $o_t$ updates the information state; the policy chooses $a_t$; the environment transitions; then it emits $r_t$, continuation/termination, and $o_{t+1}$ for the next update.", retry: "Draw a time line with the action between the two observations; attach reward and termination to that transition." },
    decision: { explanation: "State sufficiency is an empirical conditional-prediction claim, not a name assigned to a vector.", mechanism: "Find histories mapped to the same proposed state, hold the next action fixed, and test whether their next-outcome distributions differ. A difference means the representation aliases decision-relevant histories.", workedExample: "Same camera frame plus action forward yields a collision after a hidden leftward velocity history but no collision after a rightward history. A frame-only state fails; a two-frame state exposes direction.", boundary: "Passing finite alias tests does not prove perfect sufficiency for every unseen variable or horizon.", check: { prompt: "A recurrent model resets memory every frame yet predicts position well and collision poorly. What state variable is likely missing, and how do you test it?", expected: "Velocity or motion history is missing. Compare matched current frames with different preceding motion under the same action and inspect collision predictions.", retry: "Hold current observation and action fixed; vary only the earlier history that could change the outcome." } },
    transfer: { prompt: "Which representation is safer for predicting a bouncing ball?", options: [{ text: "One frame containing position", feedback: "Position alone aliases opposite velocities." }, { text: "A history-derived state containing position and velocity evidence", feedback: "Correct: it distinguishes futures that the single frame merges." }, { text: "The last reward only", feedback: "Reward does not locate or move the ball." }], answer: 1, worked: "Use at least two observations or a recurrent filter to estimate motion, then test matched positions with opposite directions.", retry: "Ask whether two different physical situations can produce the same proposed state." },
    motion: { concept: "memory", headline: "State carries forward what the current observation cannot show.", intro: "A time ledger separates sensor evidence, remembered information, action, and next emission so hidden motion becomes visible in the representation contract.", labels: ["OBSERVATION", "HISTORY", "ACTION", "NEXT EMISSION"], titles: ["Sensors reveal only part of the environment.", "A state summarizes decision-relevant history.", "The chosen action intervenes on the process.", "The environment returns the next evidence and outcome."] },
  }),
  defineWorldModelLesson({
    id: "rewards-returns-policies", track: "wm-foundations", title: "Rewards, Returns and Policies", number: 6,
    plain: "Reward scores one transition; return adds future rewards; a policy chooses actions to make expected return large while respecting constraints.",
    precise: "For rewards $r_t$, discounted return is $G_t=\\sum_{k=0}^{\\infty}\\gamma^k r_{t+k}$ with $0\\le\\gamma\\le1$. A policy $\\pi(a\\mid s)$ maps information states to action probabilities. The value $V^\\pi(s)=\\mathbb E_\\pi[G_t\\mid s_t=s]$ summarizes long-term outcome under that policy. Reward is a designed signal and can be misspecified; safety constraints and evaluation remain separate.",
    analogy: "Reward is one receipt, return is the whole trip budget, and policy is the spending rule.", analogyBreak: "a scalar reward cannot automatically encode every human preference, rare harm, or hard constraint.",
    ideas: ["Keep immediate reward distinct from accumulated return and long-term value.", "Name the policy whose future actions define a value estimate.", "Treat reward as a fallible objective that requires independent outcome and safety checks."],
    worked: "With rewards [1,2,3] and $\\gamma=0.5$, $G_0=1+0.5(2)+0.25(3)=2.75$. A greedy immediate policy sees action A reward 2 versus B reward 0, but if B leads to rewards [0,3,3], its discounted return 2.25 can exceed a one-step alternative.",
    misconception: "The world model does not choose a goal merely by predicting rewards. The reward definition, policy, planner, and constraints are distinct parts of the agent.",
    quiz: { question: "What does $V^\\pi(s)$ depend on beyond the current state?", options: ["The policy's future action choices", "Only the immediate reward", "The decoder temperature", "No future information"], answer: 0, explanation: "Value is an expectation over futures generated when policy $\\pi$ continues choosing actions." }, lab: "wm-rollout", prerequisites: ["sequential-state"], sources: [wmSources.rlBook, wmSources.dreamer, wmSources.mbrlSurvey],
    objectives: ["Calculate a discounted return and relate it to policy value", "Separate reward optimization from hard constraints and outcome evaluation"],
    vocabulary: [{ term: "Reward", meaning: "A scalar training or decision signal attached to a transition." }, { term: "Return", meaning: "The discounted sum of rewards from a time step onward." }, { term: "Policy", meaning: "A distribution or rule for selecting actions from states." }],
    stages: ["Record rewards", "Discount the future", "Condition on a policy"], checkpoints: ["Place each reward on the transition that produced it.", "Show every power of $\\gamma$ and the horizon.", "Name the future action rule assumed by the expectation."],
    primaryCheck: { prompt: "Compute return for rewards [4,2,2] with $\\gamma=0.5$ and state what changes if the future policy changes.", expected: "$G_0=4+1+0.5=5.5$. A different future policy can change later state/action distributions and rewards, so $V^\\pi$ changes even at the same starting state.", retry: "Write the powers $1,\\gamma,\\gamma^2$ above the three rewards, then name who selects actions after time zero." },
    decision: { explanation: "Optimizing a proxy reward is acceptable only when independent constraints and outcome measurements guard what the proxy omits.", mechanism: "Specify target outcomes, proxy reward, non-compensable constraints, and held-out evaluation. Search for policies that score well by exploiting gaps, then revise or reject the reward/system.", workedExample: "A cleaning robot earns +1 per object moved and pushes fragile items off a table. It maximizes the proxy but violates a zero-drop constraint. Add action validation or a constraint; do not merely praise its return.", boundary: "A penalty can still be traded against enough reward unless represented as a hard constraint or lexicographic gate.", check: { prompt: "A policy gains 10 points for speed and loses 3 for each collision, so it chooses one collision. Safety permits none. What failed?", expected: "The scalarized reward made collision compensable. Enforce a zero-collision constraint or gate before optimizing speed among feasible policies.", retry: "Ask whether any amount of positive reward is allowed to purchase the prohibited outcome." } },
    transfer: { prompt: "Which launch rule matches a non-compensable safety requirement?", options: [{ text: "Ship if average return is highest", feedback: "Average return can hide or trade off forbidden outcomes." }, { text: "Require the safety constraint first, then compare return among passing policies", feedback: "Correct: feasibility precedes optimization." }, { text: "Add a small negative reward and assume it is enough", feedback: "A finite penalty remains compensable." }], answer: 1, worked: "Use a two-stage decision: reject unsafe candidates, then optimize return, cost, or latency within the safe set.", retry: "Separate hard gates from quantities that may be averaged or traded." },
    motion: { concept: "optimization", headline: "A policy trades present and future outcomes along one trajectory distribution.", intro: "The trace accumulates rewards with discounting, then places constraints outside the scalar hill so forbidden states cannot be purchased by extra return.", labels: ["REWARD", "RETURN", "VALUE", "POLICY"], titles: ["Each transition produces an immediate signal.", "Discounted addition forms a future objective.", "Expectation fixes the policy and starting state.", "The policy chooses actions within external constraints."] },
  }),
  defineWorldModelLesson({
    id: "mdps-bellman", track: "wm-foundations", title: "MDPs and Bellman Backups", number: 7,
    plain: "A Markov decision process describes states, actions, transitions, rewards, and discounting; a Bellman backup separates one step from the value of what follows.",
    precise: "An MDP is $(\\mathcal S,\\mathcal A,P,R,\\gamma)$. For policy $\\pi$, $$V^\\pi(s)=\\mathbb E_{a\\sim\\pi,\\,s'\\sim P}[R(s,a,s')+\\gamma V^\\pi(s')].$$ The backup is a consistency equation: immediate reward plus discounted next-state value. Model-based planning estimates the expectation using known or learned $P$ and reward predictions.",
    analogy: "A Bellman backup is a route sign showing the cost of the next road plus the best estimate from the next junction.", analogyBreak: "the sign is trustworthy only if transition, reward, and downstream value estimates are accurate for the route considered.",
    ideas: ["Name all five MDP components and their timing.", "Separate immediate reward from discounted next-state value.", "Propagate model uncertainty and approximation error instead of treating a backup as ground truth."],
    worked: "At state A, action right gives reward 1 and moves to B with probability 0.8 or C with 0.2. With $V(B)=5$, $V(C)=0$, and $\\gamma=0.9$, the backup is $1+0.9(0.8\\cdot5+0.2\\cdot0)=4.6$.",
    misconception: "The Bellman equation does not make a learned model correct. A biased transition inside repeated backups can amplify a planning error.",
    quiz: { question: "In a Bellman backup, what follows the immediate reward?", options: ["A discounted expectation of next value", "The previous observation only", "A tokenizer loss", "An unweighted action count"], answer: 0, explanation: "The backup adds immediate reward to discounted value under possible next states and future policy actions." }, lab: "wm-rollout", prerequisites: ["rewards-returns-policies"], sources: [wmSources.rlBook, wmSources.mbrlSurvey, wmSources.muzero],
    objectives: ["Compute a Bellman backup using a transition distribution", "Identify how transition error changes a backed-up action value"],
    vocabulary: [{ term: "MDP", meaning: "A mathematical decision process defined by states, actions, transitions, rewards, and discount." }, { term: "Bellman backup", meaning: "An update combining one-step reward with discounted value of successor states." }, { term: "Action value", meaning: "Expected return after taking one action and then following a specified policy." }],
    stages: ["Enumerate successors", "Add reward and value", "Compare actions"], checkpoints: ["List each next state and probability for the action.", "Multiply values by probabilities and discount once.", "Use the same value and model assumptions across candidate actions."],
    primaryCheck: { prompt: "Compute $Q(s,a)$ for reward 2, successors worth 4 and 1 with probabilities 0.75 and 0.25, and $\\gamma=0.8$.", expected: "Expected next value is $0.75(4)+0.25(1)=3.25$; discount gives 2.6; add reward 2 for $Q=4.6$.", retry: "First calculate the probability-weighted next value, then discount, then add the one-step reward." },
    decision: { explanation: "A model error matters in proportion to how it changes the action comparison, not only its raw prediction distance.", mechanism: "Recompute candidate action backups under calibrated model alternatives or confidence bounds and inspect whether the selected action changes. Prioritize errors near the decision margin and high-cost outcomes.", workedExample: "Action A backs up to 4.6 and B to 4.4. If A's probability of the valuable successor may be 0.65 rather than 0.75, its value falls below B. The 0.2 margin is not robust.", boundary: "Robustness to one stated interval does not cover unmodeled successor states assigned zero probability.", check: { prompt: "Two actions differ by 0.05 value, while model uncertainty moves either estimate by ±0.2. What should the planner report?", expected: "The ranking is unresolved under the model uncertainty. Gather information, use a conservative rule, or expose the tie rather than claiming a stable optimum.", retry: "Place the uncertainty interval around both action values and check whether the intervals preserve their order." } },
    transfer: { prompt: "Which model error deserves priority?", options: [{ text: "The largest pixel error anywhere", feedback: "Pixel distance can be unrelated to the decision." }, { text: "An error that reverses the top two action backups or hides a severe outcome", feedback: "Correct: it directly changes the decision or safety boundary." }, { text: "Only errors after the planning horizon", feedback: "Terminal value can carry those effects back into the decision." }], answer: 1, worked: "Measure prediction quality and decision sensitivity separately; inspect action margins and severe successor probabilities.", retry: "Recalculate the actual action ranking after inserting each candidate model error." },
    motion: { concept: "pipeline", headline: "A Bellman backup compresses a branching future into one action estimate.", intro: "Successor branches retain probability mass before reward, discount, and next value combine, making the source of each number inspectable.", labels: ["STATE + ACTION", "SUCCESSORS", "REWARD + VALUE", "BACKUP"], titles: ["Fix the state and candidate action.", "Enumerate predicted next states and probabilities.", "Attach immediate reward and downstream value.", "Sum the discounted expectation for comparison."] },
  }),
  defineWorldModelLesson({
    id: "belief-states-filtering", track: "wm-foundations", title: "POMDPs, Belief States and Filtering", number: 8, duration: 34,
    plain: "When the true state is hidden, a belief state records probabilities over possible states and updates them using actions and new observations.",
    precise: "A partially observable MDP adds an observation model $O(o_t\\mid s_t)$. A Bayesian filter predicts a prior through the action transition, then multiplies by observation likelihood and normalizes: $$b_t(s)\\propto O(o_t\\mid s)\\sum_{s'}P(s\\mid s',a_{t-1})b_{t-1}(s').$$ Learned recurrent states approximate this filtering role when exact states and probabilities are unavailable.",
    analogy: "A belief state is a detective's probability board updated after each move and clue.", analogyBreak: "a learned hidden vector need not be calibrated, interpretable, or sufficient merely because it has memory.",
    ideas: ["Predict the hidden-state distribution forward using the previous action.", "Correct that prior with the likelihood of the new observation.", "Plan over the resulting information state while testing calibration and aliasing."],
    worked: "A hidden object is left or right with prior [0.6,0.4]. A noisy sensor says left with likelihoods [0.8,0.2]. Unnormalized posterior is [0.48,0.08]; normalization gives about [0.857,0.143]. The observation changes confidence rather than revealing certainty.",
    misconception: "Filtering is not simply appending more frames. The update must combine what the action predicted with how likely the new evidence is under each hidden state.",
    quiz: { question: "What are the two conceptual stages of a Bayes filter?", options: ["Tokenize then decode", "Predict through dynamics, then correct with observation likelihood", "Reward then reset", "Average then discard uncertainty"], answer: 1, explanation: "The action produces a prior over new states; sensor evidence reweights it into a posterior." }, lab: "wm-belief", prerequisites: ["mdps-bellman"], sources: [wmSources.pomdp, wmSources.planet, wmSources.worldModels],
    objectives: ["Calculate a discrete belief update from a prior and observation likelihood", "Use a belief-state audit to detect hidden-state aliasing before planning"],
    vocabulary: [{ term: "POMDP", meaning: "A decision process where the true environment state is not directly observed." }, { term: "Belief state", meaning: "A probability distribution or learned information state summarizing plausible hidden states." }, { term: "Filtering", meaning: "Sequentially predicting hidden state and correcting it with new evidence." }],
    stages: ["Predict prior", "Apply likelihood", "Normalize and act"], checkpoints: ["Use the action-conditioned transition from the previous belief.", "Multiply each candidate state by the likelihood of the actual observation.", "Normalize to one and keep uncertainty visible for planning."],
    primaryCheck: { prompt: "Update prior [0.5,0.5] when observation likelihood is [0.9,0.3].", expected: "Unnormalized weights are [0.45,0.15], total 0.60, so posterior is [0.75,0.25]. The sensor favors the first state but does not prove it.", retry: "Multiply prior by likelihood elementwise, add the weights, then divide each by that sum." },
    decision: { explanation: "A usable belief representation must preserve distinctions that change optimal action or risk.", mechanism: "Construct matched observations produced by different hidden states, compare belief updates and chosen actions, and score calibration, next-outcome prediction, and failure under ambiguous evidence.", workedExample: "A corridor observation looks identical whether a door behind the agent is open or closed. The return path matters later. A memoryless state merges them; a recurrent belief that saw the door preserves two action values.", boundary: "A compact recurrent state can pass behavior tests while its individual coordinates remain uninterpretable; interpretability is a separate claim.", check: { prompt: "A robot succeeds on visible goals but fails after temporary occlusion. Design one alias test and the expected state difference.", expected: "Create two histories ending in the same occluded frame but with the object previously moving left versus right. Under the same action, beliefs and next-location predictions should differ.", retry: "Hold the final observation fixed and vary only earlier evidence that changes the hidden future." } },
    transfer: { prompt: "A sensor is ambiguous but a costly collision is possible. What should the controller consume?", options: [{ text: "The most likely state only", feedback: "Argmax discards meaningful tail risk." }, { text: "A calibrated belief or uncertainty-aware information state used by the planner", feedback: "Correct: alternatives remain available for risk-sensitive planning." }, { text: "The raw reward from the previous step", feedback: "Reward alone is not a state estimate." }], answer: 1, worked: "Carry the posterior distribution or an audited learned belief into planning and apply the declared collision constraint over its futures.", retry: "Ask which representation keeps the low-probability hazardous state visible." },
    motion: { concept: "distribution", headline: "Filtering turns uncertain history into an updated distribution over hidden state.", intro: "Probability mass first moves through the action model, then sensor evidence reweights it; the normalized posterior remains a distribution rather than collapsing to a guess.", labels: ["PRIOR", "ACTION PREDICT", "OBSERVE", "POSTERIOR"], titles: ["Begin with uncertainty over hidden states.", "Move that mass through action-conditioned dynamics.", "Weight each state by the new evidence likelihood.", "Normalize and plan without erasing ambiguity."] },
  }),
] as const;
