import { defineWorldModelLesson } from "../helpers";
import { wmSources } from "../sources";

export const worldModelRepresentationSpecs = [
  defineWorldModelLesson({
    id: "sensor-representations", track: "wm-representations", title: "From Sensors to Predictive Features", number: 9,
    plain: "An encoder turns a large observation into a compact feature state that keeps information useful for predicting and acting.",
    precise: "An encoder $e_\\phi$ maps observation $o_t$—pixels, proprioception, audio, or their combination—to features $x_t=e_\\phi(o_t)$. The representation should retain controllable objects, motion, contacts, and task-relevant context while discarding nuisance variation only when downstream predictions remain valid. Spatial resolution, temporal context, augmentation, and sensor synchronization define what information is available.",
    analogy: "It is a field sketch that preserves landmarks needed for navigation instead of copying every leaf.", analogyBreak: "the model, not a human cartographer, chooses the features, so apparently irrelevant detail may hide a critical hazard.",
    ideas: ["Define every sensor input, timestamp, shape, and normalization before encoding.", "Train features against predictive or decision evidence rather than visual neatness alone.", "Audit what the bottleneck discards with controlled nuisance and hazard changes."],
    worked: "A $64\\times64\\times3$ frame has 12,288 values. An encoder maps it to $x_t[128]$. If red wall texture changes but geometry does not, the feature should stay useful; if a one-pixel obstacle appears, the feature must change enough for collision prediction.",
    misconception: "A smaller representation is not automatically more abstract or useful. Compression can erase a small but causal object.",
    quiz: { question: "Which test best checks a predictive representation?", options: ["Its 2-D plot looks clustered", "It preserves next-outcome and action differences under controlled changes", "It has the fewest dimensions", "It reconstructs background texture perfectly"], answer: 1, explanation: "A predictive representation must preserve distinctions that change future consequences." }, lab: "wm-latent", prerequisites: ["belief-states-filtering"], sources: [wmSources.planet, wmSources.jepa],
    objectives: ["Trace sensor observations through an encoder into a predictive feature state", "Audit a feature bottleneck for nuisance invariance and hazard sensitivity"],
    vocabulary: [{ term: "Encoder", meaning: "A learned transformation from raw observation space into a feature representation." }, { term: "Nuisance variable", meaning: "Variation that should not change the modeled decision, such as irrelevant texture." }, { term: "Sufficient feature", meaning: "A representation retaining the information needed for the specified predictions and decisions." }],
    stages: ["Specify sensors", "Encode features", "Probe retained information"], checkpoints: ["List modality, timing, shape, range, and missing-data behavior.", "Record the output shape and bottleneck size.", "Change one nuisance and one hazard while holding other factors fixed."],
    primaryCheck: { prompt: "Map a batch of RGB clips $o[4,6,64,64,3]$ to 128-wide features and state the output shape.", expected: "Applying the same frame encoder at each batch/time position yields $x[4,6,128]$; a temporal encoder may then combine those six feature states without changing the documented input contract.", retry: "Preserve batch and time axes, then replace the spatial/color axes with the feature width." },
    decision: { explanation: "Good invariance removes only changes that do not alter the downstream consequence.", mechanism: "Create paired observations differing in exactly one factor, then compare latent distance, next-state prediction, and chosen action. Require invariance for nuisance pairs and sensitivity for causal hazard pairs.", workedExample: "Changing floor color leaves collision label and next position unchanged, so small latent movement is acceptable. Adding a low obstacle changes collision probability, so the latent and planner must respond.", boundary: "A finite pair set cannot establish invariance to every lighting, camera, or object configuration.", check: { prompt: "An encoder ignores both wallpaper color and a small brake light. Which invariance is desirable and which is a failure?", expected: "Wallpaper invariance is desirable if dynamics are unchanged; brake-light invariance is a failure when it predicts another agent's deceleration and changes safe action.", retry: "For each changed pixel region, ask whether the verified future or required action also changes." } },
    transfer: { prompt: "A latent is stable under all image edits. Is that good?", options: [{ text: "Yes; perfect invariance is always ideal", feedback: "It may also ignore decision-relevant changes." }, { text: "Only if it changes for interventions and hazards that alter future outcomes", feedback: "Correct: invariance needs a causal boundary." }, { text: "No; every pixel must change the latent equally", feedback: "Nuisance robustness is valuable when verified." }], answer: 1, worked: "Pair nuisance and causal edits, then test latent response plus downstream prediction/action response.", retry: "Label each edit as outcome-preserving or outcome-changing before judging invariance." },
    motion: { concept: "embedding", headline: "An encoder compresses observations while preserving predictive distinctions.", intro: "The diagram moves from a sensor grid to one feature row, then contrasts nuisance stability with hazard sensitivity.", labels: ["SENSOR", "ENCODER", "FEATURE", "AUDIT"], titles: ["Raw modalities arrive with explicit shapes and timing.", "The encoder contracts spatial detail into learned features.", "The compact state feeds dynamics and planning.", "Controlled pairs reveal what was retained or erased."] },
  }),
  defineWorldModelLesson({
    id: "autoencoders-latents", track: "wm-representations", title: "Autoencoders and Latent Compression", number: 10,
    plain: "An autoencoder learns a compact code by encoding an observation and decoding it back toward the original.",
    precise: "An encoder produces $z=e_\\phi(o)$ and a decoder reconstructs $\\hat o=d_\\psi(z)$. Training minimizes a reconstruction loss suited to the observation distribution. The bottleneck limits capacity, encouraging reusable structure, but pixel loss weights large backgrounds heavily and may neglect small control-relevant details. Reconstruction evaluates information recovery, not action-conditioned dynamics.",
    analogy: "It is compressing a map into a short note and drawing the map again from that note.", analogyBreak: "a decoder can draw plausible detail that was not stored, and low average error can hide an erased stop sign.",
    ideas: ["Trace observation → latent → reconstruction with all shapes.", "Relate bottleneck size and reconstruction loss to retained information.", "Separate reconstructive fidelity from predictive and control usefulness."],
    worked: "Trace $o=[1,0,1,0]$ through the encoder to $z=[0.75,0.25]$, then through the decoder to $\\hat o=[0.8,0.1,0.6,0]$. The squared reconstruction errors are $[0.04,0.01,0.16,0]$, so their mean is $0.21/4=0.0525$. On a 4,096-pixel frame, that same averaging can hide a four-pixel switch even while background error improves.",
    misconception: "A crisp reconstruction does not prove that the latent represents objects, causality, uncertainty, or controllable state.",
    quiz: { question: "What does reconstruction loss directly measure?", options: ["How well decoded outputs match observations under the chosen loss", "Long-horizon control success", "Causal identification", "Reward correctness"], answer: 0, explanation: "Reconstruction measures recovery under a loss; other claims need separate evidence." }, lab: "wm-latent", prerequisites: ["sensor-representations"], sources: [wmSources.vae, wmSources.worldModels],
    objectives: ["Trace an autoencoder bottleneck and reconstruction loss", "Decide when a lower reconstruction loss still fails the control task"],
    vocabulary: [{ term: "Autoencoder", meaning: "An encoder-decoder pair trained to reconstruct its input." }, { term: "Latent code", meaning: "The compact internal representation between encoder and decoder." }, { term: "Reconstruction loss", meaning: "A discrepancy between the observed input and decoded reconstruction." }],
    stages: ["Encode", "Bottleneck", "Decode and compare"], checkpoints: ["Write input and latent shapes.", "State the capacity constraint and information risk.", "Inspect per-region errors, not only their mean."],
    primaryCheck: { prompt: "Commit the full trace for $o=[1,0,1,0]$, encoded $z=[0.75,0.25]$, and $\\hat o=[0.8,0.1,0.6,0]$: give each shape, every squared error, and the mean reconstruction loss.", expected: "$o[4]\\rightarrow z[2]\\rightarrow\\hat o[4]$. The squared errors are $[(1-0.8)^2,(0-0.1)^2,(1-0.6)^2,(0-0)^2]=[0.04,0.01,0.16,0]$, with mean $0.0525$. The third element dominates the loss and should be inspected for task relevance.", retry: "First preserve the observation shape across decoding; then subtract elementwise, square, sum the four values, and divide by four." },
    decision: { explanation: "Compression is acceptable only if task-relevant distinctions survive the bottleneck.", mechanism: "Slice reconstruction and downstream prediction by object size, motion, reward event, and action consequence; compare equal-capacity alternatives under the same data and control evaluation.", workedExample: "Code width 16 gives MSE 0.018 but misses the switch in 40% of frames; width 32 gives MSE 0.020 and detects it 98%. The larger code is preferable for switch control despite worse mean MSE.", boundary: "Downstream success on one switch does not show the latent retains every variable needed for other tasks.", check: { prompt: "Model A reconstructs texture better; Model B predicts contacts better. The agent manipulates objects. Which evidence decides?", expected: "Contact prediction and closed-loop manipulation decide because they match the use. Texture reconstruction is secondary unless texture changes the task.", retry: "Write the deployed decision first, then choose the metric whose failure would change that decision." } },
    transfer: { prompt: "Which report is honest?", options: [{ text: "Lowest MSE means best world model", feedback: "MSE alone omits action and control evidence." }, { text: "Lowest MSE on this dataset, but control usefulness requires contact and rollout tests", feedback: "Correct: the claim stays within measured evidence." }, { text: "A decoder proves causal state", feedback: "Reconstruction is observational." }], answer: 1, worked: "Report reconstruction by slice and separately report predictive/action-conditioned performance.", retry: "Name exactly what the loss compares and refuse claims outside that comparison." },
    motion: { concept: "compression", headline: "A bottleneck trades observation detail for a compact state.", intro: "Values contract into a small code and expand again; a tiny hazard marker makes the information-loss boundary visible.", labels: ["OBSERVATION", "ENCODE", "LATENT", "RECONSTRUCT"], titles: ["The input contains both signal and nuisance detail.", "The encoder selects a compact set of features.", "Capacity limits what can remain recoverable.", "The decoder exposes retained and missing information."] },
  }),
  defineWorldModelLesson({
    id: "stochastic-latents-vaes", track: "wm-representations", title: "Stochastic Latents and Variational Autoencoders", number: 11,
    plain: "A variational autoencoder represents an observation with a distribution over latent codes instead of one fixed code.",
    precise: "The encoder approximates a posterior $q_\\phi(z\\mid o)$, often Gaussian with $\\mu$ and $\\sigma$. Sampling uses $z=\\mu+\\sigma\\odot\\epsilon$, $\\epsilon\\sim\\mathcal N(0,I)$, so gradients pass through $\\mu,\\sigma$. The evidence lower bound balances expected reconstruction with $D_{KL}(q_\\phi(z\\mid o)\\|p(z))$. In sequential models, the prior is conditioned on previous state and action rather than fixed standard normal.",
    analogy: "Instead of pinning a place on a map, the encoder draws a probability cloud around plausible locations.", analogyBreak: "the cloud geometry is chosen by the objective and model family; it is not automatically calibrated physical uncertainty.",
    ideas: ["Distinguish posterior parameters, sampled latent, decoder, and prior.", "Use reparameterization to separate random noise from differentiable parameters.", "Balance reconstruction and KL without collapsing information or abandoning the prior."],
    worked: "For $o=[1,0]$, let $q(z\\mid o)=\\mathcal N(1,0.5^2)$ and sample $\\epsilon=-1$, giving $z=1+0.5(-1)=0.5$. If the decoder returns $[0.8,0.1]$, summed squared reconstruction error is $0.05$. Against $p(z)=\\mathcal N(0,1)$, $D_{KL}=\\frac{1}{2}(1^2+0.5^2-1-\\ln 0.5^2)\\approx0.818$. With weight $\\beta=1$, the minimized loss is $0.05+0.818=0.868$: reconstruction rewards recovery while KL tethers the posterior to the prior.",
    misconception: "Sampling from a VAE does not by itself distinguish data randomness from model ignorance or guarantee calibrated multimodal futures.",
    quiz: { question: "Why write $z=\\mu+\\sigma\\epsilon$?", options: ["To remove all randomness", "To make sampling differentiable with respect to encoder outputs", "To replace the decoder", "To set KL to zero"], answer: 1, explanation: "Noise is isolated in $\\epsilon$ while $z$ remains differentiable in $\\mu$ and $\\sigma$." }, lab: "wm-uncertainty", prerequisites: ["autoencoders-latents"], sources: [wmSources.vae, wmSources.planet],
    objectives: ["Trace posterior sampling and the two VAE loss terms", "Diagnose posterior collapse and an uninformative prior"],
    vocabulary: [{ term: "Approximate posterior", meaning: "A learned distribution over latent state after seeing the observation." }, { term: "Prior", meaning: "A latent distribution available before incorporating the current observation." }, { term: "KL divergence", meaning: "A directional discrepancy used here to regularize posterior and prior distributions." }],
    stages: ["Parameterize posterior", "Sample", "Balance losses"], checkpoints: ["Record $\\mu$, positive scale, and their shapes.", "Show the sampled noise and reparameterized latent.", "Inspect reconstruction and KL separately for collapse or mismatch."],
    primaryCheck: { prompt: "Commit the one-dimensional VAE trace for $\\mu=1$, $\\sigma=0.5$, $\\epsilon=-1$, observation $[1,0]$, reconstruction $[0.8,0.1]$, and standard-normal prior: compute $z$, summed squared reconstruction loss, KL, and their $\\beta=1$ total.", expected: "$z=0.5$; reconstruction is $(1-0.8)^2+(0-0.1)^2=0.05$; $D_{KL}=\\frac{1}{2}(1^2+0.5^2-1-\\ln0.5^2)\\approx0.818$; total $\\approx0.868$. The two terms respectively train observation recovery and prior compatibility.", retry: "Compute the sample first, then the two output errors. For KL use $\\frac{1}{2}(\\mu^2+\\sigma^2-1-\\ln\\sigma^2)$ before adding the separately reported terms." },
    decision: { explanation: "Posterior collapse occurs when the decoder predicts well while ignoring the latent; prior mismatch occurs when imagined latents do not match observation-conditioned ones.", mechanism: "Track KL by dimension/time, decoder sensitivity to shuffled latents, reconstruction, and prior-versus-posterior prediction. Test free-bits, KL balancing, decoder capacity, or objective changes one at a time.", workedExample: "KL falls near 0 for every dimension and swapping $z$ leaves reconstruction unchanged: the latent is ignored. In another run the posterior remains informative—its coordinates change verified outputs—but a prior-only five-step rollout drifts far from posterior-corrected states: the prior cannot predict that useful latent.", boundary: "A nonzero KL is not proof of useful information; the latent must affect verified predictions or decisions.", check: { prompt: "Compare two held-out cases. A: KL is zero and outputs ignore latent permutations. B: posterior coordinates change predictions, but prior-only rollout fails while observation-corrected rollout succeeds. Diagnose each and choose a separate next test or repair.", expected: "A is posterior collapse: remove decoder shortcuts or adjust KL scheduling/free bits, then require a latent intervention to change held-out prediction. B is an uninformative or mismatched dynamics prior, not collapse: inspect prior–posterior error by horizon and train/validate prior-only multi-step prediction without weakening the informative posterior.", retry: "First ask whether the output uses posterior $z$ at all; only if it does, compare the prior path with the observation-corrected path over horizon." } },
    transfer: { prompt: "Which observation most strongly indicates collapse?", options: [{ text: "The latent has many dimensions", feedback: "Width does not show whether it is used." }, { text: "KL is near zero and outputs ignore latent permutations", feedback: "Correct: posterior matches prior while decoder bypasses the code." }, { text: "Reconstruction loss is nonzero", feedback: "Nonzero loss is common and not diagnostic." }], answer: 1, worked: "Combine loss telemetry with a latent intervention; neither alone is sufficient.", retry: "Ask whether information in the current observation actually passes through the latent to outputs." },
    motion: { concept: "distribution", headline: "A stochastic latent is a sampled state plus an explicit distribution contract.", intro: "Mean and scale define a cloud, noise selects a point, and the KL tether keeps imagined prior states connected to observation-conditioned states.", labels: ["MEAN + SCALE", "NOISE", "SAMPLE", "KL + RECON"], titles: ["The encoder parameterizes a posterior distribution.", "Independent noise supplies stochasticity.", "Reparameterization creates a differentiable sample.", "Two losses balance information and predictability."] },
  }),
  defineWorldModelLesson({
    id: "action-conditioned-transitions", track: "wm-representations", title: "Action-Conditioned Transition Models", number: 12,
    plain: "An action-conditioned transition predicts how the state changes because a particular action was applied.",
    precise: "A transition model estimates $p(z_{t+1}\\mid z_t,a_t)$ or parameters of that distribution. Concatenation, additive action embeddings, FiLM-style modulation, or cross-attention can inject actions, but the essential test is intervention sensitivity: for the same $z_t$, different valid actions should produce the verified differences in $z_{t+1}$. Passive correlations can confound action effects when behavior data lacks coverage.",
    analogy: "It is a movable physics sketch: keep the scene fixed, pull a different control lever, and redraw the next state.", analogyBreak: "observational data may correlate a lever with outcomes without identifying what the lever causes in unseen states.",
    ideas: ["Specify discrete or continuous action shape, range, timing, and legality.", "Hold state fixed and vary action to expose counterfactual predictions.", "Audit action coverage and confounding before treating the learned change as causal."],
    worked: "At latent $z_t=[1,0]$, actions left $[-1]$ and right $[1]$ should predict x-coordinates 0 and 2. A model returning 2 for both has learned state momentum or policy correlation while ignoring the action input.",
    misconception: "Including an action tensor in the network does not prove the network uses it or knows the effect of actions absent from the dataset.",
    quiz: { question: "What is the simplest action-use test?", options: ["Increase model width", "Hold state fixed and compare predictions under different actions", "Lower reconstruction loss", "Shuffle next states only"], answer: 1, explanation: "A controlled action contrast directly tests whether the action changes predicted consequences." }, lab: "wm-state", prerequisites: ["stochastic-latents-vaes"], sources: [wmSources.planet, wmSources.tdmpc],
    objectives: ["Trace action injection through a latent transition", "Test whether a transition uses actions rather than policy correlations"],
    vocabulary: [{ term: "Action conditioning", meaning: "Making a prediction explicitly depend on the proposed intervention." }, { term: "Counterfactual action", meaning: "An alternative action evaluated from the same starting information state." }, { term: "Coverage", meaning: "Which state-action regions are represented by usable training evidence." }],
    stages: ["Encode action", "Combine with state", "Contrast consequences"], checkpoints: ["Name units, bounds, and time index of each action component.", "Show where action changes the transition computation.", "Compare at least two actions from an identical state."],
    primaryCheck: { prompt: "For $z[B,D]$ and continuous $a[B,A]$, what is the concatenated transition input shape?", expected: "Concatenating on the feature axis produces $[B,D+A]$; the transition network maps that input to parameters of the next-state distribution.", retry: "Preserve batch, then join only the state-feature and action-feature axes." },
    decision: { explanation: "Action sensitivity is credible only where data distinguishes the action effect from who chose the action and from hidden state.", mechanism: "Stratify or pair similar states across actions, measure action-effect error, perform action shuffles/ablations, and identify unsupported state-action cells before planning through them.", workedExample: "An expert turns left only near walls and right in open space. A model can associate walls with left outcomes without using action. Matched wall states containing both actions or randomized exploration are needed to identify the contrast.", boundary: "Randomization improves causal coverage but may be unsafe or incomplete in physical systems.", check: { prompt: "Action shuffling barely changes predictions. Give two possible diagnoses and one next experiment.", expected: "The model may ignore actions, or state/history may already reveal the behavior policy's action. Evaluate matched states with alternative actions or collect safe randomized action contrasts.", retry: "Separate architectural dependence from dataset identifiability; test each explicitly." } },
    transfer: { prompt: "A dataset contains only braking near obstacles. Can it establish the effect of accelerating there?", options: [{ text: "Yes; the action encoder can extrapolate", feedback: "Architecture cannot create missing intervention evidence." }, { text: "No; that state-action cell is unsupported and needs data or a conservative boundary", feedback: "Correct: planning should not treat extrapolation as measured dynamics." }, { text: "Yes if reconstruction is sharp", feedback: "Reconstruction does not identify the missing action effect." }], answer: 1, worked: "Mark the pair out of support, collect safe contrast data or forbid the plan, and report the limitation.", retry: "Build a state-by-action coverage table and locate the requested cell." },
    motion: { concept: "pipeline", headline: "The action must alter the predicted transition, not merely accompany it.", intro: "One fixed state branches through alternative action embeddings into different verified next-state distributions.", labels: ["STATE", "ACTION", "TRANSITION", "CONTRAST"], titles: ["Hold the current information state fixed.", "Encode one candidate intervention with units and bounds.", "Condition the next-state distribution on both inputs.", "Swap the action and verify the predicted difference."] },
  }),
  defineWorldModelLesson({
    id: "recurrent-state-space", track: "wm-representations", title: "Recurrent and State-Space Models", number: 13,
    plain: "A recurrent state-space model carries a compact state through time, predicts it forward with actions, and corrects it when new observations arrive.",
    precise: "A deterministic recurrent state may update $h_t=f(h_{t-1},z_{t-1},a_{t-1})$. A stochastic state $z_t$ is sampled from a prior conditioned on $h_t$ during imagination and from a posterior conditioned additionally on encoded observation $x_t$ during training/filtering. The pair $(h_t,z_t)$ combines memory of history with uncertainty about the current hidden state.",
    analogy: "The deterministic state is a running notebook; the stochastic state is the current uncertain hypothesis written on the active page.", analogyBreak: "neither component has a guaranteed human meaning, and recurrence can forget information or propagate errors indefinitely.",
    ideas: ["Separate deterministic memory, stochastic state, prior, and observation-conditioned posterior.", "Use the posterior when observations are available and the prior during imagined rollout.", "Audit long memory, reset behavior, and prior-posterior drift."],
    worked: "At training time, $h_t$ predicts prior $p(z_t\\mid h_t)$, then image feature $x_t$ yields posterior $q(z_t\\mid h_t,x_t)$. At imagination time $x_t$ is absent, so $z_t$ must come from the prior; a decoder that secretly requires posterior information will fail.",
    misconception: "A recurrent hidden state is not automatically a belief state; it must be tested for prediction, uncertainty, reset, and alias behavior.",
    quiz: { question: "Which distribution is available during a fully imagined rollout?", options: ["Observation-conditioned posterior only", "The dynamics prior conditioned on predicted state/history", "The true environment state", "No latent distribution"], answer: 1, explanation: "No new observation exists in imagination, so the model samples or uses the prior." }, lab: "wm-belief", prerequisites: ["action-conditioned-transitions"], sources: [wmSources.planet, wmSources.dreamer],
    objectives: ["Trace prior and posterior state updates through observation and imagination", "Diagnose reset leakage and prior-posterior drift"],
    vocabulary: [{ term: "Deterministic state", meaning: "A recurrent memory updated without sampling once its inputs are fixed." }, { term: "Stochastic state", meaning: "A sampled latent representing uncertainty or alternatives at a time step." }, { term: "Posterior", meaning: "A latent distribution corrected using the current observation." }],
    stages: ["Advance memory", "Predict prior", "Correct with observation"], checkpoints: ["List previous state, action, and reset mask entering recurrence.", "Compute prior parameters without current observation.", "Compare posterior and prior while preventing future leakage."],
    primaryCheck: { prompt: "State which inputs produce $h_t$, the prior for $z_t$, and the posterior for $z_t$.", expected: "$h_t$ uses previous deterministic/stochastic state and previous action; the prior uses $h_t$; the posterior uses $h_t$ plus encoded current observation $x_t$.", retry: "Place the observation only on the correction branch and keep imagination possible without it." },
    decision: { explanation: "Training must reproduce the information boundary encountered during imagination and at episode resets.", mechanism: "Zero recurrent state at true resets, test batched episode isolation, compare prior/posterior statistics by horizon, and decode prior-only rollouts from held-out starts.", workedExample: "If memory from episode A enters episode B, the first prediction of B improves artificially. If posterior KL grows from 0.2 at step 1 to 8 at step 20, prior rollouts drift away from observation-corrected states.", boundary: "Small KL does not guarantee correct semantics; prior and posterior may agree on the wrong state.", check: { prompt: "Commit a paired audit: identical episode B follows two different episode-A histories, and posterior-corrected validation succeeds while prior-only error rises after step five. What must remain equal across resets, what horizon trace do you record, and what does each failure diagnose?", expected: "The reset $h_0,z_0$ and first output for B must be identical across both preceding histories; a difference diagnoses cross-episode leakage. Separately record prior-versus-posterior state or decoded error at every horizon; growth beginning after step five diagnoses prior–posterior drift/exposure mismatch even when resets pass.", retry: "Treat reset and rollout as two information-boundary tests: vary only pre-reset history for the first, then remove all future observations and locate the first prior divergence for the second." } },
    transfer: { prompt: "Which test catches cross-episode memory leakage?", options: [{ text: "Train on longer episodes", feedback: "Length does not test reset isolation." }, { text: "Batch identical episode B after different episode A histories and require the same reset state/output", feedback: "Correct: prior history must not cross the reset." }, { text: "Inspect mean reconstruction loss", feedback: "Aggregate loss can improve because of leakage." }], answer: 1, worked: "Vary only the preceding episode, assert reset state equivalence, and keep the reset mask in checkpoint/resume tests.", retry: "Treat reset as an information boundary and test invariance to everything before it." },
    motion: { concept: "memory", headline: "State-space inference alternates prediction from memory and correction from evidence.", intro: "The ledger shows the prior branch continuing without observations and the posterior branch rejoining only when sensor evidence arrives.", labels: ["MEMORY", "PRIOR", "OBSERVATION", "POSTERIOR"], titles: ["Recurrent state summarizes past transitions.", "Dynamics predicts a latent distribution.", "The current sensor feature supplies new evidence.", "The posterior corrects the uncertain state."] },
  }),
  defineWorldModelLesson({
    id: "rssm-planet-case-study", track: "wm-representations", title: "Case Study — World Models to PlaNet", number: 14, duration: 38,
    plain: "This case study connects the original VAE–RNN–controller decomposition to PlaNet's recurrent state-space model and latent planning loop.",
    precise: "The 2018 World Models system encodes frames with a VAE, predicts latent sequences with an MDN-RNN, and trains a compact controller, including bounded experiments inside the learned dream. PlaNet integrates deterministic and stochastic latent state in an RSSM, trains observation/reward predictions with variational objectives and latent overshooting, then chooses continuous actions by online planning in latent space. The comparison shows an evolution of interfaces, not a universal ranking.",
    analogy: "The first design builds separate map, weather, and driver modules; PlaNet binds the map and weather into a state estimator consulted by an online route planner.", analogyBreak: "both are research systems evaluated in selected environments, not general physical simulators or safety-certified controllers.",
    ideas: ["Trace data and state through each published architecture without merging their interfaces.", "Connect PlaNet's stochastic/deterministic state and overshooting to partial observability and rollout error.", "Compare claims under environment, budget, planner, and evaluation differences."],
    worked: "For a five-step clip, World Models encodes each frame $z_t$ then fits a mixture next-latent RNN. PlaNet maintains $h_t,z_t$, predicts rewards, and can train a three-step prior toward later posterior states. During control, PlaNet samples action sequences, rolls them through its latent model, and executes only the first action.",
    misconception: "PlaNet did not prove that accurate image reconstruction is sufficient for planning, nor that every latent model should use its exact architecture or benchmark result.",
    quiz: { question: "What does PlaNet execute after planning an action sequence?", options: ["The whole sequence without observing again", "Only the first action before replanning", "No real action", "The posterior state as an action"], answer: 1, explanation: "PlaNet uses model-predictive control: plan, execute the first action, observe, and replan." }, lab: "wm-planner", prerequisites: ["recurrent-state-space"], sources: [wmSources.worldModels, wmSources.planet],
    objectives: ["Reconstruct the data, state, learning, and control flow of World Models and PlaNet", "Design a controlled tiny RSSM build that tests representation before planning claims"],
    vocabulary: [{ term: "MDN-RNN", meaning: "A recurrent network predicting mixture-distribution parameters for the next latent state." }, { term: "RSSM", meaning: "A recurrent state-space model combining deterministic memory and stochastic latent state." }, { term: "Latent overshooting", meaning: "Training multi-step prior predictions toward later inferred latent states." }],
    stages: ["Map architectures", "Match objectives", "Bound claims"], checkpoints: ["List encoder, dynamics, controller/planner, and every information boundary.", "Name each loss and the state distribution it trains.", "Keep environment, data, and evaluation differences attached to conclusions."],
    primaryCheck: { prompt: "Before revealing the answer, fill four rows—data, state, learning losses, and control—for both World Models and PlaNet.", expected: "World Models uses image sequences; VAE latents plus MDN-RNN memory; VAE reconstruction/KL and mixture next-latent learning with a separately optimized compact controller; and controller execution, including bounded dream experiments. PlaNet uses image/action/reward sequences; deterministic $h_t$ plus stochastic $z_t$ with prior/posterior paths; observation/reward variational losses plus latent overshooting; and online latent action-sequence search that executes one action, observes, filters, and replans.", retry: "Make a two-column table. Do not merge encoder, dynamics, loss, and action-selection interfaces; locate each item in the published system before comparing it." },
    decision: { explanation: "A course-scale replication should validate interfaces and failure modes rather than imitate published headline scores.", mechanism: "Use a tiny deterministic environment, document tensors and seeds, overfit one transition batch, verify resets and prior/posterior paths, measure multi-step error, then add a bounded planner only after prediction gates pass.", workedExample: "A 4-state corridor fixture stores records `{episode, reset, state[4], action[2], nextState[4], reward, continue}`. Its RSSM logs $h[H]$, prior and posterior parameters for $z[Z]$, decoded next state, reward, and continuation. Reset isolation and a no-observation prior path must pass before 10-step error is gated and CEM may consume the model.", boundary: "Passing this fixture validates implementation and arithmetic, not sample efficiency or generality in pixel-control benchmarks.", check: { prompt: "Specify the tiny RSSM fixture and its admission gates before planning: name the transition record, logged model outputs, reset test, prior/posterior test, multi-step threshold, and planner decision.", expected: "Each record contains episode/reset, state, action, next state, reward, and continuation with declared shapes. Log recurrent $h$, prior and posterior $z$ parameters, decoded state, reward, and continuation. Require identical post-reset state after different histories, forbid observations on the prior rollout, meet the predeclared multi-step error threshold, and only then admit the model to bounded CEM; otherwise planning remains blocked.", retry: "Write the fixture schema first, then order the gates: shape/overfit, reset isolation, legal prior path, multi-step prediction, reward/continuation, planner admission." } },
    transfer: { prompt: "Which capstone conclusion is defensible?", options: [{ text: "Our 4-state RSSM reproduces PlaNet's benchmark superiority", feedback: "A tiny fixture cannot reproduce that published claim." }, { text: "Our implementation passes stated reset, prior-rollout, and planner checks on the corridor fixture", feedback: "Correct: the claim matches the preserved evidence." }, { text: "Good reconstruction proves correct planning", feedback: "Planning needs action/reward and rollout evidence." }], answer: 1, worked: "Publish raw fixture transitions, configs, checks, failures, and a bounded statement about the tested environment.", retry: "Replace every broad adjective with the exact environment, metric, horizon, and gate you measured." },
    motion: { concept: "pipeline", headline: "Two landmark systems divide representation, dynamics, and control differently.", intro: "The shared pipeline highlights which modules are separate, which states are stochastic, and where action selection consults imagined futures.", labels: ["ENCODE", "DYNAMICS", "IMAGINE", "CONTROL"], titles: ["Observations become compact latent evidence.", "Recurrent dynamics carry state through actions.", "The learned model generates possible futures.", "A controller or online planner chooses real actions."] },
  }),
] as const;
