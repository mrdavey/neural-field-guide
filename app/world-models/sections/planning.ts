import { defineWorldModelLesson } from "../helpers";
import { wmSources } from "../sources";

export const worldModelPlanningSpecs = [
  defineWorldModelLesson({
    id: "imagined-rollouts", track: "wm-planning", title: "Imagined Rollouts and Return Estimates", number: 21,
    plain: "An imagined rollout applies candidate actions inside the learned model and adds predicted rewards to estimate what might happen.",
    precise: "Starting from filtered state $z_t$, a rollout samples or propagates $z_{t+k+1}\\sim p_\\theta(\\cdot\\mid z_{t+k},a_{t+k})$, predicts rewards and continuation, and computes a finite-horizon return plus optional terminal value. The rollout is conditional evidence from the learned model, not an environment measurement. Seeds, stochastic samples, horizon, discount, action bounds, and termination must be explicit.",
    analogy: "It is rehearsing several routes on a simulator before driving the first road segment.", analogyBreak: "the simulator can reward impossible shortcuts, so more rehearsal can amplify its errors.",
    ideas: ["Anchor imagination in an observation-filtered state and valid candidate actions.", "Accumulate reward, continuation, discount, and terminal value with correct timing.", "Preserve model identity, samples, uncertainty, and rollout horizon in every estimate."],
    worked: "Predicted rewards [2,1,4], continuation [1,1,0], and $\\gamma=0.9$ give $2+0.9(1)+0.9^2(4)=6.14$; no terminal value is added after predicted termination. If continuation probability were 0.5 at step two, later terms would also be survival-weighted.",
    misconception: "A high imagined return is not evidence that the real environment will deliver it; it is a model-dependent proposal for comparison and testing.",
    quiz: { question: "What should happen after predicted terminal continuation 0?", options: ["Keep adding ordinary future rewards", "Stop or zero later contribution", "Reset discount to 1", "Use posterior observations"], answer: 1, explanation: "No within-episode future follows a terminal state, so later return contribution is removed." }, lab: "wm-rollout", prerequisites: ["uncertainty-ensembles"], sources: [wmSources.dreamer, wmSources.planet],
    objectives: ["Trace latent states, rewards, continuation, and discount through an imagined return", "Bound an imagined-return claim using model and horizon evidence"],
    vocabulary: [{ term: "Imagination", meaning: "Predicted trajectories generated inside a learned dynamics model." }, { term: "Rollout horizon", meaning: "The number of model transitions simulated before stopping or bootstrapping." }, { term: "Terminal value", meaning: "An estimate of return beyond the explicit rollout horizon." }],
    stages: ["Filter start", "Roll actions", "Aggregate return"], checkpoints: ["Record the real observation time and starting latent distribution.", "Log every action, state sample, reward, continuation, and model version.", "Apply discount/termination once and label any bootstrap value."],
    primaryCheck: { prompt: "Compute return for rewards [1,2], $\\gamma=0.5$, then terminal value 6 after the second reward.", expected: "$G=1+0.5(2)+0.5^2(6)=3.5$, assuming continuation through both modeled rewards and bootstrap after two transitions.", retry: "Write each reward at powers $\\gamma^0,\\gamma^1$ and the terminal value at $\\gamma^2$." },
    decision: { explanation: "A rollout estimate must be stress-tested where planning is likely to exploit the model.", mechanism: "Compare predicted and real returns on replayable action sequences, slice by uncertainty and horizon, inspect top-ranked sequences, and reject plans outside calibrated support or safety gates.", workedExample: "Top imagined sequence scores 12 but real replay score is -3 because it passes through an unsupported wall state. The planner found model exploitation, not a successful strategy.", boundary: "Replay evaluation covers only executable recorded sequences; new policies still require bounded online validation.", check: { prompt: "Increasing planning samples raises imagined return while real return falls. What is the leading diagnosis?", expected: "Search is increasingly exploiting model error or miscalibrated reward predictions. Inspect top trajectories, support, uncertainty, and real-vs-imagined return gaps.", retry: "Compare what changed—search breadth—while model and environment stayed fixed; more search cannot repair a biased objective." } },
    transfer: { prompt: "How should a report label a browser rollout?", options: [{ text: "Measured robot performance", feedback: "No real robot was executed." }, { text: "Deterministic or stochastic model simulation under stated fixtures", feedback: "Correct: the evidence source and boundary are explicit." }, { text: "Published benchmark result", feedback: "A local fixture is not external evidence." }], answer: 1, worked: "Record model/fixture, seed, horizon, assumptions, and which claims require real execution.", retry: "Name the system that produced every number before interpreting it." },
    motion: { concept: "pipeline", headline: "An imagined return is a transparent chain of model-dependent states and outcomes.", intro: "The path begins at a filtered state, branches through candidate actions, and accumulates only valid rewards before a labeled bootstrap.", labels: ["START STATE", "ACTIONS", "PREDICT", "RETURN"], titles: ["Anchor in current observed evidence.", "Propose a bounded action sequence.", "Roll dynamics, reward, and continuation heads.", "Discount, stop, and optionally bootstrap."] },
  }),
  defineWorldModelLesson({
    id: "shooting-cem", track: "wm-planning", title: "Random Shooting and the Cross-Entropy Method", number: 22,
    plain: "Shooting planners sample action sequences, score them in the model, and keep or refine the sequences with the best predicted returns.",
    precise: "Random shooting draws $N$ bounded sequences from a proposal distribution and selects the highest predicted score. CEM iterates: sample, evaluate, choose an elite fraction, refit proposal mean/scale, and repeat. For stochastic models, each sequence needs enough outcome samples or a risk objective. Population, horizon, elite fraction, iterations, action smoothing, and compute budget jointly determine search.",
    analogy: "Random shooting throws many darts; CEM moves the dartboard's aiming distribution toward the best cluster.", analogyBreak: "the best model-scored dart may land in a simulator flaw rather than a good real outcome.",
    ideas: ["Define action bounds, proposal distribution, population, horizon, and score.", "Refit CEM only from declared elites while maintaining exploration scale.", "Compare search methods under matched model-call and sample budgets."],
    worked: "Sample four scalar actions with returns [1,5,4,0]. With elite count 2, actions producing 5 and 4 set the next mean and scale. If both are near +0.8, the next proposal concentrates there but must retain minimum variance to avoid premature collapse.",
    misconception: "CEM is not learning the world model; it is an online optimizer over actions using whatever errors the model contains.",
    quiz: { question: "What does CEM refit after each iteration?", options: ["The environment", "The action-sequence proposal from elite samples", "The observation encoder only", "The reward definition"], answer: 1, explanation: "CEM updates the sampling distribution over candidate actions, not model parameters." }, lab: "wm-planner", prerequisites: ["imagined-rollouts"], sources: [wmSources.cem, wmSources.planet, wmSources.tdmpc],
    objectives: ["Execute one CEM sample–score–elite–refit iteration", "Compare planners under a matched model-evaluation budget"],
    vocabulary: [{ term: "Shooting method", meaning: "Planning by sampling complete candidate action sequences and evaluating them." }, { term: "Elite set", meaning: "The highest-scoring samples used to refit a CEM proposal." }, { term: "Proposal distribution", meaning: "The distribution from which candidate action sequences are drawn." }],
    stages: ["Sample", "Score and select", "Refit"], checkpoints: ["Count exact model rollouts and enforce action bounds.", "Apply the same risk-adjusted score to every sequence.", "Compute elite statistics and preserve a declared variance floor."],
    primaryCheck: { prompt: "CEM samples actions [-1,0,1,2] with scores [0,3,5,1] and keeps two elites. Which actions refit the proposal?", expected: "Actions 1 and 0 are elites with scores 5 and 3; their mean is 0.5 and their population standard deviation is 0.5 before any smoothing or variance floor.", retry: "Sort by score, keep the declared count, then compute statistics from action values—not scores." },
    decision: { explanation: "Planning comparisons must match the expensive unit: model transitions or sampled futures.", mechanism: "Fix horizon, total transition evaluations, stochastic samples, model checkpoint, starts, and score; report success, regret, latency, uncertainty, and exploit failures across seeds.", workedExample: "Random shooting uses 1,000 sequences once; CEM uses 5 iterations ×200 sequences. Both receive 1,000 sequence evaluations. Comparing CEM 5,000 vs shooting 1,000 would confound algorithm and budget.", boundary: "Equal sequence counts are not equal compute if horizons, ensemble members, or stochastic samples differ.", check: { prompt: "CEM wins after ten times more model transitions. What can you conclude?", expected: "Only that the method-plus-budget performed better. Re-run at matched transition and latency budgets before attributing gain to CEM.", retry: "Count the innermost model transition calls for each planner." } },
    transfer: { prompt: "Which CEM safeguard addresses early proposal collapse?", options: [{ text: "Set variance to zero after first elite", feedback: "That removes exploration." }, { text: "Use smoothing or a minimum variance while monitoring elite diversity", feedback: "Correct: refinement remains bounded but not frozen." }, { text: "Increase reward scale", feedback: "Reward scale does not restore proposal diversity." }], answer: 1, worked: "Log proposal mean/scale each iteration and stop when improvement or diversity reaches declared criteria.", retry: "Inspect whether the next sampler can still produce alternatives outside the current elite cluster." },
    motion: { concept: "optimization", headline: "CEM moves a distribution over action sequences toward high-scoring regions.", intro: "Candidate points scatter, elites become visible, and the next proposal contracts without erasing its variance floor.", labels: ["PROPOSAL", "SAMPLES", "ELITES", "REFIT"], titles: ["Define bounded distributions for every action step.", "Draw candidates under a fixed rollout budget.", "Rank by one declared return/risk score.", "Update proposal statistics and repeat."] },
  }),
  defineWorldModelLesson({
    id: "model-predictive-control", track: "wm-planning", title: "Model-Predictive Control and Replanning", number: 23,
    plain: "Model-predictive control plans several actions, executes only the first, observes what happened, and plans again.",
    precise: "At each real step, MPC estimates current state, optimizes an action horizon under a dynamics model, applies $a_t$ only, receives $o_{t+1}$, filters state, and repeats. Replanning limits open-loop exposure and adapts to disturbances, but increases compute and cannot repair errors within the first action or unobserved hazards. Warm starts and action constraints improve continuity and feasibility.",
    analogy: "It is navigating one turn at a time while redrawing the route after every new landmark.", analogyBreak: "frequent redraws still use the same flawed map and may be too slow for fast dynamics.",
    ideas: ["Separate planning horizon from the single executed action.", "Filter new evidence before shifting or warm-starting the next plan.", "Budget latency, actuator constraints, and fallback before closed-loop use."],
    worked: "A planner proposes [right,right,up]. It executes right; unexpected slip leaves the agent lower than predicted. MPC observes the slip and replans [up,right,up] rather than blindly executing the old second right.",
    misconception: "MPC does not mean the complete optimized sequence is safe to execute open loop.",
    quiz: { question: "How many actions from an MPC plan are normally executed before observing again?", options: ["All actions", "Only the first action", "None", "A random half"], answer: 1, explanation: "Receding-horizon control executes one action and replans from updated evidence." }, lab: "wm-planner", prerequisites: ["shooting-cem"], sources: [wmSources.planet, wmSources.tdmpc],
    objectives: ["Trace one complete observe–plan–act–reobserve MPC cycle", "Choose a replanning rate and fallback from dynamics and latency evidence"],
    vocabulary: [{ term: "MPC", meaning: "Receding-horizon control that repeatedly optimizes, executes the first action, and replans." }, { term: "Warm start", meaning: "Initializing the next plan from the unused tail of the previous plan." }, { term: "Control rate", meaning: "How frequently the controller can update and issue actions." }],
    stages: ["Estimate state", "Optimize horizon", "Execute and reobserve"], checkpoints: ["Use the latest observation and reset-safe belief.", "Enforce bounds and complete within the latency budget.", "Execute one action, record outcome, and invalidate stale plans."],
    primaryCheck: { prompt: "A four-action plan is computed, the first action slips, and a new observation arrives. What happens next?", expected: "Update/filter state using the slip observation, then re-optimize or warm-start a new feasible plan; do not execute action two from the stale state blindly.", retry: "Mark the exact point where real evidence differs from the model rollout and restart the control cycle there." },
    decision: { explanation: "Replanning rate must be fast enough for disturbances and slow enough to finish reliable optimization.", mechanism: "Measure model/optimizer latency distribution, environment timescale, action hold duration, disturbance response, missed deadlines, and fallback behavior under load.", workedExample: "Robot dynamics change materially in 50 ms, but p95 planning takes 80 ms. A 20 Hz policy misses deadlines; shorten horizon/model, use a policy fallback, or lower control ambition before deployment.", boundary: "Average planning latency is insufficient; tail deadlines and sensor/actuator delays determine closed-loop stability.", check: { prompt: "Mean planner latency is 30 ms under a 50 ms deadline, but p99 is 120 ms. Is the loop ready?", expected: "No. Tail misses can leave stale actions active. Add bounded compute, deadline-aware fallback, or capacity changes and test closed-loop deadline failures.", retry: "Compare every latency percentile with the environment's safe action-hold limit." } },
    transfer: { prompt: "Why can MPC outperform open-loop execution with the same model?", options: [{ text: "It changes model weights after every action", feedback: "Ordinary MPC need not train the model online." }, { text: "It incorporates new observations and corrects plans after disturbances", feedback: "Correct: replanning limits accumulated open-loop mismatch." }, { text: "It guarantees no first-step error", feedback: "The first action can still be wrong." }], answer: 1, worked: "Attribute the gain to feedback/replanning and retain bounds on model, latency, and first-step risk.", retry: "Identify what new information is available after action one that was absent in the original plan." },
    motion: { concept: "agent", headline: "MPC closes the loop after every real action.", intro: "Plan nodes lead to one guarded execution, then observation returns the process to state estimation instead of continuing down a stale sequence.", labels: ["OBSERVE", "PLAN", "ACT ONCE", "REPLAN"], titles: ["Filter the latest real evidence.", "Optimize a bounded future sequence.", "Validate and execute only the first action.", "Use the new outcome to rebuild the plan."] },
  }),
  defineWorldModelLesson({
    id: "differentiable-planning", track: "wm-planning", title: "Differentiable Latent Planning", number: 24,
    plain: "If dynamics and rewards are differentiable, gradients can adjust a continuous action sequence to improve predicted return.",
    precise: "For continuous action variables $a_{t:t+H-1}$, unroll differentiable dynamics and objective $J$ in latent space, compute $\\nabla_a J$ through the rollout, and optimize actions subject to bounds. Reparameterization can handle stochastic latents. Gradients are efficient locally but can vanish, explode, follow nonconvex shortcuts, or exploit model derivatives outside data support.",
    analogy: "Instead of trying many routes, push the planned controls uphill along the simulator's slope.", analogyBreak: "the slope belongs to the learned simulator and may point toward a physically impossible loophole.",
    ideas: ["Treat actions as bounded optimization variables while model parameters remain fixed.", "Backpropagate through each transition, reward, and continuation term.", "Use restarts, constraints, gradient checks, and support tests before trusting a local optimum."],
    worked: "One-step model $s'=s+a$, reward $-(s'-3)^2$, start $s=0$, action $a=1$. $dJ/da=-2(s+a-3)=4$, so gradient ascent raises $a$ toward bound 2, improving predicted position toward target 3.",
    misconception: "Differentiable planning is not the same as training the dynamics model; it optimizes action inputs through fixed model parameters.",
    quiz: { question: "During differentiable planning, what is normally updated?", options: ["Candidate action variables", "Environment physics", "Training dataset", "Observation labels"], answer: 0, explanation: "Gradients change the planned actions while learned model weights are held fixed." }, lab: "wm-planner", prerequisites: ["model-predictive-control"], sources: [wmSources.universalPlanningNetworks, wmSources.differentiableMpc, wmSources.dreamer, wmSources.tdmpc],
    objectives: ["Differentiate a predicted return with respect to a continuous action", "Diagnose a gradient-planning shortcut outside model support"],
    vocabulary: [{ term: "Action gradient", meaning: "The derivative of predicted objective with respect to candidate action values." }, { term: "Projected update", meaning: "A gradient step followed by enforcement of action bounds or constraints." }, { term: "Local optimum", meaning: "A point better than nearby alternatives but not necessarily globally best." }],
    stages: ["Unroll", "Differentiate", "Project and verify"], checkpoints: ["Freeze model parameters and log the action sequence.", "Check derivative paths through dynamics/reward and horizon.", "Enforce bounds and compare with sampled alternatives and support."],
    primaryCheck: { prompt: "For objective $J=-(a-2)^2$ at $a=0$, compute $dJ/da$ and one ascent step with rate 0.25.", expected: "$dJ/da=-2(a-2)=4$; update $a\\leftarrow0+0.25(4)=1$, which raises $J$ from -4 to -1.", retry: "Differentiate the negative square, evaluate at the current action, then add the ascent step." },
    decision: { explanation: "A large action gradient may indicate useful sensitivity or a model discontinuity/shortcut.", mechanism: "Run finite-difference checks, multiple starts, action-support constraints, ensemble disagreement, and real/replay validation of optimized sequences; compare against sampling under matched calls.", workedExample: "Gradient optimizer drives throttle to 1.0 where replay covers only ≤0.6 and predicts impossible reward. Projecting into support lowers imagined score but improves real outcome.", boundary: "Hard support projection can be overly conservative when safe novel actions exist; expansion needs targeted evidence.", check: { prompt: "Optimized actions sit on bounds with high ensemble disagreement. What should happen before execution?", expected: "Treat the plan as unsupported: reject/fallback or collect safe evidence, inspect gradient/model error, and compare constrained alternatives.", retry: "Overlay action path on replay support and uncertainty; do not interpret objective alone." } },
    transfer: { prompt: "Which check distinguishes planning gradients from model-training gradients?", options: [{ text: "Verify model weights changed", feedback: "They should remain fixed during action optimization." }, { text: "Confirm gradients target action tensors while parameter gradients are disabled/cleared", feedback: "Correct: the optimization variable is explicit." }, { text: "Remove action bounds", feedback: "That worsens shortcut risk." }], answer: 1, worked: "Freeze parameters, inspect `.grad` ownership, finite-difference an action, then validate the resulting plan.", retry: "Name the variable the optimizer is allowed to mutate." },
    motion: { concept: "optimization", headline: "Differentiable planning climbs a learned return surface in action space.", intro: "Action points move along model gradients, hit feasibility boundaries, and are finally checked against support and alternative starts.", labels: ["ACTIONS", "ROLLOUT", "GRADIENT", "PROJECT"], titles: ["Initialize bounded continuous action variables.", "Compute predicted consequences and return.", "Backpropagate return into action coordinates.", "Enforce constraints and verify the candidate."] },
  }),
  defineWorldModelLesson({
    id: "actor-critic-lambda", track: "wm-planning", title: "Actor–Critic Learning and λ-Returns", number: 25,
    plain: "An actor chooses actions; a critic estimates future return; λ-returns blend short and long backups to train both from trajectories.",
    precise: "The actor $\\pi_\\eta(a\\mid z)$ produces actions; the world model transforms states and actions into imagined rewards, continuations, and next states; and the critic $V_\\xi(z)$ estimates expected future return. A recursive target is $G_t^\\lambda=r_t+\\gamma c_t[(1-\\lambda)V(z_{t+1})+\\lambda G_{t+1}^\\lambda]$. The stopped target trains critic output toward $G_t^\\lambda$, while actor parameters update toward actions with larger estimated $G_t^\\lambda$ plus declared regularizers. Model bias therefore enters both outputs.",
    analogy: "The actor drives while the critic predicts the remaining trip value and coaches future choices.", analogyBreak: "both can agree on a simulator exploit and reinforce it without real-world correctness.",
    ideas: ["Separate policy outputs, sampled actions, reward targets, and value estimates.", "Trace bootstrap timing and stop gradients through λ-return targets.", "Evaluate actor, critic, and world-model errors independently before combining them."],
    worked: "For $r_t=1$, $\\gamma=0.9$, $c_t=1$, $V(z_{t+1})=4$, longer target $G_{t+1}^\\lambda=3.8$, and $\\lambda=0.5$, the blend inside brackets is $0.5(4)+0.5(3.8)=3.9$, so $G_t^\\lambda=1+0.9(3.9)=4.51$. The critic moves $V(z_t)$ toward the stopped 4.51 target; the actor changes its action probabilities toward imagined actions with higher return, without treating critic target construction as a second dynamics model.",
    misconception: "A critic is not a second world model; it compresses expected return and may omit observation dynamics entirely.",
    quiz: { question: "What does increasing $\\lambda$ generally emphasize?", options: ["Longer sampled returns relative to short bootstraps", "Only immediate reward", "No future rewards", "More observation pixels"], answer: 0, explanation: "Higher λ shifts weight toward longer-horizon return estimates." }, lab: "wm-rollout", prerequisites: ["differentiable-planning"], sources: [wmSources.dreamer, wmSources.dreamerV3],
    objectives: ["Trace actor, critic, and λ-return roles through one update", "Separate critic error from world-model error when policy learning fails"],
    vocabulary: [{ term: "Actor", meaning: "A parameterized policy that selects actions." }, { term: "Critic", meaning: "A learned estimate of expected future return." }, { term: "λ-return", meaning: "A weighted mixture of multi-step return targets with bootstrapping." }],
    stages: ["Imagine trajectory", "Build value target", "Update actor/critic"], checkpoints: ["Log model rewards/states and sampled policy actions.", "Show discounts, continuation, λ weights, and bootstrap stop-gradient.", "Evaluate value calibration and action outcomes on held-out starts."],
    primaryCheck: { prompt: "Commit one complete update for $r_t=1$, $\\gamma=0.9$, $c_t=1$, $V(z_{t+1})=4$, $G_{t+1}^\\lambda=3.8$, and $\\lambda=0.5$: compute $G_t^\\lambda$ and state the separate critic and actor outputs.", expected: "$G_t^\\lambda=1+0.9[0.5(4)+0.5(3.8)]=4.51$. The critic output $V(z_t)$ is regressed toward a stopped 4.51 target. The actor output distribution is updated so sampled actions that lead to larger estimated returns become more likely, subject to entropy/constraint terms; actor and critic have different parameters and products.", retry: "Calculate the value/long-return mixture inside brackets, apply continuation and discount once, add reward, then name which network predicts a scalar value and which predicts an action distribution." },
    decision: { explanation: "Policy failure can originate in model predictions, critic valuation, actor optimization, or their distribution shift.", mechanism: "Use oracle components one at a time: real/replay rewards with model states, Monte Carlo returns instead of critic, or search actions instead of actor; locate the earliest replacement that closes the gap.", workedExample: "Actor chooses a bad route. Search using the same model also chooses it: model/reward is suspect. If search succeeds but actor fails, policy optimization or capacity is suspect.", boundary: "Oracle replacements diagnose a pipeline stage on the tested data; they do not eliminate interacting errors elsewhere.", check: { prompt: "The critic predicts high value, but Monte Carlo returns from the same model are low. Which stage fails first?", expected: "The critic is miscalibrated relative to model rollouts. Repair value targets/optimization before blaming actor choice, while still auditing model realism separately.", retry: "Hold model trajectory fixed and compare critic prediction with the return computed directly from its rewards." } },
    transfer: { prompt: "Which intervention isolates actor quality?", options: [{ text: "Change world model and actor together", feedback: "That confounds stages." }, { text: "Compare actor actions with a stronger planner using the same model and starts", feedback: "Correct: dynamics evidence is held fixed." }, { text: "Inspect critic loss only", feedback: "Critic loss does not directly test actor action quality." }], answer: 1, worked: "Use same model, state set, constraints, and budget; compare predicted and real outcomes for actor versus planner actions.", retry: "Replace one component at a time while freezing upstream evidence." },
    motion: { concept: "agent", headline: "Actor and critic learn different summaries from the same imagined futures.", intro: "World-model rollouts feed a return target to the critic and an action-improvement signal to the actor, with their error paths kept separate.", labels: ["IMAGINE", "λ-RETURN", "CRITIC", "ACTOR"], titles: ["The world model supplies latent trajectories and rewards.", "Multi-step targets blend evidence and bootstrap.", "The critic learns expected future return.", "The actor moves toward actions with higher estimated value."] },
  }),
  defineWorldModelLesson({
    id: "dreamer-imagination", track: "wm-planning", title: "Dreamer and Learning Behavior in Imagination", number: 26,
    plain: "Dreamer learns a world model from experience, then trains actor and critic on imagined latent trajectories before collecting more real data.",
    precise: "Dreamer alternates experience collection, replay-based RSSM learning, and latent imagination. From posterior start states it rolls the prior under actor actions, predicts reward/continuation, builds λ-returns, trains critic, and improves actor through analytic or reinforce-style gradients depending on variants. DreamerV2 uses discrete stochastic latents; DreamerV3 adds normalization, balancing, and transformations for a more uniform configuration across domains.",
    analogy: "The agent studies a simulator built from its own experience, practices many futures cheaply, then returns to gather evidence where practice is weak.", analogyBreak: "imagined volume does not add new environmental facts and can magnify systematic model errors.",
    ideas: ["Trace collection, world-model learning, imagination, actor/critic learning, and execution as separate loops.", "Keep posterior start states and prior-only imagined continuations distinct.", "Connect every reported improvement to matched environment steps, compute, and evaluation protocol."],
    worked: "Replay supplies 50-step sequences. RSSM filters them; imagination starts from selected posterior states and runs 15 prior steps using actor actions. Reward/continuation heads create λ-targets. Only the actor returns to the real environment to collect the next batch.",
    misconception: "Dreamer does not train entirely without real interaction; the learned imagination is repeatedly grounded in collected experience.",
    quiz: { question: "Where do Dreamer imagined rollouts usually start?", options: ["Arbitrary pixels only", "Posterior states inferred from replay experience", "The final benchmark score", "A human-written plan"], answer: 1, explanation: "Posterior states anchor imagination in states inferred from real recorded trajectories." }, lab: "wm-rollout", prerequisites: ["actor-critic-lambda"], sources: [wmSources.dreamer, wmSources.dreamerV3],
    objectives: ["Trace Dreamer's collection–model–imagination–behavior loop", "Audit a Dreamer comparison for matched data, compute, and model-error evidence"],
    vocabulary: [{ term: "Latent imagination", meaning: "Actor-driven future rollouts generated in compact model state space." }, { term: "Posterior start", meaning: "A rollout initial state inferred using recorded observation evidence." }, { term: "Behavior learning", meaning: "Updating actor and critic from predicted trajectories and returns." }],
    stages: ["Collect and replay", "Learn world model", "Imagine and update behavior"], checkpoints: ["Record environment steps, policy version, and replay sampling.", "Report all model losses and prior-only horizon tests.", "Separate imagined transitions from real transitions and evaluate both."],
    primaryCheck: { prompt: "Before revealing the answer, write Dreamer's full loop from one real collection batch to the next real action, naming the state path and both behavior updates.", expected: "The actor executes real actions to collect transitions; replay sequences train the RSSM observation/reward/continuation heads; filtering produces posterior start states; imagination then uses only the prior plus actor actions to predict states, rewards, and continuations; those predictions form λ-returns; the critic learns their value targets and the actor learns higher-return actions; the updated actor selects the next real action and collection repeats.", retry: "Use seven boxes in order: collect, replay/model, posterior start, prior imagination, λ-return, critic/actor, real action. Mark the last box allowed to read a recorded observation." },
    decision: { explanation: "A fair comparison controls real interaction, compute, task protocol, seeds, and evaluation—not only final return.", mechanism: "Match environment steps and evaluation starts, report wall/accelerator compute and imagined transitions, use multiple seeds/intervals, inspect failure slices and model exploitation, and preserve configs/checkpoints.", workedExample: "Agent A uses 100k real steps and 10B imagined states; B uses 1M real steps. Calling A simply 'more sample efficient' is valid for environment steps but incomplete for total compute.", boundary: "A single configuration across reported domains does not prove universal tuning-free performance outside those tasks and implementations.", check: { prompt: "Dreamer uses fewer environment steps but 5× training compute. How should the conclusion be worded?", expected: "It is more environment-interaction efficient in the matched evaluation, while total compute is higher; report both rather than claiming unqualified efficiency.", retry: "Put environment steps, model transitions, hardware time, and final outcomes in separate columns." } },
    transfer: { prompt: "Which statement matches the evidence boundary?", options: [{ text: "Imagined experience replaces real evidence", feedback: "Imagination reuses a learned model and adds no new environmental facts." }, { text: "Imagined trajectories amplify learning from collected evidence but inherit model bias", feedback: "Correct: benefit and limitation are both present." }, { text: "Posterior observations are available throughout imagination", feedback: "Only the start is observation-corrected." }], answer: 1, worked: "Label transition sources in every metric and audit the gap between predicted and real outcomes.", retry: "Ask where each state came from: recorded observation or learned prior." },
    motion: { concept: "training", headline: "Dreamer alternates real evidence collection with large-scale latent practice.", intro: "Two loops remain visually distinct: environment interaction updates replay, while model imagination updates actor and critic without pretending to be new evidence.", labels: ["COLLECT", "MODEL", "IMAGINE", "ACT"], titles: ["The current actor gathers real trajectories.", "Replay trains a filtered latent dynamics model.", "Posterior starts seed prior-only practice.", "Updated behavior returns to the environment."] },
  }),
  defineWorldModelLesson({
    id: "muzero-tree-search", track: "wm-planning", title: "MuZero and Task-Relevant Tree Search", number: 27,
    plain: "MuZero learns a hidden dynamics model that predicts rewards, values, and policies useful for search without reconstructing the environment observation.",
    precise: "A representation function maps observation history to hidden state; a dynamics function maps hidden state and action to next hidden state plus reward; a prediction function outputs policy and value. Monte Carlo tree search repeatedly expands actions using these quantities, backs up values, and produces an improved search policy used for action selection and training targets. The hidden state need preserve task-relevant search information, not literal pixels.",
    analogy: "It is a chess analyst that tracks only what helps evaluate moves rather than repainting the board after each hypothetical move.", analogyBreak: "task-relevant prediction may discard facts needed for new tasks, safety analysis, or human inspection.",
    ideas: ["Separate representation, recurrent dynamics, prediction, and tree-search roles.", "Trace selection, expansion, evaluation, and backup through one search path.", "Keep task performance claims separate from full environment-model claims."],
    worked: "Root hidden state has prior policy [0.7,0.3]. Search expands action B despite lower prior; its predicted reward 1 and next value 5 back up above action A's value 3. Visit counts shift the improved policy toward B, which may become the real action.",
    misconception: "MuZero's success does not mean it learned a human-interpretable or observation-complete simulator.",
    quiz: { question: "Which outputs does MuZero learn for planning?", options: ["Only reconstructed pixels", "Reward, value, and policy from learned hidden dynamics", "A tokenizer vocabulary", "Only terminal flags"], answer: 1, explanation: "MuZero predicts planning-relevant reward, value, and policy quantities." }, lab: "wm-planner", prerequisites: ["dreamer-imagination"], sources: [wmSources.muzero, wmSources.mbrlSurvey],
    objectives: ["Trace MuZero representation, dynamics, prediction, and MCTS backup", "Bound what task-relevant dynamics do and do not establish about the world"],
    vocabulary: [{ term: "Search policy", meaning: "An action distribution derived from tree-search visit counts." }, { term: "Task-relevant model", meaning: "A model trained to preserve quantities needed for a specified objective rather than all observations." }, { term: "Backup", meaning: "Propagating evaluated reward/value information from a search leaf toward the root." }],
    stages: ["Represent root", "Expand dynamics", "Back up search"], checkpoints: ["State which observation history enters representation.", "Log action, predicted reward, next hidden state, policy, and value.", "Show discount and visit-count update on the exact path."],
    primaryCheck: { prompt: "Trace one MuZero search path: history $o_{0:t}$, root prior $[0.7,0.3]$, selected action B, predicted edge reward 1, child policy/value $[0.2,0.8]$ and 5, with $\\gamma=0.5$. Name each learned function, expansion, and backup result.", expected: "Representation maps $o_{0:t}$ to root hidden state. Selection uses root statistics/prior to choose B. Recurrent dynamics maps root state and B to child hidden state plus reward 1. Prediction maps the child to policy [0.2,0.8] and value 5. Expansion adds that child; backup sends $1+0.5(5)=3.5$ toward the root and increments path visits, so visit counts can change the search policy used for action and training.", retry: "Label representation only at the observed root, dynamics/reward on the chosen action edge, policy/value at the reached node, then discount the leaf value once and propagate it with visit evidence." },
    decision: { explanation: "Task-relevant models can excel at one objective while being unsuitable as general simulators or transfer representations.", mechanism: "Evaluate original task search, changed rewards/tasks, observation probes, safety variables, and hidden-state interventions; state which information the training targets require and omit.", workedExample: "A model trained to win a racing game preserves lap reward and value but not dashboard fuel because fuel never affects score. It cannot support a later fuel-efficiency task without new evidence/training.", boundary: "Failure to reconstruct a variable does not invalidate performance on the original task; it narrows the model's reusable claim.", check: { prompt: "MuZero-style hidden states solve Task A but omit a variable needed by Task B. Is that contradictory?", expected: "No. The model can be sufficient for Task A's reward/search targets and insufficient for Task B. Transfer requires evaluating or retraining the missing distinction.", retry: "List the targets used during training and ask whether the new variable could change any of them." } },
    transfer: { prompt: "Which label is precise?", options: [{ text: "A complete learned physics engine", feedback: "Task targets do not establish observation-complete physics." }, { text: "An iterable task-relevant model supporting search on the evaluated domains", feedback: "Correct: it matches the demonstrated interface." }, { text: "A model-free policy", feedback: "Search explicitly uses learned dynamics." }], answer: 1, worked: "Name predicted quantities, search use, domains, and omitted reconstruction claim.", retry: "Describe the inputs and outputs of the learned recurrent model instead of using a broad label." },
    motion: { concept: "agent", headline: "MuZero uses learned hidden dynamics as an engine for tree search.", intro: "The tree separates representation at the root, recurrent action expansion, reward/value prediction, and backed-up visit evidence.", labels: ["ROOT", "EXPAND", "EVALUATE", "BACKUP"], titles: ["Observation history becomes a hidden search state.", "Candidate actions produce next hidden states and rewards.", "Policy and value score newly reached nodes.", "Returns and visits reshape the root action policy."] },
  }),
  defineWorldModelLesson({
    id: "dyna-tdmpc-case-study", track: "wm-planning", title: "Case Study — Dyna, Dreamer, MuZero and TD-MPC", number: 28, duration: 40,
    plain: "This synthesis compares four ways learned models enter behavior: replay-style planning, imagined policy learning, tree search, and short-horizon latent MPC with terminal value.",
    precise: "Dyna interleaves real experience with model-generated updates; Dreamer trains actor/critic in latent imagination; MuZero trains task-relevant recurrent dynamics for MCTS; TD-MPC jointly learns task-oriented latent dynamics and value, optimizes a short action horizon, and bootstraps with terminal value. Their model targets, online compute, data use, and claim boundaries differ, so comparison must match task and budgets.",
    analogy: "Four expedition teams use the map differently: rehearse past routes, train a driver, search a tree, or optimize the next few turns with a destination estimate.", analogyBreak: "the metaphor hides major differences in observation modality, objective, compute, and published evaluation.",
    ideas: ["Map model inputs, predicted quantities, and action-selection mechanism for each family.", "Separate environment data efficiency, training compute, and per-action planning compute.", "Choose a family from task horizon, action space, latency, uncertainty, and evidence—not fashion."],
    worked: "For a 20-step consequence, TD-MPC may plan 5 explicit steps then use $V(z_{t+5})$; MuZero expands a selective tree; Dreamer actor amortizes action choice after imagination training; Dyna adds synthetic updates to another learner. Each pays compute in a different place.",
    misconception: "These methods do not form a single ladder where the newest universally replaces the others.",
    quiz: { question: "What closes TD-MPC's short explicit planning horizon?", options: ["A terminal value estimate", "A pixel decoder only", "No future estimate", "A tokenizer"], answer: 0, explanation: "TD-MPC uses a learned value to summarize return beyond local trajectory optimization." }, lab: "wm-planner", prerequisites: ["muzero-tree-search"], sources: [wmSources.dyna, wmSources.dreamerV3, wmSources.muzero, wmSources.tdmpc],
    objectives: ["Compare Dyna, Dreamer, MuZero, and TD-MPC by model target and compute placement", "Run a matched-budget planning tournament and make a bounded controller decision"],
    vocabulary: [{ term: "Dyna", meaning: "A framework interleaving learning from real and model-generated experience." }, { term: "Amortized policy", meaning: "A learned actor that produces actions directly after paying training-time optimization cost." }, { term: "Terminal value", meaning: "A value estimate summarizing return after an explicit planning horizon." }],
    stages: ["Map contracts", "Match budgets", "Decide with failures"], checkpoints: ["List each learned target and action-selection path.", "Count real steps, model transitions, training compute, and online latency.", "Include uncertainty, exploitation, and task slices in the recommendation."],
    primaryCheck: { prompt: "Fill model target, training-time model use, and per-action online compute for Dyna, Dreamer, MuZero, and TD-MPC.", expected: "Dyna learns transition/reward predictions and spends model compute generating synthetic updates during learning, while final online cost depends on its attached learner/planner. Dreamer learns latent dynamics plus observation/reward/continuation heads, spends large training compute on imagined actor/critic updates, then usually acts directly with low search cost. MuZero learns recurrent hidden dynamics/reward plus policy/value and spends per-action compute on MCTS. TD-MPC learns task-oriented latent dynamics, reward and value, then spends per-action compute on short-horizon trajectory optimization closed by terminal value.", retry: "Make four rows. For each, first name what the learned model predicts, then locate model calls during training and finally count model evaluations before one real action." },
    decision: { explanation: "A tournament is fair only when the workload and expensive resources are predeclared.", mechanism: "Use identical environment starts/data/model observations where applicable, match real steps and at least report model-call/latency budgets, run seeds, preserve raw trajectories, and gate safety before ranking return.", workedExample: "On a deterministic corridor, CEM-MPC and TD-value MPC receive 2,000 model transitions per episode; actor receives comparable training data and measured inference latency. Report return, collisions, compute, and confidence—not a single winner.", boundary: "A tiny deterministic fixture tests planner mechanics, not published sample efficiency or complex visual representation.", check: { prompt: "Planner A has higher return but violates one collision gate; Planner B is slower and safe. Which can deploy under zero-collision policy?", expected: "Only B remains feasible, subject to latency requirements. Higher return cannot compensate for the non-compensable collision gate.", retry: "Apply safety feasibility before ranking return or speed." } },
    transfer: { prompt: "Which recommendation is well-scoped?", options: [{ text: "TD-MPC is universally best", feedback: "The fixture and budgets cannot support a universal claim." }, { text: "Under this workload and matched model-transition budget, method X met safety/latency gates and had higher return", feedback: "Correct: conditions and gates travel with the conclusion." }, { text: "Newest publication wins", feedback: "Publication date is not decision evidence." }], answer: 1, worked: "Deliver the contract table, budget ledger, raw episode rows, uncertainty, failure log, and reversible selection.", retry: "Add every condition that would make your recommendation change." },
    motion: { concept: "evaluation", headline: "Model-based agents spend prediction compute in different places.", intro: "A comparative scorecard keeps model target, training imagination, online search, value bootstrap, latency, and safety slices separate.", labels: ["MODEL TARGET", "TRAINING USE", "ONLINE USE", "DECISION"], titles: ["Identify what each learned dynamics model predicts.", "Locate synthetic updates or imagined behavior learning.", "Count search or direct policy computation per action.", "Choose only after matched budgets and gates."] },
  }),
] as const;
