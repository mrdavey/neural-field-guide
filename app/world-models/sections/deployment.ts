import { defineWorldModelLesson } from "../helpers";
import { wmSources } from "../sources";

export const worldModelDeploymentSpecs = [
  defineWorldModelLesson({
    id: "world-model-evaluation", track: "wm-deployment", title: "Evaluating World Models", number: 35,
    plain: "World-model evaluation asks whether predictions preserve the particular consequences a downstream decision needs, not merely whether outputs look plausible.",
    precise: "An evaluation contract fixes observations, actions, horizon, start-state distribution, model checkpoint, sampling budget, and target decision. It then measures one-step and multi-step prediction, calibrated uncertainty, reward/value accuracy, controllability, planning regret, and slice-specific failures against recorded or live outcomes. Pixel or feature error can diagnose representations, but closed-loop return and constraint violations test decision usefulness. Every result must identify whether it came from a deterministic fixture, replay, simulator, or real system.",
    analogy: "It is testing a map with route questions, scale checks, and field trips instead of judging its colors.", analogyBreak: "even a passed route set covers only the starts, interventions, and hazards actually tested.",
    ideas: ["Start from the downstream decision and list the quantities that could change it.", "Evaluate open-loop prediction, uncertainty, and closed-loop control under matched starts and budgets.", "Report distributions, slices, failures, provenance, and confidence intervals rather than one average."],
    worked: "For collision-avoiding MPC, create rows such as: `replay / 10 steps / obstacle slice / contact recall / A=98%, B=80%`; `replay / 10 steps / all paths / expected calibration error (lower is better) / A=.04, B=.03`; `simulator / closed loop / same starts and 2,000 model calls / collisions / A=1, B=6`; and `simulator / closed loop / same protocol / return / A=72, B=75`. Expected calibration error summarizes gaps between predicted probabilities and observed frequencies, so its direction differs from recall/return. Each row records checkpoint, seed set, source, actions, horizon, metric, uncertainty, and artifact path. Safety gating selects A despite B's slightly higher return; the matrix does not collapse the conflict into one score.",
    misconception: "A visually sharp rollout is not automatically accurate, calibrated, controllable, or useful for planning.",
    quiz: { question: "Which metric most directly tests a model used for constrained planning?", options: ["Demo sharpness", "Closed-loop task return and constraint violations under a fixed protocol", "Training loss alone", "Parameter count"], answer: 1, explanation: "The deployment decision depends on achieved outcomes and constraint behavior, with supporting diagnostics." }, lab: "wm-evaluation", prerequisites: ["foundation-world-models-case-study"], sources: [wmSources.mbrlSurvey, wmSources.controlSuite],
    objectives: ["Design a world-model evaluation matrix from a stated downstream decision", "Interpret conflicting prediction and control metrics without collapsing them into one score"],
    vocabulary: [{ term: "Open-loop evaluation", meaning: "Comparing predictions with outcomes without correcting state from new observations." }, { term: "Closed-loop evaluation", meaning: "Testing decisions while observations repeatedly update the controller." }, { term: "Planning regret", meaning: "The outcome gap between a chosen plan and a suitable reference under the same task." }],
    stages: ["Specify contract", "Measure layers", "Decide with slices"], checkpoints: ["Pin starts, actions, horizons, checkpoint, environment, and evidence source.", "Collect predictive, calibration, decision, safety, and resource measurements.", "Inspect uncertainty and failures before making a bounded go/no-go decision."],
    primaryCheck: { prompt: "Build the minimum evaluation matrix for choosing a collision-avoiding MPC model. Include protocol/provenance, prediction, calibration, control, and safety rows, then state how a changed pedestrian-crossing failure affects the decision.", expected: "Pin checkpoint, start distribution, action sequences, horizon, planner budget, seeds, and whether evidence is fixture/replay/simulator/real. Report multi-step/contact prediction by slice, a proper calibration metric, matched closed-loop return/regret, and constraint violations/interventions with uncertainty and artifact links. If the changed pedestrian slice misses crossings, that predeclared safety row fails and blocks street deployment even when aggregate pixels or return improve.", retry: "Make five columns before deciding: evidence contract, prediction, calibration, control, and safety. Add provenance to every result and apply the pedestrian gate before averaging anything." },
    decision: { explanation: "Conflicting metrics are resolved through the decision contract, not an arbitrary average.", mechanism: "Map each metric to a required capability or failure cost, set predeclared gates, estimate uncertainty, and compare alternatives only after data, horizon, actions, compute, and evaluator are matched.", workedExample: "Model A wins reconstruction by 4% but causes 6/100 constraint violations; Model B has zero violations and equivalent task return. A safety-gated deployment selects B while retaining A for representation research.", boundary: "A gate limits the evaluated operating region; it cannot establish safety outside that region.", check: { prompt: "Model X predicts rewards better; Model Y achieves better return under twice the planner budget. Which model is better?", expected: "The comparison is confounded. Match planner/model-call budgets and report reward accuracy and closed-loop return separately before drawing a task-specific conclusion.", retry: "List every factor besides model weights that changed between the two runs." } },
    transfer: { prompt: "Which evaluation report is strongest?", options: [{ text: "One average score and the best video", feedback: "This hides slices and provenance." }, { text: "Pinned protocol, matched baselines, distributions, failure slices, artifacts, and bounded claims", feedback: "Correct: the decision and evidence remain inspectable." }, { text: "Training loss with no downstream test", feedback: "Training fit does not establish planning usefulness." }], answer: 1, worked: "Publish the evaluation contract, per-slice table, uncertainty, failures, and the exact decision it supports.", retry: "Ask whether another learner could reproduce the comparison and discover the same failure cases." },
    motion: { concept: "evaluation", headline: "Evaluation keeps prediction, calibration, control, and safety on separate evidence lanes.", intro: "The scorecard fills from a pinned protocol and exposes disagreements before a decision gate is crossed.", labels: ["CONTRACT", "PREDICT", "CONTROL", "DECIDE"], titles: ["Fix starts, actions, horizons, and evidence source.", "Measure multi-step error and uncertainty by slice.", "Run matched closed-loop tasks and constraint checks.", "Apply predeclared gates and state the remaining boundary."] },
  }),
  defineWorldModelLesson({
    id: "compounding-error-exploitation", track: "wm-deployment", title: "Compounding Error and Model Exploitation", number: 36,
    plain: "Small prediction errors can accumulate across imagined steps, and a planner can deliberately seek the states where those errors promise impossible rewards.",
    precise: "When learned transitions are composed, the state distribution drifts away from training support and later inputs become model-generated rather than observed. Correlated bias can grow faster than independent noise. Optimization adds a second effect: among many candidate trajectories, the planner selects extreme predicted returns, including false positives caused by dynamics or reward error. Diagnosis compares error versus horizon, planner breadth, support density, ensemble disagreement, and real-versus-imagined outcomes.",
    analogy: "A one-degree compass error grows over a long walk, while a bargain hunter specifically chooses the most suspiciously cheap listing.", analogyBreak: "rollout errors can cancel, branch, or be corrected by feedback, so growth is not a fixed linear law.",
    ideas: ["Separate passive compounding from optimizer-driven exploitation.", "Plot prediction and decision gaps against rollout horizon and search strength.", "Mitigate with short horizons, feedback, uncertainty penalties, support constraints, ensembles, and real validation."],
    worked: "A model overestimates forward progress by 0.05 m per step. A 20-step straight rollout claims 1 m extra. With 10,000 sampled plans, the selected plan also passes through an unsupported corner whose reward is overestimated by 8, so search magnifies more than simple accumulation.",
    misconception: "More planning compute does not monotonically improve real performance when the objective is evaluated by a flawed model.",
    quiz: { question: "Imagined score rises with planner population while real score falls. What is the leading concern?", options: ["Underfitting the optimizer", "Model exploitation or winner's-curse selection", "Too many observations", "A guaranteed exploration benefit"], answer: 1, explanation: "Broader search can find increasingly extreme model errors." }, lab: "wm-uncertainty", prerequisites: ["world-model-evaluation"], sources: [wmSources.plannerOverfitting, wmSources.mbpo, wmSources.mbrlSurvey, wmSources.tdmpc],
    objectives: ["Distinguish compounding rollout error from planner exploitation in a recorded trace", "Select and test a mitigation using horizon, support, uncertainty, and feedback evidence"],
    vocabulary: [{ term: "Distribution shift", meaning: "A mismatch between states used for training and states encountered during prediction or control." }, { term: "Model exploitation", meaning: "Choosing actions that score well because of model error rather than real consequences." }, { term: "Winner's curse", meaning: "Extreme selected estimates tend to contain unusually favorable noise or bias." }],
    stages: ["Measure drift", "Increase search", "Stress-test elites"], checkpoints: ["Compare one-step teacher-forced and free-running error by horizon.", "Hold model fixed while varying planner breadth and horizon.", "Replay top plans, inspect support/disagreement, and record false positives."],
    primaryCheck: { prompt: "A trace has signed one-step progress bias +0.1 for five prior-only steps. With 100 candidate plans the selected elite also crosses an unsupported wall, predicted return 12 but reference return 2. Label and quantify the two failure paths.", expected: "Near unit sensitivity, the repeated signed bias compounds to about +0.5 by step five; this drift occurs even without selection. The wall-crossing elite has a +10 return false positive selected from broad search in unsupported state space, so it is planner exploitation/winner's-curse behavior. Record horizon error separately from elite imagined-versus-reference gaps while varying search breadth.", retry: "First follow one fixed plan across horizon and add its signed residuals. Then hold the model fixed, compare many candidates, and ask whether search selected an unusually favorable model error." },
    decision: { explanation: "A mitigation should target the observed failure pathway and preserve enough useful planning range.", mechanism: "Shorten or replan for horizon drift; penalize disagreement or reject unsupported states for epistemic gaps; validate elite trajectories and cap search when optimization exposes false positives.", workedExample: "Real score degrades only beyond horizon 8 while uncertainty rises. Receding-horizon control at 6 steps plus a terminal value restores return; a generic larger model is not required by this evidence.", boundary: "Ensemble agreement can be confidently wrong when members share data and architecture bias.", check: { prompt: "All ensemble members agree on an unseen wall-crossing plan. May the safety gate accept it?", expected: "No. Agreement alone is insufficient outside support; add geometric/constraint checks, support evidence, or real bounded validation.", retry: "Distinguish low disagreement from evidence that the input lies inside a validated region." } },
    transfer: { prompt: "Which experiment isolates exploitation?", options: [{ text: "Change model and planner together", feedback: "That confounds the causes." }, { text: "Hold the model fixed, vary search breadth, and replay selected plans in the reference environment", feedback: "Correct: changing optimization pressure reveals selection of model errors." }, { text: "Inspect only training loss", feedback: "Training loss does not show elite-plan mismatch." }], answer: 1, worked: "Plot imagined and reference outcome of selected elites against exact model-call budget.", retry: "Vary only how aggressively the planner searches the same learned score." },
    motion: { concept: "optimization", headline: "Planning can climb a predicted return surface into regions where the model is least trustworthy.", intro: "Rollout paths drift from data support while the optimizer selects the brightest false peak.", labels: ["ONE-STEP BIAS", "ROLLOUT DRIFT", "SEARCH PRESSURE", "REALITY CHECK"], titles: ["Measure local residuals and their direction.", "Compose transitions and track support by horizon.", "Watch selected estimates become more extreme with search.", "Replay elites and constrain the failure route."] },
  }),
  defineWorldModelLesson({
    id: "goal-conditioned-robotics", track: "wm-deployment", title: "Goal-Conditioned Robotics", number: 37,
    plain: "A goal-conditioned world model predicts how actions change the robot's state relative to a requested outcome such as an image, pose, or object relation.",
    precise: "The controller receives an observation history and goal $g$, filters latent state $z_t$, predicts action-conditioned futures, and scores goal progress, cost, continuation, and constraints. Goals may be pixels, embeddings, coordinates, language-conditioned predicates, or learned rewards; each choice defines invariances and loopholes. Evaluation must separate perception, dynamics, planning, actuation, reset, and safety failures across held-out objects and starts.",
    analogy: "It is a navigator that compares every simulated route with a destination marker.", analogyBreak: "a goal embedding may say two images are close while the physical gripper pose is unsafe or unreachable.",
    ideas: ["Specify the goal representation and observable success predicate before training.", "Trace sensing, belief, candidate actions, predicted outcomes, goal cost, and execution latency.", "Test held-out starts, distractors, occlusion, recovery, and constraint violations on real hardware only under bounded protocols."],
    worked: "The goal is cup-left-of-plate. Plan A produces embedding distance 0.1 but predicts gripper collision; Plan B distance 0.2 and no collision. A constrained planner rejects A despite better raw goal similarity.",
    misconception: "Reaching a visually similar final frame does not prove the robot achieved the intended physical relation safely.",
    quiz: { question: "What must accompany a learned image-goal distance?", options: ["A larger screenshot", "An operational success predicate and safety constraints", "Only more planning samples", "A language model judge"], answer: 1, explanation: "The system needs a testable task definition and independent constraint checks." }, lab: "wm-planner", prerequisites: ["compounding-error-exploitation"], sources: [wmSources.vjepa2, wmSources.tdmpc],
    objectives: ["Trace a goal-conditioned world-model controller from sensing through one executed action", "Design a locally checkable success and safety contract for a robot goal"],
    vocabulary: [{ term: "Goal conditioning", meaning: "Providing a desired outcome as an input to prediction, policy, value, or planning." }, { term: "Success predicate", meaning: "A deterministic or instrumented condition that decides whether the task outcome was achieved." }, { term: "Recovery policy", meaning: "A bounded behavior used when the nominal plan becomes unsafe or invalid." }],
    stages: ["Encode state and goal", "Plan under constraints", "Execute and verify"], checkpoints: ["Record sensors, frames, goal representation, and reset condition.", "Score goal progress separately from collision and feasibility gates.", "Apply one action, reobserve, and verify success with independent measurements."],
    primaryCheck: { prompt: "A robot uses MPC with a three-action plan. After action one, the object moves unexpectedly. Trace the next step.", expected: "Acquire the new observation, update the latent belief and goal relation, invalidate the stale plan, then optimize a new constrained sequence before executing another action.", retry: "Insert the real observation between execution and the next planning call." },
    decision: { explanation: "A robot success contract must be observable without asking the learned model to grade itself.", mechanism: "Define geometric/contact/task thresholds from calibrated sensors or deterministic state in simulation, predeclare timeouts and collisions, preserve logs, and require retries/recovery behavior.", workedExample: "Success requires cup centroid at least 5 cm left of plate for two frames, gripper clear by 2 cm, no force-limit event, and completion within 20 actions.", boundary: "Simulator state can check a fixture exactly; a real deployment inherits sensor uncertainty and must report it.", check: { prompt: "Why is 'the final image looks right' an incomplete success rule?", expected: "It is subjective and can miss depth, contact, identity, and safety. Replace it with instrumented relations and thresholds plus uncertainty.", retry: "List the physical claims hidden inside the visual judgment and assign a sensor-backed check to each." } },
    transfer: { prompt: "Which goal is most operational?", options: [{ text: "Make the scene nice", feedback: "This has no deterministic success condition." }, { text: "Place block A inside marked region with pose tolerance and no force-limit violation", feedback: "Correct: outcome and safety are locally checkable." }, { text: "Maximize the model's own confidence", feedback: "Self-confidence can reward model error." }], answer: 1, worked: "Write task, tolerance, timeout, safety events, reset, and artifact schema before collecting results.", retry: "Ask what a sensor or simulator state can decide without interpretation." },
    motion: { concept: "agent", headline: "Goal-conditioned control repeatedly compares predicted futures with a concrete desired state.", intro: "The loop keeps goal progress and safety gates visible while only one planned action crosses into the real robot.", labels: ["OBSERVE + GOAL", "IMAGINE", "GATE", "ACT + VERIFY"], titles: ["Filter current evidence and encode the success target.", "Roll bounded candidate actions through learned dynamics.", "Reject collisions, infeasibility, and unsupported states.", "Execute one action, reobserve, and apply the success predicate."] },
  }),
  defineWorldModelLesson({
    id: "system-identification-sim-to-real", track: "wm-deployment", title: "System Identification and Sim-to-Real", number: 38,
    plain: "System identification estimates a particular system's dynamics parameters from input–response traces. A parameter is identifiable only when the available safe inputs make its effect distinguishable from other parameter choices; sim-to-real methods then address the remaining training-simulation versus reality gap.",
    precise: "Given timestamped inputs and observed responses, identification estimates parameters or latent context such as mass, friction, delay, actuator gain, and sensor bias. Offline fits require excitation and held-out trajectories; online adaptation must distinguish parameter change from observation noise. Domain randomization trains across declared parameter distributions, while calibration updates the simulator toward measured reality. Transfer evidence needs matched real trials, uncertainty, safety limits, and a gap decomposition.",
    analogy: "It is learning how this specific bicycle responds before using routes rehearsed on a generic bicycle simulator.", analogyBreak: "many parameter combinations can produce similar recorded motion, and the real world can contain missing mechanisms rather than different numbers.",
    ideas: ["Design excitation that makes relevant parameters observable without exceeding limits.", "Separate calibration, randomization, adaptation, and robust control as different responses to mismatch.", "Measure transfer gaps by sensor, dynamics, contact, latency, appearance, and task slice."],
    worked: "For $v_{t+1}=v_t+g a_t$, observed $v_t=1.0$, $a_t=0.5$, and $v_{t+1}=1.2$ imply $g=(0.2)/(0.5)=0.4$ for this noiseless fixture. A constant zero action could not identify $g$.",
    misconception: "One fitted trace need not identify a unique physical parameter: several mass, friction, delay, or gain combinations can produce the same observed response, especially without informative excitation. Randomization also cannot cover true values outside its declared range or physics omitted from the simulator.",
    quiz: { question: "Why does an all-zero control log poorly identify actuator gain?", options: ["It is too safe", "The output contains no response to distinguish gains", "The model has too few pixels", "Gain is always one"], answer: 1, explanation: "Identification needs informative excitation of the parameter's effect." }, lab: "wm-state", prerequisites: ["goal-conditioned-robotics"], sources: [wmSources.persistentExcitation, wmSources.simulatorCalibration, wmSources.domainRandomization, wmSources.controlSuite],
    objectives: ["Estimate a simple dynamics parameter from an excitation trace and state its identifiability limit", "Choose calibration, randomization, adaptation, or robust control for a diagnosed transfer gap"],
    vocabulary: [{ term: "System identification", meaning: "Estimating a system's dynamics or parameters from controlled input-output data." }, { term: "Excitation", meaning: "Input variation that makes the effect of unknown parameters observable." }, { term: "Domain randomization", meaning: "Training across a declared distribution of simulated conditions to encourage robustness." }],
    stages: ["Excite safely", "Fit and validate", "Transfer with bounds"], checkpoints: ["Select inputs that reveal the parameter while respecting constraints.", "Fit on one set and test residuals on held-out trajectories.", "Run staged real trials, log the gap, and preserve a fallback."],
    primaryCheck: { prompt: "Using $x_{t+1}=x_t+g a_t$, calculate $g$ for $x_t=2$, $a_t=-0.5$, $x_{t+1}=1.8$. Then state what an otherwise identical all-zero-action log can and cannot identify.", expected: "$g=(1.8-2)/(-0.5)=0.4$ in the fixture's units. If every $a_t=0$, the prediction is $x_{t+1}=x_t$ for every possible $g$, so that log cannot distinguish gains at all. Even the nonzero one-sample estimate is unique only inside this declared noiseless one-parameter model; noise or omitted friction/delay requires more excitation and held-out residual checks.", retry: "First rearrange to $g=(x_{t+1}-x_t)/a_t$ and retain the sign. Then substitute $a_t=0$ without choosing a gain and see whether the output changes when $g$ changes." },
    decision: { explanation: "The response should match whether the gap is parameter variation, rapid context change, or missing structure.", mechanism: "Calibrate stable identifiable parameters; randomize a plausible known range; adapt when context changes and observations identify it; use robust control or revise the model for residual unmodeled dynamics.", workedExample: "Measured payload changes actuator response between episodes. Train over a measured payload range and infer a per-episode context from an excitation prefix; do not randomize camera color and claim the dynamics gap is solved.", boundary: "Online adaptation can destabilize control when parameters are not identifiable quickly or safely.", check: { prompt: "Held-out errors remain structured after all plausible friction values are fitted. What next?", expected: "Treat this as missing model structure, sensor timing, or another confound; inspect residuals and revise the dynamics/interface rather than widening friction indefinitely.", retry: "Ask whether changing the named parameter can reproduce the residual's shape and timing." } },
    transfer: { prompt: "Which sim-to-real claim is justified?", options: [{ text: "Randomization guarantees real safety", feedback: "Coverage and safety remain empirical and bounded." }, { text: "Transfer improved on the stated real trials under these parameter ranges and safety limits", feedback: "Correct: the evidence and boundary are explicit." }, { text: "One successful run proves the simulator is accurate", feedback: "A policy may succeed despite compensating errors." }], answer: 1, worked: "Publish simulator ranges, real protocol, residual slices, failures, and stop criteria.", retry: "Replace universal transfer language with the exact systems and trials observed." },
    motion: { concept: "coordinates", headline: "Identification maps controlled inputs and measured responses back to uncertain dynamics parameters.", intro: "The trace separates excitation, parameter fit, held-out residuals, and the remaining reality gap.", labels: ["INPUT", "RESPONSE", "FIT", "TRANSFER GAP"], titles: ["Apply bounded signals that reveal relevant dynamics.", "Measure state changes with units and timing.", "Estimate parameters and uncertainty on training traces.", "Test held-out simulation and staged real behavior separately."] },
  }),
  defineWorldModelLesson({
    id: "safe-constrained-planning", track: "wm-deployment", title: "Safe and Constrained Planning", number: 39,
    plain: "Constrained planning searches for useful actions only inside explicit safety limits and hands control to a fallback when those limits cannot be certified.",
    precise: "A constrained planner optimizes task return subject to state/action constraints, risk limits, or a shield. Constraints may be analytic, learned, or monitored by independent systems. Chance constraints require calibrated probabilities; robust constraints quantify a declared uncertainty set. A runtime assurance design separates the learned nominal controller, safety monitor, invariant or recoverable set, deadline, and verified fallback. No learned world model alone can certify hazards absent from its sensors, data, or constraint language.",
    analogy: "It is route planning inside guardrails with an emergency driver ready to take over.", analogyBreak: "the guardrails are only as complete as their sensors, assumptions, timing, and fallback authority.",
    ideas: ["Translate safety requirements into observable constraints, margins, and intervention deadlines.", "Keep task reward separate from feasibility and safety gates.", "Test false negatives, false positives, latency, fallback reachability, and recovery under distribution shift."],
    worked: "Plan A predicts reward 10 and collision probability 0.08; Plan B predicts reward 7 and probability 0.01. With a calibrated chance limit $p<0.05$, A is infeasible and B becomes the nominal proposal. Before execution, an independent monitor receives distance 0.25 m below its 0.30 m threshold; within its 20 ms deadline it uses final override authority to block B and output the tested braking/stop fallback. If probability or distance is uncalibrated OOD, uncertainty also routes to fallback rather than letting reward compensate.",
    misconception: "Adding a large negative collision reward is not equivalent to a hard constraint; a planner may trade the penalty for a larger predicted reward.",
    quiz: { question: "What happens when no candidate plan satisfies the safety gate?", options: ["Choose the highest reward anyway", "Invoke the declared safe fallback or stop", "Hide the constraint", "Increase horizon indefinitely"], answer: 1, explanation: "The constraint defines feasibility; failure to find a feasible plan activates the bounded fallback." }, lab: "wm-safety", prerequisites: ["system-identification-sim-to-real"], sources: [wmSources.safeMpc, wmSources.mbrlSurvey],
    objectives: ["Trace reward, constraint, uncertainty, monitor, and fallback through a planning decision", "Write a falsifiable runtime safety contract with limits and remaining assumptions"],
    vocabulary: [{ term: "Chance constraint", meaning: "A limit requiring estimated violation probability to stay below a threshold." }, { term: "Safety shield", meaning: "A mechanism that blocks or replaces an unsafe proposed action." }, { term: "Runtime assurance", meaning: "Monitoring and fallback architecture that keeps execution within declared conditions." }],
    stages: ["Propose", "Check constraints", "Execute or fallback"], checkpoints: ["Generate actions inside physical and deadline bounds.", "Evaluate independent constraints, uncertainty, calibration, and recoverability.", "Authorize one action or transfer control, then log the reason."],
    primaryCheck: { prompt: "Trace this runtime decision: candidate violation probabilities are [0.02,0.07,0.04] under a calibrated 0.05 limit; the independent distance monitor receives 0.25 m with a 0.30 m threshold; it must decide within 20 ms and can override the planner. What reaches the actuator?", expected: "The chance constraint first rejects 0.07 and leaves 0.02/0.04 for reward ranking. Regardless of nominal choice, the monitor's 0.25 m input violates its 0.30 m threshold; by the 20 ms deadline its independent override authority blocks the learned command and emits the declared fallback—such as bounded braking/stop—whose execution and acknowledgement are logged.", retry: "Follow five gates in order: chance feasibility, reward among feasible plans, monitor sensor input, deadline plus final authority, and the concrete fallback command/output." },
    decision: { explanation: "A safety contract is testable only when sensors, thresholds, timing, authority, and fallback outcome are explicit.", mechanism: "State operational domain, monitored variables, conservative margins, calibration evidence, maximum detection-to-action latency, fallback invariant/recovery assumptions, test cases, and stop criteria.", workedExample: "If distance falls below 0.30 m or uncertainty exceeds 0.15, a 20 ms monitor overrides within 50 ms and commands braking known to stop before 0.20 m under the tested speed range.", boundary: "The stopping claim is invalid outside the tested speed, surface, actuator, sensing, or latency range.", check: { prompt: "A monitor detects hazards but cannot override the learned controller. Is it a safety shield?", expected: "No. It is an alarm. A shield needs timely authority to block or replace actions and a tested fallback.", retry: "Trace which component has final command authority after the detector fires." } },
    transfer: { prompt: "Which statement preserves the safety boundary?", options: [{ text: "The learned model proves the system safe", feedback: "The model is one fallible component." }, { text: "Within the tested operating range, the monitor and fallback met these violation and latency criteria", feedback: "Correct: scope and evidence are explicit." }, { text: "No observed collision means zero risk", feedback: "Finite trials cannot establish zero risk." }], answer: 1, worked: "Report trial count, exposure, confidence, near misses, interventions, assumptions, and untested hazards.", retry: "Name the exact operating region and event frequency the experiment can support." },
    motion: { concept: "routing", headline: "A safety gate routes every proposed action either to execution or to a tested fallback.", intro: "Reward cannot cross the constraint boundary; uncertainty and deadlines remain visible inputs to authority.", labels: ["PROPOSAL", "CONSTRAINTS", "AUTHORITY", "FALLBACK"], titles: ["The nominal planner suggests a bounded action.", "Independent checks evaluate risk, support, and timing.", "Only an authorized safe proposal reaches actuators.", "Uncertain or infeasible cases enter a known recovery path."] },
  }),
  defineWorldModelLesson({
    id: "world-model-operations-case-study", track: "wm-deployment", title: "Case Study — Operating a World-Model Controller", number: 40, duration: 44,
    plain: "Operating a world-model controller means preserving a traceable loop from data and model version through plans, real outcomes, monitors, incidents, and rollback.",
    precise: "A production contract versions encoder/dynamics/reward/value components, planner configuration, observation/action schemas, calibration sets, constraints, and fallback. Shadow evaluation compares proposals without authority; canary rollout limits exposure; telemetry joins belief state, uncertainty, candidate scores, chosen action, deadline, monitor result, and real outcome. Drift and incident review distinguish sensing, model, objective, planner, actuator, and process failures. Rollback restores a compatible bundle, not weights alone.",
    analogy: "It is an aircraft logbook and control room for a learned prediction-and-planning loop.", analogyBreak: "logging does not prevent harm, and a rollback cannot undo a physical action already executed.",
    ideas: ["Version every interface and decision component as one compatible release bundle.", "Stage shadow, replay, simulation, canary, and bounded live evidence with stop criteria.", "Make telemetry support causal incident reconstruction, learner-visible limits, and rapid rollback."],
    worked: "Release wm-17 changes latent normalization. Replay passes, but canary uncertainty alerts triple and p99 planning latency crosses 50 ms. The stop rule returns to wm-16 plus its matching encoder and planner config; mixing wm-16 dynamics with wm-17 normalization is prohibited.",
    misconception: "Monitoring only task success cannot reveal near misses, model exploitation, deadline failures, or a degrading safety margin.",
    quiz: { question: "What should a world-model rollback restore?", options: ["Only the newest weight file", "A versioned compatible model, schemas, planner, calibration, constraints, and fallback bundle", "Only dashboard colors", "The last successful action"], answer: 1, explanation: "Decision components and interfaces must remain mutually compatible." }, lab: "wm-evaluation", prerequisites: ["safe-constrained-planning"], sources: [wmSources.mlTestScore, wmSources.rssmCode, wmSources.mbrlSurvey],
    objectives: ["Design a staged release and telemetry contract for a world-model controller", "Diagnose an incident across sensing, model, objective, planner, actuator, and operations evidence"],
    vocabulary: [{ term: "Shadow mode", meaning: "Producing and logging decisions without giving them execution authority." }, { term: "Canary rollout", meaning: "A deliberately limited deployment used to test a new bundle under controlled exposure." }, { term: "Release bundle", meaning: "A versioned set of mutually compatible models, schemas, planner settings, calibration, and safeguards." }],
    stages: ["Qualify bundle", "Limit authority", "Observe and recover"], checkpoints: ["Pin artifacts, schemas, data, tests, and known limits.", "Advance through replay, shadow, and canary only after declared gates.", "Join decisions to outcomes, trigger stops, preserve incidents, and test rollback."],
    primaryCheck: { prompt: "Commit the staged release trace for bundle wm-17: name replay, shadow, canary, expansion, their telemetry join, one stop rule, and the compatible rollback output.", expected: "Pinned replay first checks schemas, prediction, safety fixtures, and known failures; shadow logs wm-17 proposals without actuator authority; a bounded canary grants limited authority; expansion occurs only after predeclared gates. Every stage joins observation/time, filtered state, uncertainty, candidates/objective, selected action, monitor decision, actuator acknowledgement, and real outcome to bundle/seed IDs. If p99 latency exceeds 50 ms or alerts triple, stop expansion and atomically restore wm-16's matching encoder, dynamics, planner, calibration, schemas, constraints, and fallback—not one weight file.", retry: "Draw authority increasing across replay → shadow → canary → expansion. Under it, write the decision-to-outcome join; above it, write a numeric stop; at the end, list every component restored together." },
    decision: { explanation: "Incident diagnosis follows evidence across the full control loop instead of blaming the learned model by default.", mechanism: "Reconstruct raw/timestamped observations, filtered state, uncertainty, candidates, objective terms, chosen action, monitor decision, actuator acknowledgement, environment outcome, and human/process changes; compare with a known-good bundle.", workedExample: "Collision alerts rise although predictions are stable. Logs show 70 ms actuator acknowledgement delay after firmware change, so the primary fault is timing/interface drift; model retraining would not repair it.", boundary: "A log can be incomplete or wrong; safety-critical signals need integrity, clock synchronization, retention, and privacy controls.", check: { prompt: "Imagined trajectories are unchanged but executed paths lag after a firmware release. Which branch should be investigated first?", expected: "Actuation timing/interface and timestamp alignment, while still checking end-to-end evidence. The model is not the leading changed variable.", retry: "Compare the last known-good and incident bundles component by component." } },
    transfer: { prompt: "Which release sequence best limits risk?", options: [{ text: "Global live deployment after training loss improves", feedback: "Training loss is not an operational gate." }, { text: "Pinned replay → shadow → bounded canary → gated expansion with rollback drills", feedback: "Correct: authority grows only with compatible evidence." }, { text: "Canary without stop criteria", feedback: "Exposure is not bounded without explicit stops." }], answer: 1, worked: "Deliver bundle manifest, test matrix, telemetry schema, stop rules, incident template, and rollback proof.", retry: "At each stage, state what new authority is granted and what evidence permits it." },
    motion: { concept: "systems", headline: "Operations wraps the world-model loop in versioning, staged authority, observability, and rollback.", intro: "The deployment path grows from offline evidence to bounded execution while every decision remains joined to its outcome.", labels: ["BUNDLE", "SHADOW", "CANARY", "ROLLBACK"], titles: ["Freeze compatible models, schemas, planner, and safeguards.", "Log proposals with no actuator authority.", "Grant limited authority under explicit gates and stops.", "Restore a tested bundle and preserve incident evidence."] },
  }),
] as const;
