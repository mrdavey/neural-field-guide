import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { py, rlSeed } from "../seed";
const m = plannedCourseManifests.rl;

export const rlFoundationSpecs = [
  defineResearchLesson(m, rlSeed({ id: "sequential-decision-systems",
    plain: "Reinforcement learning studies an agent that repeatedly observes, acts, receives consequences, and changes future behavior to improve a long-run objective.",
    precise: "At time t, environment state $s_t$ produces observation $o_t$; policy $\\pi(a_t|h_t)$ chooses action; environment returns reward $r_{t+1}$, next observation, termination, and possible truncation. The return $G_t=\\sum_{k\\ge0}\\gamma^k r_{t+k+1}$ assigns time-indexed consequences. A complete interface separates agent state, environment state, reward, termination, reset, and random sources.",
    mentalModel: "Treat the loop as a laboratory protocol whose arrows name who owns each value.", ideas: ["Separate policy choice from environment transition and measurement.", "Record termination and truncation because they change return bootstrapping.", "Define success and failure before optimizing reward."],
    worked: "Rewards [1,0,3] with gamma=.5 give return $1+.5(0)+.25(3)=1.75$. If the final event is a time-limit truncation rather than terminal failure, a value estimate may still bootstrap beyond it.",
    boundary: "An agent/environment loop is not evidence that the chosen reward represents the intended task or that learned behavior is safe.",
    objectives: { primary: "Trace an agent-environment trajectory with correctly timed rewards and endings", decision: "Write a decision-system contract that separates objective, measurement, and authority" },
    vocabulary: [{ term: "Agent", meaning: "The component that selects actions from its available information." }, { term: "Environment", meaning: "The process that receives actions and emits observations, rewards, and endings." }, { term: "Return", meaning: "A discounted or undiscounted sum of future rewards from one time step." }],
    primaryCheck: { prompt: "For rewards [1,0,3] and gamma=.5, compute the return from the first action and name the reward timing convention.", expected: "The return is 1.75 when r1=1 follows action a0, r2=0 follows a1, and r3=3 follows a2.", retry: "Write each action arrow with the reward emitted after it, then attach discount powers 0,1,2." },
    decision: { explanation: "A valid decision-system contract separates the reward optimized by the learner, the success and safety measurements used to judge it, and the authority allowed to execute, stop, or reset actions.", mechanism: "Specify observation/action spaces and timing; name reward terms, independent success metrics, non-compensable constraints, termination/truncation, reset distribution, seeds, evaluation episodes, and the policy, environment, evaluator, and supervisor that own each decision before training.", workedExample: "A warehouse policy optimizes speed reward; evaluation separately measures delivered parcels and collisions; the environment applies bounded motion, while a supervisor—not the policy—stops on collision and authorizes reset.", boundary: "A clearly separated contract can expose reward/measurement/authority errors without proving that its simulator, metrics, or supervisor cover real deployment hazards.", check: { prompt: "A policy earns high speed reward but delivers no parcels, and it can disable its own collision stop. Which contract separations failed?", expected: "Reward diverged from the independent delivery metric, and stop authority was incorrectly given to the optimizing policy instead of an external supervisor.", retry: "Label one field optimized, one field measured independently, and one component with non-overridable stop authority." } },
    quiz: { question: "Which component chooses an action?", options: ["Policy/agent", "Reward alone", "Environment reset", "Metric logger"], answer: 0, explanation: "The policy maps available information to an action distribution; the environment produces consequences." },
    transfer: { prompt: "A 100-step cutoff stops observation but the physical task could continue. What ending is it?", correct: "A truncation that may permit value bootstrapping", wrong: ["A terminal state with guaranteed zero future value", "A reward function"], worked: "Label it truncation and preserve the final observation so evaluation code can bootstrap according to the task contract.", retry: "Separate why the task ended from why data collection stopped." },
    lab: { title: "Trajectory ownership trace", question: "How does ending type change the target attached to the final transition?", controlLabel: "Ending record", boundary: "The browser uses fixed trajectory arithmetic and does not execute an environment.", cases: [{ label: "terminal success", resultLabel: "bootstrap", resultValue: "0", meter: 0, detail: "The task truly ended, so no continuation value follows the success transition." }, { label: "time truncation", resultLabel: "bootstrap", resultValue: "V(next)", meter: 70, detail: "Collection stopped while the underlying process could continue." }, { label: "missing flag", resultLabel: "contract", resultValue: "INVALID", meter: 100, detail: "The target cannot distinguish true ending from evaluator cutoff." }] }, motionConcept: "agent",
    code: { title: "Trace a typed environment loop", language: "Python 3", setup: "A dependency-free fixture keeps action, reward, and ending ownership explicit.", predict: "What discounted return follows the three rewards?", code: py("rewards=[1,0,3]", "gamma=.5", "G=sum((gamma**t)*r for t,r in enumerate(rewards))", "transition={'action':'right','reward_after':1,'terminated':False,'truncated':False}", "print(G, transition)"), observe: "The return is 1.75 and the record names reward timing and both ending flags.", tryIt: "Set truncated true and write the bootstrapped target separately from a terminal target." }, sourceKeys: ["book", "gymnasium"] })),
  defineResearchLesson(m, rlSeed({ id: "mdps-rewards",
    plain: "A Markov decision process is a compact model where the current state and action determine the distribution of the next state and reward.",
    precise: "An MDP is $(S,A,P,R,\\gamma)$ with transition $P(s'|s,a)$, reward $R(s,a,s')$, and dimensionless discount factor $0\\le\\gamma\\le1$. The Markov property says state contains all history information needed for future prediction under actions. The return $G_t=\\sum_{k=0}^{T-t-1}\\gamma^k r_{t+k+1}$ gives rewards $k$ steps away weight $\\gamma^k$: smaller $\\gamma$ emphasizes near consequences, while $\\gamma$ near one preserves distant consequences and has an effective planning scale of roughly $1/(1-\\gamma)$ continuing steps. Rewards define optimization incentives; success metrics and constraints independently evaluate whether those incentives match intent.",
    mentalModel: "Think of an MDP as a probability table for controlled state changes plus a score ledger whose discount dial controls how quickly future entries fade.", ideas: ["Test whether the declared state is Markov for the prediction needed.", "Keep reward shaping separate from success and constraint measurements.", "Inspect transition and reward units, then compare policies under the declared horizon and discount before applying Bellman equations."],
    worked: "From safe, action fast reaches goal with probability .8 for reward 5 and crash with .2 for reward −20; expected immediate reward is $0.8(5)+0.2(-20)=0$, hiding the severe outcome distribution. For a separate three-step reward trace [0,0,10], $G_0$ is $.5^2(10)=2.5$ at $\\gamma=.5$ but $.9^2(10)=8.1$ at $\\gamma=.9$: the outcomes did not change, only the weight assigned to delay.",
    boundary: "A scalar expected reward can hide tail risk, reward hacking, and state aliasing; optimizing it does not prove task success.",
    objectives: { primary: "Specify and inspect a finite MDP transition-reward table", decision: "Diagnose reward misspecification and non-Markov state" },
    vocabulary: [{ term: "Markov state", meaning: "A state representation sufficient for predicting future outcomes under actions without earlier history." }, { term: "Transition kernel", meaning: "A probability distribution over next states for each state-action pair." }, { term: "Discount factor", meaning: "The dimensionless multiplier gamma that gives a reward k future steps away weight gamma to the power k." }, { term: "Reward shaping", meaning: "Adding intermediate reward signals intended to make learning easier without changing desired behavior." }],
    primaryCheck: { prompt: "Fast gives .8×5 and .2×(−20). What is expected immediate reward, what does it hide, and what return does delayed trace [0,0,10] have at gamma .5 versus .9?", expected: "Expected immediate reward is 0, which hides a 20% catastrophic crash. The delayed returns are .5²×10=2.5 and .9²×10=8.1; the larger discount preserves more weight on the same delayed consequence.", retry: "First retain and average the immediate outcome branches. Then attach discount powers 0, 1, and 2 to the three-step trace and calculate each gamma separately." },
    decision: { explanation: "Diagnosis must test both reward misspecification and whether the declared state is sufficient for predicting action-conditioned futures.", mechanism: "Audit reward terms against independent success/constraints, then group transitions with the same declared state and action by earlier history; different next-outcome distributions expose non-Markov aliasing and require history, a belief, or an augmented state.", workedExample: "A cleaner earns motion reward by circling, while two identical low-battery observations followed by move have different stall rates depending on hidden charge history; dirt removal exposes the proxy and the split transition rows expose non-Markov state.", boundary: "Finite shortcut and aliasing tests can reveal concrete failures but cannot prove reward alignment or Markov sufficiency for every future policy.", check: { prompt: "The same observation/action reaches goal 90% after a recent charge but 20% otherwise, while motion reward rises. What two failures are present?", expected: "Motion reward is a poor task proxy, and the observation aliases hidden battery history because its action-conditioned next-state distribution depends on that history.", retry: "Compare reward with independent success, then split identical state-action rows by history and compare their outcome distributions." } },
    quiz: { question: "What does the Markov property require?", options: ["Current state is sufficient for future transition prediction under action", "Every state is observed perfectly", "Rewards are positive", "The policy is deterministic"], answer: 0, explanation: "The sufficiency claim concerns future conditional distributions, not observability or reward sign." },
    transfer: { prompt: "Two identical observations need different actions because hidden battery differs. What failed?", correct: "The observation is not a sufficient Markov state for this decision", wrong: ["The discount must be zero", "The action space is too large"], worked: "Add battery estimate/history to state or model partial observability explicitly, then retest transition sufficiency.", retry: "Construct two histories with the same observation and different next-outcome distributions." },
    lab: { title: "Reward, discount, and state audit", question: "Which MDP record exposes tail risk, delay weighting, or state aliasing?", controlLabel: "MDP record", boundary: "These finite tables demonstrate contract behavior, not learned policy performance or the ethically correct discount.", cases: [{ label: "mean only", resultLabel: "risk", resultValue: "HIDDEN", meter: 90, detail: "Expected reward zero hides a 20% crash outcome." }, { label: "delayed 10: gamma .5 → .9", resultLabel: "return", resultValue: "2.5 → 8.1", meter: 81, detail: "The same delayed consequence receives more weight when gamma rises; this changes the objective, not the evidence." }, { label: "aliased state", resultLabel: "Markov check", resultValue: "FAIL", meter: 100, detail: "Same state/action has history-dependent transition distributions." }] }, motionConcept: "agent",
    code: { title: "Inspect outcomes and discounted return", language: "Python 3", setup: "The dependency-free fixture retains immediate branches and changes only gamma for the delayed trace.", predict: "Which gamma assigns more weight to the final reward?", code: py("from math import isclose", "outcomes=[(.8,5),(.2,-20)]", "expected=sum(p*r for p,r in outcomes)", "rewards=[0,0,10]", "returns={g:sum((g**k)*r for k,r in enumerate(rewards)) for g in (.5,.9)}", "print(expected, outcomes, returns)", "assert expected==0 and isclose(returns[.5],2.5) and isclose(returns[.9],8.1)"), observe: "The immediate mean is zero but keeps its crash branch; the same delayed reward returns 2.5 at gamma .5 and 8.1 at gamma .9.", tryIt: "Add a non-compensable crash constraint, then change the trace length without changing rewards and predict how each discounted return responds." }, sourceKeys: ["book", "safety"] })),
  defineResearchLesson(m, rlSeed({ id: "partial-observation",
    plain: "Under partial observation, the agent cannot see the true state directly and must use history or a belief distribution to choose well.",
    precise: "A POMDP adds observations $O(o|s)$ to latent MDP dynamics. A belief $b_t(s)=P(s_t=s|o_{\\le t},a_{<t})$ predicts through the action transition and updates with observation likelihood before normalization. Exact beliefs are often intractable; histories, filters, recurrent states, or learned estimators approximate decision-relevant information.",
    mentalModel: "A belief is a probability-weighted shortlist of hidden situations consistent with current evidence.", ideas: ["Predict the hidden-state distribution through actions before using new evidence.", "Normalize observation-weighted mass and retain uncertainty.", "Test recovery after misleading or missing observations."],
    worked: "Action prediction maps prior [0.6,0.4] through transition [[.8,.2],[.1,.9]] to [.52,.48]. Likelihood [.2,.8] gives raw [.104,.384], evidence .488, and posterior [.213,.787].",
    boundary: "A confident belief may be wrong when observation or transition models are misspecified; low entropy is not a safety certificate.",
    objectives: { primary: "Compute a finite belief prediction and observation update", decision: "Choose history or belief representations from decision evidence" },
    vocabulary: [{ term: "POMDP", meaning: "A decision process whose true state is not directly observed." }, { term: "Belief state", meaning: "A posterior distribution over possible hidden states given action-observation history." }, { term: "Observation model", meaning: "The likelihood of sensor observations under each hidden state." }],
    primaryCheck: { prompt: "Predict prior [.6,.4] through transition [[.8,.2],[.1,.9]], then update with likelihood [.2,.8]. What posterior results?", expected: "Prediction is [.6×.8+.4×.1, .6×.2+.4×.9]=[.52,.48]; raw update [.104,.384] normalizes by .488 to about [.213,.787].", retry: "First multiply the prior row vector by the transition matrix; only then multiply by likelihood and normalize." },
    decision: { explanation: "State estimators should be evaluated by the decisions and recovery they support, not compression alone.", mechanism: "Compare memoryless, finite-history, and belief controllers on aliased starts, sensor dropout, misleading evidence, calibration, return, and recovery under matched policies or planners.", workedExample: "A latest-observation controller turns left after one false signal; a Bayesian filter retains prior mass and waits for confirming evidence.", boundary: "A filter using the same biased sensor model can be confidently wrong across every tested controller.", check: { prompt: "Belief entropy is low but actions fail after sensor shift. What evidence is missing?", expected: "Calibration and model-shift evidence; confident internal probability does not establish correct hidden-state tracking.", retry: "Compare predicted belief probabilities with actual hidden outcomes on the shifted slice." } },
    quiz: { question: "What happens before normalization in a Bayes observation update?", options: ["Prior mass is multiplied by observation likelihood", "Actions are averaged", "Rewards become probabilities", "The state is revealed"], answer: 0, explanation: "The unnormalized posterior is prior times likelihood." },
    transfer: { prompt: "One observation is impossible under every modeled state. What should code do?", correct: "Trigger an explicit impossible-evidence recovery policy", wrong: ["Divide by zero and continue", "Choose the first state"], worked: "Detect zero evidence mass, preserve the event, then reset, smooth under a declared rule, or escalate rather than silently normalize.", retry: "Inspect the normalization denominator before division." },
    lab: { title: "Belief update clinic", question: "How do action prediction, evidence quality, and impossible evidence change the posterior?", controlLabel: "Filter trace", boundary: "The two-state filter is an exact arithmetic fixture, not a calibrated real sensor.", cases: [{ label: "predict then [.2,.8]", resultLabel: "P(B|o)", resultValue: ".787", meter: 79, detail: "Action dynamics first move the prior to [.52,.48]; observation weighting and normalization then favor B." }, { label: "predict then [.5,.5]", resultLabel: "posterior", resultValue: "[.52,.48]", meter: 48, detail: "Uninformative evidence leaves the action-predicted belief unchanged." }, { label: "predict then [0,0]", resultLabel: "update", resultValue: "RECOVER", meter: 100, detail: "Zero evidence mass makes ordinary normalization undefined." }] }, motionConcept: "distribution",
    code: { title: "Predict and update a finite belief", language: "Python 3", setup: "The dependency-free filter performs action prediction before evidence and refuses an impossible observation.", predict: "After the action and observation, which hidden state gains mass?", code: py("prior=[.6,.4]; transition=[[.8,.2],[.1,.9]]", "predicted=[sum(prior[i]*transition[i][j] for i in range(2)) for j in range(2)]", "likelihood=[.2,.8]", "raw=[p*l for p,l in zip(predicted,likelihood)]", "assert sum(raw)>0, 'impossible evidence'", "posterior=[x/sum(raw) for x in raw]", "print([round(x,3) for x in predicted],[round(x,3) for x in posterior])"), observe: "Prediction is [.52,.48] and the posterior becomes [.213,.787], matching the worked trace.", tryIt: "Set both likelihoods to zero and implement a named recovery branch without discarding the failed observation." }, sourceKeys: ["book"] })),
  defineResearchLesson(m, rlSeed({ id: "policies-occupancy",
    plain: "A policy chooses actions, while its occupancy distribution records which state-action pairs the policy actually visits over time.",
    precise: "Policy $\\pi(a|s)$ induces trajectories through environment transitions. Discounted occupancy $d^\\pi(s,a)=(1-\\gamma)\\sum_t\\gamma^tP(s_t=s,a_t=a|\\pi)$ weights visited pairs and sums to one under continuing assumptions. Policy value depends on both action choice and resulting visitation; off-policy evaluation must account for mismatch between behavior and target occupancy.",
    mentalModel: "A policy is a route rule; occupancy is the heat map left by repeatedly following it.", ideas: ["Normalize action probabilities separately at each state.", "Compute visitation from policy plus transition dynamics, not policy probabilities alone.", "Audit target-policy support under logged behavior before off-policy claims."],
    worked: "With discounted state occupancy d(s)=.4 and policy pi(right|s)=.7, state-action occupancy is d(s,right)=.28. If behavior occupancy there is zero, that .28 target mass is unsupported by logged outcomes.",
    boundary: "Matching average action frequencies does not imply matching state-conditioned occupancy or returns.",
    objectives: { primary: "Compute policy action and simple occupancy probabilities", decision: "Diagnose behavior-target support and evaluation mismatch" },
    vocabulary: [{ term: "Policy", meaning: "A conditional distribution or rule for selecting actions from agent information." }, { term: "Occupancy distribution", meaning: "The state-action visitation distribution induced by a policy and environment." }, { term: "Behavior policy", meaning: "The policy that generated logged trajectories used for learning or evaluation." }, { term: "Importance ratio", meaning: "Target-policy probability divided by behavior-policy probability for a recorded choice." }, { term: "Effective sample size", meaning: "A weight-concentration diagnostic estimating how many equally weighted observations carry comparable information." }],
    primaryCheck: { prompt: "Discounted state occupancy is .4 and the target chooses right with probability .7. Compute state-action occupancy, then diagnose behavior occupancy zero there.", expected: "Target state-action occupancy is .4×.7=.28; behavior has no support for that positive target mass, so direct logged evaluation and the importance ratio are undefined there.", retry: "Multiply state visitation by conditional action probability, then compare the resulting positive mass with behavior occupancy." },
    decision: { explanation: "Policy comparison must inspect where each policy goes, not only its action table at familiar states.", mechanism: "Estimate state/action occupancy, returns, termination, coverage, and critical slices over matched starts; for off-policy data, report importance weights or model assumptions and abstain where support fails.", workedExample: "Two policies choose left 50% overall, but one does so only in safe states and the other only near hazards; marginal action frequency hides opposite occupancy.", boundary: "Finite logged coverage cannot establish performance in unvisited states or under environment shift.", check: { prompt: "Importance weights explode on a few trajectories. What diagnosis is likely?", expected: "Behavior and target occupancies are poorly overlapping, so the estimator has high variance and weak effective sample size.", retry: "Inspect per-step action ratios and count how many trajectories dominate the weighted estimate." } },
    quiz: { question: "What determines occupancy?", options: ["Policy, transitions, starts, and horizon/discount", "Policy parameters only", "Reward sign only", "Random seed name"], answer: 0, explanation: "Visitation emerges from policy decisions interacting with environment dynamics." },
    transfer: { prompt: "Logged data contains no target action in a critical state. What is defensible?", correct: "Mark the target evaluation unsupported there or collect new evidence", wrong: ["Assign the unseen branch average reward", "Claim zero risk"], worked: "Expose the support gap, restrict the target, gather data, or introduce a validated model with a separate assumption boundary.", retry: "Find direct evidence for every target-positive critical state-action pair." },
    lab: { title: "Occupancy support map", question: "Which behavior dataset supports the target policy's critical action choices?", controlLabel: "Coverage card", boundary: "The cards show finite support logic, not a real off-policy estimate.", cases: [{ label: "full overlap", resultLabel: "support", resultValue: "PASS", meter: 20, detail: "Every target-positive state-action pair has behavior coverage." }, { label: "thin overlap", resultLabel: "weights", resultValue: "HIGH VARIANCE", meter: 80, detail: "Rare target actions create large ratios and low effective sample size." }, { label: "zero overlap", resultLabel: "evaluation", resultValue: "ABSTAIN", meter: 100, detail: "A critical target action has no logged outcome evidence." }] }, motionConcept: "distribution",
    code: { title: "Audit state-action support", language: "Python 3", setup: "A table check identifies target-positive actions absent from behavior.", predict: "Which action is unsupported?", code: py("behavior={'left':1.0,'right':0.0}", "target={'left':.3,'right':.7}", "unsupported=[a for a,p in target.items() if p>0 and behavior.get(a,0)==0]", "print(unsupported)", "assert unsupported==['right']"), observe: "Right is explicitly unsupported rather than assigned an invented value.", tryIt: "Add epsilon behavior probability and calculate the resulting importance ratio." }, sourceKeys: ["book", "d4rl"] })),
  defineResearchLesson(m, rlSeed({ id: "tabular-control-capstone", duration: 34,
    plain: "The tabular-control workbench implements a finite MDP, exact policy evaluation, planning baselines, seeded rollouts, and coverage diagnostics behind one tested interface.",
    precise: "The package defines states/actions/transitions/rewards/endings, validates probability tables, solves values by iteration or linear equations, samples episodes with local RNG, and reports return, occupancy, success, constraints, and support. Changed-case tests alter reward, transition, observation, or start distribution one at a time while retaining raw trajectories.",
    mentalModel: "Build a glass-box environment and solver where every probability and backup can be recomputed by hand.", ideas: ["Validate the MDP schema and exact solution before sampling estimates.", "Keep environment truth separate from what an online learner later estimates.", "Use changed transitions and starts to test transfer rather than replay one table."],
    worked: "A three-state chain has exact value 1.8 from start under policy right; 1,000 rollouts estimate it with uncertainty. Both artifacts are preserved instead of calling one simulation exact.",
    boundary: "Passing a tiny tabular workbench does not establish scalability, function approximation stability, or real-system validity.",
    objectives: { primary: "Build and verify a reusable finite-MDP solver and rollout evaluator", decision: "Run a controlled changed-environment control experiment" },
    vocabulary: [{ term: "Tabular method", meaning: "An algorithm storing a separate value or model entry for every finite state-action pair." }, { term: "Exact solution", meaning: "A value computed from the declared finite model rather than estimated from sampled episodes." }, { term: "Changed case", meaning: "A held-out intervention modifying one environment property under matched evaluation." }],
    primaryCheck: { prompt: "Why compare sampled rollout estimates with an exact finite-MDP value?", expected: "The exact solution validates environment and estimator logic, while rollout error and uncertainty reveal finite-sample behavior.", retry: "Label each result by whether it uses the transition table directly or samples trajectories." },
    decision: { explanation: "The capstone comparison changes one environment mechanism and applies predeclared gates.", mechanism: "Reproduce exact baseline, choose one transition/reward/start intervention, keep policy/evaluation seeds or budgets matched, preserve all rows, compute value and constraints, and report a bounded conclusion.", workedExample: "Increasing slip from .1 to .3 reverses which policy meets a collision gate even though nominal shortest-path reward still favors the riskier route.", boundary: "The result belongs to the finite fixture and declared objective; it cannot certify an algorithm under continuous or partially observed tasks.", check: { prompt: "Return improves but collision gate fails after the changed case. Which policy passes?", expected: "The high-return policy fails the non-compensable gate; select a feasible alternative or report that no policy passes.", retry: "Apply constraints before optimizing return within the feasible policy set." } },
    quiz: { question: "Which workbench result can be exact?", options: ["Value solved from a fully declared finite transition table", "Mean of ten stochastic episodes", "A generalization claim", "GPU runtime"], answer: 0, explanation: "Finite model equations can be solved exactly up to numerical tolerance." },
    transfer: { prompt: "A rollout mean differs from exact value by .1. What next?", correct: "Report sampling uncertainty and verify estimator convergence across declared seeds", wrong: ["Change the exact value", "Delete low-return episodes"], worked: "Increase episodes or compute an interval while keeping exact model truth fixed and inspecting any persistent bias.", retry: "Separate Monte Carlo error from a mismatch in transition, reward, or ending implementation." },
    lab: { title: "Tabular evidence gate", question: "Which artifact is exact model evidence, sampled evidence, or an invalid overclaim?", controlLabel: "Evidence artifact", boundary: "This gate classifies evidence; it does not solve the learner's custom MDP.", cases: [{ label: "Bellman solution", resultLabel: "evidence", resultValue: "EXACT MODEL", meter: 20, detail: "The finite transition table directly determines the value within tolerance." }, { label: "1k rollouts", resultLabel: "evidence", resultValue: "ESTIMATE", meter: 55, detail: "The empirical mean needs uncertainty and seed/episode provenance." }, { label: "all tasks", resultLabel: "claim", resultValue: "OVERREACH", meter: 100, detail: "A three-state fixture cannot support universal control conclusions." }] }, motionConcept: "evaluation",
    code: { title: "Iterate one Bellman policy backup", language: "Python 3", setup: "A two-state deterministic policy makes convergence inspectable.", predict: "What values appear after repeated backups?", code: py("gamma=.9", "V={'s0':0.0,'s1':0.0,'terminal':0.0}", "for _ in range(50):", "    V['s1']=1+gamma*V['terminal']", "    V['s0']=0+gamma*V['s1']", "print(V)", "assert abs(V['s0']-.9)<1e-9"), observe: "The exact policy values converge to .9 and 1.0.", tryIt: "Add a slip transition and compute the probability-weighted backup." }, sourceKeys: ["book", "gymnasium"] })),
];
