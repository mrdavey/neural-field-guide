import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { py, rlSeed } from "../seed";
const m = plannedCourseManifests.rl;

export const rlModelBasedSpecs = [
  defineResearchLesson(m, rlSeed({ id: "learned-dynamics-control",
    plain: "A learned dynamics model predicts how state and reward change after an action so an agent can test candidate futures before acting.",
    precise: "A model $f_\\phi(s_t,a_t)$ predicts next state distribution, reward, termination, or a latent equivalent. Training minimizes one-step and multistep losses on transition data; control composes predictions under candidate actions. Action conditioning, stochastic futures, rollout horizon, data coverage, and decision-relevant error determine usefulness more than one average prediction loss.",
    mentalModel: "Build a flight simulator for decisions, then verify the simulator in exactly the maneuvers the controller will attempt.", ideas: ["Assert action changes predicted next state on controlled pairs.", "Slice model error by horizon, occupancy, reward, and constraint events.", "Evaluate decisions made through the model, not prediction loss alone."],
    worked: "From x=1,a=2, the model predicts next 2.6, reward 1.8, terminated false; the fixture truth is next 3, reward 1.5, terminated true at x'≥3. State error .4, reward error .3, and missed ending make the same action rank incorrectly.",
    boundary: "Low average error can hide rare catastrophic transition error and policy-induced exploitation outside training support.",
    objectives: { primary: "Build and verify an action-conditioned transition-reward model", decision: "Audit model error by rollout and control consequence" },
    vocabulary: [{ term: "Learned dynamics", meaning: "A fitted predictor of next state or observation under current state and action." }, { term: "Open-loop rollout", meaning: "Repeated model prediction without correcting state from new real observations." }, { term: "Decision-aware error", meaning: "Prediction error weighted or sliced by its effect on action selection and outcomes." }],
    primaryCheck: { prompt: "At x=1,a=2, model outputs next 2.6/reward1.8/not-ended; truth is next3/reward1.5/terminal at x'≥3. Audit all heads.", expected: "State absolute error is .4, reward error is .3, and termination is a false negative; all three outputs can alter a planner target and sequence length.", retry: "Compare next state, reward, and ending predictions field by field before forming any imagined control target." },
    decision: { explanation: "A control model succeeds only where its errors remain bounded for the candidate policies and horizons that consume it.", mechanism: "Hold transition data fixed, evaluate one- and multistep error on behavior and planner-induced states, test action swaps, reward/termination calibration, constraints, and compare real outcomes of model-selected versus baseline actions.", workedExample: "Validation MSE is low because most states are safe; the planner finds an unseen high-action region where rewards are overpredicted and repeatedly chooses it.", boundary: "Finite adversarial rollout tests cannot prove absence of every exploitable model error.", check: { prompt: "Model MSE falls but model-selected actions have worse real return than random shooting. What conclusion follows?", expected: "Average prediction improved without decision usefulness; inspect planner-induced support and action-ranking errors before using the model for control.", retry: "Compare predicted and real outcome ordering for the exact candidate actions the planner selected." } },
    quiz: { question: "What must a control dynamics model condition on?", options: ["The action as well as current state", "Only average reward", "Only random seed", "Only final success"], answer: 0, explanation: "Control requires counterfactual next predictions for different possible actions." },
    transfer: { prompt: "Changing action leaves every model prediction identical. What failed?", correct: "The action-conditioning path or learned action dependence", wrong: ["The model is necessarily optimal", "Only discounting"], worked: "Use paired states with action swaps and trace action tensors through the transition network.", retry: "Hold state fixed and change only action." },
    lab: { title: "Dynamics consequence probe", question: "How do one-step accuracy, rollout bias, and action sensitivity affect control?", controlLabel: "Model trace", boundary: "The scalar traces are deterministic fixtures, not trained-model measurements.", cases: [{ label: "action sensitive", resultLabel: "one-step error", resultValue: ".1", meter: 20, detail: "Paired action effects are preserved and require longer-horizon validation." }, { label: "bias ×5", resultLabel: "rollout error", resultValue: "2.0", meter: 80, detail: "Small systematic error compounds across the chosen horizon." }, { label: "action ignored", resultLabel: "control", resultValue: "INVALID", meter: 100, detail: "Candidate actions cannot be compared when predictions are identical." }] }, motionConcept: "pipeline",
    code: { title: "Roll out a learned scalar transition", language: "Python 3", setup: "A biased transition exposes compounding error.", predict: "Where does five repeated actions end?", code: py("def learned(x,a): return x+.8*a", "def true(x,a): return x+a", "xl=xt=1.0", "for _ in range(5): xl=learned(xl,2); xt=true(xt,2)", "print(xl,xt,abs(xl-xt))"), observe: "The learned rollout ends at 9 versus true 11, accumulating error 2.", tryIt: "Replan from a real observation after every step and compare closed-loop error." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "shooting-mpc",
    plain: "Shooting planners score complete candidate action sequences in a model; model predictive control executes only the first action and replans after a new observation.",
    precise: "Random shooting samples action sequences; CEM iteratively fits a proposal to elite sequences. For each candidate, a model predicts discounted rewards, termination, constraints, and optional terminal value. MPC repeats plan → first action → observe → update state. Horizon, population, elites, iterations, warm starts, random streams, model calls, and constraint handling define the planner budget.",
    mentalModel: "Draft several routes, refine the promising ones, take one step, then redraw the map from what actually happened.", ideas: ["Count candidate sequences, horizon, iterations, and ensemble members as primitive model calls.", "Separate predicted score from real executed outcome.", "Apply hard constraints before ranking feasible action sequences."],
    worked: "Random shooting scores sequences [−1,+1]→1, [+1,+1]→4, [+1,−1]→2. CEM keeps the top two, whose per-step action mean refits to [+1,0], resamples around that proposal, and repeats; 100×10×4 costs 4,000 predicted transitions.",
    boundary: "More search can worsen real behavior when it exploits a biased model or overfits noisy predicted rewards.",
    objectives: { primary: "Trace shooting and CEM sequence optimization", decision: "Choose MPC horizon and search budget from cost, uncertainty, and real outcomes" },
    vocabulary: [{ term: "Shooting method", meaning: "Planning by evaluating whole sampled action sequences through a model." }, { term: "Cross-entropy method", meaning: "An optimizer that repeatedly refits a sampling distribution to the best candidate sequences." }, { term: "MPC", meaning: "Receding-horizon control that replans after executing the first action." }],
    primaryCheck: { prompt: "Sequences [−1,+1],[+1,+1],[+1,−1] score [1,4,2]. Keep two elites: which are selected and what per-step mean proposal follows?", expected: "Elites are [+1,+1] and [+1,−1]; their per-step mean is [+1,0], which becomes the next CEM proposal center before resampling and rescoring.", retry: "Rank complete sequence scores, retain the top two sequences, then average their action at each horizon position separately." },
    decision: { explanation: "Planner depth and search width trade computational cost, feedback frequency, and exposure to model error.", mechanism: "Sweep horizon and population at matched model-call budgets, pair seeds/starts, record predicted and real return, constraint violations, planning latency, model disagreement, warm-start benefit, and failure onset along trajectories.", workedExample: "Horizon 30 has higher predicted reward than 10 but lower real reward because model bias grows after step 12; MPC-10 replans before that region dominates.", boundary: "A horizon chosen for one latency, model revision, and task timescale may not transfer.", check: { prompt: "Longer horizon improves predicted score but worsens real outcome while disagreement rises after step 12. What should change?", expected: "Shorten or uncertainty-penalize planning beyond the reliable horizon and verify with matched real outcomes; do not trust predicted score alone.", retry: "Plot model error/disagreement and action-ranking reversal by rollout depth." } },
    quiz: { question: "What does MPC execute from a planned sequence?", options: ["Usually the first action before replanning", "The entire sequence without feedback", "Only the terminal value", "No action"], answer: 0, explanation: "Receding-horizon feedback limits open-loop exposure to disturbances and model error." },
    transfer: { prompt: "A CEM treatment uses four times the model calls. Is higher predicted reward a matched win?", correct: "No; compare at matched calls or present a quality-cost frontier", wrong: ["Yes, sequence count is irrelevant", "Only real actions count"], worked: "Count every predicted transition and measured planning latency separately.", retry: "Expand population, horizon, iterations, and ensemble size." },
    lab: { title: "MPC budget frontier", question: "How do horizon, feedback, and model calls change planner evidence?", controlLabel: "Planner", boundary: "Costs and outcomes are teaching fixtures, not runtime measurements.", cases: [{ label: "H5 / replan", resultLabel: "calls", resultValue: "1k", meter: 25, detail: "Frequent feedback limits depth while requiring repeated planning." }, { label: "H20 / replan", resultLabel: "calls", resultValue: "4k", meter: 70, detail: "Deeper search costs more and enters weaker model regions." }, { label: "H20 / open loop", resultLabel: "feedback", resultValue: "NONE", meter: 100, detail: "Disturbances and model bias persist for the whole sequence." }] }, motionConcept: "agent",
    code: { title: "Count a CEM plan budget", language: "Python 3", setup: "A cost ledger prevents nominal iterations from hiding primitive calls.", predict: "How many predicted transitions are used?", code: py("population=100; horizon=10; iterations=4; ensemble=1", "calls=population*horizon*iterations*ensemble", "print(calls)", "assert calls==4000"), observe: "The plan uses 4,000 predicted transitions per real action.", tryIt: "Add five ensemble members and compare quality only at a matched call budget." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "dyna-imagination",
    plain: "Dyna combines real experience with model-generated imagined transitions so each environment interaction can support additional planning or learning updates.",
    precise: "A Dyna loop performs a real transition, updates value or policy, updates a model, then samples state-action starts and generates imagined targets. Imagination ratio is model updates per real step. Search control decides which states to rehearse. Because imagined samples inherit model bias and policy distribution, real/imagined labels, horizon, model revision, uncertainty, and update counts must remain visible.",
    mentalModel: "Learn from the real event, update the simulator, then rehearse extra cases without forgetting which were imagined.", ideas: ["Tag every training row as real or imagined with model revision.", "Match total updates and environment steps separately.", "Gate imagination by support, uncertainty, horizon, and real-validation checks."],
    worked: "After real row (s0,right,1,s1), Dyna updates Q and fits model M(s0,right)=(1,s1). Search control prioritizes s0 after its large TD error, M generates that imagined row, and a second Q backup occurs; ratio 10 yields 10 imagined rows per real step.",
    boundary: "More imagined data can accelerate the wrong solution when the model's errors are systematic or exploitable.",
    objectives: { primary: "Assemble a Dyna real-model-imagination loop", decision: "Choose imagination ratio and search control from bias and efficiency evidence" },
    vocabulary: [{ term: "Dyna", meaning: "A family combining direct learning from real transitions with planning updates from a learned model." }, { term: "Imagination ratio", meaning: "The number of model-generated update samples per real environment transition." }, { term: "Search control", meaning: "The rule selecting which states and actions receive planning or imagination updates." }],
    primaryCheck: { prompt: "Trace one Dyna iteration after real (s0,right,1,s1) and name which rows update model and value before one imagined backup.", expected: "The real row first updates Q and fits M(s0,right); search control selects a start such as high-error s0, M emits an explicitly imagined transition, and that row drives a separate value backup.", retry: "Write four boxes in order: real transition, direct value update, model update, selected imagined transition and planning update." },
    decision: { explanation: "Imagination ratio chooses how much model evidence is reused, while search control chooses where that potentially biased evidence is concentrated.", mechanism: "At matched real steps, vary ratio separately from uniform, recent-state, or prioritized-start search control; log selected starts, real/imagined losses, model error/support, return, constraints, updates, calls, and wall time.", workedExample: "Ratio 10 with uniform starts spends half its model calls on solved states; prioritized starts reach high-TD states sooner but amplify one unsupported model error, revealing efficiency and bias together.", boundary: "A favorable real-step curve does not establish compute efficiency, and priority based on model-contaminated TD error can focus learning on the wrong states.", check: { prompt: "Uniform and prioritized Dyna use equal ratio/calls; prioritized improves early return but repeatedly selects unsupported high-error states. What trade-off appears?", expected: "Search control improves allocation toward high-error states but concentrates model bias outside support; gate priority by support/uncertainty and preserve both efficiency and failure evidence.", retry: "Hold the imagination count fixed, compare the selected start-state distributions, and join each priority with model error and support." } },
    quiz: { question: "What distinguishes imagined transitions in a Dyna artifact?", options: ["They are produced by a learned model and carry its revision/provenance", "They came from the real environment", "They require no action", "They are exact"], answer: 0, explanation: "Imagined rows inherit model assumptions and must not be counted as real experience." },
    transfer: { prompt: "Imagined rows are mixed with real rows without labels. What is lost?", correct: "Evidence provenance and the ability to diagnose model-bias effects", wrong: ["Nothing if loss falls", "Only the random seed name"], worked: "Restore source/model-revision tags and slice every metric by provenance.", retry: "Ask which transition could be verified against the environment." },
    lab: { title: "Imagination ratio ledger", question: "How do real exposure, imagined updates, and bias risk change with ratio?", controlLabel: "Ratio", boundary: "The row counts are exact design fixtures, not learned performance.", cases: [{ label: "0", resultLabel: "imagined rows", resultValue: "0", meter: 0, detail: "All updates use real transitions and no model bias enters through planning." }, { label: "5", resultLabel: "imagined rows", resultValue: "5k", meter: 50, detail: "Five model updates accompany each of 1,000 real steps." }, { label: "20 unsupported", resultLabel: "risk", resultValue: "AMPLIFIED", meter: 100, detail: "Many updates repeatedly propagate error from unsupported model regions." }] }, motionConcept: "pipeline",
    code: { title: "Keep real and imagined rows separate", language: "Python 3", setup: "A ledger calculates data provenance and update exposure.", predict: "How many rows are real?", code: py("real_steps=1000; ratio=10", "rows={'real':real_steps,'imagined':real_steps*ratio}", "rows['total']=sum(rows.values())", "print(rows)", "assert rows['real']==1000"), observe: "The ledger reports 1,000 real and 10,000 imagined rows.", tryIt: "Add model revision and uncertainty threshold to every imagined row." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "model-uncertainty-exploitation",
    plain: "A planner exploits model error when it selects actions because the model is confidently wrong about their consequences.",
    precise: "An ensemble provides predictions $f_i(s,a)$ whose disagreement can flag epistemic uncertainty. Planning may penalize mean reward by $\\beta\\sigma$, reject candidates above a threshold, shorten horizon, or fall back to a safe controller. Shared ensemble bias, aleatoric randomness, calibration, data support, and correlation limit disagreement. The causal test executes model-selected actions in trusted evaluation and compares predicted versus realized rankings.",
    mentalModel: "Several imperfect maps can warn where they disagree, but identical maps copied from one bad survey can all be wrong together.", ideas: ["Separate ensemble mean error, disagreement, and actual error.", "Pair planner choices with real or trusted-simulator outcomes.", "Make fallback authority and constraint thresholds explicit."],
    worked: "Candidate A predicted return 10±.5 and realizes 9; B predicts 14±5 and realizes −2. Mean-only planning selects B, while penalty beta=1 gives scores 9.5 and 9, selecting A.",
    boundary: "Low disagreement does not establish accuracy when ensemble members share data, architecture, and optimization bias.",
    objectives: { primary: "Compute uncertainty-penalized model-based action scores", decision: "Design exploitation detection, abstention, and fallback tests" },
    vocabulary: [{ term: "Model exploitation", meaning: "Choosing actions that look favorable mainly because of model prediction error." }, { term: "Ensemble disagreement", meaning: "Variation among predictions from separately initialized or trained model members." }, { term: "Fallback controller", meaning: "A bounded alternative used when model-based planning lacks sufficient evidence or violates a gate." }],
    primaryCheck: { prompt: "A has mean 10, sigma .5; B mean 14, sigma 5. With score mean−sigma, compute both and select.", expected: "A scores 9.5 and B scores 9; the uncertainty-penalized rule selects A despite B's higher predicted mean.", retry: "Subtract one standard-deviation penalty from each candidate before comparing." },
    decision: { explanation: "Uncertainty control needs calibration against realized error and a deterministic authority path when evidence is weak.", mechanism: "Bin disagreement against actual rollout error, test planner-induced and random actions, sweep penalty/threshold at matched calls, report return, constraints, false-safe/false-alarm rates, and verify fallback activation and recovery.", workedExample: "All ensemble members agree on an unseen shortcut because they share training gaps; a support-distance gate catches it while disagreement alone does not.", boundary: "No finite detector proves every out-of-distribution action will be rejected safely.", check: { prompt: "Disagreement is low but support distance and realized error are high. May the planner call the action safe?", expected: "No. Shared ensemble bias is plausible; the combined gate should abstain or fall back based on support and realized calibration evidence.", retry: "Cross-tab disagreement, support, actual error, and gate outcome rather than treating one score as truth." } },
    quiz: { question: "What does high ensemble disagreement most directly indicate?", options: ["Models produce different predictions at that input", "The mean prediction is certainly wrong", "The action is safe", "Reward is normalized"], answer: 0, explanation: "Disagreement is a diagnostic requiring calibration, not a correctness oracle." },
    transfer: { prompt: "Five models share one biased dataset and agree off support. What remains possible?", correct: "Correlated shared error despite low disagreement", wrong: ["Agreement proves accuracy", "No model exploitation can occur"], worked: "Add support and realized-outcome checks plus a fallback.", retry: "Identify error sources common to every ensemble member." },
    lab: { title: "Exploitation guard", question: "Which evidence permits planning, penalization, or fallback?", controlLabel: "Candidate", boundary: "Scores are hypothetical fixtures and not calibrated controller measurements.", cases: [{ label: "mean10 ±.5", resultLabel: "penalized", resultValue: "9.5", meter: 25, detail: "Low disagreement and support evidence make this candidate testable." }, { label: "mean14 ±5", resultLabel: "penalized", resultValue: "9.0", meter: 80, detail: "High disagreement erases the apparent mean advantage." }, { label: "agree / off support", resultLabel: "authority", resultValue: "FALLBACK", meter: 100, detail: "Low disagreement cannot override the independent support gate." }] }, motionConcept: "distribution",
    code: { title: "Rank uncertainty-penalized candidates", language: "Python 3", setup: "A transparent score shows the decision rule.", predict: "Which action wins at beta one?", code: py("candidates={'A':(10,.5),'B':(14,5)}", "beta=1", "scores={a:mean-beta*sd for a,(mean,sd) in candidates.items()}", "print(scores,max(scores,key=scores.get))"), observe: "A scores 9.5, B 9, so A is selected.", tryIt: "Sweep beta and calculate the exact decision boundary." }, sourceKeys: ["book", "safety"] })),

  defineResearchLesson(m, rlSeed({ id: "model-based-capstone", duration: 42,
    plain: "This capstone builds an action-conditioned model and bounded planner, verifies them separately, then compares one horizon, search, imagination, or uncertainty intervention.",
    precise: "The build includes transition/reward/ending data, learned model or ensemble, one/multistep validation, shooting or CEM planner, MPC feedback loop, constraints and fallback, real/imagined provenance, checkpoints, and paired evaluation. Gates cover action sensitivity, analytic fixtures, overfit, rollout error by horizon, planner budget, resume, real-outcome ranking, constraint handling, and baseline reproduction.",
    mentalModel: "Assemble a simulator, a search process, and a feedback controller with separate calibration certificates.", ideas: ["Validate the model before asking a planner to optimize it.", "Count every predicted transition and every real interaction.", "Force an exploitation case and verify fallback authority."],
    worked: "Baseline MPC horizon 10 is compared with uncertainty-penalized horizon 20 over five seeds and matched 20,000 model transitions; real return, disagreement, constraints, and predicted-versus-real rank reversals are preserved.",
    boundary: "A small model-based result cannot certify real robotics, long-horizon fidelity, or universal planner settings.",
    objectives: { primary: "Build and verify a complete learned-model MPC agent", decision: "Conduct a matched planner or uncertainty intervention" },
    vocabulary: [{ term: "Model-call budget", meaning: "The counted state-action transition predictions consumed by planning or imagination." }, { term: "Rank reversal", meaning: "A candidate ordering under predicted outcomes that reverses under realized outcomes." }, { term: "Abstention", meaning: "A deliberate refusal to use the learned planner when evidence or constraints fail." }],
    primaryCheck: { prompt: "Why must model prediction gates and planner real-outcome gates both pass before the intervention?", expected: "Prediction gates establish basic model behavior, while planner gates test whether optimization selects actions whose realized rankings and constraints agree; either can fail independently.", retry: "Separate the predictor's outputs from the action selection and realized environment consequences." },
    decision: { explanation: "The intervention isolates one planner/model mechanism at matched real and imagined budgets.", mechanism: "Reproduce baseline, pin starts/seeds/real steps/model calls/evaluation, branch one horizon, penalty, ensemble, or ratio change, retain every seed and trajectory, apply constraint/fallback gates, and analyze predicted-real rank errors with uncertainty.", workedExample: "Longer horizon raises predicted return but increases rank reversals and fallback events; the predeclared real-return rule rejects it.", boundary: "The result remains conditional on model class, dataset, planner, task, seed set, and budgets.", check: { prompt: "Treatment uses equal real steps but twice the model-call budget. Which comparison claim remains valid?", expected: "Real-interaction exposure matches, but planner compute does not; report interaction efficiency separately or rerun at matched model calls.", retry: "Count real transitions, predicted transitions, gradient updates, and measured runtime in separate columns." } },
    quiz: { question: "What is the strongest exploitation test?", options: ["Compare model-selected predicted rankings with realized outcomes", "Inspect training loss only", "Increase horizon indefinitely", "Choose the best seed"], answer: 0, explanation: "Exploitation is a decision consequence of model error, so selected actions must be checked in trusted outcomes." },
    transfer: { prompt: "The planner violates a hard constraint but mean return improves. What is the decision?", correct: "Fail the treatment and preserve the constraint trace", wrong: ["Average return compensates", "Delete the episode"], worked: "Apply the non-compensable gate before quality trade-offs and test fallback.", retry: "Join each selected action with constraint and authority fields." },
    lab: { title: "Model-based release gate", question: "Which package permits planner use, intervention, or a bounded conclusion?", controlLabel: "Package", boundary: "The checklist is not a model or planner execution.", cases: [{ label: "model overfit", resultLabel: "status", resultValue: "MODEL DEBUG", meter: 25, detail: "Tiny-fit success precedes multistep and action-ranking validation." }, { label: "planner accepted", resultLabel: "status", resultValue: "BRANCH", meter: 65, detail: "Model, budget, constraint, fallback, and real-ranking gates pass." }, { label: "paired full rows", resultLabel: "status", resultValue: "DECIDE", meter: 100, detail: "Matched real/model budgets and all failure traces support a scoped result." }] }, motionConcept: "pipeline",
    code: { title: "Specify a bounded model-based study", language: "Python 3 pseudocode", setup: "The skeleton separates model, planner, controller, and evidence.", predict: "Which outcome validates predicted rankings?", code: py("model=fit(real_transitions)", "verify(action_swaps,multistep_slices,checkpoint_resume)", "planner=CEM(model,budget=model_calls,constraints=hard_gates)", "for observation in environment:", "    action=planner.first_action_or_fallback(observation)", "    realized=environment.step(action)", "    record(predicted,realized,disagreement,support,authority)", "compare_paired_seed_rows(baseline,treatment)"), observe: "Realized transitions validate or reverse the model-based action ranking.", tryIt: "Inject an overoptimistic off-support action and assert fallback.", caveat: "Supply a real environment or trusted simulator, tensor model, planner, controller, RNGs, and artifact writer." }, codeGuidance: { mode: "pseudocode", requirements: "Implement the named model-based stack; optional GPU execution follows the pinned RL protocol." }, sourceKeys: ["book", "safety"] })),
];
