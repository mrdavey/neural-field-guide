import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { py, rlSeed } from "../seed";
const m = plannedCourseManifests.rl;

export const rlValueSpecs = [
  defineResearchLesson(m, rlSeed({ id: "dynamic-programming",
    plain: "Dynamic programming solves a known finite decision process by repeatedly backing up future value through its transition table.",
    precise: "For policy evaluation, $V_{k+1}(s)=\\sum_a\\pi(a|s)\\sum_{s'}P(s'|s,a)[R(s,a,s')+\\gamma V_k(s')]$. Value iteration replaces the policy average with a maximum over actions. Contraction for $\\gamma<1$ gives convergence under standard finite assumptions; stopping tolerance bounds residual, not real-environment error.",
    mentalModel: "A Bellman backup sends downstream consequences one edge backward through a completely known map.", ideas: ["Separate policy evaluation from greedy policy improvement.", "Trace probability, reward, discount, and continuation value in every backup.", "Stop on a declared residual and verify the greedy policy independently."],
    worked: "With current V(s)=.5, right has backup .8(1)+.2(.9×.5)=.89 and left has backup .4. A policy choosing left .6/right .4 evaluates to .6(.4)+.4(.89)=.596, while value iteration takes max(.4,.89)=.89.",
    boundary: "Planning from a declared table is exact only for that table; model error or omitted states can make the computed policy poor in the actual process.",
    objectives: { primary: "Compute policy-evaluation and value-iteration backups", decision: "Diagnose convergence and model-mismatch claims" },
    vocabulary: [{ term: "Bellman backup", meaning: "Replacing a value by expected immediate reward plus discounted continuation value." }, { term: "Policy iteration", meaning: "Alternating exact or approximate policy evaluation with greedy improvement." }, { term: "Bellman residual", meaning: "The discrepancy between current values and one application of the relevant Bellman operator." }],
    primaryCheck: { prompt: "Right backs up to .89, left to .4, and policy probabilities are [.6 left,.4 right]. Compute fixed-policy and value-iteration backups.", expected: "Policy evaluation averages .6×.4+.4×.89=.596. Value iteration applies the greedy maximum max(.4,.89)=.89 and selects right.", retry: "First compute one Q backup per action; weight those Q values by policy probabilities for evaluation, but use their maximum for value iteration." },
    decision: { explanation: "A small residual establishes an internally solved model, not external model validity.", mechanism: "Validate transition rows, compare iterative and direct solutions, monitor residual and policy stability, then test the policy in changed transition tables and sampled rollouts with separate uncertainty.", workedExample: "Values converge to residual 1e-8, yet a changed slip probability doubles collisions; the solver is correct while its original transition model is no longer adequate.", boundary: "Convergence theorems do not repair misspecified rewards, state aliasing, partial observation, or distribution shift.", check: { prompt: "Value iteration converges but rollout return is poor only after slip rises. Is the leading failure solver convergence or model transfer?", expected: "Model transfer is the leading failure: the solver converged for its pinned table, while changed dynamics invalidate that table for the rollout condition.", retry: "Evaluate residual on the training table and transition calibration on the changed table as two separate checks." } },
    quiz: { question: "What distinguishes value iteration from fixed-policy evaluation?", options: ["It maximizes over actions in the backup", "It removes transitions", "It requires no rewards", "It samples only one state"], answer: 0, explanation: "Value iteration applies an optimality backup; policy evaluation averages under the declared policy." },
    transfer: { prompt: "Residual is tiny but changed-environment return collapses. What follows?", correct: "The table was solved, but its dynamics do not transfer to the changed environment", wrong: ["The Bellman equation is disproved", "The rollout must be deleted"], worked: "Keep solver correctness evidence and separately diagnose transition mismatch on the changed slice.", retry: "Ask which artifact—the equation solution or environment model—was tested by each observation." },
    lab: { title: "Bellman backup bench", question: "How do policy choice, optimality choice, and changed dynamics alter one backup?", controlLabel: "Backup record", boundary: "The values are exact table fixtures, not sampled control performance.", cases: [{ label: "policy average", resultLabel: "backup", resultValue: ".596", meter: 60, detail: "The fixed policy gives .6×.4+.4×.89=.596." }, { label: "greedy max", resultLabel: "backup", resultValue: ".89", meter: 89, detail: "The optimality operator selects the larger action backup." }, { label: "changed slip", resultLabel: "backup", resultValue: ".68", meter: 68, detail: "A different transition row changes the result even with identical current values." }] }, motionConcept: "optimization",
    code: { title: "Run value iteration on a tiny MDP", language: "Python 3", setup: "A two-action table exposes every deterministic backup.", predict: "Which action becomes greedy after repeated backups?", code: py("gamma=.9; V={'s':0.0,'goal':0.0}", "for _ in range(50):", "    q_left=.2 + gamma*V['s']", "    q_right=.8*1 + .2*gamma*V['s']", "    V['s']=max(q_left,q_right)", "print(round(V['s'],4), 'right' if q_right>=q_left else 'left')"), observe: "The exact printed observation is `1.9933 left`. Right wins the one-step backup at V(s)=.5, but repeated .2 reward plus the self-loop raises left's converged continuation value; the code makes that policy reversal diagnostic rather than hiding it.", tryIt: "Remove the recurring .2 reward or change the stay probability and find where the converged greedy action changes." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "monte-carlo-estimation",
    plain: "Monte Carlo value estimation averages complete sampled returns after an episode ends, without needing a transition model.",
    precise: "For visits to state s, first-visit Monte Carlo estimates $V(s)$ from one return per episode; under the standard episodic assumptions, its sample mean is an unbiased estimate of the corresponding return expectation. Every-visit uses every occurrence, so repeated dependent visits within one episode can make its finite-sample mean biased, although it is consistent under the standard assumptions. Episode boundaries, discounting, dependence, seed hierarchy, and confidence intervals at the independent-run level are part of the estimator contract.",
    mentalModel: "Wait for the whole trip to finish, then assign its realized downstream receipt to earlier visited states.", ideas: ["Compute returns backward from the terminal or truncated boundary.", "Declare first-visit or every-visit counting before aggregation.", "Separate within-episode samples from independent training or evaluation seeds."],
    worked: "Rewards [0,1,2] with gamma=.5 give returns [1.0,2.0,2.0]. If state s appears at times 0 and 2, first-visit records 1.0 while every-visit records both 1.0 and 2.0 (a within-episode mean of 1.5); the second visit is not a new independent episode.",
    boundary: "First-visit unbiasedness does not transfer to every-visit finite samples; more correlated episodes or visits do not create independent algorithm replications, and truncation mishandling can bias either return estimator.",
    objectives: { primary: "Compute episodic returns and Monte Carlo value estimates", decision: "Design episode and seed aggregation with honest uncertainty" },
    vocabulary: [{ term: "First-visit Monte Carlo", meaning: "Using only the first occurrence of a state in each episode for that episode's estimate." }, { term: "Every-visit Monte Carlo", meaning: "Using every occurrence of a state and its following return." }, { term: "Independent run", meaning: "A separately seeded algorithm execution, the appropriate unit for run-to-run uncertainty." }],
    primaryCheck: { prompt: "For rewards [0,1,2] and gamma=.5, calculate returns at times 0,1,2 and compare first-visit with every-visit for a state at times 0 and 2.", expected: "Returns are [1,2,2]. First-visit contributes 1 for that episode; every-visit contributes both 1 and 2 under the declared convention.", retry: "Start from the final reward and recurse backward with G_t=r_{t+1}+gamma G_{t+1}; then mark visit indices." },
    decision: { explanation: "Uncertainty must be computed at the level of independent variation relevant to the claim.", mechanism: "Preserve per-episode returns inside each run, aggregate a fixed evaluation episode count per seed, then compare algorithms using run-level summaries and intervals without treating all episodes as independent training replications.", workedExample: "Five seeds with 100 episodes each yield five independent algorithm summaries, not 500 independent trained policies.", boundary: "A narrow interval over evaluation episodes cannot capture training-seed variation when only one policy was trained.", check: { prompt: "One trained policy is evaluated for 1,000 episodes. How many independent training runs support a training-reliability claim?", expected: "One. The 1,000 episodes estimate that policy's environment-return distribution but do not measure variation across independent training runs.", retry: "Draw the hierarchy seed → trained policy → evaluation episodes and count independent units at the claim's level." } },
    quiz: { question: "When can an ordinary episodic Monte Carlo return be completed?", options: ["After its episode outcome is available", "Before any reward", "Only with a transition table", "Only during value iteration"], answer: 0, explanation: "The full return uses later rewards, so ordinary episodic Monte Carlo waits for the episode boundary." },
    transfer: { prompt: "A paper treats 500 episodes from one policy as 500 training seeds. What is wrong?", correct: "Episodes and independently trained policies are different levels of variation", wrong: ["Nothing; all rows are independent", "Only discounting matters"], worked: "Report episode uncertainty for that policy and obtain multiple training seeds for algorithm-reliability uncertainty.", retry: "Identify which random choices are shared across all 500 episodes." },
    lab: { title: "Return aggregation inspector", question: "Which counting rule and uncertainty unit match the declared value claim?", controlLabel: "Aggregation record", boundary: "The episode rows are fixed arithmetic fixtures rather than measured benchmark results.", cases: [{ label: "first visit", resultLabel: "samples", resultValue: "1 / episode", meter: 35, detail: "Only the earliest occurrence contributes for each episode." }, { label: "every visit", resultLabel: "samples", resultValue: "ALL VISITS", meter: 65, detail: "Repeated state occurrences contribute correlated returns under the stated convention." }, { label: "1 seed × 1k", resultLabel: "training runs", resultValue: "1", meter: 100, detail: "Many evaluation episodes do not create independent training replications." }] }, motionConcept: "distribution",
    code: { title: "Compute returns and visit samples", language: "Python 3", setup: "A complete fixture exposes return ownership and the dependence between repeated visits.", predict: "Which returns belong to the first-visit and every-visit samples for s?", code: py("rewards=[0,1,2]; states=['s','x','s']; gamma=.5", "G=0; returns=[]", "for reward in reversed(rewards):", "    G=reward+gamma*G", "    returns.append(G)", "returns.reverse()", "visits=[G for state,G in zip(states,returns) if state=='s']", "first_visit=visits[:1]", "print(returns,first_visit,visits)"), observe: "Returns are [1.0,2.0,2.0]; first-visit contributes [1.0], while every-visit contributes the dependent within-episode pair [1.0,2.0]. This trace does not make the every-visit finite-sample mean unbiased.", tryIt: "Generate many looping episodes and compare first-visit and every-visit bias while keeping episodes—not visits—as the independent clusters." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "temporal-difference-learning",
    plain: "Temporal-difference learning updates a value from one observed reward plus a current estimate of what comes next.",
    precise: "TD(0) uses target $Y_t=r_{t+1}+\\gamma(1-d_t)V(s_{t+1})$, error $\\delta_t=Y_t-V(s_t)$, and update $V(s_t)\\leftarrow V(s_t)+\\alpha\\delta_t$. It bootstraps before the episode finishes, trading lower variance for bias from current estimates. True termination sets continuation to zero; time-limit truncation follows the declared task continuation contract.",
    mentalModel: "Correct today's estimate using one new consequence and tomorrow's current forecast.", ideas: ["Compute target, TD error, then parameter update as separate values.", "Mask bootstrap by true termination rather than a generic done flag.", "Compare learning curves and asymptotic bias against Monte Carlo or exact values."],
    worked: "With V(s)=2, reward 1, gamma=.9, V(next)=3, and alpha=.1, target=3.7, delta=1.7, and updated V(s)=2.17.",
    boundary: "A small TD error can reflect self-consistent wrong values under function approximation, state aliasing, or off-policy instability.",
    objectives: { primary: "Compute and implement a TD(0) update", decision: "Diagnose bootstrapping bias and ending-mask failures" },
    vocabulary: [{ term: "Bootstrapping", meaning: "Using a current estimate of a future quantity inside the present learning target." }, { term: "TD error", meaning: "Observed reward plus discounted next estimate minus current estimate." }, { term: "Step size", meaning: "The coefficient controlling how much of the TD error changes the estimate." }],
    primaryCheck: { prompt: "Given V(s)=2, reward=1, gamma=.9, V(next)=3, and alpha=.1, calculate target, TD error, and updated value.", expected: "Target is 1+.9×3=3.7; delta is 3.7−2=1.7; updated V(s) is 2+.1×1.7=2.17.", retry: "Do not merge steps: first form the target, subtract current value, then multiply only the error by alpha." },
    decision: { explanation: "TD correctness depends on target ownership and the semantics of episode endings.", mechanism: "Unit-test terminal and truncated targets, compare online estimates with an exact or Monte Carlo reference, sweep alpha under matched transitions, and inspect error by state and visit count.", workedExample: "Treating every time limit as terminal removes a positive continuation term and depresses values near the horizon even though code remains finite.", boundary: "Agreement on a small on-policy fixture does not guarantee stability with nonlinear function approximation or off-policy replay.", check: { prompt: "At a time-limit truncation, code multiplies V(next) by zero although the task continues. What directional bias is expected?", expected: "Targets are too small by the omitted discounted continuation value, so nearby state values tend to be underestimated.", retry: "Compute the same target once with bootstrap and once without it, then compare the difference." } },
    quiz: { question: "What makes TD a bootstrapping method?", options: ["Its target contains the current estimate V(next)", "It waits for the complete return", "It knows the transition table", "It removes rewards"], answer: 0, explanation: "TD learns from an estimated continuation before the full future return is observed." },
    transfer: { prompt: "A continuing task's timeout is masked like terminal. What should change?", correct: "Preserve continuation bootstrapping at truncation under the declared task", wrong: ["Set every reward to zero", "Increase network width"], worked: "Use separate terminated and truncated flags and test their targets numerically.", retry: "Ask whether future reward becomes impossible or collection merely stopped." },
    lab: { title: "TD target clinic", question: "How do next value and ending semantics change the same one-step target?", controlLabel: "Transition record", boundary: "This exact scalar fixture does not establish neural TD stability.", cases: [{ label: "continuing", resultLabel: "target", resultValue: "3.7", meter: 74, detail: "Reward one plus discounted next value three forms the complete target." }, { label: "terminal", resultLabel: "target", resultValue: "1.0", meter: 20, detail: "True termination removes continuation value from the target." }, { label: "truncated", resultLabel: "target", resultValue: "3.7", meter: 74, detail: "The declared continuing task retains bootstrap despite data cutoff." }] }, motionConcept: "training",
    code: { title: "Apply one TD update", language: "Python 3", setup: "The update keeps target, error, and value mutation inspectable.", predict: "What updated value is printed?", code: py("value=2.0; reward=1.0; next_value=3.0", "gamma=.9; alpha=.1; terminated=False", "target=reward+gamma*(not terminated)*next_value", "delta=target-value", "value += alpha*delta", "print(target,delta,value)"), observe: "The values are 3.7, 1.7, and 2.17.", tryIt: "Toggle termination and add a separate truncation flag." }, sourceKeys: ["book", "gymnasium"] })),

  defineResearchLesson(m, rlSeed({ id: "sarsa-q-learning",
    plain: "SARSA learns from the next action actually chosen by its behavior policy, while Q-learning learns toward the best estimated next action.",
    precise: "For a true-termination indicator $d_t$, SARSA target is $r+\\gamma(1-d_t)Q(s',a')$ with $a'\\sim\\pi_b$, so ordinary one-step SARSA is on-policy for the behavior policy that supplies $a'$. Tabular Q-learning instead uses $r+\\gamma(1-d_t)\\max_aQ(s',a)$ and is off-policy: it targets a greedy policy while exploratory behavior supplies data, under the usual coverage and step-size assumptions. A time-limit truncation keeps continuation when the declared task continues; exploration, ties, endings, and coverage determine observed risk.",
    mentalModel: "SARSA evaluates the road the exploratory driver will really take; Q-learning backs up the best-looking road at the next junction.", ideas: ["Use the sampled next action only in the SARSA target.", "Use a max with declared tie behavior only in Q-learning.", "Compare safety under the behavior actually executed, not only greedy evaluation."],
    worked: "Reward 0, gamma=.9, next Q values safe=4 and risky=6, but behavior selects safe. SARSA target=3.6; Q-learning target=5.4.",
    boundary: "The larger Q-learning target is not automatically more accurate; maximization bias and risky exploratory occupancy can reverse performance.",
    objectives: { primary: "Compute SARSA and Q-learning targets from one transition", decision: "Choose and evaluate on-policy versus greedy-target control under exploration" },
    vocabulary: [{ term: "On-policy", meaning: "Learning the value of the same policy that generates behavior." }, { term: "Off-policy", meaning: "Learning about a target policy from data generated by another behavior policy." }, { term: "Maximization bias", meaning: "Upward bias caused when a maximum selects among noisy value estimates." }],
    primaryCheck: { prompt: "Reward=0, gamma=.9, next Q values [4,6], and behavior chose the value-4 action. Compute SARSA and Q-learning targets.", expected: "SARSA uses the chosen next action: .9×4=3.6. Q-learning uses the maximum: .9×6=5.4.", retry: "Mark the behavior-selected action before calculating SARSA; calculate the separate maximum only for Q-learning." },
    decision: { explanation: "Algorithm choice depends on the behavior policy, risk surface, estimator bias, and evaluation contract.", mechanism: "Hold starts, transitions, updates, exploration schedule, and seed set fixed; compare return, constraint violations, occupancy, Q overestimation, and greedy versus behavior-policy evaluation.", workedExample: "Near a cliff, exploratory SARSA learns a safer route because its target includes exploratory actions, while Q-learning's greedy target prefers the narrow edge despite behavior risk.", boundary: "The cliff pattern is a finite example, not a universal ranking between algorithms.", check: { prompt: "Q-learning has higher greedy return but more collisions while its exploratory behavior is deployed. Which result controls deployment?", expected: "Evaluate the behavior actually deployed and apply the collision gate; greedy-policy return cannot compensate for unsafe exploratory occupancy.", retry: "Name the deployed action distribution, then join its return and constraint rows before deciding." } },
    quiz: { question: "Which next value enters a SARSA target?", options: ["Q of the next action actually sampled by behavior", "Always the maximum Q", "The full episode return only", "No continuation value"], answer: 0, explanation: "SARSA's name records state, action, reward, next state, and next sampled action." },
    transfer: { prompt: "The evaluator reports only greedy Q-learning return while training exploration remains active in deployment. What is missing?", correct: "Behavior-policy occupancy, return, and constraint evidence", wrong: ["A larger replay buffer only", "A different state name"], worked: "Evaluate the exact deployed epsilon policy and preserve greedy evaluation as a separate counterfactual.", retry: "Compare the policy that generated each action with the policy named by the metric." },
    lab: { title: "Control-target comparator", question: "Which next-action rule produces each control target and risk profile?", controlLabel: "Target record", boundary: "The scalar targets demonstrate update semantics, not benchmark superiority.", cases: [{ label: "SARSA / safe chosen", resultLabel: "target", resultValue: "3.6", meter: 45, detail: "The sampled safe action determines the on-policy continuation." }, { label: "Q / greedy max", resultLabel: "target", resultValue: "5.4", meter: 68, detail: "The largest estimated action value supplies the off-policy target." }, { label: "Q / noisy max", resultLabel: "diagnosis", resultValue: "BIAS RISK", meter: 95, detail: "Selecting the maximum among noisy estimates can create optimistic targets." }] }, motionConcept: "routing",
    code: { title: "Compare tabular control targets", language: "Python 3", setup: "One nonterminal transition makes next-action ownership explicit while retaining the ending mask.", predict: "Which target is larger?", code: py("reward=0; gamma=.9; terminated=False", "q_next={'safe':4.0,'risky':6.0}", "chosen='safe'; continuation=int(not terminated)", "sarsa=reward+gamma*continuation*q_next[chosen]", "qlearning=reward+gamma*continuation*max(q_next.values())", "print(sarsa,qlearning)"), observe: "The nonterminal targets are 3.6 and 5.4; setting terminated=True makes both equal the immediate reward, while a continuing-task truncation leaves continuation enabled.", tryIt: "Test true termination and time-limit truncation separately, then add double-Q selection/evaluation tables to expose maximization bias." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "value-methods-capstone", duration: 38,
    plain: "This capstone builds exact planning, Monte Carlo, TD, SARSA, and Q-learning around one finite environment, then compares them on a changed transition process.",
    precise: "The package owns a validated MDP, exact reference values, seeded episode generator, first-visit Monte Carlo, TD(0), SARSA, and Q-learning with shared interfaces. Required evidence includes hand traces, terminal/truncation tests, learning curves by independent seed, occupancy, constraint events, model-call or transition budgets, and a held-out slip or reward intervention.",
    mentalModel: "It is a calibration range where every learner aims at the same finite target before the target dynamics change.", ideas: ["Make every algorithm pass the same transition and ending tests.", "Use exact values to separate estimator error from environment mismatch.", "Preserve all independent seeds and changed-case failures."],
    worked: "On baseline slip .1, all methods approach the safe policy; at held-out slip .3, Q-learning's greedy return stays high but deployed exploratory collisions cross the gate on two of five seeds.",
    boundary: "A finite comparison establishes implementation and mechanism in scope, not a ranking for nonlinear or continuous control.",
    objectives: { primary: "Build and verify a common value-learning workbench", decision: "Conduct a seed-level changed-dynamics comparison" },
    vocabulary: [{ term: "Reference value", meaning: "The value solved from the fully declared finite model for comparison with sampled estimates." }, { term: "Learning curve", meaning: "A metric recorded against environment interactions or updates, with its aggregation unit declared." }, { term: "Held-out dynamics", meaning: "A transition configuration reserved for changed-case evaluation rather than tuning." }],
    primaryCheck: { prompt: "Why must every method share the same termination, truncation, reward timing, and transition-budget implementation?", expected: "Otherwise target and exposure differences are confounded with algorithm choice, so observed value or return gaps cannot be attributed to the learning rule.", retry: "List every value entering a target and every counted environment transition, then require one shared owner for each." },
    decision: { explanation: "The original experiment begins only after exact and sampled baseline acceptance gates pass.", mechanism: "Pin five or more seeds, branch identical baseline configurations into one held-out transition change, count interactions, evaluate behavior and greedy policies, calculate seed-level effects, apply safety gates, and retain divergent traces.", workedExample: "TD converges faster in interactions but has a small biased plateau; Monte Carlo is slower and noisier but approaches the exact value. The report preserves both sample efficiency and final error.", boundary: "Five seeds provide bounded uncertainty rather than universal reliability, and the exact model exists only for this course fixture.", check: { prompt: "Treatment improves mean return but fails the collision gate on two declared seeds. What conclusion follows?", expected: "It fails the predeclared reliability rule unless the rule explicitly allowed those failures; report the mean improvement and failed seeds as a mixed or negative result.", retry: "Apply hard gates per seed before averaging compensable performance metrics." } },
    quiz: { question: "What should happen before the changed-dynamics experiment?", options: ["Shared baseline correctness and exact-reference checks pass", "Choose the best treatment seed", "Remove failed episodes", "Tune on the held-out case"], answer: 0, explanation: "Baseline acceptance separates implementation failure from treatment effects." },
    transfer: { prompt: "One method receives twice the environment transitions. Is a raw final-return comparison matched?", correct: "No; compare at matched interaction budgets or show the full quality-versus-interaction curve", wrong: ["Yes because both used five seeds", "Yes if its name is newer"], worked: "Align curves by counted transitions and preserve any additional-compute result separately.", retry: "Write the primitive data exposure beside every endpoint." },
    lab: { title: "Value-study release gate", question: "Which artifact package permits baseline acceptance, intervention, or a bounded conclusion?", controlLabel: "Evidence package", boundary: "The gate audits artifacts and does not execute the learner's algorithms.", cases: [{ label: "unit traces", resultLabel: "status", resultValue: "DEBUG", meter: 25, detail: "Targets are correct, but exact baseline and seed curves are incomplete." }, { label: "baseline accepted", resultLabel: "status", resultValue: "BRANCH", meter: 65, detail: "Exact values, rollout checks, and shared budgets permit the intervention." }, { label: "all seed rows", resultLabel: "status", resultValue: "DECIDE", meter: 100, detail: "Matched changed-case results and failures support the scoped decision." }] }, motionConcept: "evaluation",
    code: { title: "Specify the common update ledger", language: "Python 3 pseudocode", setup: "Pseudocode names shared inputs, one ending rule, and algorithm-specific continuation values.", predict: "Which continuation value differs across TD, SARSA, and Q-learning?", code: py("transition=(state,action,reward,next_state,terminated,truncated)", "# This continuing task bootstraps at time limits; true termination alone masks continuation.", "continuation=0 if terminated else 1", "mc_target=complete_return(trajectory,truncation_bootstrap=V[next_state])", "td_target=reward+gamma*continuation*V[next_state]", "sarsa_target=reward+gamma*continuation*Q[next_state,next_behavior_action]", "q_target=reward+gamma*continuation*max_a(Q[next_state,a])", "record(seed,interactions,target,error,constraints)"), observe: "All one-step methods use the same true-termination mask and declared truncation rule; only the continuation estimate—V, behavior-selected Q, or maximal Q—changes.", tryIt: "Assert that all three terminal targets equal reward and that their continuing-task truncation targets retain the appropriate bootstrap.", caveat: "Supply the environment, policy, tables, RNGs, and record function from the capstone specification." }, codeGuidance: { mode: "pseudocode", requirements: "Implement the named environment, tables, policies, RNG streams, and artifact writer before execution." }, sourceKeys: ["book", "gymnasium"] })),
];
