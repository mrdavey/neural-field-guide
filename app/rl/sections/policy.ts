import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { py, rlSeed } from "../seed";
const m = plannedCourseManifests.rl;

export const rlPolicySpecs = [
  defineResearchLesson(m, rlSeed({ id: "policy-gradients",
    plain: "A policy gradient directly changes action probabilities so actions followed by better-than-expected returns become more likely.",
    precise: "The likelihood-ratio identity gives $\\nabla_\\theta J=\\mathbb E[\\sum_t\\nabla_\\theta\\log\\pi_\\theta(a_t|s_t)G_t]$. A baseline independent of the sampled action can reduce variance without changing the expected gradient. Implementations must distinguish sampled actions, log probabilities under the behavior policy, returns or advantages, entropy, reduction units, and whether trajectories remain on-policy.",
    mentalModel: "Increase the log-probability of decisions credited with positive evidence and decrease it for negative evidence.", ideas: ["Trace logits to probabilities, sampled action, log probability, return, and gradient sign.", "Treat trajectories as owned by the policy revision that generated them.", "Evaluate return, entropy, KL change, and seed variance together."],
    worked: "For two-action logits [0,0], probabilities [.5,.5]. Choosing action 0 with positive advantage 2 gives loss −2 log(.5); its gradient pushes action-0 logit up and action-1 logit down.",
    boundary: "One high-return trajectory produces a noisy gradient and does not prove the credited actions caused the outcome.",
    objectives: { primary: "Compute a categorical likelihood-ratio policy-gradient term", decision: "Design on-policy collection and variance-aware evaluation" },
    vocabulary: [{ term: "Policy gradient", meaning: "A gradient of expected return with respect to policy parameters." }, { term: "Log-probability trick", meaning: "Rewriting a distribution-dependent expectation gradient using the sampled action's log-probability gradient." }, { term: "On-policy batch", meaning: "Trajectories generated by the policy revision whose objective is being updated." }],
    primaryCheck: { prompt: "At logits [0,0], action 0 is sampled with advantage +2. What loss term is used and which logit direction follows?", expected: "Loss is −2 log(.5); minimizing it raises the sampled action-0 logit and lowers its competitor through the softmax coupling.", retry: "Write negative advantage times sampled log probability, then ask which probability must rise to reduce that scalar." },
    decision: { explanation: "Policy-gradient evidence must preserve policy ownership and compare estimator reliability across complete independent runs, not only within one reused batch.", mechanism: "Collect trajectories with a frozen behavior revision, compute leakage-free advantages, update for declared passes, then aggregate fixed evaluation episodes within each training seed and report seed-level return, variance/intervals, entropy, KL, gradients, constraints, and failures.", workedExample: "Methods A and B both have seed mean 10: A returns [10,14,6], B [9,10,11]. B is more reliable in this three-seed fixture; reusing A's episodes cannot manufacture more independent training runs.", boundary: "A few seeds provide bounded variance evidence, and low KL does not prove that every probability changed safely.", check: { prompt: "Two methods have seed returns A=[10,14,6] and B=[9,10,11] at matched budgets. What variance evidence matters beyond equal means?", expected: "Both means are 10, but A varies much more across independent runs; retain each seed, compute a run-level spread or interval, and inspect failures before choosing.", retry: "Aggregate evaluation episodes inside each seed first, then compare the resulting independent seed summaries without pooling timesteps." } },
    quiz: { question: "Which action does a basic policy-gradient update reinforce or suppress?", options: ["The action that was sampled in the recorded state", "Only the action with maximum Q", "Every action equally", "An action unrelated to the trajectory"], answer: 0, explanation: "The update changes the sampled action's likelihood according to the observed return or advantage. Optional technical depth gives the exact log-probability expression." },
    transfer: { prompt: "Positive advantage is paired with a stale log probability from another policy. What fails?", correct: "Policy ownership and the on-policy estimator contract", wrong: ["Nothing; all log probabilities are interchangeable", "Only reward units"], worked: "Recompute or reject the batch under an explicit correction rather than silently mixing revisions.", retry: "Record the parameter revision that generated each action." },
    lab: { title: "Policy-gradient sign probe", question: "How do positive, zero, and negative advantages change a sampled action's probability?", controlLabel: "Advantage", boundary: "The scalar categorical fixture does not measure learned control performance.", cases: [{ label: "+2", resultLabel: "sampled action", resultValue: "INCREASE", meter: 85, detail: "Positive advantage raises the sampled action's log probability under gradient descent." }, { label: "0", resultLabel: "policy term", resultValue: "NO CHANGE", meter: 0, detail: "The likelihood-ratio contribution is zero before entropy or other terms." }, { label: "−2", resultLabel: "sampled action", resultValue: "DECREASE", meter: 85, detail: "Negative advantage lowers probability of the sampled action." }] }, motionConcept: "training",
    code: { title: "Trace a two-action policy loss", language: "Python 3", setup: "Analytic softmax arithmetic exposes the sampled log-probability term.", predict: "What is the loss for advantage two?", code: py("import math", "logits=[0.0,0.0]", "p=[math.exp(x)/sum(math.exp(y) for y in logits) for x in logits]", "action=0; advantage=2.0", "loss=-advantage*math.log(p[action])", "print(p,round(loss,3))"), observe: "Probabilities are [.5,.5] and loss is about 1.386.", tryIt: "Change advantage sign and calculate the gradient sign before using autograd." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "baselines-advantages",
    plain: "An advantage compares an action's outcome with what was expected from that state, helping policy gradients assign credit with less variance.",
    precise: "$A^\\pi(s,a)=Q^\\pi(s,a)-V^\\pi(s)$. A one-step estimate is $\\delta_t=r_{t+1}+\\gamma V(s_{t+1})-V(s_t)$. Generalized advantage estimation uses $\\hat A_t=\\sum_{l\\ge0}(\\gamma\\lambda)^l\\delta_{t+l}$, where lambda trades shorter, more biased estimates against longer, more variable estimates. Ending masks, value fit, normalization scope, and leakage determine correctness.",
    mentalModel: "Judge a decision relative to the local forecast, then decide how many future forecast errors to combine.", ideas: ["Compute TD residuals before accumulating GAE backward.", "Mask only true terminal continuation and document truncation bootstrap.", "Report value error and advantage scale beside policy return."],
    worked: "Deltas [1,.5,−.5], gamma=.9, lambda=.8 give final A2=−.5; A1=.5+.72(−.5)=.14; A0=1+.72(.14)=1.1008.",
    boundary: "A learned baseline can add bias through bad bootstrapping or leakage even while reducing apparent variance.",
    objectives: { primary: "Compute TD residuals and GAE backward", decision: "Choose baseline and lambda from bias, variance, and ending evidence" },
    vocabulary: [{ term: "Advantage", meaning: "Action value minus the state's expected value under the policy." }, { term: "GAE", meaning: "A discounted accumulation of future TD residuals controlled by lambda." }, { term: "Lambda", meaning: "The trace parameter balancing short bootstraps against longer return information." }],
    primaryCheck: { prompt: "For deltas [1,.5,−.5] and gamma×lambda=.72, compute GAE values backward.", expected: "A2=−.5; A1=.5+.72(−.5)=.14; A0=1+.72(.14)=1.1008, so earlier credit includes discounted later residuals.", retry: "Start at the final residual and recurse A_t=delta_t+gamma lambda A_{t+1}." },
    decision: { explanation: "Baseline and lambda are separate estimator choices: the baseline centers credit around expected state value, while lambda controls how many future residuals enter it.", mechanism: "On identical trajectories compare no baseline, constant/state baseline, and lambda settings; verify action-independence, endings, and leakage, then measure advantage mean/variance, critic error/bias, policy KL, return, and seed failures.", workedExample: "Returns [10,0] become centered signals [5,−5] with action-independent baseline 5 instead of [10,0]; then lambda .5 versus 1 changes residual reach, so both baseline and trace horizon are logged separately.", boundary: "A baseline correlated with the sampled action can bias the gradient, and the best baseline/lambda on one horizon or critic need not transfer.", check: { prompt: "No-baseline signals are [10,0]; baseline 5 gives [5,−5]. What changed, and what additional lambda evidence is still needed?", expected: "The action-independent baseline centers the samples and can reduce variance without changing the expected policy gradient; lambda still controls future-residual reach and must be compared with ending and critic-error evidence.", retry: "Separate subtraction of expected state value from geometric weighting of later TD residuals, then test each choice independently." } },
    quiz: { question: "What does a shorter advantage trace emphasize?", options: ["The immediate prediction error and its next value estimate", "The complete undiscounted return", "Zero learning signal", "A maximum over every action"], answer: 0, explanation: "A short trace relies more on the nearest prediction error and learned value estimate; longer traces include more later sampled outcomes." },
    transfer: { prompt: "Time limits are masked terminal during GAE for a continuing task. What bias follows?", correct: "Continuation value is omitted near cutoffs", wrong: ["No effect on any advantage", "Only action entropy changes"], worked: "Retain bootstrap at truncation and recompute backward traces.", retry: "Compare the final residual with and without next value." },
    lab: { title: "GAE horizon dial", question: "How does lambda change residual reach and critic dependence?", controlLabel: "Lambda", boundary: "The meter illustrates estimator structure, not measured variance.", cases: [{ label: "0", resultLabel: "reach", resultValue: "1 STEP", meter: 20, detail: "Only the current residual contributes, maximizing critic dependence." }, { label: ".8", resultLabel: "reach", resultValue: "DECAYED", meter: 65, detail: "Later residuals enter with geometrically shrinking weights." }, { label: "1", resultLabel: "reach", resultValue: "RETURN-LIKE", meter: 100, detail: "Longer credit increases while sampled-return variance can rise." }] }, motionConcept: "distribution",
    code: { title: "Compute GAE backward", language: "Python 3", setup: "A three-residual fixture makes recursion inspectable.", predict: "What first advantage results?", code: py("deltas=[1.0,.5,-.5]; gl=.72", "gae=0; advantages=[]", "for delta in reversed(deltas):", "    gae=delta+gl*gae; advantages.append(gae)", "advantages.reverse(); print(advantages)"), observe: "The advantages are approximately [1.1008,.14,−.5].", tryIt: "Set the last transition terminal versus truncated and recompute its residual." }, sourceKeys: ["gae", "book"] })),

  defineResearchLesson(m, rlSeed({ id: "actor-critic",
    plain: "Actor-critic methods learn a policy actor and a value critic from the same trajectories: the critic estimates credit and the actor changes action probabilities.",
    precise: "The critic minimizes a value loss toward returns or bootstrapped targets. The actor minimizes $-\\log\\pi(a_t|s_t)\\hat A_t$, often plus entropy; shared encoders receive gradients from both objectives. Update ordering, target detachment, loss coefficients, recurrent state, and on-policy ownership determine whether critic targets leak gradients or one loss overwhelms the other. PPO adds a clipped probability-ratio surrogate to limit each policy update.",
    mentalModel: "The critic grades decisions relative to expectation; the actor uses those grades to revise its behavior.", ideas: ["Detach advantages from the actor loss unless a deliberate coupled objective is derived.", "Log actor, critic, entropy, KL, and gradient contributions separately.", "Evaluate policy behavior independently of critic training loss."],
    worked: "The behavior policy chose the sampled action with probability .4; the updated policy [.6,.4] assigns that same action probability .6. The ratio is $.6/.4=1.5$, clipped to 1.2, so the positive-advantage surrogate is $1.2(2)=2.4$. Critic .6 versus target 1 gives squared loss .16, and updated-policy entropy is .673. With value coefficient .5 and entropy .01, total loss is $-2.4+.5(.16)-.01(.673)=-2.3267$.",
    boundary: "A low critic loss does not establish good actions, and aggressive clipping can hide stalled learning or poor advantage estimates.",
    objectives: { primary: "Trace actor, critic, entropy, and clipped-ratio losses", decision: "Diagnose shared-gradient and policy-update failures" },
    vocabulary: [{ term: "Actor", meaning: "The policy component that produces an action distribution." }, { term: "Critic", meaning: "A learned value estimator used to assess states or actions." }, { term: "PPO", meaning: "Proximal Policy Optimization, an on-policy method using a probability-ratio surrogate and bounded-update diagnostics." }, { term: "Probability ratio", meaning: "New-policy probability of a sampled action divided by its behavior-policy probability." }, { term: "Clipped surrogate", meaning: "A PPO objective limiting how much the probability ratio can improve the per-sample term." }],
    primaryCheck: { prompt: "Given clipped actor surrogate 2.4, critic prediction .6/target 1, entropy .673, value coefficient .5, entropy coefficient .01, compute the total minimization loss.", expected: "Actor loss is −2.4; critic squared loss is (.6−1)^2=.16; total is −2.4+.5×.16−.01×.673≈−2.3267, with each term retaining separate ownership.", retry: "Compute actor, critic, and entropy contributions separately with their signs and coefficients, then add them only at the final optimizer loss." },
    decision: { explanation: "Actor-critic debugging joins policy movement, critic quality, shared gradients, and behavior outcomes.", mechanism: "Verify detached targets and action log probabilities, compare actor/critic gradient norms, explained variance, entropy, clip fraction, KL, return, constraints, and seed-level failures under matched trajectory budgets.", workedExample: "Critic coefficient 10 makes shared-encoder gradients 50× actor gradients; value loss falls while entropy and return collapse.", boundary: "Balancing gradient norms is a diagnostic, not proof that losses optimize compatible representations.", check: { prompt: "Critic loss falls, actor gradient norm approaches zero, and policy return is flat. What is the leading shared-system concern?", expected: "The critic objective or shared representation may dominate, starving actor learning; inspect coefficients, detachments, and per-loss gradients.", retry: "Backpropagate each loss separately through shared parameters and compare directions and magnitudes." } },
    quiz: { question: "Why store old action log probabilities in PPO?", options: ["To compute new-to-behavior probability ratios", "To replace rewards", "To build a transition model", "To remove the critic"], answer: 0, explanation: "The clipped surrogate needs the probability under the policy that generated each action." },
    transfer: { prompt: "Advantages backpropagate into the critic through actor loss unintentionally. What failed?", correct: "Target detachment and loss ownership", wrong: ["The action space must be larger", "This is always required"], worked: "Detach the advantage for the actor objective, then test actor and critic gradients independently.", retry: "Draw which parameters each loss is intended to update." },
    lab: { title: "Actor-critic loss board", question: "Which trace reveals healthy coupling, critic domination, or an invalid PPO ratio?", controlLabel: "Loss trace", boundary: "Values are diagnostic fixtures and not an executed PPO benchmark.", cases: [{ label: "balanced", resultLabel: "status", resultValue: "TEST", meter: 35, detail: "Both gradient paths are finite and behavior evidence remains required." }, { label: "critic ×50", resultLabel: "status", resultValue: "DOMINATED", meter: 95, detail: "Shared parameters receive little effective actor signal." }, { label: "missing old logp", resultLabel: "PPO", resultValue: "INVALID", meter: 100, detail: "The behavior ratio cannot be reconstructed without stored probabilities." }] }, motionConcept: "preference",
    code: { title: "Compute a PPO clipped term", language: "Python 3", setup: "Scalar arithmetic uses the sampled action's old and new probabilities from the worked trace.", predict: "Which ratio is used for positive advantage?", code: py("old_p,new_policy,action,adv,eps=.4,[.6,.4],0,2.0,.2", "new_p=new_policy[action]", "ratio=new_p/old_p", "clipped=max(1-eps,min(1+eps,ratio))", "surrogate=min(ratio*adv,clipped*adv)", "print(ratio,clipped,surrogate)"), observe: "Ratio 1.5 clips to 1.2 and the surrogate is 2.4; the same [.6,.4] updated policy supplies the entropy term.", tryIt: "Use negative advantage and explain why the minimum changes which side is conservative." }, sourceKeys: ["ppo", "gae"] })),

  defineResearchLesson(m, rlSeed({ id: "on-policy-capstone", duration: 40,
    plain: "This capstone builds a categorical policy-gradient and actor-critic pipeline, verifies its estimators, and compares one credit or update intervention across independent seeds.",
    precise: "The build owns a seeded vector environment, frozen rollout policy, action/log-prob/value/reward/ending buffer, return and GAE calculators, actor and critic objectives, optimizer, evaluation policy, checkpoints, and artifact writer. Tests cover categorical probabilities, log-prob ownership, hand returns/GAE, detachment, gradient signs, tiny-batch overfit, resume, and bounded baseline learning.",
    mentalModel: "Build a closed evidence loop from behavior-policy trajectory to credit estimate to bounded policy change.", ideas: ["Verify estimator arithmetic before long rollouts.", "Keep behavior revision and update count attached to every batch.", "Change one lambda, critic, entropy, or clip setting under matched interactions."],
    worked: "Baseline GAE lambda .95 is compared with .5 over five paired seeds at identical interactions and update epochs; return, variance, KL, entropy, critic error, and failures are retained.",
    boundary: "A small on-policy result does not establish scalable continuous control or universal PPO defaults.",
    objectives: { primary: "Build and verify a complete on-policy actor-critic agent", decision: "Conduct a paired estimator or policy-update intervention" },
    vocabulary: [{ term: "Rollout buffer", meaning: "A batch of trajectories and behavior-policy statistics reserved for one on-policy update phase." }, { term: "Clip fraction", meaning: "The share of PPO samples whose probability ratio crosses the clipping bound." }, { term: "Explained variance", meaning: "A diagnostic comparing critic prediction error with variance of the value targets." }],
    primaryCheck: { prompt: "Why must rollout actions, old log probabilities, values, rewards, and ending flags be saved together before an update?", expected: "They jointly define behavior ownership, returns or advantages, probability ratios, critic targets, and bootstrap masks; recomputing after policy change breaks the batch contract.", retry: "Map each saved field to one formula in actor loss, critic loss, GAE, or PPO ratio." },
    decision: { explanation: "The intervention must isolate one estimator or update mechanism while preserving interaction and optimization exposure.", mechanism: "Pass baseline gates, pin seeds/interactions/update epochs/evaluation, branch one setting, preserve every batch and seed row, compute paired effects and intervals, and apply KL, entropy, constraint, and finite-value gates before the conclusion.", workedExample: "Lower lambda reduces advantage variance but lowers delayed-reward return; neither is a win until the predeclared task criterion and stability gates are applied.", boundary: "The result belongs to the chosen environment, policy class, horizon, seeds, and update budget.", check: { prompt: "Treatment uses the same interactions but twice the update epochs. Is the policy-update comparison matched?", expected: "No. Data exposure matches but optimization exposure differs; match epochs or report a two-axis quality-versus-interaction-and-update comparison.", retry: "Count environment steps, samples reused, minibatch updates, and network evaluations separately." } },
    quiz: { question: "What is the independent unit for training reliability?", options: ["A separately seeded complete agent run", "Every timestep in one rollout", "Every action dimension", "One loss term"], answer: 0, explanation: "Timesteps within one trained run share policy and training randomness." },
    transfer: { prompt: "One seed violates the KL gate but is omitted. What must happen?", correct: "Restore and diagnose the seed under the predeclared exclusion rule", wrong: ["Keep only passing seeds", "Average before restoring it"], worked: "Treatment reliability includes unstable updates; preserve the row and sensitivity analysis.", retry: "Reconcile declared seeds with artifacts before aggregation." },
    lab: { title: "On-policy study gate", question: "Which package supports arithmetic debugging, baseline branching, or a bounded result?", controlLabel: "Package", boundary: "The gate classifies evidence and does not train an agent.", cases: [{ label: "GAE trace", resultLabel: "status", resultValue: "DEBUG", meter: 25, detail: "Estimator arithmetic passes while policy learning and resume remain unverified." }, { label: "baseline accepted", resultLabel: "status", resultValue: "BRANCH", meter: 65, detail: "Policy, critic, rollout, checkpoint, and bounded-learning gates permit treatment." }, { label: "paired seeds", resultLabel: "status", resultValue: "DECIDE", meter: 100, detail: "All rows, update budgets, intervals, and failures support the scoped conclusion." }] }, motionConcept: "pipeline",
    code: { title: "Specify an on-policy update", language: "Python 3 pseudocode", setup: "The skeleton preserves behavior ownership and separate actor/critic losses.", predict: "When does the rollout policy change?", code: py("freeze(policy_revision)", "buffer=collect(actions,old_logp,values,rewards,endings)", "advantages,returns=gae(buffer,gamma,lambda_)", "for epoch in declared_epochs:", "    actor_loss=clipped_policy_loss(buffer.old_logp,advantages.detach())", "    critic_loss=value_loss(returns)", "    optimize(actor_loss+coef*critic_loss-entropy_bonus)", "evaluate(separate_seed_set)"), observe: "Collection finishes under one frozen revision before declared update epochs begin.", tryIt: "Attach revision IDs and assert no batch mixes them.", caveat: "Supply a tensor library, vector environment, networks, buffer, RNG streams, and artifact writer." }, codeGuidance: { mode: "pseudocode", requirements: "Implement the named on-policy system; optional accelerator execution follows the pinned RL runbook." }, sourceKeys: ["gae", "ppo", "gymnasium"] })),
];
