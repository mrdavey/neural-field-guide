import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { py, rlSeed } from "../seed";
const m = plannedCourseManifests.rl;

export const rlOfflineSpecs = [
  defineResearchLesson(m, rlSeed({ id: "behavior-cloning",
    plain: "Behavior cloning trains a policy to predict the action demonstrated in each recorded state or observation.",
    precise: "Given dataset $D={(o_t,a_t)}$, discrete behavior cloning minimizes action cross-entropy; continuous cloning often minimizes Gaussian negative log-likelihood or regression loss. It learns the demonstrator's conditional action distribution only on logged support. Episode splits, temporal leakage, action scaling, multimodal actions, class imbalance, and deployment covariate shift define correctness and limits.",
    mentalModel: "Learn a driver's action choices from recorded views, knowing the learned driver may later create views absent from the recordings.", ideas: ["Split by episode or scenario before creating overlapping windows.", "Trace observation and action shapes, units, masks, and normalization.", "Evaluate closed-loop recovery in addition to held-out imitation loss."],
    worked: "For logits [0,0] and demonstrated turn (class 1), softmax is [.5,.5], cross-entropy is −log(.5)=.693, gradient p−y=[.5,−.5], and learning rate .1 updates logits to [−.05,.05], raising turn probability.",
    boundary: "Low supervised loss does not establish closed-loop control because small errors change future observations and can compound off data support.",
    objectives: { primary: "Build and verify a supervised trajectory policy", decision: "Diagnose dataset imbalance and closed-loop covariate shift" },
    vocabulary: [{ term: "Behavior cloning", meaning: "Supervised learning of a policy from demonstrated observation-action pairs." }, { term: "Demonstrator", meaning: "The policy, expert, or operator whose actions produced the training data." }, { term: "Covariate shift", meaning: "A change in the distribution of observations encountered by the learned policy relative to the training data." }],
    primaryCheck: { prompt: "At logits [0,0] with demonstrated class 1 and learning rate .1, compute softmax, cross-entropy, gradient p−y, and updated logits.", expected: "Softmax is [.5,.5], loss is .693, gradient [.5,−.5], and gradient descent gives logits [−.05,.05], increasing probability of the demonstrated action.", retry: "Compute probabilities and one-hot target, subtract target from probabilities, then subtract learning rate times that gradient from each logit." },
    decision: { explanation: "Cloning acceptance joins class/scenario balance with closed-loop occupancy, recovery, and constraints so aggregate supervised fit cannot hide two distinct failures.", mechanism: "Split by episode; report per-action/state/scenario loss and calibration; compare balanced versus natural weighting; then roll out matched and perturbed starts, join occupancy with demonstration support, and record first compounding error and interventions.", workedExample: "A 90%-straight dataset lets always-straight reach 90% accuracy but 0% turn success; after a lateral perturbation the same missing recovery actions push observations farther outside demonstration support.", boundary: "Reweighting can expose rare actions without creating missing recovery states, and a finite perturbation suite cannot prove universal recovery.",check:{prompt:"Aggregate accuracy is 90%, turn accuracy 0%, and perturbed rollouts drift beyond logged states. What two diagnoses and repairs follow?",expected:"Class imbalance hides rare-turn failure, while closed-loop covariate shift exposes missing recovery coverage; report slices/reweighting and collect or query recovery states before acceptance.",retry:"Separate errors present in the logged class distribution from new observations caused by the learned policy."}},
    quiz: { question: "What is behavior cloning's basic training signal?", options: ["Demonstrated actions for recorded observations", "Online reward only", "A learned dynamics maximum", "Unlabeled images only"], answer: 0, explanation: "Behavior cloning is supervised policy fitting on logged observation-action pairs." },
    transfer: { prompt: "Randomly splitting overlapping windows from one episode gives great validation. What leakage risk remains?", correct: "Near-duplicate temporal context appears in train and validation", wrong: ["No risk because rows differ", "Only action scaling matters"], worked: "Split complete episodes or scenarios before windowing and rerun the metric.", retry: "Identify which raw timesteps contribute to each window across splits." },
    lab: { title: "Cloning evidence matrix", question: "Which evaluation exposes majority-class success, leakage, or closed-loop recovery failure?", controlLabel: "Record", boundary: "The percentages are deterministic teaching scenarios, not policy measurements.", cases: [{ label: "90% aggregate", resultLabel: "turn success", resultValue: "0%", meter: 90, detail: "Majority action accuracy hides total failure on rare junction decisions." }, { label: "window split", resultLabel: "leakage", resultValue: "HIGH", meter: 100, detail: "Overlapping temporal frames contaminate the validation boundary." }, { label: "perturbed rollout", resultLabel: "recovery", resultValue: "FAIL", meter: 95, detail: "Off-trajectory states reveal missing corrective behavior." }] }, motionConcept: "training",
    code: { title: "Audit class-conditional cloning accuracy", language: "Python 3", setup: "A small label table exposes the majority baseline.", predict: "What aggregate and turn accuracy result?", code: py("labels=['straight']*9+['turn']", "pred=['straight']*10", "accuracy=sum(a==b for a,b in zip(labels,pred))/10", "turn=[i for i,a in enumerate(labels) if a=='turn']", "turn_accuracy=sum(pred[i]==labels[i] for i in turn)/len(turn)", "print(accuracy,turn_accuracy)"), observe: "Aggregate accuracy is .9 while turn accuracy is zero.", tryIt: "Add balanced loss weights and verify whether closed-loop recovery actually changes." }, sourceKeys: ["dagger", "book"] })),

  defineResearchLesson(m, rlSeed({ id: "covariate-shift-dagger",
    plain: "DAgger reduces imitation covariate shift by running the learner, asking an expert for the correct action on learner-visited states, and aggregating those labeled states into training data.",
    precise: "At iteration i, a mixture or learner policy collects states from its own occupancy; expert labels those states; the aggregate dataset retrains the policy. The mixture schedule, query budget, expert authority, unsafe-state handling, episode reset, duplicate weighting, and stopping rule are part of the algorithm. DAgger changes state coverage, not merely dataset size.",
    mentalModel: "Let the student drive, pause at the situations it creates, and ask the instructor what should have happened there.", ideas: ["Keep executed action separate from expert label.", "Track learner-visited state coverage and expert query provenance by iteration.", "Define safety shields and stop authority before learner rollouts."],
    worked: "D0 has 100 nominal rows and no recovery states. Learner iteration 1 visits 40 recovery states; expert labels 35 right and 5 stop. Aggregate D1=D0∪40 rows, retrain policy pi1, then a held-out recovery rollout chooses right in 8/10 cases versus pi0's 0/10.",
    boundary: "DAgger can be unsafe or expensive when learner mistakes cannot be tolerated or expert labels are inconsistent.",
    objectives: { primary: "Trace a complete DAgger collection and aggregation iteration", decision: "Choose query, mixture, and safety policies from coverage and intervention evidence" },
    vocabulary: [{ term: "DAgger", meaning: "Dataset Aggregation: iterative expert labeling of states visited by the evolving learner." }, { term: "Expert query", meaning: "A request for the reference action at a collected learner state." }, { term: "Mixture policy", meaning: "A behavior rule combining expert and learner action selection during collection." }],
    primaryCheck: { prompt: "D0 has 100 nominal rows; learner collects 40 recovery rows labeled 35 right/5 stop. Trace aggregation, retraining, and the next-policy check.", expected: "D1 contains 140 rows with provenance; retrain pi1 on D1, then compare pi1 with pi0 on held-out recovery states, such as 8/10 versus 0/10 correct recovery actions.",retry:"Write the iteration as collect learner states → query expert → union with D0 → retrain pi1 → evaluate pi1 on a separate recovery set."},
    decision: { explanation: "DAgger settings balance coverage repair, expert cost, learner autonomy, and rollout safety.", mechanism: "Predeclare mixture and query schedules, label every state with collector/expert/action provenance, compare coverage, interventions, expert disagreements, closed-loop success, constraints, and query cost per iteration.", workedExample: "Pure learner collection discovers recovery states fastest but crosses the safety gate; a shielded mixture collects fewer novel states while remaining feasible.", boundary: "Expert labels on encountered states do not supply evidence for states the mixture and reset distribution never visit.", check: { prompt: "Query cost falls because the learner never enters difficult states after a restrictive shield. Has recovery coverage improved?", expected: "Not necessarily. Low query count may reflect blocked coverage; inspect learner occupancy and held-out recovery rollouts.", retry: "Put expert queries beside unique critical states rather than interpreting count alone." } },
    quiz: { question: "What data does DAgger specifically add?", options: ["Expert labels on learner-visited states", "Only more original demonstrations", "Unlabeled rewards", "A transition table"], answer: 0, explanation: "The key intervention repairs occupancy mismatch by labeling states the learner creates." },
    transfer: { prompt: "Expert and executed actions are stored in one field. What is lost?", correct: "Behavior provenance and the ability to distinguish supervision from action taken", wrong: ["Nothing if actions often match", "Only episode length"], worked: "Store collector policy, executed action, expert action, intervention, and iteration separately.", retry: "Ask which action caused the next state and which action became a label." },
    lab: { title: "DAgger collection gate", question: "How do learner occupancy, expert queries, and shields change usable recovery evidence?", controlLabel: "Collection", boundary: "The counts are design fixtures, not real expert interactions.", cases: [{ label: "expert only", resultLabel: "recovery states", resultValue: "0", meter: 10, detail: "Safe nominal behavior never exposes learner-induced recovery states." }, { label: "learner only", resultLabel: "safety", resultValue: "FAIL", meter: 100, detail: "Coverage rises but unsafe states exceed the collection gate." }, { label: "shielded mix", resultLabel: "status", resultValue: "FEASIBLE", meter: 60, detail: "Some recovery evidence is gathered while interventions remain explicit." }] }, motionConcept: "agent",
    code: { title: "Preserve DAgger action provenance", language: "Python 3", setup: "A record distinguishes executed and expert actions.", predict: "Which action determines the next state?", code: py("row={'iteration':1,'collector':'learner','state':'drift-left',", "     'executed_action':'stop','expert_action':'steer-right','intervened':True}", "print(row)", "assert row['executed_action'] == 'stop'", "assert row['expert_action'] == 'steer-right'"), observe: "The executed action owns the transition; the expert action owns supervision.", tryIt: "Aggregate rows by iteration and critical-state coverage." }, sourceKeys: ["dagger"] })),

  defineResearchLesson(m, rlSeed({ id: "offline-rl-coverage",
    plain: "Offline reinforcement learning improves a policy using a fixed logged dataset without collecting corrective transitions during training.",
    precise: "Offline RL combines rewards and temporal targets with a behavior dataset whose state-action support is fixed. Maximizing over unseen actions can exploit extrapolated Q values. Conservative methods penalize unsupported action values or constrain policy deviation. Dataset composition, terminal handling, coverage, behavior heterogeneity, reward relabeling, evaluator validity, and off-policy assumptions bound every claim.",
    mentalModel: "Choose a route using only old travel logs; roads never taken cannot acquire trustworthy outcomes just because a model scores them highly.", ideas: ["Audit critical state-action coverage before optimization.", "Compare behavior cloning, value learning, and conservative variants on the same logs.", "Abstain where target actions have no credible support or validated model evidence."],
    worked: "At state s, behavior action counts are [100,0], while Q estimates are [5,20]. With explicit zero-based labels, action index 1 is the second action and has zero logged support; greedily selecting it is an extrapolation claim, not logged evidence.",
    boundary: "Conservatism reduces some unsupported optimism but cannot recover absent causal outcomes or guarantee safe policy improvement.",
    objectives: { primary: "Audit offline dataset coverage and value extrapolation", decision: "Choose a conservative offline policy and evaluation claim" },
    vocabulary: [{ term: "Offline RL", meaning: "Policy learning from a fixed logged transition dataset without online collection during training." }, { term: "Support mismatch", meaning: "Target policy choosing state-action pairs rare or absent under the behavior data." }, { term: "Conservatism", meaning: "Biasing value or policy learning away from unsupported high-value actions." }],
    primaryCheck: { prompt: "Behavior counts at state s are [100,0] while Q estimates are [5,20]. What evidence supports choosing the second action?", expected: "No direct logged outcome evidence supports it at s; the value 20 is extrapolation requiring a validated model or new data, otherwise the policy should constrain or abstain.", retry: "Join each target-positive action with its behavior count and logged outcomes before reading its Q estimate." },
    decision: { explanation: "Offline policy choice joins coverage, pessimism or deviation, and an evaluator whose assumptions match the data.", mechanism: "Build state-action coverage and return slices, compare behavior cloning and conservative baselines at matched updates, inspect target behavior ratios/Q scale, use only valid off-policy or trusted-simulator evaluation, and block unsupported critical actions.", workedExample: "A conservative policy scores lower under learned Q but higher in a trusted holdout simulator because it avoids an unseen action with optimistic extrapolation.", boundary: "A learned evaluator trained on the same narrow data can share the policy's extrapolation error.", check: { prompt: "A new policy's estimated value rises only on actions absent from data. What is the defensible conclusion?", expected: "Improvement is unsupported and likely extrapolative; constrain the policy, obtain new evidence, or use a separately validated model/evaluator with stated assumptions.", retry: "Decompose value gain by behavior action count and remove zero-support contributions from the claim." } },
    quiz: { question: "What makes offline RL distinct during training?", options: ["The transition dataset is fixed and no corrective online data are collected", "Rewards are absent", "Policies cannot change", "Only supervised loss is allowed"], answer: 0, explanation: "Optimization may change the policy, but evidence support cannot expand through online interaction." },
    transfer: { prompt: "A policy improvement relies on a learned evaluator trained on identical narrow logs. What concern remains?", correct: "Shared extrapolation and evaluator invalidity off support", wrong: ["The result is automatically causal", "No coverage check is needed"], worked: "Validate on held-out support, trusted simulation, or new data and keep zero-support claims bounded.", retry: "Identify evidence independent of the optimistic Q estimates." },
    lab: { title: "Offline support tribunal", question: "Which target action is supported, thin, or requires abstention?", controlLabel: "Action row", boundary: "Counts and Q values are finite fixtures rather than dataset measurements.", cases: [{ label: "count 100", resultLabel: "support", resultValue: "DIRECT", meter: 20, detail: "Many logged outcomes support in-distribution evaluation within the slice." }, { label: "count 2", resultLabel: "support", resultValue: "THIN", meter: 70, detail: "High variance and sensitivity require uncertainty and conservative treatment." }, { label: "count 0 / Q20", resultLabel: "decision", resultValue: "ABSTAIN", meter: 100, detail: "An optimistic value cannot replace missing outcome evidence." }] }, motionConcept: "distribution",
    code: { title: "Block unsupported greedy actions", language: "Python 3", setup: "A simple support mask prevents a raw maximum from becoming a policy claim.", predict: "Which action remains eligible?", code: py("counts=[100,0]; q=[5.0,20.0]", "eligible=[i for i,n in enumerate(counts) if n>0]", "action=max(eligible,key=lambda i:q[i])", "print(action,q[action])", "assert action==0"), observe: "Only the logged action is eligible under the strict fixture rule.", tryIt: "Replace the binary rule with a declared count threshold and uncertainty interval." }, sourceKeys: ["cql", "d4rl"] })),

  defineResearchLesson(m, rlSeed({ id: "sequence-policy-capstone", duration: 42,
    plain: "This capstone builds a trajectory-sequence policy from logged data and audits whether sequence modeling improves decisions without escaping dataset support.",
    precise: "The build serializes return or goal context, states, actions, timesteps, masks, and episode boundaries into causal training windows; predicts actions only from allowed prefixes; splits by trajectory; evaluates action loss, return-conditioned behavior, coverage, and closed-loop outcomes in a trusted fixture. A behavior-cloning baseline, support guard, checkpoint/resume, and all-seed artifact are required.",
    mentalModel: "Turn each logged episode into a causal decision notebook while keeping every page tied to the behavior that produced it.", ideas: ["Test serialization, target shifting, masks, and episode boundaries exactly.", "Compare with behavior cloning under the same data and update budget.", "Treat desired return as context, not a guarantee of achieving it."],
    worked: "For one episode, serialize `[R0,s0,a0,R1,s1,a1]`. The logit at state position `s0` predicts target `a0` and may read only `[R0,s0]`; the logit at `s1` predicts `a1` and may read `[R0,s0,a0,R1,s1]`. With batch 2, six token positions, width 32, and two actions, token states have shape `[2,6,32]`, the broadcast causal-plus-padding mask is `[2,1,6,6]`, and action logits are `[2,6,2]` before selecting state positions. Replacing future `a1` must leave the earlier `a0` logit unchanged. After this leakage check, if high-return context still chooses unseen action B in a critical state, a support guard routes to logged action A and records abstention rather than claiming high-return control.",
    boundary: "Conditioning on a high desired return does not create causal knowledge of unsupported actions or prove online performance.",
    objectives: { primary: "Build and verify a causal sequence policy from logged trajectories", decision: "Conduct a matched sequence-versus-cloning coverage study" },
    vocabulary: [{ term: "Sequence policy", meaning: "A policy predicting actions from a serialized prefix of trajectory context." }, { term: "Return conditioning", meaning: "Supplying a desired or remaining return token as policy context." }, { term: "Trajectory split", meaning: "Assigning whole episodes to train or evaluation to prevent overlapping-window leakage." }, { term: "Causal mask", meaning: "A visibility rule that blocks each prediction position from reading later token positions." }, { term: "Target shift", meaning: "Aligning a prediction position with the next value it must predict, so the target itself is absent from that position's input prefix." }],
    primaryCheck: { prompt: "Why split whole trajectories before windows, and which prefix may predict a0 in `[R0,s0,a0,R1,s1,a1]`?", expected: "Episode-first splitting keeps overlapping windows from one trajectory out of both train and evaluation. The a0 target is predicted from the state-position prefix `[R0,s0]`; a0 and every later token must be invisible to that logit. Changing future a1 must therefore leave the a0 logit unchanged.", retry: "First assign an episode ID wholly to one split. Then mark each target action position and cross out that action plus every token to its right from the corresponding prediction prefix." },
    decision: { explanation: "Sequence modeling is accepted only if changed-case behavior improves without unsupported actions, leakage, or unmatched compute.", mechanism: "Pin trajectory split, tokens/windows, updates, model calls, seeds, and evaluator; compare cloning and sequence policies on loss, critical-action support, desired-versus-real return, trusted rollouts, constraints, and abstentions.", workedExample: "Sequence loss is lower, but its apparent return gain disappears after trajectory-level splitting; the original gain was leakage.", boundary: "A small trusted simulator cannot establish policy value in a different real environment.", check: { prompt: "Sequence policy has lower validation loss but chooses zero-count critical actions more often. Is it a clear improvement?", expected: "No. Supervised fit improved while coverage risk regressed; apply the support gate and compare trusted closed-loop outcomes.", retry: "Join every chosen action to behavior count and separate loss from decision validity." } },
    quiz: { question: "What does return conditioning provide?", options: ["A context variable expressing desired or remaining outcome", "A guarantee of achieved reward", "New transitions", "A safety proof"], answer: 0, explanation: "The model learns correlations in logged trajectories; the condition is not causal evidence outside support." },
    transfer: { prompt: "High-return context drives an unseen action. What is defensible?", correct: "Abstain, constrain to supported behavior, or obtain new evidence", wrong: ["Trust the desired return token", "Call it proven improvement"], worked: "Record the support failure and route through the declared guard.", retry: "Find logged consequences for the exact state-action choice." },
    lab: { title: "Sequence-policy release gate", question: "Which artifact permits debugging, baseline comparison, or a bounded policy conclusion?", controlLabel: "Package", boundary: "The checklist does not train or evaluate a real sequence model.", cases: [{ label: "causal windows", resultLabel: "status", resultValue: "DEBUG", meter: 25, detail: "Serialization and masks pass while split and closed-loop evidence remain." }, { label: "matched baselines", resultLabel: "status", resultValue: "TEST", meter: 60, detail: "Cloning and sequence models share data, updates, seeds, and evaluator." }, { label: "support + rollout", resultLabel: "status", resultValue: "DECIDE", meter: 100, detail: "All seed rows, support gates, abstentions, and trusted outcomes are preserved." }] }, motionConcept: "pipeline",
    code: { title: "Specify causal trajectory windows", language: "Python 3 pseudocode", setup: "The skeleton names token positions, target ownership, tensor shapes, padding, and a changed-future leakage test.", predict: "Can the state-position logit for a0 read token a0?", code: py("train_eps,test_eps=split_whole_trajectories_before_windowing(episodes,seed)", "tokens,valid=serialize(train_eps)  # [B,T]=[2,6]; R0,s0,a0,R1,s1,a1", "state_pos=[1,4]; action_targets=tokens[:,[2,5]]  # [B,2], never input at its predictor", "mask=lower_triangular(T)[None,None,:,:] & valid[:,None,None,:]  # [B,1,T,T]", "hidden=model.embed(tokens)  # [B,T,D]=[2,6,32]", "logits=model(hidden,mask)  # [B,T,A]=[2,6,2]", "loss=action_loss(logits[:,state_pos,:],action_targets,valid[:,state_pos])", "assert earlier_logit(tokens)==earlier_logit(replace_future_a1(tokens))", "evaluate_support_abstention_and_trusted_rollouts(model,test_eps)"), observe: "The a0 predictor sits at s0, reads only R0 and s0, and cannot see its target or future tokens; padding keys are blocked and padded prediction rows are excluded from loss.", tryIt: "Also replace future return R1 and assert the a0 logit is unchanged, then deliberately enable one future mask cell and confirm the test fails.", caveat: "Supply the concrete tokenizer, tensor library, model, episode dataset, optimizer, support audit, and trusted evaluator; the displayed interfaces are pseudocode." }, codeGuidance: { mode: "pseudocode", requirements: "Implement the complete sequence-policy system before execution; the required lesson remains inspectable without external compute." }, sourceKeys: ["d4rl", "cql"] })),
];
