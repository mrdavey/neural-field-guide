import { plannedCourseManifests } from "../../research-curriculum-manifests";
import { defineResearchLesson } from "../../research-courses/helpers";
import { py, rlSeed } from "../seed";
const m = plannedCourseManifests.rl;

export const rlDeepValueSpecs = [
  defineResearchLesson(m, rlSeed({ id: "function-approximation",
    plain: "Function approximation uses shared parameters to predict values for many states instead of storing a separate table entry for each one.",
    precise: "A value approximator $V_w(s)$ or $Q_w(s,a)$ maps features to a scalar. For a fixed regression target y, semi-gradient update is $w\\leftarrow w+\\alpha[y-Q_w(s,a)]\\nabla_wQ_w(s,a)$. Linear features make sharing explicit; neural networks learn features but add nonlinearity, initialization, optimization, and distribution-shift failure modes. With bootstrapped off-policy targets, approximation can destabilize the learning process.",
    mentalModel: "One adjustable surface covers many states, so changing one point can lift or lower nearby predictions too.", ideas: ["Name input features, parameter shapes, scalar output, and target ownership.", "Test generalization on changed states separately from training fit.", "Track target drift, gradient norms, and prediction scale under bootstrapping."],
    worked: "For source features [1,2], w=[.5,−.25] predicts 0; target 1 and alpha=.1 update w to [.6,−.05]. Neighbor features [0,1] therefore change prediction from −.25 to −.05 even though that state was not sampled.",
    boundary: "Low training error can coexist with harmful extrapolation, interference between states, and unstable bootstrapped targets.",
    objectives: { primary: "Compute a linear value-function update and trace parameter sharing", decision: "Diagnose approximation generalization and instability" },
    vocabulary: [{ term: "Feature vector", meaning: "A numeric representation of a state or state-action pair supplied to an approximator." }, { term: "Semi-gradient", meaning: "A gradient update that treats a bootstrapped target as fixed while differentiating the current prediction." }, { term: "Interference", meaning: "A parameter update for one example changing predictions for other examples that share parameters." }],
    primaryCheck: { prompt: "Update source phi=[1,2], w=[.5,−.25], target 1, alpha=.1; then trace neighbor phi=[0,1] before and after.", expected: "Source prediction 0 gives error 1 and w'=[.6,−.05]. The unsampled neighbor changes from [0,1]·w=−.25 to [0,1]·w'=−.05 through shared parameters.", retry: "Compute source update first, then take the neighbor dot product with both old and new weight vectors." },
    decision: { explanation: "Approximation quality must include where updates help, where they interfere, and how far evaluation moves from training support.", mechanism: "Measure train and held-out error by state region, perturb one sample and inspect prediction changes elsewhere, compare linear and neural models at matched data/updates, and monitor scale, gradients, and bootstrapped-target drift.", workedExample: "An update improves visited left states but flips the action ranking in unvisited right states because both depend on the same feature coordinate.", boundary: "One held-out split cannot prove robustness to every policy-induced distribution change.", check: { prompt: "Training TD error falls while unseen-state values explode. Which evidence rules out simple underfitting?", expected: "Training fit is adequate; the failure is unsupported extrapolation or bootstrapped instability, requiring held-out support and prediction-scale diagnostics.", retry: "Place training error, evaluation support, target scale, and unseen prediction scale in separate columns." } },
    quiz: { question: "Why can one update change several state values?", options: ["They share approximator parameters", "Each state has an isolated table cell", "Rewards are normalized", "Episodes are independent"], answer: 0, explanation: "Function approximation reuses parameters across inputs, creating generalization and interference." },
    transfer: { prompt: "Visited-state loss is low but unvisited Q values are huge. What is the leading concern?", correct: "Unsupported extrapolation or bootstrapped instability", wrong: ["Guaranteed optimal generalization", "Too many independent table entries"], worked: "Audit support, prediction scale, and target drift before trusting the greedy policy.", retry: "Compare where training examples exist with where the policy takes a maximum." },
    lab: { title: "Shared-parameter probe", question: "How does feature sharing alter both the trained point and an untrained neighbor?", controlLabel: "Feature case", boundary: "The two-feature regression is an exact fixture, not a neural convergence result.", cases: [{ label: "orthogonal", resultLabel: "neighbor change", resultValue: "0", meter: 0, detail: "The neighbor uses no updated feature coordinate, so its prediction stays fixed." }, { label: "shared", resultLabel: "neighbor change", resultValue: "+.2", meter: 60, detail: "The shared coordinate transfers part of the update to the neighbor." }, { label: "opposed", resultLabel: "neighbor change", resultValue: "−.2", meter: 90, detail: "Opposite feature sign creates harmful interference despite lower source error." }] }, motionConcept: "coordinates",
    code: { title: "Update a linear value approximator", language: "Python 3", setup: "A dependency-free vector update exposes sharing.", predict: "What weights follow one update?", code: py("phi=[1.0,2.0]; w=[.5,-.25]", "target=.1*0+1.0; alpha=.1", "prediction=sum(a*b for a,b in zip(phi,w))", "error=target-prediction", "w=[wi+alpha*error*x for wi,x in zip(w,phi)]", "print(prediction,error,w)"), observe: "Prediction 0 and error 1 produce [.6,−.05].", tryIt: "Evaluate a second feature vector before and after the update to measure interference." }, sourceKeys: ["book"] })),

  defineResearchLesson(m, rlSeed({ id: "replay-target-networks",
    plain: "Replay buffers reuse past transitions, while target networks hold the bootstrapped target nearly fixed for several updates.",
    precise: "A replay buffer samples transitions away from their collection order, improving reuse and reducing short-range correlation but mixing policies and ages. A target network $Q_{\\bar w}$ forms $y=r+\\gamma\\max_aQ_{\\bar w}(s',a)$; hard copies or Polyak averaging update $\\bar w$ more slowly than online weights w. Capacity, eviction, sampling, target lag, and update-to-data ratio define the data-and-target system.",
    mentalModel: "Replay is a case archive; the target network is a periodically revised answer key rather than one rewritten during every exercise.", ideas: ["Record transition age, source policy, priority, and ending flags.", "Measure target drift separately from online prediction error.", "Sweep replay ratio and target lag under a fixed environment-interaction budget."],
    worked: "A replay sampler selects row {age:40, source:policy-3, reward:1, terminated:false}; target values [4,4.5] give y=1+.9×4.5=5.05. With online weight 10 and target 8, Polyak tau=.1 updates target to .1(10)+.9(8)=8.2; a hard copy would jump to 10.",
    boundary: "Replay does not make old data on-policy, and a stale target can slow adaptation or preserve obsolete errors.",
    objectives: { primary: "Trace replay sampling and target-network TD targets", decision: "Choose buffer and target-update settings from stability and freshness evidence" },
    vocabulary: [{ term: "Replay buffer", meaning: "A finite store of past transitions sampled for later learning updates." }, { term: "Target network", meaning: "A delayed copy of value-network parameters used to construct bootstrapped targets." }, { term: "Polyak averaging", meaning: "Moving target parameters partway toward online parameters with a small interpolation coefficient each update." }, { term: "Update-to-data ratio", meaning: "The number of gradient updates performed per newly collected environment transition." }],
    primaryCheck: { prompt: "A sampled replay row has reward 1, not-terminal, age 40, and target Q [4,4.5] at gamma=.9. What row fields and target enter learning?", expected: "The sampled row retains age/source/action/ending provenance; its not-terminal target is 1+.9×4.5=5.05, paired with the logged action prediction.", retry: "Select one complete stored transition first, inspect age/source/ending, then form the delayed-network target from that same row." },
    decision: { explanation: "Replay and target lag trade data reuse and target stability against freshness and distribution mismatch; both buffer and delayed-update choices need direct evidence.", mechanism: "Hold interactions fixed, change capacity or target rule separately, and report transition age/policy mismatch plus target drift, Q scale, return, and cost; compare hard-copy intervals or Polyak coefficients with the same online weight path.", workedExample: "A huge buffer predating a reward change delays adaptation; separately, online weight 10 and target 8 becomes 8.2 with Polyak tau=.1 but 10 under hard copy, exposing different target lag.", boundary: "Stable loss can come from obsolete replay or excessively stale targets and does not prove current-policy improvement.", check: { prompt: "Replay age is acceptable, but target drift stays large and return reacts too slowly after change. What target-setting intervention is testable?", expected: "Shorten the hard-copy interval or increase the declared Polyak coefficient under matched interactions, then compare drift, Q scale, stability, and return without changing the buffer.", retry: "Hold replay rows fixed, vary only how delayed parameters move toward online weights, and measure both stability and adaptation." } },
    quiz: { question: "Why use a target network?", options: ["To slow movement of bootstrapped targets", "To remove rewards", "To make replay on-policy", "To guarantee convergence"], answer: 0, explanation: "Delayed parameters make the regression target less coupled to the just-updated online network." },
    transfer: { prompt: "A reward change occurs but 95% of replay predates it. What should be diagnosed?", correct: "Replay staleness and objective inconsistency", wrong: ["Guaranteed better coverage", "A missing action space"], worked: "Measure sample age and change capacity, weighting, or phase boundaries with the modification recorded.", retry: "Compare the reward definition attached to stored rows with the current one." },
    lab: { title: "Replay-lag dashboard", question: "How do buffer age and target lag change stability and adaptation?", controlLabel: "System setting", boundary: "The dashboard values are deterministic design fixtures, not DQN measurements.", cases: [{ label: "tiny / fast", resultLabel: "risk", resultValue: "CORRELATED", meter: 75, detail: "Fresh samples dominate but consecutive experience remains highly correlated." }, { label: "balanced", resultLabel: "status", resultValue: "TEST", meter: 35, detail: "Moderate age and lag form a candidate requiring return and scale evidence." }, { label: "huge / stale", resultLabel: "risk", resultValue: "OBSOLETE", meter: 95, detail: "Old policies and targets can delay adaptation after dynamics change." }] }, motionConcept: "systems",
    code: { title: "Construct a delayed-network target", language: "Python 3", setup: "The fixture distinguishes online and target predictions.", predict: "Which network supplies target magnitude?", code: py("reward=1; gamma=.9", "online=[5.0,7.0]; target=[4.0,4.5]", "y=reward+gamma*max(target)", "moving=reward+gamma*max(online)", "print(y,moving)"), observe: "The delayed target is 5.05 versus 7.3 from current online values.", tryIt: "Implement a Polyak update and inspect lag after three online changes." }, sourceKeys: ["dqn", "book"] })),

  defineResearchLesson(m, rlSeed({ id: "dqn",
    plain: "A deep Q-network predicts an action value for every discrete action and trains those predictions from replayed one-step targets.",
    precise: "DQN maps a batched observation $[B,\\ldots]$ to Q values $[B,A]$. It gathers $Q_w(s,a)$ for recorded actions and regresses toward $y=r+\\gamma(1-d)\\max_{a'}Q_{\\bar w}(s',a')$. The complete loop includes preprocessing, epsilon-greedy collection, replay warmup, minibatch sampling, target updates, gradient control, evaluation without training exploration, and checkpoints containing all RNG and buffer state.",
    mentalModel: "One network scores every available lever; replay supplies old consequences and the delayed copy supplies a steadier next-score target.", ideas: ["Assert observation, action, reward, ending, and Q tensor shapes.", "Gather only the logged action prediction for each loss row.", "Separate collection policy, training updates, and evaluation policy in artifacts."],
    worked: "One complete step collects (s,action 1,reward 1,s',not-terminal), appends it to replay, samples a batch [B,·], gathers Q(s,a) from [B,A], builds delayed targets, reduces Huber loss, updates online weights, conditionally copies target weights, then evaluates with epsilon zero and no updates.",
    boundary: "A decreasing DQN loss can accompany Q overestimation, replay collapse, insufficient exploration, or worse policy return.",
    objectives: { primary: "Assemble and verify a complete minimal DQN loop", decision: "Diagnose DQN learning from Q scale, coverage, and policy evidence" },
    vocabulary: [{ term: "DQN", meaning: "A neural action-value learner using replay and a delayed target network for discrete actions." }, { term: "Epsilon-greedy", meaning: "Choosing a random action with probability epsilon and a greedy action otherwise." }, { term: "Huber loss", meaning: "A regression loss quadratic near zero and linear for large residuals." }],
    primaryCheck: { prompt: "Order a minimal DQN iteration after one transition: collection, replay, target, loss/update, target copy, and evaluation. What ownership invariant matters?", expected: "Store the applied transition, sample complete rows, gather logged-action Q values, form no-grad delayed targets, update online weights, copy on schedule, and evaluate a frozen greedy policy; each consequence stays with its applied action.", retry: "Trace one row from environment step through replay and target arithmetic before naming any network update or evaluation." },
    decision: { explanation: "DQN progress requires agreement among implementation invariants, data coverage, value calibration, and policy evaluation.", mechanism: "Gate on shapes, finite gradients, one-batch overfit, replay contents, target lag, action coverage, Q magnitude, greedy and behavior return, constraint failures, and seed-level curves.", workedExample: "Loss falls while max Q grows from 5 to 500 and return falls; scale and return reject the optimization-only success story.", boundary: "Passing a small discrete environment does not establish continuous-action support or robustness to visual and dynamics shifts.", check: { prompt: "TD loss halves, max Q grows 100×, and return falls. What is the strongest diagnosis?", expected: "Training is numerically fitting its moving targets while value estimates become miscalibrated or unstable; DQN performance has not improved.", retry: "Plot loss, target scale, prediction scale, return, and action occupancy on the same interaction axis." } },
    quiz: { question: "Which Q entries receive direct regression loss in DQN?", options: ["Values for actions stored in the sampled transitions", "Every action as the same target", "Only target-network values", "No Q values"], answer: 0, explanation: "The transition records which action produced the observed consequence, so gather selects that action's prediction." },
    transfer: { prompt: "Training epsilon remains active during a claimed greedy evaluation. What is confounded?", correct: "Exploration randomness is mixed with target-policy performance", wrong: ["Nothing; epsilon never affects actions", "Only tensor shape"], worked: "Run a separately seeded evaluation policy with declared epsilon, usually zero, and no learning updates.", retry: "Name which policy selected each evaluation action." },
    lab: { title: "DQN evidence monitor", question: "Which joint signal indicates learning, instability, or an invalid evaluation?", controlLabel: "Run trace", boundary: "The traces are teaching fixtures, not benchmark results.", cases: [{ label: "loss↓ Q bounded", resultLabel: "status", resultValue: "PROMISING", meter: 30, detail: "Return and coverage still need to agree before accepting the run." }, { label: "loss↓ Q×100", resultLabel: "status", resultValue: "UNSTABLE", meter: 100, detail: "Exploding value scale contradicts the loss-only progress story." }, { label: "epsilon mixed", resultLabel: "evaluation", resultValue: "CONFOUNDED", meter: 85, detail: "Exploratory actions prevent attribution to the greedy policy." }] }, motionConcept: "training",
    code: { title: "Trace a DQN minibatch", language: "Python 3 pseudocode", setup: "Framework-neutral pseudocode keeps shapes and target ownership explicit.", predict: "Which axis does gather remove?", code: py("q_all=online(observation)          # [B,A]", "q_taken=gather(q_all,action)       # [B]", "with no_grad:", "    next_q=max(target(next_obs),axis=1)", "    y=reward+gamma*(not terminated)*next_q", "loss=huber(q_taken,y)", "optimize(loss); maybe_copy_target()"), observe: "Action gather converts [B,A] to one prediction [B] per transition.", tryIt: "Add assertions for every shape and a time-limit truncation target.", caveat: "Implement with a real tensor library, replay buffer, networks, optimizer, and RNGs before running." }, codeGuidance: { mode: "pseudocode", requirements: "Supply a tensor library, discrete environment, online/target networks, replay, optimizer, schedules, and checkpoint state." }, sourceKeys: ["dqn"] })),

  defineResearchLesson(m, rlSeed({ id: "exploration-deep-rl",
    plain: "Exploration chooses actions that may reveal useful outcomes instead of always exploiting current value estimates.",
    precise: "Epsilon-greedy randomizes uniformly; count bonuses add reward-like optimism to rarely visited regions; uncertainty methods use ensembles or posterior approximations; entropy regularization spreads policy probability. Exploration must be evaluated through state-action coverage, regret or return, rare-event discovery, constraint exposure, and the behavior actually deployed—not action randomness alone.",
    mentalModel: "Exploration is an information budget: spend risky or costly actions only when their possible learning value justifies them.", ideas: ["Distinguish action entropy from meaningful state coverage.", "Measure discovery and safety costs on the same interaction budget.", "Predeclare exploration decay, reset distribution, and constraint policy."],
    worked: "Epsilon-greedy picks random actions without state knowledge; count scores start=.1 and door=.5, so bonus favors door. An ensemble predicts door values [2,6,4] versus start [3,3.2,2.8], so higher disagreement also selects door while exposing calibration risk.",
    boundary: "More coverage is not automatically better when exploration violates constraints or visits irrelevant regions.",
    objectives: { primary: "Compare epsilon, bonus, and uncertainty exploration mechanisms", decision: "Choose an exploration policy from coverage, learning, and safety evidence" },
    vocabulary: [{ term: "Exploration", meaning: "Selecting actions partly to gather information or reach insufficiently known outcomes." }, { term: "Count bonus", meaning: "An additional signal that favors states or actions with low visit counts." }, { term: "Epistemic uncertainty", meaning: "Uncertainty caused by limited knowledge that can shrink with informative data." }],
    primaryCheck: { prompt: "Compare epsilon randomness, count scores start=.1/door=.5, and ensemble values door [2,6,4] versus start [3,3.2,2.8]. Which mechanisms prefer door?", expected: "Epsilon may choose door only randomly; the count bonus explicitly prefers its .5 novelty; the uncertainty method prefers door because ensemble disagreement is much larger, subject to calibration and safety gates.", retry: "For each method name its decision signal: uniform random branch, visit-derived bonus, or spread across value predictors." },
    decision: { explanation: "Exploration selection is a constrained information-gathering decision rather than a search for maximum randomness.", mechanism: "Match interactions and starts, record occupancy, unique/critical states, reward, uncertainty calibration, hazard events, intervention rate, and seed-level learning; reject methods crossing non-compensable gates.", workedExample: "An ensemble bonus finds the sparse goal sooner but enters a forbidden region on one seed; the safety gate rejects that setting despite higher mean return.", boundary: "Uncertainty estimates can be shared and wrong across an ensemble, especially under common data and architecture bias.", check: { prompt: "Action entropy is high but every episode remains in one room. Has meaningful exploration been demonstrated?", expected: "No. Action randomness increased, but state or outcome coverage did not; inspect occupancy and task-relevant discovery.", retry: "Map actions to resulting states and count new decision-relevant regions rather than action labels." } },
    quiz: { question: "Which metric distinguishes random actions from useful exploration?", options: ["Task-relevant state-action coverage and discovery", "Action entropy alone", "Network parameter count", "Checkpoint filename"], answer: 0, explanation: "Useful exploration changes the evidence available for consequential decisions." },
    transfer: { prompt: "An uncertainty bonus raises return but violates a hard constraint. What is the decision?", correct: "Reject or constrain the setting under the predeclared gate", wrong: ["Average return overrides safety", "Delete the failing seed"], worked: "Preserve the failure and test a shield, budget, or different exploration scale.", retry: "Apply non-compensable constraints before compensable performance." },
    lab: { title: "Exploration frontier", question: "How do randomness, coverage, and constraint exposure move across exploration policies?", controlLabel: "Policy", boundary: "The cards are deterministic comparison scenarios rather than executed agents.", cases: [{ label: "epsilon only", resultLabel: "coverage", resultValue: "4/10", meter: 40, detail: "Actions vary while environment bottlenecks keep occupancy local." }, { label: "count bonus", resultLabel: "coverage", resultValue: "8/10", meter: 80, detail: "Novel states increase together with more hazard exposure." }, { label: "shielded bonus", resultLabel: "coverage", resultValue: "7/10", meter: 70, detail: "A constraint filter sacrifices some discovery while keeping hazards below gate." }] }, motionConcept: "agent",
    code: { title: "Compute a count bonus", language: "Python 3", setup: "A tiny visit table exposes how novelty changes the score.", predict: "Which state receives the larger bonus?", code: py("import math", "counts={'start':100,'door':4}", "bonus={s:1/math.sqrt(n) for s,n in counts.items()}", "print({s:round(v,3) for s,v in bonus.items()})"), observe: "The rarely visited doorway receives .5 versus .1 at the start.", tryIt: "Add a hard hazard mask and explain what evidence it prevents collecting." }, sourceKeys: ["book", "safety"] })),

  defineResearchLesson(m, rlSeed({ id: "deep-value-capstone", duration: 40,
    plain: "This capstone builds a minimal DQN, proves its replay and target mechanics, reproduces a bounded baseline, and runs one stability or exploration intervention across declared seeds.",
    precise: "The system includes a discrete environment, preprocessing, online and target networks, replay with full ending fields, epsilon collection, warmup, update schedule, evaluation policy, checkpoints, and artifact writer. Acceptance requires shape tests, deterministic replay sampling, target arithmetic, one-batch overfit, finite gradients, resume equivalence, baseline learning on a course-scale task, and seed-level return/Q-scale/coverage evidence.",
    mentalModel: "Treat DQN as an instrumented data-and-target system, not one neural-network call inside a loop.", ideas: ["Pass micro-tests before spending environment interactions.", "Reproduce the baseline acceptance range without inventing expected scores.", "Change one replay, target, loss, or exploration factor under matched interactions."],
    worked: "Five seeds reproduce a small control baseline; treatment doubles target-copy interval with identical interactions and evaluation episodes. All seed curves, Q maxima, replay ages, failures, and checkpoints remain in the dossier.",
    boundary: "Course-scale DQN evidence does not establish Atari performance, continuous control, or safe deployment.",
    objectives: { primary: "Assemble and audit every state transition in an end-to-end DQN-style agent", decision: "Run a matched seed-level DQN intervention with failure evidence" },
    vocabulary: [{ term: "Replay warmup", meaning: "A collection period before optimization begins so minibatches contain enough transitions." }, { term: "Target-copy interval", meaning: "The number of updates or interactions between hard copies to the target network." }, { term: "Resume equivalence", meaning: "A check that restored state produces the same next sampled batch, update, and transition under a deterministic contract." }],
    primaryCheck: { prompt: "Which saved states are required to test exact next-update resume in DQN?", expected: "Online and target weights, optimizer, replay contents and cursor, exploration/update schedules, environment or data position, counters, and every RNG stream are required.", retry: "Trace all values that choose the next transition, minibatch, target, gradient update, and schedule change." },
    decision: { explanation: "The intervention is valid only after baseline acceptance and matched primitive budgets.", mechanism: "Branch from a common validated revision, predeclare seed list and interaction/update budget, alter one setting, preserve all checkpoints and rows, compute seed-level paired effects, apply Q-scale and constraint gates, and diagnose every failed run.", workedExample: "Longer target lag lowers TD variance but slows return recovery after shift; the conclusion reports a stability-freshness trade-off rather than a winner.", boundary: "A null result at five seeds and one environment remains unresolved beyond its interval and scope.", check: { prompt: "Treatment return improves on four seeds but one seed has Q=NaN. Can the failed row be removed?", expected: "No. Preserve and diagnose it; treatment reliability includes numerical failure, and any exclusion must follow a predeclared rule with sensitivity analysis.", retry: "Reconcile the declared seed list with artifacts before computing the treatment summary." } },
    quiz: { question: "When may the capstone intervention begin?", options: ["After DQN micro-tests and baseline acceptance pass", "After seeing one attractive curve", "Before target arithmetic is tested", "After choosing the best seed"], answer: 0, explanation: "Acceptance prevents implementation failure from masquerading as an experimental effect." },
    transfer: { prompt: "Treatment uses twice as many updates per interaction. What must the report do?", correct: "Match update budget or present a quality-versus-update curve and name the cost difference", wrong: ["Call the comparison matched", "Hide update counts"], worked: "Separate interaction efficiency from compute/update efficiency in the evidence table.", retry: "Count environment steps, gradient updates, target calls, and runtime separately." },
    lab: { title: "DQN capstone gate", question: "Which evidence package permits a long run, an intervention, or a conclusion?", controlLabel: "Package", boundary: "The cards audit readiness and contain no measured DQN result.", cases: [{ label: "shapes only", resultLabel: "status", resultValue: "MICRO-DEBUG", meter: 25, detail: "Data flow exists but target, overfit, resume, and baseline gates remain." }, { label: "baseline accepted", resultLabel: "status", resultValue: "BRANCH", meter: 65, detail: "Correctness and bounded learning evidence permit one controlled treatment." }, { label: "all seed artifacts", resultLabel: "status", resultValue: "DECIDE", meter: 100, detail: "Matched rows, failures, Q scale, coverage, and budgets support a scoped result." }] }, motionConcept: "pipeline",
    code: { title: "Specify the DQN experiment contract", language: "Python 3 pseudocode", setup: "The skeleton names state, ownership, and stop rules for a real implementation.", predict: "Which state makes replay sampling resume exactly?", code: py("config=pin(environment,network,replay,seeds,budgets)", "smoke=run(max_steps=200,assert_finite=True)", "save(online,target,optimizer,replay,schedules,rngs,counters)", "baseline=run(seed_set,interaction_budget)", "treatment=run(seed_set,interaction_budget,change_one='target_interval')", "analyze_all_rows(baseline,treatment,gates=['q_scale','constraints'])"), observe: "The protocol separates smoke, reproduced baseline, one change, and all-row analysis.", tryIt: "Add a forced NaN seed and verify the analysis retains it.", caveat: "Supply a real environment, tensor library, networks, replay implementation, and artifact schema." }, codeGuidance: { mode: "pseudocode", requirements: "Implement or adapt the complete named DQN system; optional GPU execution must follow the separate pinned runbook." }, sourceKeys: ["dqn", "gymnasium"] })),
];
