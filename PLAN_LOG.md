# PLAN_LOG.md

## Objective
Implement a unified concept-first curriculum across all five Neural Field Guide courses: reorder math-heavy openings behind intuitive mechanisms, make required mastery explanation- and decision-based rather than calculation-based, preserve rigorous mathematics and code as optional technical depth, update every affected dependency and learning surface, verify all 182 lessons and 187 pages, then commit and push the complete change.

## Plan 1
1. [x] Add shared concept-first and optional technical-depth contracts, renderer placement, activity labeling, and non-gating completion behavior - Done when technical depth is reusable across all course families, appears after core Test in Extend, and core progress remains backward compatible.
2. [x] Make phase order explicit and reorder every course opening behind intuitive mechanisms - Done when all five opening sequences match the approved concept-first order, phase navigation follows canonical lesson order, prerequisites precede consumers, and lesson IDs/counts/storage keys remain stable.
3. [x] Migrate LLM and World Models content to explanation-first core plus optional formal depth - Done when their calculation-led objectives, quizzes, transfers, code, validations, capstones, guides, and continuity records no longer require hand calculation for core mastery while exact mathematics remains available.
4. [x] Migrate Generative Models, Reinforcement Learning, and Embodied AI through the shared research-course contracts - Done when all seeds and rendered pages follow the same core/technical split and their opening orders, assessments, labs, code, capstones, and prerequisites are coherent.
5. [x] Repair program-wide concept ownership, cross-course prerequisites, continuity, documentation, audits, and reader-grade evidence - Done when every changed handoff and objective is represented consistently and the 187-page source of truth is current.
6. [x] Add and update executable tests for concept-first guarantees and all changed exact contracts - Done when focused curriculum, objective, continuity, rendering, progress, and course tests pass.
7. [x] Run full verification and visual QA, fix every discovered gap, and commit only the complete in-scope implementation - Done when npm run lint and npm test pass, representative desktop/narrow/keyboard/reduced-motion checks pass, PLAN_LOG has no unresolved gaps, and the scoped commit succeeds.

### Clarifications
- Constraints: One coordinated five-course release; no pilot or partial completion; preserve 182 lesson IDs, 187 canonical pages, routes, storage keys, static export, evidence honesty, accessibility, and unrelated work.
- Checks/Tests: Focused node tests during implementation; node --test tests/objective-coverage.test.mjs; node --test tests/concept-first-curriculum.test.mjs; node --test tests/cross-course-continuity.test.mjs; node --test tests/course-page-reader-snapshot.test.mjs; npm run lint; npm test; browser QA at desktop and narrow widths.

- Implementation: Shared concept-first policy now renders a formula-free, lesson-specific operation check in all 182 lessons, defers all exact objectives and code until after mastery, moves calculation-led activities with the formal material, and reorders every course opening without changing IDs or storage keys.
### Verification

- First all-page verification found two implementation gaps rather than accepting the initial structural pass: plain-text equations and arithmetic traces without LaTeX delimiters escaped the classifier, and sanitizing entire original formal chapters produced awkward learner-facing prose. Both issues affected the pipeline because a test could report “formula-free” while a beginner still encountered equations or degraded explanations.
- The shared detector now reads the full authored lesson and recognizes equation-like plain text as well as rendered notation. Concept-first cores are purpose-built from the lesson's plain summary, mental model, key ideas, misconception, and conceptual quiz; original narrative, complete vocabulary, all exact objective checks, calculations, labs, transfers, capstones, validation, and code remain untouched in optional technical depth.
- Current structural evidence: 182/182 required lesson cores use one topic-specific Input → Operation → Result → Limit trace and contain no rendered formula syntax or equation-like expression; all 408 exact objective records remain intact after mastery; all 152 code notebooks follow mastery; lesson IDs, routes, storage keys, counts, and static-export contracts are unchanged. The 158 math-bearing lessons also defer calculation-led activities, while formula-free activities remain in the required Try/Test flow.
- At that intermediate checkpoint, focused curriculum, objective, continuity, reader-dossier, interaction, rendering-source, documentation, and course tests passed; intentionally stale blind-grade evidence and pre-build HTML checks remained for Plan 2 rather than being accepted as final failures.

### Gaps -> Plan 2

1. [x] Catch un-delimited equations, arithmetic traces, and calculation-led quizzes that escaped the first notation-only audit; rewrite required mastery checks to explain or decide without hand calculation.
2. [x] Replace degraded whole-chapter sanitization and the rejected generic scaffold with topic-specific authored prose, plain prerequisite/next-use bridges, conceptual changed cases, and operation traces while retaining the complete original formal narrative and vocabulary after mastery.
3. [x] Complete a fresh independent blind semantic review of all 187 pages and every exact objective, repair any reported page, and regenerate current grade evidence only after a zero-gap pass.
4. [x] Re-run the full lint/build/artifact/test suite, inspect representative desktop/narrow/keyboard/reduced-motion states, perform final objective and context re-checks, then commit and push the complete implementation.

### Plan 2 verification

- The first independent World Models pass rejected the initial concise scaffold: although all 92 exact objectives and placement contracts passed, 42/47 pages repeated a generic input/operation/output outline instead of reading as a topic-specific textbook chapter.
- The repair now keeps each lesson's authored, non-calculation narrative in the required core, filters formula and numeric worked-calculation paragraphs, supplies an authored summary and mental-model fallback only when an opening would otherwise be empty, and uses the existing conceptual quiz as a changed case. The full original guide, vocabulary, examples, objective checks, activities, and code remain after mastery in optional technical depth.
- At that checkpoint, the reader audit rejected boilerplate policy copy, orphaned worked-example transitions, and degraded formula placeholders in every deferred core; the fresh blind whole-page review described below was still the remaining semantic gate.
- The next blind LLM and Generative passes found that the guide walkthrough had improved but the visual, guided reveal, practice, and core check still reused older design/audit triples. The repair adds an explicit authored operation trace for every released lesson and makes every required surface consume that same record. It also moves the remaining six LLM exact-objective sets after mastery and adds six objective remediations requested by the blind reviewer.
- A subsequent Generative regrade passed all 60 exact objectives and 29/31 pages, then rejected two malformed phrases introduced by the shared Jacobian-to-plain-language transform. The transform now distinguishes local volume corrections from local change maps, filters determinant-heavy vocabulary from the required core, and an all-lesson regression check rejects repeated-article and malformed volume-language patterns.
- Narrow-width browser QA now passes representative LLM, World Models, Generative Models, RL, and Embodied AI routes at 390 px: required cores contain no formula syntax, optional technical depth starts collapsed, controls remain usable, and document overflow is zero. The scoped mobile lesson container clips decorative/story overflow while nested math and table regions keep their own horizontal scrolling.
- Desktop browser QA at 1440 × 900 confirms zero overflow and a formula-free core; optional technical depth starts closed and opens from keyboard focus with Enter. The browser backend exposes viewport control but no reduced-motion media emulation, so the reduced-motion fallback is verified by the executable source contract: semantic copy stays visible, story animation is disabled, and the WebGL layer is removed under `prefers-reduced-motion: reduce`.
- The final grading audit found that the two new concept-first source modules and the shared research manifest were missing from the course-level source fingerprint. They are now part of every course fingerprint, with a focused provenance regression test; all final independent grades have been regenerated against that complete source bundle.
- Final independent evidence passes 187/187 canonical pages and all 408/408 exact authored objectives with no blocker. After the keyboard disclosure handler received a type-safe `HTMLDetailsElement` lookup, all 187 reader hashes remained unchanged; only the course-source provenance fingerprints required refresh.
- Final repository verification passes `npm run lint` and `npm test`: the Pages production build and TypeScript check succeed, 182 canonical lesson routes plus 44 legacy forwards export correctly, preserved artifacts and all 182 lesson visuals verify, and the complete regression run closes at 240 pass, 0 fail.
- Release closure: commit `80ccb34` contains the complete implementation and passed the isolated direct-main hook, including both Pages URL shapes, before being pushed to `origin/main`.
